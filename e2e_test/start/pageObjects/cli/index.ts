import {
  nonInteractiveOutput,
  historyOutput,
  historyInput,
  currentGuidance,
} from './outputAssertions'

export const cli = {
  nonInteractiveOutput,
  historyOutput,
  historyInput,
  currentGuidance,
}
