---
phase: 10-overlap-alias-append-util
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/utils/appendOverlapWikiLinkToNoteContent.ts
  - frontend/tests/utils/appendOverlapWikiLinkToNoteContent.spec.ts
autonomous: true
requirements: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A named util appendOverlapWikiLinkToNoteContent appends a whole-item wiki-link alias token ([[Title]] or [[Notebook:Title]]) into note frontmatter aliases — never a bare title for overlap intent (D-01, D-03, D-04, D-08; Pitfall 5)"
    - "Same merge/null contract as plain alias append: creates aliases when absent, merges into an existing YAML list, returns null on duplicate / unparseable / non-list aliases / empty token; preserves existing plain alias items when appending a wiki-link (D-02, D-05, D-06)"
    - "Existing accidental-match and OVERLAP try-again user flows are observably unchanged — no resolve-dialog CTA, no updateTextField, no AnsweredSpellingQuestion / AccidentalMatch chrome edits (D-07)"
    - "No backend, OpenAPI, AnswerOutcome, or SRS changes (D-09)"
    - "Capability-named Vitest at the util boundary is green; no E2E required this Structure phase (D-08)"
  artifacts:
    - path: frontend/src/utils/appendOverlapWikiLinkToNoteContent.ts
      provides: "Named sibling util: buildWikiLinkText (no displayText) then appendAliasToNoteContent; export appendOverlapWikiLinkToNoteContent"
    - path: frontend/tests/utils/appendOverlapWikiLinkToNoteContent.spec.ts
      provides: "Util-boundary Vitest locking wiki-link shape, merge/null, cross-notebook, mixed list, authoredAliasesValidation acceptance"
  key_links:
    - from: appendOverlapWikiLinkToNoteContent
      to: buildWikiLinkText
      via: "target + source.notebookId only; omit displayText so tokens stay whole-item [[…]] (D-03, D-04)"
    - from: appendOverlapWikiLinkToNoteContent
      to: appendAliasToNoteContent
      via: "Pass the [[…]] token into existing frontmatter merge path — do not add a second YAML parser (D-02, D-05)"
    - from: Phase 11 (deferred)
      to: appendOverlapWikiLinkToNoteContent
      via: "Future resolve-row CTA will call this util then updateTextField; not wired this phase (D-07)"
---

<objective>
Ship a Structure-only named overlap wiki-link append util so Phase 11 can declare overlap without bloating the resolve dialog or teaching callers to pass plain titles into appendAliasToNoteContent (Pitfall 5 / D-01).

Purpose: Stop-safe prep for AMR-08 — pure helper + Vitest only; accidental-match and OVERLAP try-again flows stay unchanged.
Output: `appendOverlapWikiLinkToNoteContent.ts` + capability-named util Vitest green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-overlap-alias-append-util/10-CONTEXT.md
@.planning/phases/10-overlap-alias-append-util/10-RESEARCH.md
@.planning/phases/10-overlap-alias-append-util/10-PATTERNS.md
@.planning/phases/10-overlap-alias-append-util/10-UI-SPEC.md
@.cursor/rules/frontend-testing.mdc
@.cursor/rules/unit-testing.mdc
@.cursor/rules/planning.mdc
</context>

## Artifacts this phase produces

- Named sibling util `appendOverlapWikiLinkToNoteContent` under `frontend/src/utils/`
- Capability-named Vitest at `frontend/tests/utils/appendOverlapWikiLinkToNoteContent.spec.ts`
- No UI, no E2E, no backend (Structure fence)

## UI Considerations (from 10-UI-SPEC)

N/A Structure — no visual work. Confirm accidental-match + OVERLAP try-again chrome files stay untouched and no new CTA testids appear.

## Multi-Source Coverage (this plan)

| SOURCE | ID | Item | Status |
|--------|-----|------|--------|
| GOAL | — | Pure overlap wiki-link append helper for Phase 11 | COVERED |
| REQ | — | Structure phase; no AMR IDs (enables AMR-08 in Phase 11) | COVERED (N/A) |
| RESEARCH | — | Compose buildWikiLinkText → appendAliasToNoteContent; zero new packages | COVERED |
| RESEARCH | — | Vitest cases: wiki-link shape, create/merge/null, cross-notebook, mixed list | COVERED |
| CONTEXT | D-01..D-09 | Named sibling, composition, token shape, merge contract, scope fence, Vitest, no backend | COVERED |
| CONTEXT | Deferred | Phase 11 CTA / Phase 12 / AMR-10..13 / SEED-001 | Excluded (deferred) |

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: wiki-link overlap append util happy path</name>
  <reversibility rating="reversible">D-01 named sibling util — delete the two new files to roll back; no call sites yet.</reversibility>
  <files>frontend/tests/utils/appendOverlapWikiLinkToNoteContent.spec.ts, frontend/src/utils/appendOverlapWikiLinkToNoteContent.ts</files>
  <read_first>
    - frontend/src/utils/buildWikiLinkText.ts
    - frontend/src/utils/wikidataTitleActions.ts (appendAliasToNoteContent)
    - frontend/tests/utils/buildWikiLinkText.spec.ts
    - frontend/src/utils/authoredAliasesValidation.ts
    - frontend/src/utils/noteContentFrontmatter.ts
    - frontend/src/utils/noteProperties.ts (listPropertyValue)
    - .planning/phases/10-overlap-alias-append-util/10-CONTEXT.md (D-01..D-06, D-08)
    - .planning/phases/10-overlap-alias-append-util/10-PATTERNS.md (composition + Vitest stub)
    - .planning/phases/10-overlap-alias-append-util/10-RESEARCH.md (Pattern 1; Suggested Vitest; Pitfall 6 YAML quoting)
  </read_first>
  <behavior>
    - Same-notebook: appendOverlapWikiLinkToNoteContent("## Body\n", target("Sedation", 1, "NB"), { notebookId: 1 }) returns markdown whose aliases list item contains [[Sedation]] as a whole-item wiki-link (D-01, D-03, D-04, D-08)
    - Creates aliases frontmatter when content had none (D-05, D-08)
    - Prefer positive wiki-link containment / parse-back of list items over brittle full-string YAML equality (YAML quotes [[…]] scalars — RESEARCH Pitfall 6)
    - Assert authoredAliasesValidationErrorForPropertyValue(listPropertyValue(parsedItems)) is undefined for the produced wiki-link item (D-08)
  </behavior>
  <action>
    Per D-08, write failing Vitest first (RED) at frontend/tests/utils/appendOverlapWikiLinkToNoteContent.spec.ts — capability name only, no phase numbers. Mirror makeTarget stubs from buildWikiLinkText.spec.ts. Cover the tracer happy path: body-only markdown → result includes [[Sedation]] wiki-link list item under aliases for same-notebook source/target; assert authoredAliasesValidation acceptance on the produced item(s). Then implement per D-01..D-04: new file frontend/src/utils/appendOverlapWikiLinkToNoteContent.ts exporting appendOverlapWikiLinkToNoteContent(contentMarkdown, target, source) that calls buildWikiLinkText(target, { notebookId: source.notebookId }) with no displayText, then returns appendAliasToNoteContent(contentMarkdown, token) (D-02 composition; prefer one-line wrap per RESEARCH Pattern 1 / Open Question 2 — new file, do not co-locate in wikidataTitleActions). Leave appendAliasToNoteContent semantics unchanged. Empty-title hardening is optional (RESEARCH Open Question 1) — tests use non-empty titles. Zero new packages.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/utils/appendOverlapWikiLinkToNoteContent.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: frontend/src/utils/appendOverlapWikiLinkToNoteContent.ts and frontend/tests/utils/appendOverlapWikiLinkToNoteContent.spec.ts
    - Export appendOverlapWikiLinkToNoteContent composes buildWikiLinkText then appendAliasToNoteContent (D-01, D-02)
    - Vitest green for same-notebook append creating aliases with [[Sedation]] (or equivalent title) wiki-link item (D-03, D-04, D-08)
    - Spec asserts authoredAliasesValidation acceptance for produced wiki-link item(s) (D-08)
  </acceptance_criteria>
  <done>Happy-path Structure tracer green: named util appends a whole-item wiki-link alias into frontmatter (D-01..D-04, D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Expand Vitest: merge, null, cross-notebook, mixed list, scope fence</name>
  <reversibility rating="reversible">D-05..D-09 contract and fence — test/util-only; no product call sites.</reversibility>
  <files>frontend/tests/utils/appendOverlapWikiLinkToNoteContent.spec.ts, frontend/src/utils/appendOverlapWikiLinkToNoteContent.ts</files>
  <read_first>
    - frontend/tests/utils/appendOverlapWikiLinkToNoteContent.spec.ts
    - frontend/src/utils/appendOverlapWikiLinkToNoteContent.ts
    - frontend/tests/notes/WikidataAssociationDialog.spec.ts (describe "append alias to frontmatter")
    - frontend/tests/utils/authoredAliasesValidation.spec.ts
    - frontend/tests/utils/buildWikiLinkText.spec.ts (cross-notebook case)
    - .planning/phases/10-overlap-alias-append-util/10-CONTEXT.md (D-05..D-09)
    - .planning/phases/10-overlap-alias-append-util/10-RESEARCH.md (Suggested Vitest; Pitfalls 1–3, 6)
  </read_first>
  <behavior>
    - Cross-notebook with notebookName: result contains [[Other NB:Deep Note]] (D-03, D-08)
    - Duplicate same wiki-link token already in aliases → null (D-05, D-08)
    - aliases present but not a YAML list → null (D-05, D-08)
    - Existing plain alias list (e.g. puppy) preserved when appending [[Canine]] (D-06, D-08)
    - Merges into an existing aliases list that did not yet contain this wiki-link (D-05, D-08)
    - Focused deltas only — do not re-assert the full tracer payload in every sibling case
  </behavior>
  <action>
    Per D-05, D-06, D-08, add focused Vitest deltas (RED then green) for: cross-notebook [[Notebook:Title]]; duplicate → null; non-list aliases → null; merge into existing list; mixed plain + wiki-link preservation. Prefer parseNoteContentMarkdown / list-item containment over full YAML string equality. Keep util as composition wrapper — only change production file if a delta exposes a real contract bug (should not need new merge logic). Per D-07 and D-09 scope fence: touch only the util + its spec; leave AnsweredSpellingQuestion, AccidentalMatch resolve components, MatchedNoteLinkOffer, updateTextField call sites, backend Java, OpenAPI, and AnswerOutcome untouched. After green: confirm git status shows only the two capability-named util files (plus planning artifacts if any). Local execute wrap-up (Jidoka / post-change-refactor / plan update / commit+push) follows execute-plan when the orchestrator runs it — not part of this task body.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/utils/appendOverlapWikiLinkToNoteContent.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec covers cross-notebook [[Notebook:Title]], duplicate null, non-list aliases null, merge, and plain+wiki mixed list (D-03, D-05, D-06, D-08)
    - Full util spec file green under Nix-prefixed Vitest command above
    - Production change set limited to appendOverlapWikiLinkToNoteContent.ts (+ its .spec.ts); no resolve-dialog / AnsweredSpellingQuestion / backend / OpenAPI diffs (D-07, D-09)
  </acceptance_criteria>
  <done>D-05..D-09 locked: merge/null/cross-notebook/mixed-list Vitest green; Structure scope fence held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → appendOverlapWikiLinkToNoteContent | Untrusted markdown + match target fields enter a pure client transform (no I/O this phase) |
| Util → frontmatter aliases list | Produced wiki-link tokens must match authoredAliasesValidation / backend FrontmatterAliases wiki-link shape |
| Phase 11 (future) → content-edit API | Out of scope; existing authenticated updateTextField + readonly gates remain the persist boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | appendOverlapWikiLinkToNoteContent | medium | mitigate | Always build wiki-link via buildWikiLinkText before merge (D-01, D-03); Vitest asserts [[…]] shape + authoredAliasesValidation acceptance (D-08) — blocks plain-title overlap intent (Pitfall 5) |
| T-10-02 | Tampering | displayText / pipe tokens | low | mitigate | Omit displayText so overlap items stay whole-item [[Title]] / [[Notebook:Title]] (D-04); specs assert non-pipe emit shape |
| T-10-03 | Elevation of Privilege | New declare-overlap API | high | mitigate | Do not add endpoints or AnswerOutcome flips (D-09); Phase 11 reuses existing content-edit + AMR-07-style gates |
| T-10-04 | Information Disclosure | Util tests / fixtures | low | accept | Fixtures use synthetic titles/notebook names only; no secrets |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — Package Legitimacy N/A; compose in-repo utils only |
</threat_model>

<verification>
Util Vitest green with Nix prefix. Diff fence: only the new util + spec. No E2E. Existing accidental-match / OVERLAP chrome unchanged by absence of those file edits.
</verification>

<success_criteria>
- Named util appends whole-item wiki-link overlap aliases via composition (D-01..D-06)
- Vitest proves create/merge/null/cross-notebook/mixed-list + authored validation (D-08)
- No UI / updateTextField / backend / SRS changes (D-07, D-09)
- ROADMAP Structure success criteria 1–3 satisfied
</success_criteria>

<output>
Create `.planning/phases/10-overlap-alias-append-util/10-01-SUMMARY.md` when done
</output>
