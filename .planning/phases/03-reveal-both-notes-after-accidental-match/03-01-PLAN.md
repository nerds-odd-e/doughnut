---
phase: 03-reveal-both-notes-after-accidental-match
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java
  - backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java
  - backend/src/main/java/com/odde/doughnut/controllers/dto/AnsweredQuestion.java
  - backend/src/main/java/com/odde/doughnut/controllers/RecallPromptController.java
  - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java
autonomous: true
requirements: [AM-03]
must_haves:
  truths:
    - "After an accidental match, AnsweredQuestion.matchedNotes is a non-empty List<NoteTopology> for every readable match (title ∪ alias), ordered by note id ascending — not null/empty when outcome is ACCIDENTAL_MATCH (D-01, D-02)."
    - "Answer.matchedNoteId equals the first matchedNotes entry id (lowest id); Answer.outcome stays ACCIDENTAL_MATCH; correct stays false."
    - "findAccidentalMatch remains first-of-list over findAllAccidentalMatches (backward-compatible singular API)."
    - "Plain wrong, correct, blank, and IDOR-unreadable paths still leave matchedNotes null/absent."
    - statement: "When title and alias candidates both match the same answer string, both notes appear in matchedNotes (union semantics), and matchedNoteId is min(id) across the union — not title-short-circuit preference."
      verification: backstop
  artifacts:
    - path: "backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java"
      provides: "findAllAccidentalMatches(answer, reviewedNote, viewer) — title ∪ alias, userMayReadNotebook filter, exclude reviewed, dedupe by id, id ASC; findAccidentalMatch = first of that list"
      contains: "findAllAccidentalMatches"
    - path: "backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java"
      provides: "answerSpelling uses findAll once; sets matchedNoteId from first match; returns match list for DTO assembly"
      contains: "findAllAccidentalMatches"
    - path: "backend/src/main/java/com/odde/doughnut/controllers/dto/AnsweredQuestion.java"
      provides: "from(RecallPrompt, List<Note> matches) or equivalent sets matchedNotes topologies"
      contains: "setMatchedNotes"
    - path: "backend/src/main/java/com/odde/doughnut/controllers/RecallPromptController.java"
      provides: "answer-spelling response includes populated matchedNotes on ACCIDENTAL_MATCH"
      contains: "answerSpelling"
    - path: "backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java"
      provides: "AccidentalMatch tests assert matchedNotes populated + id alignment; title∪alias union rewrite"
      contains: "getMatchedNotes"
  key_links:
    - from: "MemoryTrackerService.answerSpelling"
      to: "WikiLinkResolver.findAllAccidentalMatches"
      via: "single lookup drives matchedNoteId + list for AnsweredQuestion"
      pattern: "findAllAccidentalMatches"
    - from: "RecallPromptController.answerSpelling"
      to: "AnsweredQuestion.matchedNotes"
      via: "DTO assembly after from() with Note.getNoteTopology() per match"
      pattern: "setMatchedNotes"
    - from: "WikiLinkResolver.findAllAccidentalMatches"
      to: "AuthorizationService.userMayReadNotebook"
      via: "every candidate filtered before list inclusion (IDOR)"
      pattern: "userMayReadNotebook"
---

## Phase Goal

**As a** learner doing spelling recall, **I want to** see the reviewed note and all matched notes revealed together after an accidental match, **so that** my confusion becomes visible.

<objective>
Wire the end-to-end backend path that collects all readable accidental matches and returns them as AnsweredQuestion.matchedNotes on the answer-spelling response, with matchedNoteId aligned to the first list entry (AM-03 / D-01 / D-02).

Purpose: Phase 3 Behavior starts with a tracer proving the contract field Phase 2 left empty is filled through the controller — the UI wave builds on a real list, not a stub.
Output: findAllAccidentalMatches + service/controller assembly; AccidentalMatch controller tests green with populated matchedNotes (including title∪alias union rewrite).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-CONTEXT.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-RESEARCH.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-PATTERNS.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-VALIDATION.md
@.planning/phases/02-accidental-match-grading-penalty/02-CONTEXT.md
@.planning/phases/01-extend-answer-outcome-api/01-01-SUMMARY.md
</context>

<assumption_delta_decision>
Phase 3 success criterion #2 requires a plural surface ("all matched notes"). Phase 2 only wrote singular matchedNoteId.

- Noun primary: `matchedNotes` list (all readable matches) on AnsweredQuestion.
- Decision: `promote` — the list is the primary accidental-match surface; `matchedNoteId` is first-of-list detail kept for Phase 4 preselection continuity.
- Rationale: A singular id alone cannot satisfy revealing every match; promoting the list avoids treating matchedNotes as optional garnish.
- What would force a later change: if Phase 4 needed a different selection rule than lowest-id-first, matchedNoteId semantics would be revisited without dropping the list.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): findAllAccidentalMatches + populate matchedNotes on answer-spelling</name>
  <precondition>Phase 2 AccidentalMatch grading is green: findAccidentalMatch exists, answerSpelling sets outcome=ACCIDENTAL_MATCH + matchedNoteId, AnsweredQuestion.matchedNotes field exists but is null on accidental paths.</precondition>
  <reversibility rating="reversible">Additive findAll + DTO list population; singular matchedNoteId path retained (D-01, D-02).</reversibility>
  <files>backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java, backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java, backend/src/main/java/com/odde/doughnut/controllers/dto/AnsweredQuestion.java, backend/src/main/java/com/odde/doughnut/controllers/RecallPromptController.java, backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java</files>
  <read_first>
    - backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java (findAccidentalMatch 43–55, aliasAccidentalCandidates 57–77, firstReadableAccidentalCandidate 79–90)
    - backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java (answerSpelling 260–297)
    - backend/src/main/java/com/odde/doughnut/controllers/RecallPromptController.java (answerSpelling 81–95)
    - backend/src/main/java/com/odde/doughnut/controllers/dto/AnsweredQuestion.java (matchedNotes + from 35–50)
    - backend/src/main/java/com/odde/doughnut/entities/Note.java (getNoteTopology)
    - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java (@Nested AccidentalMatch — especially shouldGradeAsAccidentalMatchWhenWrongAnswerMatchesAnotherReadableNoteTitle and shouldPreferTitleMatchOverAliasMatchWhenBothExist)
    - .planning/phases/03-reveal-both-notes-after-accidental-match/03-PATTERNS.md
    - .planning/phases/03-reveal-both-notes-after-accidental-match/03-RESEARCH.md (Pitfall 1 title∪alias; Pattern 1 findAll then first)
  </read_first>
  <behavior>
    - Accidental title match: answer-spelling returns matchedNotes with one NoteTopology whose id equals secondNote.id and matchedNoteId; outcome ACCIDENTAL_MATCH; overlap still null.
    - Title∪alias: when one note matches by title and another by alias for the same answer string, matchedNotes contains both ids in ascending order; matchedNoteId equals the minimum id (rewrite former title-prefer short-circuit test).
    - findAccidentalMatch(Optional) returns the same note as findAllAccidentalMatches(...).stream().findFirst().
    - Single-title multi-note case already covered by shouldPreferLowestNoteIdWhenMultipleReadableNotesShareTitle: extend asserts so matchedNotes contains both second and third note ids in order and matchedNoteId equals first list entry.
  </behavior>
  <action>
    Write/adjust failing controller assertions FIRST (RED), then implement (GREEN). Tracer covers ONE vertical path: collection → grading → DTO list on the wire.

    1. Tests (RED): In RecallPromptControllerTests.AccidentalMatch:
      a. Flip shouldGradeAsAccidentalMatchWhenWrongAnswerMatchesAnotherReadableNoteTitle: replace assertNull(getMatchedNotes()) with assertThat matchedNotes is non-null, size 1, id equals secondNote.getId(), title present; assert matchedNoteId equals matchedNotes.getFirst().getId().longValue() (per D-02). Keep assertNull(overlap).
      b. Rewrite shouldPreferTitleMatchOverAliasMatchWhenBothExist into union semantics (per D-01 / RESEARCH Pitfall 1): assert both noteA and noteB appear in matchedNotes ids (containsInRelativeOrder by id ascending); matchedNoteId equals Math.min(noteA.id, noteB.id); do not assert title-only short-circuit. Rename the method to reflect union (e.g. shouldIncludeTitleAndAliasMatchesInMatchedNotesOrderedById).
      c. Extend shouldPreferLowestNoteIdWhenMultipleReadableNotesShareTitle to assert matchedNotes ids contain secondNote then thirdNote in order and matchedNoteId equals first topology id.
      d. Flip assertNull(getMatchedNotes()) on other ACCIDENTAL_MATCH success paths (alias + alias-after-trim) to assert non-empty list aligned with matchedNoteId. Leave plain-wrong / blank / IDOR / skip-when-correct assertNull(matchedNotes) unchanged.
      Run backend tests — expect RED (matchedNotes still null / title-prefer expectations).

    2. Implement (GREEN) per D-01 / D-02 / RESEARCH Pattern 1–2:
      a. WikiLinkResolver: add public findAllAccidentalMatches(String answer, Note reviewedNote, User viewer) returning List of Note. Blank/null answer → empty list. Merge title candidates (findByNoteTitleOrderByIdAsc) union aliasAccidentalCandidates; filter notebook not null AND userMayReadNotebook AND id not equal reviewed; dedupe by note id; sort by id ascending; return immutable list. Change findAccidentalMatch to return findAll….stream().findFirst() (keep blank early-empty). Reuse existing readability rules — no new auth API.
      b. MemoryTrackerService.answerSpelling: call findAllAccidentalMatches once when !correct and answer non-blank; if non-empty, set matchedNoteId from matches.getFirst().getId().longValue(), outcome ACCIDENTAL_MATCH, markAsAccidentalMatch; else plain markAsRecalled(false). Prefer a small package-visible result type (e.g. record carrying RecallPrompt plus List of Note matches) so the controller does not re-query — discretion: single lookup is preferred over double findAll.
      c. AnsweredQuestion: add from(RecallPrompt, List&lt;Note&gt; matches) that calls from(prompt) then setMatchedNotes(matches.stream().map(Note::getNoteTopology).toList()) when matches non-empty; when empty/null, leave matchedNotes unset (null). Do not put matchedNotes on Answer.
      d. RecallPromptController.answerSpelling: use the service result to return AnsweredQuestion.from(answered, matches) (or setMatchedNotes after from). Existing assertCanMutateRecallPrompt unchanged.

    3. Confirm full backend suite green. No OpenAPI regen (types already have matchedNotes). No Flyway. No Phase 4 UI.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - WikiLinkResolver.java defines findAllAccidentalMatches and findAccidentalMatch delegates to it (grep -v '^[[:space:]]*//' WikiLinkResolver.java | grep -c 'findAllAccidentalMatches' is at least 2).
    - AccidentalMatch title-match test asserts getMatchedNotes() non-null with matching id; no assertNull(getMatchedNotes()) remains on ACCIDENTAL_MATCH success paths (plain-wrong/IDOR/correct may still assert null).
    - Former title-prefer test now asserts both title and alias notes in matchedNotes (union).
    - shouldPreferLowestNoteIdWhenMultipleReadableNotesShareTitle asserts both notes in matchedNotes and matchedNoteId == first list id.
    - CURSOR_DEV=true nix develop -c pnpm backend:test_only exits 0.
    - git status shows no new/changed files under backend/src/main/resources/db/migration/.
  </acceptance_criteria>
  <done>answer-spelling returns populated matchedNotes for accidental matches; matchedNoteId is first-of-list; findAll is source of truth; AccidentalMatch controller suite green including union rewrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IDOR — matchedNotes never lists unreadable notes</name>
  <precondition>Task 1 green: findAllAccidentalMatches filters with userMayReadNotebook; controller returns matchedNotes on readable accidental matches.</precondition>
  <reversibility rating="reversible">Test-only strengthening of existing IDOR path (T-03-01).</reversibility>
  <files>backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java</files>
  <read_first>
    - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java (shouldNotLeakMatchedNoteIdFromUnreadableNotebook)
    - backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java (findAllAccidentalMatches readability filter)
    - .planning/phases/03-reveal-both-notes-after-accidental-match/03-RESEARCH.md (Security Domain / Pitfall 4)
  </read_first>
  <behavior>
    - When the only title hit is in an unreadable notebook: outcome null, matchedNoteId null, matchedNotes null/empty — unreadable id and title must not appear in matchedNotes.
    - When one readable and one unreadable note share the answer title: matchedNotes contains only the readable note; matchedNoteId is that readable id; unreadable id absent from the list.
  </behavior>
  <action>
    Strengthen IDOR coverage for the promoted matchedNotes list (T-03-01 / ASVS V4):

    1. Keep/confirm shouldNotLeakMatchedNoteIdFromUnreadableNotebook still asserts null outcome, null matchedNoteId, and null/empty matchedNotes when the only candidate is unreadable.

    2. Add shouldOmitUnreadableNotesFromMatchedNotesWhenReadableMatchAlsoExists: create an unreadable note (other user) and a readable note (currentUser) with the SAME title as the spelling answer (wrong for reviewed). Assert ACCIDENTAL_MATCH; matchedNotes size 1 with only the readable id; matchedNoteId equals that id; unreadable id does not appear in matchedNotes stream map of ids.

    One behavior per test; use makeMe builders; no production code change expected if Task 1 filter is correct — if RED, fix filter in findAllAccidentalMatches only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - AccidentalMatch contains shouldOmitUnreadableNotesFromMatchedNotesWhenReadableMatchAlsoExists (or equivalent capability name).
    - shouldNotLeakMatchedNoteIdFromUnreadableNotebook still asserts matchedNotes null/empty.
    - CURSOR_DEV=true nix develop -c pnpm backend:test_only exits 0.
  </acceptance_criteria>
  <done>IDOR proofs cover matchedNotes list: unreadable topologies never leak; mixed readable/unreadable keeps only readable entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/recall-prompts/{id}/answer-spelling | Authenticated mutator; spelling string untrusted |
| AnsweredQuestion JSON → client | matchedNotes id+title must not disclose unreadable notebooks |
| Client → NoteShow / showNote | Per-note read re-auth (defense in depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | WikiLinkResolver.findAllAccidentalMatches / AnsweredQuestion.matchedNotes | high | mitigate | Filter every candidate with userMayReadNotebook before list assembly; Task 2 controller tests prove unreadable ids/titles never appear in matchedNotes |
| T-03-02 | Elevation of Privilege | RecallPromptController.answerSpelling | medium | mitigate | Keep existing assertCanMutateRecallPrompt; no new public endpoint |
| T-03-03 | Tampering | Matched list used later for link write | low | accept | Phase 4 owns writes; Phase 3 is reveal-only (D-06) |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
Full backend suite green; accidental-match responses carry matchedNotes aligned with matchedNoteId; IDOR list tests pass.
</verification>

<success_criteria>
- findAllAccidentalMatches is the source of truth for accidental matches
- ACCIDENTAL_MATCH responses populate matchedNotes (D-01, D-02)
- Unreadable notes never appear in matchedNotes (T-03-01)
</success_criteria>

<output>
Create `.planning/phases/03-reveal-both-notes-after-accidental-match/03-01-SUMMARY.md` when done
</output>
