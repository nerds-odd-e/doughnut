---
phase: 03-reveal-both-notes-after-accidental-match
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/components/recall/AnsweredSpellingQuestion.vue
  - frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts
  - packages/doughnut-test-fixtures/src/AnsweredQuestionBuilder.ts
autonomous: true
requirements: [AM-03]
must_haves:
  truths:
    - "When answer.outcome is ACCIDENTAL_MATCH, the result alert shows distinct copy: Your answer `{spelling}` names another note — not correct for this review. (D-05 / UI-SPEC)."
    - "Plain wrong answers (correct false, outcome not ACCIDENTAL_MATCH) still show exactly: Your answer `{spelling}` is incorrect."
    - "Accidental-match and plain-wrong alerts both use daisy-alert-error (non-success); correct still uses daisy-alert-success."
    - "Reviewed note remains first: NoteUnderQuestion + ViewMemoryTrackerLink + NoteShow(expand-children=false) unchanged in order."
    - "When ACCIDENTAL_MATCH and matchedNotes has length ≥ 1, a Matched note(s) section (data-testid=matched-notes-section) renders one NoteShow per entry with expand-children=false, keys=matched.id, vertical stack with mt-6 and gap-4 (D-03, D-04 / UI-SPEC)."
    - "Matched section omitted when matchedNotes absent/empty; accidental-match alert still shown (defensive empty)."
    - "Zero Phase 3 usages of primary/accent CTA chrome; no Phase 4 add-link controls on this surface (D-06)."
    - statement: "Many matched NoteShows remain reachable via normal page scroll; alert stays above the stack (overflow backstop)."
      verification: backstop
    - statement: "Long NoteShow bodies and long titles inherit existing note layout wrapping without breaking the single-column result stack (overflow/long-text backstops)."
      verification: backstop
  artifacts:
    - path: "frontend/src/components/recall/AnsweredSpellingQuestion.vue"
      provides: "ACCIDENTAL_MATCH alert branch + vertical Matched note(s) NoteShow stack"
      contains: "matched-notes-section"
    - path: "frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts"
      provides: "Vitest coverage for accidental-match vs plain-wrong alert + matched NoteShow props"
      contains: "AnsweredSpellingQuestion"
    - path: "packages/doughnut-test-fixtures/src/AnsweredQuestionBuilder.ts"
      provides: "Fixture helpers for outcome and/or matchedNotes in frontend tests"
      contains: "withMatchedNotes"
  key_links:
    - from: "AnsweredSpellingQuestion.vue"
      to: "answeredQuestion.matchedNotes"
      via: "v-for NoteShow :note-id=matched.id :expand-children=false"
      pattern: "matched-notes-section"
    - from: "AnsweredSpellingQuestion.vue"
      to: "answeredQuestion.answer.outcome"
      via: "alert copy branch ACCIDENTAL_MATCH vs plain incorrect"
      pattern: "ACCIDENTAL_MATCH"
---

## Phase Goal

**As a** learner doing spelling recall, **I want to** see the reviewed note and all matched notes revealed together after an accidental match, **so that** my confusion becomes visible.

<objective>
Update AnsweredSpellingQuestion so an accidental-match result shows distinct miss copy and reveals the reviewed note plus every matched note via full NoteShow in a vertical Matched note(s) stack (AM-03 / D-03 / D-04 / D-05).

Purpose: Make confusion visible in the spelling answer UI using the matchedNotes list from Plan 01 — without shipping Phase 4 link-building (D-06).
Output: Updated Vue component + Vitest specs (+ fixture builder helpers as needed).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-CONTEXT.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-UI-SPEC.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-PATTERNS.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-RESEARCH.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-01-PLAN.md
</context>

<assumption_delta_decision>
UI consumes the promoted matchedNotes list as the primary plural surface (same promote decision as Plan 01). matchedNoteId is not required for Phase 3 reveal layout (list drives NoteShows); keep reading outcome for alert branching.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vitest — accidental-match alert + matched NoteShow stack vs plain wrong</name>
  <precondition>Plan 03-01 complete: AnsweredQuestion.matchedNotes is populated on ACCIDENTAL_MATCH from the API; generated TS types already include matchedNotes and ACCIDENTAL_MATCH.</precondition>
  <reversibility rating="reversible">Presentation-only Vue + tests (D-03–D-05).</reversibility>
  <files>frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts, packages/doughnut-test-fixtures/src/AnsweredQuestionBuilder.ts</files>
  <read_first>
    - frontend/src/components/recall/AnsweredSpellingQuestion.vue
    - frontend/tests/components/recall/AnsweredQuestionComponent.spec.ts
    - packages/doughnut-test-fixtures/src/AnsweredQuestionBuilder.ts
    - .planning/phases/03-reveal-both-notes-after-accidental-match/03-UI-SPEC.md (Copywriting, data-testid, Component Inventory)
    - .planning/phases/03-reveal-both-notes-after-accidental-match/03-PATTERNS.md (AnsweredSpellingQuestion.spec pattern)
  </read_first>
  <behavior>
    - ACCIDENTAL_MATCH fixture: wrapper text includes names another note — not correct for this review; does not use the plain is incorrect string; matched-notes-section exists; each matched NoteShow receives note-id from matchedNotes and expand-children false.
    - Plain wrong fixture (correct false, outcome unset): exact Your answer `{spelling}` is incorrect.; matched-notes-section absent.
    - Multiple matchedNotes: section contains one NoteShow wrapper per id (optional matched-note-{id} testids).
    - Defensive: ACCIDENTAL_MATCH with empty matchedNotes shows accidental alert and omits matched-notes-section.
  </behavior>
  <action>
    Create failing frontend specs FIRST (RED), then implement UI in Task 2. Optionally extend AnsweredQuestionBuilder with withMatchedNotes(NoteTopology[]) and ensure withAnswer can carry outcome ACCIDENTAL_MATCH / matchedNoteId through do() so fixtures stay tidy.

    New file frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts mirroring AnsweredQuestionComponent.spec.ts mount style (helper.component(...).withProps({ answeredQuestion }).mount(); flushPromises; prefer data-testid / text — avoid getByRole).

    Cover D-05 and D-03/D-04 behaviors listed above. Capability-named tests only (no phase numbers in describe/it names). Stub or mock NoteShow/NoteRealmLoader as existing recall specs do if full realm load is heavy — assert props passed into NoteShow.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestion.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - AnsweredSpellingQuestion.spec.ts exists and covers ACCIDENTAL_MATCH vs plain-wrong alert copy and matched-notes-section presence/absence.
    - Spec fails before Task 2 implementation for the right reason (missing section / wrong copy).
  </acceptance_criteria>
  <done>RED Vitest specs encode UI-SPEC reveal + alert contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AnsweredSpellingQuestion — distinct alert + Matched note(s) NoteShow stack</name>
  <precondition>Task 1 specs exist and fail on current AnsweredSpellingQuestion (plain incorrect only; no matched section).</precondition>
  <reversibility rating="reversible">Layout/copy only; Phase 4 can attach actions under matched-notes-section later (D-06).</reversibility>
  <files>frontend/src/components/recall/AnsweredSpellingQuestion.vue</files>
  <read_first>
    - frontend/src/components/recall/AnsweredSpellingQuestion.vue
    - frontend/src/components/notes/NoteShow.vue (props note-id, expand-children)
    - frontend/src/components/notes/Cards.vue (v-for :key on noteTopology.id)
    - frontend/src/components/notebook/NotebookWorkspaceSettings.vue (text-lg font-semibold section heading)
    - .planning/phases/03-reveal-both-notes-after-accidental-match/03-UI-SPEC.md
    - .planning/phases/03-reveal-both-notes-after-accidental-match/03-PATTERNS.md
  </read_first>
  <behavior>
    - Same behaviors as Task 1 — specs must go GREEN.
  </behavior>
  <action>
    Implement AnsweredSpellingQuestion.vue per D-03, D-04, D-05 and 03-UI-SPEC (do not contradict UI-SPEC):

    1. Alert: keep daisy-alert + success/error classes from correct boolean. Branch message text: if outcome === ACCIDENTAL_MATCH use UI-SPEC accidental copy; else if correct use Correct!; else keep exact plain incorrect template string. Optional data-testid accidental-match-alert on the accidental branch.

    2. Keep reviewed stack order: NoteUnderQuestion → ViewMemoryTrackerLink → reviewed NoteShow(expand-children=false).

    3. After reviewed NoteShow, when outcome === ACCIDENTAL_MATCH and matchedNotes has length greater than 0, render section data-testid=matched-notes-section with class mt-6; heading Matched note(s) using text-lg font-semibold (not bare unstyled h3 at 24px); flex flex-col gap-4; v-for matched in matchedNotes with :key=matched.id; optional data-testid matched-note-{id}; NoteShow :note-id=matched.id :expand-children=false. Do not wrap matches in NoteUnderQuestion.

    4. Scope guard (D-06): reveal-only — no add-link / preselection / relationship finalize controls; reserve primary accent for Phase 4 (zero daisy-btn-primary / accent badges on this stack).

    5. Empty defensive: if ACCIDENTAL_MATCH but no matchedNotes, omit section; keep accidental alert (optional muted one-liner only if a test requires it — UI-SPEC empty body).

    Run Task 1 spec until GREEN.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestion.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - AnsweredSpellingQuestion.vue contains matched-notes-section and ACCIDENTAL_MATCH alert branch.
    - Plain incorrect string remains for non-accidental wrong answers.
    - CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestion.spec.ts exits 0.
    - Component file does not import or render add-link / relationship-finalize UI widgets (D-06).
  </acceptance_criteria>
  <done>Spelling answer UI reveals reviewed + matched NoteShows with distinct accidental-match alert; plain wrong unchanged; Vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API AnsweredQuestion → Vue props | Client renders matchedNotes ids; must not invent note ids |
| Vue → NoteShow / showNote | Each reveal re-authorizes server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | matchedNotes rendered as NoteShow | high | mitigate | Only render ids from server matchedNotes; NoteShow/showNote assertReadAuthorization (defense in depth). Backend Plan 01 already filters list. |
| T-03-04 | Tampering / scope | AnsweredSpellingQuestion | medium | mitigate | D-06 — no link-write controls this phase; structure matched-notes-section for Phase 4 hooks only |
| T-03-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
Frontend AnsweredSpellingQuestion.spec.ts green; UI matches 03-UI-SPEC alert + vertical stack contracts.
</verification>

<success_criteria>
- Distinct ACCIDENTAL_MATCH alert (D-05)
- Vertical Matched note(s) full NoteShow stack (D-03, D-04)
- No Phase 4 add-link UI (D-06)
- Plain wrong copy unchanged
</success_criteria>

<output>
Create `.planning/phases/03-reveal-both-notes-after-accidental-match/03-02-SUMMARY.md` when done
</output>
