---
phase: 03-reveal-both-notes-after-accidental-match
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - e2e_test/features/recall/accidental_match_reveal.feature
  - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
  - e2e_test/step_definitions/recall.ts
autonomous: false
requirements: [AM-03]
must_haves:
  truths:
    - "E2E: after typing another readable note title as a spelling answer, the user sees the accidental-match result messaging and both the reviewed note and the matched note content on the spelling result surface."
    - "E2E page object distinguishes accidental-match messaging from the plain Your answer … is incorrect. expectation (no regression of plain-wrong steps)."
    - "Feature is capability-named accidental_match_reveal (no phase numbers in product/test artifact names)."
    - statement: "When multiple notes match, the E2E or existing controller coverage already proves all appear; this plan's E2E may use a single matched note for the happy path and rely on Plan 01 multi-match controller proofs for more than one match."
      verification: backstop
  artifacts:
    - path: "e2e_test/features/recall/accidental_match_reveal.feature"
      provides: "Capability-named Cypress/Cucumber scenario for accidental-match reveal"
      contains: "accidental match"
    - path: "e2e_test/start/pageObjects/AnsweredQuestionPage.ts"
      provides: "Expectations for accidental-match alert + matched-notes-section visibility"
      contains: "matched-notes-section"
    - path: "e2e_test/step_definitions/recall.ts"
      provides: "Steps wiring the accidental-match reveal scenario"
      contains: "accidental"
  key_links:
    - from: "accidental_match_reveal.feature"
      to: "AnsweredSpellingQuestion UI"
      via: "answer spelling with other note title → assert alert + matched-notes-section + note content"
      pattern: "matched-notes-section"
---

## Phase Goal

**As a** learner doing spelling recall, **I want to** see the reviewed note and all matched notes revealed together after an accidental match, **so that** my confusion becomes visible.

<objective>
Add a capability-named E2E scenario that proves the full accidental-match reveal path through the running app (AM-03), with page-object expectations that do not break plain-wrong spelling steps.

Purpose: UI hint is yes — close the phase with observable end-to-end coverage after backend list + Vue stack land.
Output: accidental_match_reveal.feature (+ page object / steps); tagged @wip until green, then untag.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-CONTEXT.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-UI-SPEC.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-PATTERNS.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-VALIDATION.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-01-PLAN.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-02-PLAN.md
e2e_test/features/recall/recall_quiz_spelling_question.feature
e2e_test/start/pageObjects/AnsweredQuestionPage.ts
e2e_test/step_definitions/recall.ts
</context>

<assumption_delta_decision>
E2E asserts the promoted matchedNotes reveal surface (section + note content), not merely matchedNoteId in network payloads.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2E feature + page object for accidental-match reveal (@wip)</name>
  <precondition>Plans 03-01 and 03-02 are complete and unit/controller coverage is green; pnpm sut (or equivalent) can serve the app for Cypress. Phase 2 accidental grading is live in the environment under test.</precondition>
  <reversibility rating="reversible">Test-only E2E artifacts; product behavior already shipped in prior waves.</reversibility>
  <files>e2e_test/features/recall/accidental_match_reveal.feature, e2e_test/start/pageObjects/AnsweredQuestionPage.ts, e2e_test/step_definitions/recall.ts</files>
  <read_first>
    - e2e_test/features/recall/recall_quiz_spelling_question.feature
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts (expectSpellingAnswerToBeIncorrect)
    - e2e_test/step_definitions/recall.ts
    - .cursor/rules/e2e-authoring.mdc (if present) / 03-PATTERNS.md E2E analogs
    - .planning/phases/03-reveal-both-notes-after-accidental-match/03-UI-SPEC.md (copy + testids)
  </read_first>
  <behavior>
    - Given a reviewed spelling note and a different readable note with a distinct title, when the user answers with the other note title, then the result shows accidental-match messaging (names another note) and shows matched-notes-section with the matched note visible, and the reviewed note remains visible.
    - Plain-wrong spelling step still uses expectSpellingAnswerToBeIncorrect with the unchanged incorrect copy — add a separate expectation method for accidental match (do not overload plain incorrect).
  </behavior>
  <action>
    1. Add e2e_test/features/recall/accidental_match_reveal.feature (capability-named; no phase numbers). Tag the new scenario @wip until it passes. Seed via existing recall/note helpers mirroring spelling quiz features: two notes owned/readable by the test user; answer with the other note title.

    2. Extend AnsweredQuestionPage.ts with expectAccidentalMatchReveal (or similar): assert accidental-match alert text from UI-SPEC; assert data-testid matched-notes-section visible; assert reviewed note still present. Do not change the exact string assertion used by expectSpellingAnswerToBeIncorrect.

    3. Wire Then steps in recall.ts to the new page-object methods.

    4. Run targeted Cypress: CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature (with sut already running). Fix product only if a genuine gap remains after 03-01/03-02; otherwise fix test seeding/selectors.

    5. When green, remove @wip from the scenario (CI cap: ≤5 @wip).

    Scope: one happy-path single-match E2E is enough; multi-match remains proven by Plan 01 controller tests. No Phase 4 add-link assertions (D-06).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature</automated>
  </verify>
  <acceptance_criteria>
    - accidental_match_reveal.feature exists under e2e_test/features/recall/ and has no phase-number tokens in the filename or scenario title.
    - AnsweredQuestionPage has a dedicated accidental-match expectation separate from expectSpellingAnswerToBeIncorrect.
    - Scenario passes without @wip (or @wip removed after green).
    - Targeted cypress run for the feature exits 0.
  </acceptance_criteria>
  <done>Capability-named E2E proves reviewed + matched notes reveal after accidental match; plain-wrong E2E expectations untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human spot-check accidental-match reveal stack</name>
  <what-built>
    Backend populates matchedNotes; AnsweredSpellingQuestion shows distinct alert + vertical Matched note(s) NoteShow stack; E2E covers the happy path.
  </what-built>
  <how-to-verify>
    1. With the app running, open a spelling recall for note A.
    2. Submit the exact title of a different readable note B.
    3. Confirm alert says the answer names another note (not the plain is incorrect string).
    4. Confirm reviewed note A content is shown, then Matched note(s) with note B NoteShow below.
    5. Confirm no add-link / preselection controls appear (Phase 4).
    6. Optionally submit a nonsense wrong answer and confirm plain incorrect copy still appears with no matched section.
  </how-to-verify>
  <resume-signal>Type "approved" or describe UI issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser E2E → app APIs | Same auth session as product; no new trust boundary |
| Rendered matchedNotes | Must reflect server-filtered list only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | E2E reveal surface | high | mitigate | Scenario uses only notes the user can read; do not assert titles from unreadable notebooks |
| T-03-04 | Tampering / scope | E2E assertions | low | mitigate | Do not assert Phase 4 link UI presence |
| T-03-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
Targeted accidental_match_reveal Cypress feature green without @wip; human verify approved.
</verification>

<success_criteria>
- End-to-end AM-03 reveal observable in the app
- Plain-wrong E2E path not regressed
- D-06: no add-link UI in the reveal surface
</success_criteria>

<output>
Create `.planning/phases/03-reveal-both-notes-after-accidental-match/03-03-SUMMARY.md` when done
</output>
