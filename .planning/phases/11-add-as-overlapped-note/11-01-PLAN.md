---
phase: 11-add-as-overlapped-note
plan: 01
type: execute
wave: 1
status: done
depends_on: []
files_modified:
  - frontend/src/components/recall/AccidentalMatchResolveDialog.vue
  - frontend/src/components/recall/AccidentalMatchResolveRow.vue
  - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
  - frontend/tests/components/recall/AnsweredSpellingQuestionAddAsOverlapped.spec.ts
autonomous: true
requirements:
  - AMR-08
  - AMR-09
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "From an opened Resolve dialog, each eligible match row shows Add as overlapped note with data-testid add-as-overlapped-note-{id} beside Build a link when realms are writable and loaded (D-01, D-08, D-09, D-10; AMR-08; UI empty/loading/populated)"
    - "Clicking Add as overlapped note runs appendOverlapWikiLinkToNoteContent on the reviewed note then updateTextField(reviewedNoteId, edit content, …) with a wiki-link token; Modal stays on the list step (D-02, D-03, D-04, D-10; AMR-08)"
    - "After successful declare, accidental-match alert remains; overlap-try-again and overlap-try-again-alert stay absent; wrapper does not emit retry; answer stays ACCIDENTAL_MATCH (D-06, D-07, D-10; AMR-09; UI populated post-declare)"
    - "Add as overlapped note is omitted (same shared mutating gate as Build a link) when reviewed notebook is readonly or reviewed/matched realms required for the write are not loaded; titles and path still show when available (D-08, D-09, D-10; AMR-07 reuse; UI empty/partial)"
    - "When appendOverlapWikiLinkToNoteContent returns null, updateTextField is not called; list and ACCIDENTAL_MATCH chrome stay unchanged with silent no-op (D-05, D-10; UI error null-append)"
  artifacts:
    - path: frontend/src/components/recall/AccidentalMatchResolveDialog.vue
      provides: "Shared canOfferMutatingAction gate; host addAsOverlappedNote handler; appendOverlapWikiLinkToNoteContent → conditional updateTextField; stay on list"
    - path: frontend/src/components/recall/AccidentalMatchResolveRow.vue
      provides: "Gated peer CTA row Build a link then Add as overlapped note; emit addAsOverlapped; add-as-overlapped-note-{id}"
    - path: frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
      provides: "Boundary Vitest: writable CTA+wiki-link write+no try-again; readonly/unload omit; null-append no update (D-10)"
  key_links:
    - from: AccidentalMatchResolveRow Add as overlapped note
      to: AccidentalMatchResolveDialog.addAsOverlappedNote
      via: "@add-as-overlapped with matched.id when canMutate true (D-01, D-03)"
    - from: addAsOverlappedNote
      to: appendOverlapWikiLinkToNoteContent → storedApi.updateTextField
      via: "reviewed content + matched topology/notebook; null → stop; string → edit content on reviewedNoteId (D-02, D-05)"
    - from: canOfferMutatingAction
      to: both Build a link and Add as overlapped note
      via: "Generalize canOfferBuildLink; currentUser + reviewedRealm readonly + matched realm presence (D-08, D-09)"
---

<objective>
Prove AMR-08 and AMR-09 at the Vitest boundary: per-row Add as overlapped note declares a wiki-link overlap on the reviewed note via Phase 10 util + updateTextField, stays on the accidental-match list/result, and never shows try-again or emits retry (D-01..D-10).

Purpose: Deliver Phase 11 Behavior stop-safely before E2E — unit evidence first (same wave shape as Phase 9).
Output: Shared mutating gate + gated row CTA + host declare handler; AccidentalMatch Vitest green for declare + gates + no try-again.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-add-as-overlapped-note/11-CONTEXT.md
@.planning/phases/11-add-as-overlapped-note/11-RESEARCH.md
@.planning/phases/11-add-as-overlapped-note/11-PATTERNS.md
@.planning/phases/11-add-as-overlapped-note/11-UI-SPEC.md
@.planning/phases/11-add-as-overlapped-note/11-VALIDATION.md
@.cursor/rules/frontend-testing.mdc
@.cursor/rules/unit-testing.mdc
@.cursor/rules/planning.mdc
</context>

## Artifacts this phase produces

- Shared mutating-action gate on AccidentalMatchResolveDialog feeding both peer CTAs
- Per-row gated **Add as overlapped note** CTA on AccidentalMatchResolveRow (`add-as-overlapped-note-{id}`)
- Host declare path: `appendOverlapWikiLinkToNoteContent` → conditional `updateTextField`
- Vitest boundary coverage for declare + AMR-07 gates + AMR-09 no try-again (capability-named; no phase numbers)

## UI Considerations (from 11-UI-SPEC)

Lifted into must_haves / tasks: empty = omit both mutating CTAs when gated (no unavailable copy); loading = progressive hide until realms loaded (no skeleton); error null-append = silent stay on list; populated = peer row Build a link then Add as overlapped note, click stays on list; post-declare = accidental-match alert + Resolve CTA remain, Modal on list; partial = path and CTAs may appear independently; overflow = existing Modal scroll + flex-wrap peer row; zero-one-many = same per-row peer CTAs; long-text = fixed **Add as overlapped note** label.

## Multi-Source Coverage (this plan)

| SOURCE | ID | Item | Status |
|--------|-----|------|--------|
| GOAL | — | Declare overlap from dialog; no try-again / reclaim | COVERED (with 11-02 for E2E) |
| REQ | AMR-08 | Add as overlapped note + wiki-link persist | COVERED (Vitest; E2E in 11-02) |
| REQ | AMR-09 | No try-again / schedule unchanged | COVERED (Vitest chrome + no retry; E2E in 11-02) |
| RESEARCH | — | Host handler; shared gate; content-only; zero new packages | COVERED |
| CONTEXT | D-01..D-10 | CTA, persist, host, stay, null, no retry, gates, Vitest | COVERED |
| CONTEXT | D-11 | Targeted E2E | Deferred to 11-02 |

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: Add as overlapped note persist without try-again</name>
  <reversibility rating="costly">D-06 content-only declare — coupling this path to emit retry or OVERLAP try-again chrome would violate ADR 0003 and corrupt the ACCIDENTAL_MATCH session narrative; flagged so executors keep AnsweredSpellingQuestion outcome chrome untouched.</reversibility>
  <files>frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts, frontend/src/components/recall/AccidentalMatchResolveDialog.vue, frontend/src/components/recall/AccidentalMatchResolveRow.vue</files>
  <read_first>
    - frontend/src/components/recall/AccidentalMatchResolveDialog.vue
    - frontend/src/components/recall/AccidentalMatchResolveRow.vue
    - frontend/src/components/recall/MatchedNoteLinkOffer.vue
    - frontend/src/components/recall/AnsweredSpellingQuestion.vue
    - frontend/src/utils/appendOverlapWikiLinkToNoteContent.ts
    - frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts
    - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
    - frontend/tests/components/recall/MatchedNoteLinkOffer.spec.ts
    - .planning/phases/11-add-as-overlapped-note/11-CONTEXT.md (D-01..D-10)
    - .planning/phases/11-add-as-overlapped-note/11-PATTERNS.md (host declare + row CTA + Vitest stub)
    - .planning/phases/11-add-as-overlapped-note/11-UI-SPEC.md (peer CTA row + testids + copy)
    - .planning/phases/11-add-as-overlapped-note/11-RESEARCH.md (Pattern 1–3; Vitest focus)
  </read_first>
  <behavior>
    - Canonical ACCIDENTAL_MATCH with two matches, currentUser + seedRealms for reviewed + matched A/B: open Resolve; document.body shows add-as-overlapped-note-* buttons labeled Add as overlapped note beside Build a link (D-01, D-08, D-10; UI populated)
    - Click add-as-overlapped-note for match id 10: TextContentController.updateNoteContent (via updateTextField) called once with path.note = reviewed id and body.content containing a [[ wiki-link token (D-02, D-10; AMR-08)
    - After success: accidental-match-resolve-dialog list still present; accidental-match-alert still present; overlap-try-again and overlap-try-again-alert absent; wrapper.emitted("retry") undefined (D-04, D-06, D-07, D-10; AMR-09)
    - Prefer one canonical case for writable CTA + persist + stay + no try-again; sibling gate and null-append deltas land in the next task
  </behavior>
  <action>
    Per D-10, write failing Vitest at the AnsweredSpellingQuestion boundary first (RED): mount with currentUser and seedRealms via mountAnsweredSpellingQuestion; mockSdkService TextContentController.updateNoteContent; openResolveAccidentalMatch; assert Add as overlapped note CTAs; click add-as-overlapped-note-10; assert update spy called with reviewed note id and wiki-link token in content; assert list + accidental-match-alert remain and retry/try-again chrome stay absent. Then implement per D-01..D-04, D-06, D-09: generalize canOfferBuildLink to a shared canOfferMutatingAction (or equivalent one helper) passed as one canMutate prop to AccidentalMatchResolveRow for both CTAs (D-08, D-09); on AccidentalMatchResolveRow add peer CTA row flex flex-wrap gap-2 with Build a link first then Add as overlapped note — face/title/aria-label Add as overlapped note, data-testid add-as-overlapped-note-{id}, daisy-btn daisy-btn-secondary daisy-btn-sm, emit addAsOverlapped (D-01; UI-SPEC); host addAsOverlappedNote loads reviewed + matched realms, calls appendOverlapWikiLinkToNoteContent with matched topology/notebook and reviewed notebookId, and on non-null string awaits updateTextField(reviewedNoteId, "edit content", composed) (D-02, D-03); stay on list step — skip closeDialogThen and skip step swap (D-04). Call appendOverlapWikiLinkToNoteContent only — leave plain-title appendAliasToNoteContent out of this path (D-02 / Pitfall 5). Keep mutate handler in AccidentalMatchResolveDialog; leave AnsweredSpellingQuestion outcome chrome and emit("retry") wiring unchanged (D-03, D-06). Zero new packages. Capability-named testids only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - AccidentalMatchResolveDialog hosts addAsOverlappedNote using appendOverlapWikiLinkToNoteContent then conditional updateTextField on reviewedNoteId
    - AccidentalMatchResolveRow renders gated Add as overlapped note with add-as-overlapped-note-{id} beside Build a link under shared canMutate
    - Vitest: writable+seeded open dialog shows Add as overlapped note; click updates reviewed content with [[ token; list + accidental-match-alert remain; overlap-try-again / overlap-try-again-alert absent; retry emit undefined
    - AnsweredSpellingQuestion.vue unchanged for outcome chrome; MatchedNoteLinkOffer.vue left as reference only
  </acceptance_criteria>
  <done>Happy path AMR-08/AMR-09 at Vitest: declare persists wiki-link on reviewed note; stays on list; ACCIDENTAL_MATCH chrome without try-again (D-01..D-04, D-06, D-07, D-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Expand Vitest: AMR-07 gates and null-append no-op</name>
  <files>frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts, frontend/src/components/recall/AccidentalMatchResolveDialog.vue</files>
  <read_first>
    - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
    - frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts
    - frontend/src/components/recall/AccidentalMatchResolveDialog.vue
    - frontend/src/utils/appendOverlapWikiLinkToNoteContent.ts
    - .planning/phases/11-add-as-overlapped-note/11-RESEARCH.md (Pattern 2; Pitfalls 3–4)
    - .planning/phases/11-add-as-overlapped-note/11-CONTEXT.md (D-05, D-08, D-09, D-10)
    - .planning/phases/11-add-as-overlapped-note/11-UI-SPEC.md (empty/loading/error null-append)
  </read_first>
  <behavior>
    - Readonly delta: reviewedRealm.notebookRealm.readonly = true with currentUser + seedRealms; open Resolve; zero add-as-overlapped-note-* CTAs (and Build a link still omitted via same gate); titles/path may still show (D-08, D-09, D-10; UI empty)
    - Unloaded delta: never-settling showNote mock and no seedRealms; open Resolve; zero Add as overlapped note CTAs (D-08, D-09, D-10; UI loading)
    - Null-append delta: seed reviewed content that already includes the overlap wiki-link alias for the clicked match; click Add as overlapped note; updateNoteContent spy not called; list stays (D-05, D-10; UI error null-append)
    - Assert only gate / null deltas — do not re-assert full writable success payload in each sibling case
  </behavior>
  <action>
    Per D-08 / D-09 / D-10, add focused Vitest deltas (RED then green): (1) mutate reviewedRealm.notebookRealm.readonly = true before seed/mount; open Resolve; expect zero add-as-overlapped-note-* on document.body; (2) mockSdkServiceWithImplementation NoteController.showNote never-settling Promise and omit seedRealms; open Resolve; expect zero Add as overlapped note CTAs; (3) fixture reviewed content already containing the overlap wiki-link for the target match via makeMe; click CTA; expect update spy call count stays 0 and list remains (D-05 — silent; prefer real content fixture over mocking the util). Confirm canOfferMutatingAction is the single helper feeding both CTAs so gates cannot drift (D-09). Prefer hide via v-if canMutate — skip disabled-looking CTAs (D-08). Keep overlap try-again OVERLAP cases untouched. Small-test style: AnsweredSpellingQuestion boundary; focused asserts; capability-named tests.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec has a readonly case with zero add-as-overlapped-note-* after open Resolve
    - Spec has an unloaded-realm case with zero Add as overlapped note CTAs after open Resolve
    - Spec has a null-append case where updateNoteContent is not called after click
    - AccidentalMatch Vitest file fully green including Phase 7–9 cases and Plan 01 tracer declare case
  </acceptance_criteria>
  <done>AMR-07 reuse + D-05 unit side complete: Add as overlapped note omitted when readonly/unloaded; null-append skips updateTextField (D-05, D-08..D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Result chrome → Resolve Modal | Authenticated learner opens dialog over graded ACCIDENTAL_MATCH matches |
| Dialog gate → Add as overlapped note CTA | Client uses currentUser + notebookRealm.readonly + realm load presence before offering mutate UI |
| Declare handler → content API | appendOverlapWikiLinkToNoteContent output → updateTextField on **reviewed** note only; backend enforces notebook write authz |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | addAsOverlappedNote / updateTextField | high | mitigate | Always mutate props.reviewedNoteId reviewed content; match supplies wiki-link target only (D-02, D-03); Vitest asserts path.note equals reviewed id |
| T-11-02 | Elevation of Privilege | Declare vs OVERLAP try-again / SRS | high | mitigate | Content mutation only (D-06); leave AnsweredSpellingQuestion outcome chrome alone; Vitest asserts absent overlap-try-again* and undefined retry emit (D-07; AMR-09) |
| T-11-03 | Elevation of Privilege | Add as overlapped CTA / canOfferMutatingAction | high | mitigate | Hide CTA when readonly or realms unloaded (D-08, D-09); server-side write authz unchanged on updateNoteContent |
| T-11-04 | Tampering | Alias token shape | medium | mitigate | Call appendOverlapWikiLinkToNoteContent only (D-02); Vitest asserts body.content contains [[ wiki-link token |
| T-11-05 | Tampering / XSS | Match titles / CTA labels | medium | mitigate | Reuse NoteTitleWithLink + text CTA labels; no v-html for titles |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — reuse-only; Package Legitimacy N/A |
</threat_model>

<verification>
AccidentalMatch Vitest file green with Nix prefix. Manual visual optional; automated declare + gate + no-try-again asserts are the Plan 01 gate.
</verification>

<success_criteria>
- Add as overlapped note persists wiki-link on reviewed note; Modal stays on list; alert stays
- Try-again / retry absent after declare; ACCIDENTAL_MATCH chrome remains
- CTA omitted for readonly and unloaded-realm cases; null-append skips update
- Vitest proves AMR-08 + AMR-09 (+ AMR-07 reuse) at the answered-question boundary (D-01..D-10)
</success_criteria>

<output>
Create `.planning/phases/11-add-as-overlapped-note/11-01-SUMMARY.md` when done
</output>
