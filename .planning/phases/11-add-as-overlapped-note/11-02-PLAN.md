---
phase: 11-add-as-overlapped-note
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
  - e2e_test/features/recall/accidental_match_reveal.feature
  - e2e_test/step_definitions/recall.ts
autonomous: true
requirements:
  - AMR-08
  - AMR-09
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Accidental-match E2E can open Resolve then Add as overlapped note for a matched title and remain on the accidental-match result (D-11; AMR-08)"
    - "After Add as overlapped note, overlap-try-again and overlap-try-again-alert are absent; accidental-match alert / Resolve chrome remain (D-07, D-11; AMR-09; UI post-declare)"
    - "Page object openAddAsOverlappedNote uses Resolve → add-as-overlapped-note-* — not stacked matched-notes section; prefer page-object + thin When step over rewriting existing Gherkin (D-11)"
    - "overlap_try_again.feature stays green and uncoupled from declare (D-11)"
  artifacts:
    - path: e2e_test/start/pageObjects/AnsweredQuestionPage.ts
      provides: "openAddAsOverlappedNote + reuse expectStillOnAccidentalMatchResult; no-try-again asserts"
    - path: e2e_test/features/recall/accidental_match_reveal.feature
      provides: "Capability-named Add as overlapped stay-on-result scenario (no phase numbers)"
    - path: e2e_test/step_definitions/recall.ts
      provides: "Thin When step delegating to openAddAsOverlappedNote"
  key_links:
    - from: AnsweredQuestionPage.openAddAsOverlappedNote
      to: resolve-accidental-match → add-as-overlapped-note-*
      via: "waitUntilAppIsNotBusy after Resolve; Add as overlapped note label; stay on list (D-04, D-11)"
    - from: E2E post-success asserts
      to: accidental-match-alert + absent overlap-try-again*
      via: "expectStillOnAccidentalMatchResult + try-again absence (D-07, D-11; AMR-09)"
---

<objective>
Close AMR-08 and AMR-09 with targeted E2E: page-object Resolve → Add as overlapped note, stay on accidental-match result without try-again, keep overlap_try_again uncoupled (D-11).

Purpose: Accept the declare Behavior after Plan 01 Vitest wiring.
Output: AnsweredQuestionPage helper + reveal-feature scenario + thin When step; targeted Cypress green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-add-as-overlapped-note/11-CONTEXT.md
@.planning/phases/11-add-as-overlapped-note/11-RESEARCH.md
@.planning/phases/11-add-as-overlapped-note/11-PATTERNS.md
@.planning/phases/11-add-as-overlapped-note/11-UI-SPEC.md
@.planning/phases/11-add-as-overlapped-note/11-01-PLAN.md
@.cursor/rules/e2e-authoring.mdc
</context>

## Artifacts this phase produces

- Page-object path: Resolve → Add as overlapped note → stay on accidental-match (AMR-08/AMR-09 E2E)
- Capability-named Gherkin scenario on accidental_match_reveal.feature (no @wip once green; no phase numbers)

## UI Considerations (from 11-UI-SPEC)

E2E covers populated Add as overlapped + post-declare stay-on-result without try-again; AMR-07 gates remain Vitest-only (Plan 01); AMR-05 reopen stays Phase 12; silent null-append not required in E2E this wave.

## Multi-Source Coverage (this plan)

| SOURCE | ID | Item | Status |
|--------|-----|------|--------|
| REQ | AMR-08 | E2E Resolve → Add as overlapped | COVERED |
| REQ | AMR-09 | E2E no try-again after declare | COVERED |
| CONTEXT | D-11 | Targeted E2E via page-object | COVERED |
| CONTEXT | D-04 / D-07 | Stay on list/result; no try-again | COVERED |

<tasks>

<task type="auto">
  <name>E2E page object: Resolve then Add as overlapped note</name>
  <reversibility rating="reversible">Page-object path only; undo is revert of openAddAsOverlappedNote helper (D-11).</reversibility>
  <files>e2e_test/start/pageObjects/AnsweredQuestionPage.ts</files>
  <read_first>
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/step_definitions/recall.ts
    - .planning/phases/11-add-as-overlapped-note/11-PATTERNS.md (E2E page-object section)
    - .planning/phases/11-add-as-overlapped-note/11-RESEARCH.md (Wave 2 sketch)
    - .planning/phases/09-build-a-link-from-resolve-dialog/09-02-SUMMARY.md
  </read_first>
  <action>
    Per D-11, add openAddAsOverlappedNote(matchedNoteTitle) mirroring openLinkToMatchedNote: click resolve-accidental-match, waitUntilAppIsNotBusy, assert accidental-match-resolve-dialog contains matchedNoteTitle, within dialog click visible add-as-overlapped-note-* labeled Add as overlapped note, waitUntilAppIsNotBusy. Reuse expectStillOnAccidentalMatchResult for stay-on-result; after declare also assert overlap-try-again and overlap-try-again-alert do not exist (D-07). Prefer page-object behavior change; leave existing Build a link helpers intact. Skip stacked matched-notes-section asserts. Capability-named testids only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature</automated>
  </verify>
  <acceptance_criteria>
    - openAddAsOverlappedNote opens Resolve then clicks Add as overlapped note via add-as-overlapped-note-*
    - Post-success path can assert accidental-match stay chrome plus absent overlap-try-again / overlap-try-again-alert
    - Existing non-declare accidental_match_reveal scenarios still green under targeted Cypress (assume pnpm sut running)
  </acceptance_criteria>
  <done>Page object drives Add as overlapped note from resolve dialog; stay + no-try-again asserts ready (D-04, D-07, D-11).</done>
</task>

<task type="auto">
  <name>E2E scenario: Add as overlapped stays without try-again</name>
  <files>e2e_test/features/recall/accidental_match_reveal.feature, e2e_test/step_definitions/recall.ts</files>
  <read_first>
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/features/recall/overlap_try_again.feature
    - e2e_test/step_definitions/recall.ts
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
    - .planning/phases/11-add-as-overlapped-note/11-CONTEXT.md (D-11)
    - .planning/phases/11-add-as-overlapped-note/11-PATTERNS.md (Gherkin + When sketch)
  </read_first>
  <action>
    Per D-11, extend accidental_match_reveal.feature with one capability-named scenario (no phase numbers): after accidental-match reveal for spelling answer sedation / reviewed sedition / matched sedation, When I add the matched note "sedation" as overlapped from the accidental match result, Then still on accidental match result, And should not see overlap try-again on the accidental match result. Wire a thin When in recall.ts that calls openAddAsOverlappedNote. Prefer @wip only while red, then remove @wip once green. Leave overlap_try_again.feature untouched; after green run targeted Cypress on overlap_try_again.feature to prove uncoupled. Skip AMR-05 reopen polish (Phase 12).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature,e2e_test/features/recall/overlap_try_again.feature</automated>
  </verify>
  <acceptance_criteria>
    - accidental_match_reveal.feature includes Add as overlapped stay-without-try-again scenario without @wip when green
    - recall.ts When step delegates to openAddAsOverlappedNote
    - Targeted Cypress for accidental_match_reveal and overlap_try_again both exit 0
    - overlap_try_again.feature file content unchanged
  </acceptance_criteria>
  <done>AMR-08/AMR-09 E2E complete: declare stays on accidental-match without try-again; overlap suite uncoupled (D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress → app UI | E2E drives authenticated recall; asserts Plan 01 declare + stay chrome |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | E2E declare write path | high | mitigate | Relies on Plan 01 reviewed-note updateTextField; E2E stays on ACCIDENTAL_MATCH result after Add as overlapped |
| T-11-02 | Elevation of Privilege | E2E try-again after declare | high | mitigate | Scenario asserts overlap-try-again* absent; overlap_try_again.feature remains separate and green |
| T-11-03 | Elevation of Privilege | E2E mutate on readonly | high | mitigate | Readonly gate covered in Plan 01 Vitest; E2E uses writable learner notebooks |
| T-11-SC | Tampering | npm installs | high | accept | No package installs in this plan |
</threat_model>

<verification>
Targeted accidental_match_reveal + overlap_try_again Cypress green with Nix prefix (pnpm sut assumed running). Full E2E suite not required locally.
</verification>

<success_criteria>
- Page object opens Resolve → Add as overlapped note
- E2E scenario green: stay on accidental-match without try-again
- overlap_try_again remains green and uncoupled
- AMR-08 + AMR-09 fully evidenced (Vitest Plan 01 + E2E Plan 02)
</success_criteria>

<output>
Create `.planning/phases/11-add-as-overlapped-note/11-02-SUMMARY.md` when done
</output>
