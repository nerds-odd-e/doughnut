---
phase: 02-cancel-refinement-layout-generation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/tests/components/recall/noteRefinementTestSupport.ts
  - frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
autonomous: true
requirements:
  - CANC-01
  - CANC-02
  - CANC-03
  - CANC-04
  - REFN-01
  - REFN-02
must_haves:
  truths:
    - statement: "While initial layout generation is pending, the global blocker shows AI is generating layout... and an accessible Cancel control."
      verification: "Vitest pending-gate asserts loadingModalMask, message text, and getByText(Cancel)."
    - statement: "Cancel is visible for layout generation only because that call opts into cancelable blocking; extract-preview and create-note paths are not asserted cancelable here."
      verification: "Pending layout Cancel present; scope-boundary tests leave extract/create options unchanged (existing loading specs remain green in later waves)."
    - statement: "Activating Cancel removes the layout blocker promptly, shows no error toast, does not emit contentUpdated, and leaves the Refine-note dialog path open."
      verification: "After Cancel: mask null; toast.error not called; wrapper.emitted(contentUpdated) absent; NoteRefinement still mounted."
    - statement: "After cancel (and after empty layout), data-test-id retry-refinement-layout is present and starts a fresh layout request."
      verification: "Post-cancel find retry-refinement-layout; click increments generateRefinementSuggestions call count."
    - statement: "Double Cancel / repeated activation is idempotent — no throw, no second toast, layout stays empty."
      verification: "Click Cancel twice after pending; still cancelled UI; no extra toast."
    - statement: "Cancelling layout leaves any older concurrent loading state intact (message and mask survive)."
      verification: "Older blocker started first; after layout Cancel its message remains visible."
    - statement: "Late settlement after cancel does not populate refinement-layout items."
      verification: "resolve deferred mock after Cancel; refinement-layout absent."
    - statement: "Zero items after cancel, empty success, or cleared failure render refinement-layout-empty with Ask AI to retry; dialog remains open."
      verification: "Post-cancel empty panel + retry control; populated panel absent."
    - statement: "Ordinary API failures keep existing toast path; accepted cancel renders no error UI."
      verification: "Cancel path: toast.error not called; separate failure path not required in this RED plan beyond silence on cancel."
    - statement: "Happy path with one or many items keeps existing refinement-layout list; empty panel absent."
      verification: "Covered when implementation lands; RED suite focuses cancel path first, with optional completed-items case if helper supports it."
    - statement: "Existing max-h modal overflow strategy remains; empty panel is compact."
      verification: "backstop — no new scroll strategy; assert empty panel exists without inventing overflow assertions."
    - statement: "Fixed copy Cancel and Ask AI to retry; loading message uses AI is generating layout..."
      verification: "Exact string assertions in cancel spec."
    - statement: "After cancel, no selected checkboxes / no extract actions until a populated layout returns."
      verification:
        backstop: true
        held_out: "Assert extract-refinement-layout absent and no selected layout item checkboxes after cancel."
  artifacts:
    - path: frontend/tests/components/recall/noteRefinementTestSupport.ts
      provides: "Pending-layout mount, Cancel click, and retry-layout helpers without flushing the layout request."
    - path: frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
      provides: "Failing observable cancel+retry product evidence for REFN-01/02 and CANC-01–04."
  key_links:
    - from: NoteRefinement.layoutGeneration.cancel.spec.ts
      to: noteRefinementTestSupport.ts
      via: "Deferred gate installed before mount; Cancel via teleported overlay; retry via data-test-id."
    - from: cancel spec
      to: GlobalApiLoadingModal harness
      via: "Existing NoteRefinementWithGlobalLoading composition surfaces selected-blocker Cancel."
---

## Phase Goal

**As a** note author using Refine note, **I want to** cancel the initial AI layout wait and retry in the same dialog, **so that** I recover without changing note content or losing my place.

## Artifacts this phase produces

| File / symbol | Role |
|---|---|
| `mountNoteRefinementPendingLayout` (or equivalent) | Mounts NoteRefinement with deferred `generateRefinementSuggestions` gate; does not `flushPromises` until caller asserts Cancel. |
| `clickLoadingModalCancel` (or equivalent) | Activates teleported overlay Cancel via Testing Library `getByText("Cancel")` + native click. |
| `clickRetryRefinementLayout` | Clicks `[data-test-id="retry-refinement-layout"]`. |
| `NoteRefinement.layoutGeneration.cancel.spec.ts` | Capability-named cancel+retry browser specs (RED until Plan 02). |

<objective>
Author the failing Vitest browser suite and pending-mount helpers that lock Phase 2 observable cancel+retry behavior before product code changes.

Purpose: Wave 0 / TDD RED for the single Behavior slice (layout cancel + retry). Per D-01–D-13 (Phase 1) and CANC-01–04 / REFN-01–02.
Output: Failing focused cancel spec + support helpers; no product file changes.
</objective>

<execution_context>
@/Users/terryyin/.cursor/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cancel-refinement-layout-generation/02-RESEARCH.md
@.planning/phases/02-cancel-refinement-layout-generation/02-PATTERNS.md
@.planning/phases/02-cancel-refinement-layout-generation/02-VALIDATION.md
@.planning/phases/02-cancel-refinement-layout-generation/02-UI-SPEC.md
@.planning/phases/01-shared-cancellation-contract/01-CONTEXT.md
@.planning/phases/01-shared-cancellation-contract/01-01-SUMMARY.md
@.planning/phases/01-shared-cancellation-contract/01-02-SUMMARY.md
@frontend/tests/components/recall/noteRefinementTestSupport.ts
@frontend/tests/components/recall/NoteRefinement.removeLayout.loading.spec.ts
@frontend/tests/components/commons/LoadingModal.spec.ts
@frontend/src/components/recall/NoteRefinement.vue

<interfaces>
Reuse `createDeferredGate`, `loadingModalMask`, `setupNoteRefinementTests`, and the existing `GlobalApiLoadingModal` harness in `noteRefinementTestSupport.ts`. Do not flush layout completion in the pending helper. Cancel clicks target the teleported overlay (body), not the Vue wrapper. Product `loadRefinementLayout` remains thin-bar-only until Plan 02 — this plan must stay RED.
</interfaces>

<planner_assumptions>
- A1: Retry copy is exactly `Ask AI to retry` (UI-SPEC / RESEARCH).
- A2: Empty successful AI layouts share the same empty/retry panel as cancel (UI-SPEC).
- A3: No Cypress E2E required if Vitest browser coverage is thorough (RESEARCH).
- Probe edges without SPEC.md: idempotent double-cancel, concurrent older-blocker survival, silent cancel, and retry-fresh-request are authored as must_haves truths above (not dropped).
</planner_assumptions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pending-layout mount and Cancel/retry helpers</name>
  <files>frontend/tests/components/recall/noteRefinementTestSupport.ts</files>
  <read_first>
    - .planning/phases/02-cancel-refinement-layout-generation/02-PATTERNS.md
    - .planning/phases/02-cancel-refinement-layout-generation/02-RESEARCH.md
    - frontend/tests/components/recall/noteRefinementTestSupport.ts
    - frontend/tests/components/recall/NoteRefinement.removeLayout.loading.spec.ts
    - frontend/tests/components/commons/LoadingModal.spec.ts
  </read_first>
  <behavior>
    - Pending mount installs a deferred `generateRefinementSuggestions` gate before mount and returns without awaiting flushPromises.
    - Cancel helper clicks accessible name Cancel on the teleported overlay.
    - Retry helper clicks data-test-id retry-refinement-layout.
  </behavior>
  <action>
    Extend `noteRefinementTestSupport.ts` only. Add a pending-layout mount helper that (1) uses `mockSdkServiceWithImplementation` on `AiController.generateRefinementSuggestions` with `createDeferredGate`, (2) mounts via the existing GlobalApiLoadingModal harness, (3) awaits at most `nextTick` (not flushPromises), and (4) returns `{ wrapper, resolve, gate }` (or equivalent). Add `clickLoadingModalCancel` using Testing Library `getByText("Cancel")` + native `.click()` against document/body (mirror LoadingModal.spec.ts). Add `clickRetryRefinementLayout` mirroring `clickRetryExtractionPreview` but targeting `retry-refinement-layout`. Keep existing ready-mount helpers that flush unchanged so other specs stay stable. Do not modify product Vue files in this task.
  </action>
  <acceptance_criteria>
    - New helpers are exported from noteRefinementTestSupport.ts.
    - Pending helper does not call flushPromises before returning.
    - Cancel helper uses getByText Cancel (no role queries).
    - Retry helper uses data-test-id retry-refinement-layout.
    - No changes under frontend/src/.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/NoteRefinement.removeLayout.loading.spec.ts</automated>
  </verify>
  <done>Pending-mount and Cancel/retry helpers exist; existing remove-layout loading specs still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author failing layout-generation cancel suite</name>
  <files>frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts</files>
  <read_first>
    - .planning/phases/02-cancel-refinement-layout-generation/02-UI-SPEC.md
    - .planning/phases/02-cancel-refinement-layout-generation/02-VALIDATION.md
    - .planning/phases/02-cancel-refinement-layout-generation/02-PATTERNS.md
    - frontend/tests/components/recall/noteRefinementTestSupport.ts
    - frontend/tests/managedApi/clientSetup.spec.ts
  </read_first>
  <behavior>
    - Pending: mask + AI is generating layout... + Cancel (REFN-01, CANC-01).
    - Cancel clears mask promptly; late resolve does not populate refinement-layout (CANC-02, D-05).
    - No toast.error and no contentUpdated on cancel (CANC-03, D-11).
    - Older concurrent blocker survives layout Cancel (CANC-04, D-04).
    - After cancel: refinement-layout-empty + retry-refinement-layout; retry starts a fresh generateRefinementSuggestions (REFN-02).
    - Second Cancel click is a no-op / non-throwing (CANC-02 idempotency probe).
  </behavior>
  <action>
    Create capability-named `NoteRefinement.layoutGeneration.cancel.spec.ts`. Use `setupNoteRefinementTests` and the new pending helpers. Cover REFN-01/02 and CANC-01–04 with deterministic deferred gates (no fixed waits). Mock `vue-toastification` `useToast` as in clientSetup.spec.ts to assert cancel silence. Spy or count `generateRefinementSuggestions` for retry-fresh-request. For CANC-04, start an older noncancelable `apiCallWithLoading(..., { blockUi: true, message: ... })` deferred call before pending layout mount, cancel the newest Cancel, then assert the older message remains. Assert partial-selection backstop: after cancel, extract control and populated layout are absent. Run the file and confirm it fails for missing product cancelable opt-in / empty retry UI (right reason: Cancel or retry-refinement-layout missing), not harness typos. Do not implement NoteRefinement.vue changes here — RED only per MVP Wave 0.
  </action>
  <acceptance_criteria>
    - Spec file exists and imports pending helpers.
    - Assertions include message AI is generating layout..., Cancel, mask cleanup, late non-apply, toast silence, no contentUpdated, concurrent older survivor, empty retry panel ids, and retry call increment.
    - Focused run exits non-zero (RED) with failures pointing at missing Cancel and/or retry-refinement-layout / empty panel.
    - No frontend/src product edits in this plan.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts</automated>
  </verify>
  <done>Cancel+retry suite is authored and fails for the correct missing product behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Teleported Cancel control → selected loading identity | Test must click only the visible Cancel and must not invent a second abort path. |
| Mocked AI layout request → component state | Late mock resolve after cancel must not apply layout (tampering of client state). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Integrity ambiguity | Cancel UX / docs in tests | medium | mitigate | Specs assert client-only silent cancel; no assertion that server AI stopped. |
| T-02-02 | Tampering | Late layout apply after cancel | high | mitigate | Assert late deferred resolve does not create refinement-layout. |
| T-02-03 | Denial of Service | Stale Cancel retarget | high | mitigate | Rely on Phase 1 identity-bound action; concurrent survivor assertion proves wrong-target cleanup does not occur. |
| T-02-04 | Tampering | Accidental cancelable mutation | high | mitigate | This plan does not opt extract/create/remove into cancelable; only layout generation helpers/mocks. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this phase. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Cancel initial layout and remain ready to retry | 02-01 (RED) + 02-02/03 (GREEN) | COVERED |
| REQ | CANC-01..04, REFN-01..02 | Observable cancel+retry outcomes | 02-01 locks via failing tests | COVERED |
| RESEARCH | Wave 0 gaps | Pending helpers + cancel spec | 02-01 tasks 1-2 | COVERED |
| CONTEXT | D-01..D-13 | Phase 1 contract (caller branch + silence) | cited in behaviors; product in 02-02 | COVERED |
| UI-SPEC | UI Considerations | lifted into must_haves truths | 02-01 frontmatter | COVERED |
</source_audit>

<verification>
Focused cancel spec fails RED for missing Cancel / empty retry. Existing remove-layout loading specs remain green. No product src changes.
</verification>

<success_criteria>
- Pending helpers exist without flushing layout.
- Cancel suite encodes REFN-01/02 and CANC-01–04 including probe edges (idempotent cancel, concurrent survivor, silent cancel, retry-fresh).
- Suite fails for the right product gap before Plan 02.
</success_criteria>

<output>
Create `.planning/phases/02-cancel-refinement-layout-generation/02-01-SUMMARY.md` when done.
</output>
