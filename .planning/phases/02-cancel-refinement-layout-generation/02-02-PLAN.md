---
phase: 02-cancel-refinement-layout-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/src/components/recall/NoteRefinement.vue
autonomous: true
requirements:
  - CANC-01
  - CANC-02
  - CANC-03
  - REFN-01
  - REFN-02
must_haves:
  truths:
    - statement: "While initial layout generation is pending, the global blocker shows AI is generating layout... and an accessible Cancel control."
      verification: "Cancel suite pending assertions green."
    - statement: "Activating Cancel aborts only that browser request, promptly removes its loading state, produces no error toast / success handling / navigation / note-content change, and leaves the dialog open."
      verification: "Cancel suite: mask null; toast silence; no contentUpdated; empty retry visible."
    - statement: "After cancel (and after empty layout), retry-refinement-layout is present and invokes the same cancelable loadRefinementLayout path."
      verification: "Empty panel + retry control; click starts fresh request (full call-count proof may complete in 02-03)."
    - statement: "loadRefinementLayout uses literal blockUi true, cancelable true, message AI is generating layout..., passes AbortSignal into generateRefinementSuggestions, and narrows status before unwrapping result (D-01, D-03)."
      verification: "Cancel suite green; TypeScript compiles; cancelled branch returns without applying items."
    - statement: "Zero items after cancel, empty completed result, or cleared failure render refinement-layout-empty with Ask AI to retry; populated refinement-layout replaces empty when items arrive."
      verification: "Cancel path empty panel; optional empty-success path shares same panel."
    - statement: "Ordinary API failures keep existing toast path; accepted cancel renders no error UI (D-11)."
      verification: "Cancel path toast.error not called."
    - statement: "Extract-preview and create-note / remove blockers remain noncancelable in this plan (no option changes)."
      verification: "Diff touches only loadRefinementLayout + empty/retry template; extract/create/remove messages unchanged."
    - statement: "After cancel, no selected checkboxes / no extract actions until a populated layout returns."
      verification:
        backstop: true
        held_out: "clearSelection on cancel/reload; extract control absent while empty."
  artifacts:
    - path: frontend/src/components/recall/NoteRefinement.vue
      provides: "Cancelable layout load opt-in; empty/cancelled retry panel; status-narrowed outcome handling."
  key_links:
    - from: loadRefinementLayout
      to: apiCallWithLoading cancelable overload
      via: "signal callback + CancelableApiResult status branch before layout apply."
    - from: empty retry button
      to: loadRefinementLayout
      via: "Ask AI to retry re-enters the same cancelable path."
---

## Phase Goal

**As a** note author using Refine note, **I want to** cancel the initial AI layout wait and retry in the same dialog, **so that** I recover without changing note content or losing my place.

## Artifacts this phase produces

| File / symbol | Role |
|---|---|
| `loadRefinementLayout` (updated) | Cancelable `apiCallWithLoading` with `blockUi: true`, `cancelable: true`, message `AI is generating layout...`; early return on `status === "cancelled"`. |
| `layoutLoadSettled` (or equivalent flag) | Becomes true after first layout attempt settles so empty/retry appears only after an attempt (UI-SPEC). |
| Empty panel `data-test-id="refinement-layout-empty"` | Compact accent panel with heading `Note layout:` and retry CTA. |
| Retry control `data-test-id="retry-refinement-layout"` | Ghost-sm button labeled `Ask AI to retry` calling `loadRefinementLayout`. |

<objective>
Deliver the thinnest vertical product slice: opt layout generation into the Phase 1 cancelable blocker and show an in-dialog empty/retry surface so Cancel + recovery work end-to-end.

Purpose: Make Plan 01 cancel suite go GREEN for REFN-01, CANC-01–03, and REFN-02 core path without touching extract-preview or create-note.
Output: Working cancelable layout load + empty/retry UI in NoteRefinement.vue only.
</objective>

<execution_context>
@/Users/terryyin/.cursor/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cancel-refinement-layout-generation/02-01-PLAN.md
@.planning/phases/02-cancel-refinement-layout-generation/02-RESEARCH.md
@.planning/phases/02-cancel-refinement-layout-generation/02-PATTERNS.md
@.planning/phases/02-cancel-refinement-layout-generation/02-UI-SPEC.md
@.planning/phases/01-shared-cancellation-contract/01-CONTEXT.md
@frontend/src/components/recall/NoteRefinement.vue
@frontend/src/managedApi/clientSetup.ts
@frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts

<interfaces>
Adopt existing exported `CancelableApiLoadingOptions` / `CancelableApiResult` from `clientSetup.ts`. Callback form: `(signal) => AiController.generateRefinementSuggestions({ path: { note: props.note.id }, signal })`. Options must be the literal object `{ blockUi: true, cancelable: true, message: "AI is generating layout..." }`. Do not change `runWithBlockingApiLoading` call sites for extract/create/remove. Do not emit `contentUpdated` from layout load. Do not redesign LoadingModal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Opt loadRefinementLayout into cancelable blocking</name>
  <files>frontend/src/components/recall/NoteRefinement.vue</files>
  <read_first>
    - .planning/phases/02-cancel-refinement-layout-generation/02-PATTERNS.md
    - .planning/phases/02-cancel-refinement-layout-generation/02-RESEARCH.md
    - .planning/phases/01-shared-cancellation-contract/01-CONTEXT.md
    - frontend/src/components/recall/NoteRefinement.vue
    - frontend/src/managedApi/clientSetup.ts
    - frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
  </read_first>
  <behavior>
    - Pending layout shows global blocker message AI is generating layout... and Cancel (REFN-01, CANC-01).
    - Cancel resolves cancelled; caller returns without applying items, toast, or contentUpdated (CANC-02, CANC-03, D-03, D-11, D-12).
    - Signal is passed into generateRefinementSuggestions Options.
  </behavior>
  <action>
    Replace the thin-bar `loadRefinementLayout` try/catch with the Phase 1 cancelable overload per D-01–D-03 and 02-PATTERNS Pattern 1. Pass the wrapper-owned AbortSignal into `AiController.generateRefinementSuggestions`. On `outcome.status === "cancelled"`, set items to empty, `clearSelection()`, `resetExtractionPreview()`, mark the attempt settled, and return without emit. On completed, unwrap `outcome.result` only after status narrowing; apply items or empty array as today; clear selection and reset preview. Prefer removing the local try/catch console.error path if the shared wrapper already owns ordinary SDK errors; do not add a second AbortController or toast filter (COHE-01 / D-12). Leave extract-preview, create-note, and remove paths untouched. Prefer minimal Behavior change in this ~437-line file; defer any extract-only cohesion split to post-change-refactor if required.
  </action>
  <acceptance_criteria>
    - loadRefinementLayout uses cancelable apiCallWithLoading with blockUi true, cancelable true, and message AI is generating layout....
    - Signal is forwarded to generateRefinementSuggestions.
    - Cancelled branch returns before layout apply and does not emit contentUpdated.
    - No cancelable: true on extract/create/remove call sites.
    - Pending Cancel assertions in the cancel suite begin to pass (may still fail on empty retry until Task 2).
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts</automated>
  </verify>
  <done>Layout generation opts into the shared cancelable blocker and status-narrows cancelled outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add empty/cancelled layout retry panel</name>
  <files>frontend/src/components/recall/NoteRefinement.vue</files>
  <read_first>
    - .planning/phases/02-cancel-refinement-layout-generation/02-UI-SPEC.md
    - .planning/phases/02-cancel-refinement-layout-generation/02-PATTERNS.md
    - frontend/src/components/recall/NoteRefinement.vue
    - frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
  </read_first>
  <behavior>
    - After cancel or empty completed load, refinement-layout-empty shows Note layout: and Ask AI to retry (REFN-02, UI-SPEC).
    - Retry button data-test-id is retry-refinement-layout and calls loadRefinementLayout.
    - When items.length &gt; 0, existing refinement-layout list shows and empty panel is absent.
    - Empty panel is not the recovery UI before the first layout attempt settles.
  </behavior>
  <action>
    Per 02-UI-SPEC empty/cancelled panel contract and 02-PATTERNS empty-branch analog: replace the single `v-if="refinementLayoutItems.length &gt; 0"` root gate with an explicit populated vs empty branch. Show empty panel when the first layout attempt has settled and items length is zero (introduce `layoutLoadSettled` or equivalent set true after cancelled or completed return). Empty panel: `data-test-id="refinement-layout-empty"`, accent shell matching populated panel, heading `Note layout:`, button `data-test-id="retry-refinement-layout"` with classes equivalent to `daisy-btn daisy-btn-ghost daisy-btn-sm`, label exactly `Ask AI to retry`, `@click="loadRefinementLayout"`. Do not close AssimilationSettings, do not emit contentUpdated from retry, do not invent body paragraph copy, do not reuse `retry-extraction-preview` id. Shared empty surface for cancel, zero-item success, and cleared-after-error (A2). Keep parent Close ownership unchanged.
  </action>
  <acceptance_criteria>
    - After Cancel, mask is gone and refinement-layout-empty + retry-refinement-layout exist.
    - refinement-layout is absent after cancel; late resolve still does not populate it.
    - Retry control text is Ask AI to retry.
    - Populated items still use existing refinement-layout panel without empty panel.
    - Focused cancel suite core REFN-01/02 and CANC-01–03 cases are green (CANC-04 may remain for Plan 03 if not yet green).
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts</automated>
  </verify>
  <done>Cancel leaves an in-dialog Ask AI to retry affordance; populated layout path remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User Cancel → AbortSignal → generated fetch | Browser abort only; does not stop server AI. |
| CancelableApiResult → NoteRefinement state | Status narrowing is the only gate before layout mutation. |
| Retry button → loadRefinementLayout | Fresh opt-in cancelable request; same note id from props. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Integrity ambiguity | Loading message / Cancel | medium | mitigate | Keep Phase 1 Cancel copy; document client-only abort in rule update (Plan 03); no server-stop claim in UI. |
| T-02-02 | Tampering | Late response after cancel | high | mitigate | Early cancelled return + shared latch; cancel suite late-resolve assertion. |
| T-02-03 | Denial of Service | Stale Cancel | high | mitigate | Use Phase 1 identity-bound Cancel; do not re-bind cancel in the caller. |
| T-02-04 | Tampering | Accidental cancelable mutation | high | mitigate | Do not set cancelable on create-note / extract / remove in this plan. |
| T-02-SC | Tampering | Package installs | low | accept | No new packages. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Cancel + remain ready to retry | 02-02 vertical slice | COVERED |
| REQ | REFN-01, CANC-01, CANC-02, CANC-03, REFN-02 | Core cancel+retry product path | 02-02 tasks 1-2 | COVERED |
| REQ | CANC-04 | Concurrent survivor | primarily 02-03 | COVERED in set |
| RESEARCH | Pattern 1+2 | Cancelable load + empty retry | 02-02 | COVERED |
| CONTEXT | D-01..D-06, D-11..D-12 | Outcome, silence, caller-owned retry | 02-02 | COVERED |
| UI-SPEC | empty/retry + loading | Product surfaces | 02-02 | COVERED |
</source_audit>

<verification>
Focused cancel suite is green for pending Cancel, silent cancel, empty retry, and late non-apply. Extract/create/remove option lines unchanged.
</verification>

<success_criteria>
- User can cancel initial layout generation from the global blocker.
- Dialog stays open with Ask AI to retry and unchanged note content (no contentUpdated).
- One coherent Behavior slice: Cancel UI, abort, cleanup, and retry affordance land together.
</success_criteria>

<output>
Create `.planning/phases/02-cancel-refinement-layout-generation/02-02-SUMMARY.md` when done.
</output>
