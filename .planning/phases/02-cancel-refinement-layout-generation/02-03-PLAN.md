---
phase: 02-cancel-refinement-layout-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
  - frontend/src/components/recall/NoteRefinement.vue
  - .cursor/rules/frontend-api.mdc
autonomous: true
requirements:
  - CANC-02
  - CANC-04
  - REFN-02
must_haves:
  truths:
    - statement: "Cancelling layout leaves any older concurrent loading state intact."
      verification: "Cancel suite CANC-04 case green: older blocker message survives layout Cancel."
    - statement: "Activating Ask AI to retry starts a fresh generateRefinementSuggestions request under the same cancelable blocker contract."
      verification: "Call count increments; pending Cancel appears again on retry."
    - statement: "Double Cancel is idempotent at the product seam (no throw, no toast, empty retry remains)."
      verification: "Double-click Cancel assertions green."
    - statement: "If a retry is interrupted, the same cancel contract applies (concurrency of sequential retries)."
      verification: "Cancel during a second pending layout returns to empty retry without applying late data."
    - statement: "frontend-api.mdc documents literal cancelable opt-in, status narrowing, silent accepted cancel, and identity-bound Cancel projection."
      verification: "Rule file contains cancelable: true guidance without inventing a parallel API."
    - statement: "Extract-preview and create-note blockers remain visually/behaviorally unchanged (no Cancel on those paths)."
      verification: "Existing NoteRefinement.removeLayout.loading.spec.ts and extract loading cases remain green; rg shows cancelable true only at layout load in frontend/src components."
    - statement: "After cancel, no selected checkboxes / no extract actions until a populated layout returns."
      verification:
        backstop: true
        held_out: "Cancel suite asserts extract control absent and selection cleared."
  artifacts:
    - path: frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
      provides: "Green concurrent, retry-fresh, idempotent, and sequential-retry interrupt coverage."
    - path: .cursor/rules/frontend-api.mdc
      provides: "Documented cancelable blocking opt-in for future callers."
  key_links:
    - from: retry-refinement-layout
      to: generateRefinementSuggestions
      via: "Each retry creates a new cancelable apiCallWithLoading invocation with a fresh signal."
    - from: layout Cancel
      to: older concurrent blocker
      via: "Exact-id finishLoading leaves the older state selected after cancel."
---

## Phase Goal

**As a** note author using Refine note, **I want to** cancel the initial AI layout wait and retry in the same dialog, **so that** I recover without changing note content or losing my place.

## Artifacts this phase produces

| File / symbol | Role |
|---|---|
| Cancel suite edge cases | Concurrent older-blocker survival; retry-fresh call count; double-cancel; cancel-during-retry. |
| `.cursor/rules/frontend-api.mdc` cancelable subsection | Documents `{ blockUi: true, cancelable: true }`, `CancelableApiResult` status narrowing, silent accepted cancel, identity-bound Cancel. |
| Any minimal NoteRefinement.vue fixups | Only if edge cases reveal gaps in settled-flag / retry re-entry (no Phase 3/4 scope). |

<objective>
Harden Phase 2 edges required by CANC-04 and REFN-02 probes, prove scope boundaries, and document the first product cancelable opt-in in frontend-api.mdc.

Purpose: Close specless concurrency/idempotency probes and RESEARCH open question on rule docs without expanding to extract-preview cancel.
Output: Fully green cancel suite including concurrent survivor + retry interrupt; updated frontend-api rule; unchanged extract/create paths.
</objective>

<execution_context>
@/Users/terryyin/.cursor/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cancel-refinement-layout-generation/02-02-PLAN.md
@.planning/phases/02-cancel-refinement-layout-generation/02-RESEARCH.md
@.planning/phases/02-cancel-refinement-layout-generation/02-VALIDATION.md
@.planning/phases/02-cancel-refinement-layout-generation/02-UI-SPEC.md
@.cursor/rules/frontend-api.mdc
@frontend/src/components/recall/NoteRefinement.vue
@frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
@frontend/tests/components/recall/NoteRefinement.removeLayout.loading.spec.ts
@frontend/tests/components/recall/NoteRefinement.extractNote.spec.ts

<interfaces>
Shared exact-id cancellation already owns concurrent cleanup (Phase 1). Product work here only proves layout cancel does not clear other states and that retry re-enters `loadRefinementLayout`. Rule docs must describe the literal overload already exported from `clientSetup.ts` — do not invent AbortError-name matching or a parallel helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Green concurrent survival and retry-interrupt edges</name>
  <files>frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts, frontend/src/components/recall/NoteRefinement.vue</files>
  <read_first>
    - .planning/phases/02-cancel-refinement-layout-generation/02-VALIDATION.md
    - .planning/phases/02-cancel-refinement-layout-generation/02-RESEARCH.md
    - frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
    - frontend/tests/components/commons/LoadingModal.spec.ts
    - frontend/src/components/recall/NoteRefinement.vue
  </read_first>
  <behavior>
    - Older concurrent blocker survives layout Cancel (CANC-04, D-04).
    - Retry increments generateRefinementSuggestions and can show Cancel again (REFN-02).
    - Double Cancel does not throw or toast (CANC-02 idempotency).
    - Cancel during a second pending layout returns to empty retry without applying late data (REFN-02 concurrency).
  </behavior>
  <action>
    Ensure the Plan 01 cancel suite cases for concurrent older-blocker survival, retry-fresh call count, idempotent double Cancel, and cancel-during-retry are present and green. If any fail for a product reason, apply the smallest NoteRefinement.vue fix (for example settled-flag reset on retry start, or ensuring cancelled branch clears selection) without touching extract/create/remove options. Do not re-implement shared race coverage already in clientSetup.loading.spec.ts. After green cancel suite, run remove-layout and extract loading regressions to prove Phase 3/4 boundaries were not crossed.
  </action>
  <acceptance_criteria>
    - CANC-04 concurrent survivor assertion passes.
    - Retry increases generateRefinementSuggestions invocation count by at least one.
    - Double Cancel leaves empty retry UI without toast.error.
    - Cancel during retry leaves empty retry and does not apply late items.
    - NoteRefinement.removeLayout.loading.spec.ts and NoteRefinement.extractNote.spec.ts still pass.
    - Under frontend/src, cancelable: true appears only in NoteRefinement.vue (layout opt-in) and clientSetup.ts (type/overload), not on extract/create/remove call sites.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c bash -lc 'set -euo pipefail; pnpm frontend:test tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts tests/components/recall/NoteRefinement.removeLayout.loading.spec.ts tests/components/recall/NoteRefinement.extractNote.spec.ts; MATCHES=$(rg -n "cancelable:\\s*true" frontend/src || true); echo "$MATCHES"; echo "$MATCHES" | rg -q "NoteRefinement\\.vue"; echo "$MATCHES" | rg -q "clientSetup\\.ts"; ! echo "$MATCHES" | rg -v "NoteRefinement\\.vue|clientSetup\\.ts" | rg -q .'</automated>
  </verify>
  <done>Probe edges for concurrency/idempotency and scope-boundary regressions are green.</done>
</task>

<task type="auto">
  <name>Task 2: Document cancelable opt-in in frontend-api.mdc</name>
  <files>.cursor/rules/frontend-api.mdc</files>
  <read_first>
    - .cursor/rules/frontend-api.mdc
    - frontend/src/managedApi/clientSetup.ts
    - .planning/phases/02-cancel-refinement-layout-generation/02-RESEARCH.md
    - .planning/phases/01-shared-cancellation-contract/01-CONTEXT.md
  </read_first>
  <action>
    Update the Loading indicators section of `.cursor/rules/frontend-api.mdc` to document the first product cancelable pattern per RESEARCH open question and Phase 1 D-01–D-03 / D-11: literal `{ blockUi: true, cancelable: true, message? }` returns `CancelableApiResult` requiring status narrowing before result access; accepted cancel is silent (no toast); Cancel on the global modal is projected only from the selected blocker's identity-bound action; browser abort does not promise server work stopped. Keep existing noncancelable `{ blockUi: true, message? }` examples. Do not document AbortError-name classification, do not invent a cancelable `runWithBlockingApiLoading`, and do not hand-edit generated SDK docs.
  </action>
  <acceptance_criteria>
    - frontend-api.mdc mentions cancelable: true alongside blockUi: true.
    - Docs require status narrowing before using completed result.
    - Docs state accepted cancel is silent and identity-bound / selected-blocker Cancel.
    - Docs note client-only abort (no server-stop guarantee).
    - Noncancelable blockUi guidance remains present.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c bash -lc 'set -euo pipefail; RULE=.cursor/rules/frontend-api.mdc; SRC=frontend/src/components/recall/NoteRefinement.vue; rg -n "cancelable:\\s*true" "$RULE" "$SRC"; rg -n -i "status narrowing|narrow.*status|CancelableApiResult" "$RULE"; rg -n -i "silent|no toast" "$RULE"; rg -n -i "identity-bound|selected blocker|selected-blocker" "$RULE"; rg -n -i "client-only|does not.*(stop|promise).*server|server work" "$RULE"'</automated>
  </verify>
  <done>Rule docs match the shipped cancelable layout opt-in without inventing a parallel API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Layout Cancel → concurrent loading states | Exact-id removal must not clear unrelated blockers. |
| Docs → future callers | Rule text must not encourage cancelable mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Integrity ambiguity | frontend-api.mdc | medium | mitigate | Explicitly state browser abort does not stop server work. |
| T-02-02 | Tampering | Late apply on retry cancel | high | mitigate | Cancel-during-retry + late resolve assertions. |
| T-02-03 | Denial of Service | Concurrent cancel targeting | high | mitigate | Older-blocker survival assertion at product seam. |
| T-02-04 | Tampering | Accidental mutation cancel | high | mitigate | rg gate + extract/remove regression specs; docs say opt-in only for safe read-only blockers. |
| T-02-SC | Tampering | Package installs | low | accept | No new packages. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| REQ | CANC-04 | Concurrent loading intact | 02-03 task 1 | COVERED |
| REQ | CANC-02 | Idempotency / interrupt probes | 02-03 task 1 | COVERED |
| REQ | REFN-02 | Retry fresh + sequential cancel | 02-03 task 1 | COVERED |
| RESEARCH | Open Q1 | frontend-api.mdc cancelable docs | 02-03 task 2 | COVERED |
| RESEARCH | Pitfall 4 | No extract/create cancel | 02-03 task 1 rg + regressions | COVERED |
| UI-SPEC | lift hints | extract/create unchanged | 02-03 | COVERED |
</source_audit>

<verification>
Full cancel suite green including CANC-04 and retry edges. remove-layout loading green. frontend-api.mdc documents cancelable opt-in. Only layout generation uses cancelable: true under frontend/src components.
</verification>

<success_criteria>
- Concurrent older blockers survive layout cancel.
- Retry starts a fresh cancelable request; interruptible with the same silent cancel outcome.
- Docs and scope boundary prevent unsafe copy-paste of cancelable onto mutations.
</success_criteria>

<output>
Create `.planning/phases/02-cancel-refinement-layout-generation/02-03-SUMMARY.md` when done.
</output>
