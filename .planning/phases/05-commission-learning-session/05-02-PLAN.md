---
phase: 05-commission-learning-session
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - backend/src/main/java/com/odde/doughnut/entities/repositories/SessionItemRepository.java
  - backend/src/main/java/com/odde/doughnut/services/RecallService.java
  - backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java
  - e2e_test/features/learning_session/commissioned_learning_session.feature
autonomous: true
requirements:
  - COM-01
  - COM-02
  - COM-03

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Trackers linked to AWAITING_REPORT session items excluded from dueCommissioned after commission (RESEARCH recommendation; prevents accidental abandon-on-recommission)
    - requestDueRecallsRefresh after commission removes potential-session row for commissioned notebook when exclusion active
    - All scenarios in commissioned_learning_session.feature pass without @wip on commission scenario (phase E2E gate)
    - Phase 3 multi-notebook and ordinary-empty scenarios remain green (regression)
    - Ordinary recall toRepeat unchanged when only commissioned trackers due (TRK-03 / D-05)
  artifacts:
    - path: backend/src/main/java/com/odde/doughnut/entities/repositories/SessionItemRepository.java
      provides: findMemoryTrackerIdsInAwaitingReportSessions query
    - path: backend/src/main/java/com/odde/doughnut/services/RecallService.java
      provides: dueCommissioned stream filtered by awaiting-report tracker ids
    - path: backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java
      provides: exclusion assertion after commission
    - path: e2e_test/features/learning_session/commissioned_learning_session.feature
      provides: Commission scenario without @wip
  key_links:
    - from: SessionItemRepository JPQL
      to: RecallService.getDueMemoryTrackers dueCommissioned list
      via: filter memoryTrackerId not in awaiting set for current user
      pattern: AWAITING_REPORT
    - from: commission success + requestDueRecallsRefresh
      to: potential-session strip
      via: GET recalling returns smaller dueCommissioned
      pattern: dueCommissioned
---

<objective>
Expansion: exclude trackers in AWAITING_REPORT sessions from dueCommissioned so the potential-session strip updates after commission; graduate commission E2E and run full learning_session feature regression.

Purpose: Close recommission pitfall (05-RESEARCH Pitfall 1) and satisfy phase gate — commission scenario green without @wip alongside Phase 2–3 scenarios.
Output: Backend exclusion filter + green Cypress commissioned_learning_session.feature.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-commission-learning-session/05-01-SUMMARY.md
@.planning/phases/05-commission-learning-session/05-RESEARCH.md
@.planning/phases/05-commission-learning-session/05-PATTERNS.md
@.planning/phases/01-commissioned-tracker-model/CONTEXT.md (lifecycle abandon on recommission)
@backend/src/main/java/com/odde/doughnut/services/RecallService.java
@backend/src/main/java/com/odde/doughnut/entities/repositories/SessionItemRepository.java
@backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java
@backend/src/test/java/com/odde/doughnut/controllers/LearningSessionControllerTests.java
</context>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL SC4 | Commission abandons prior unfinished sessions | Phase 4 backend; 05-02 prevents UX that triggers silent abandon |
| RESEARCH | Exclude awaiting-report trackers from dueCommissioned | 05-02 Task 1 |
| RESEARCH | requestDueRecallsRefresh after commission | 05-01; verified with exclusion in 05-02 |
| RESEARCH | Graduate @wip commission E2E | 05-02 Task 2 |
| CONTEXT Phase 1 | Abandon unfinished on new commission | 05-02 exclusion reduces accidental recommission |
| VALIDATION | nyquist_compliant gate — commission without @wip | 05-02 Task 2 |
| REQ COM-01 | Commission flow complete with strip refresh | 05-02 |
| REQ COM-02 | E2E request assertions green | 05-02 Task 2 |
| REQ COM-03 | E2E awaiting-report step green | 05-02 Task 2 |

## Artifacts this phase produces

| Symbol | Kind | Plan |
|--------|------|------|
| `SessionItemRepository.findMemoryTrackerIdsInAwaitingReportSessions` | JPQL query method | 05-02 |
| `RecallService` awaiting-report filter | Service change | 05-02 |
| `RecallsControllerTests` post-commission dueCommissioned empty | JUnit | 05-02 |
| `RecallsControllerTests` two-user cross-leakage (T-05-05) | JUnit | 05-02 |
| `commissioned_learning_session.feature` without @wip on commission | E2E graduation | 05-02 |

<tasks>

<task type="auto" tdd="true">
  <name>Exclude awaiting-report trackers from dueCommissioned</name>
  <files>
    backend/src/main/java/com/odde/doughnut/entities/repositories/SessionItemRepository.java
    backend/src/main/java/com/odde/doughnut/services/RecallService.java
    backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java
  </files>
  <read_first>
    backend/src/main/java/com/odde/doughnut/services/RecallService.java (dueCommissioned mapping, lines 72–87)
    backend/src/main/java/com/odde/doughnut/entities/repositories/SessionItemRepository.java (summarizeRecordedFeedbackByMemoryTrackerId JPQL style)
    backend/src/main/java/com/odde/doughnut/entities/LearningSessionStatus.java
    backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java (shouldListDueCommissionedTrackersSeparatelyFromOrdinaryRecall)
    backend/src/test/java/com/odde/doughnut/controllers/LearningSessionControllerTests.java (commission happy path)
    backend/src/test/java/com/odde/doughnut/testability/MakeMe.java (aLearningSession builders if present)
    .planning/phases/05-commission-learning-session/05-RESEARCH.md (Recommended dueCommissioned exclusion sketch)
  </read_first>
  <reversibility rating="costly">Adds SessionItemRepository query and RecallService constructor dependency; reversible but touches recalling DTO contract consumed by frontend.</reversibility>
  <behavior>
    - Before commission: dueCommissioned includes due commissioned tracker
    - After commission via LearningSessionController: same tracker excluded from dueCommissioned
    - Ordinary toRepeat still empty when only commissioned trackers due
    - Trackers in RECORDED sessions with feedback still follow existing due logic (only AWAITING_REPORT exclusion)
  </behavior>
  <action>
    Add SessionItemRepository @Query method findMemoryTrackerIdsInAwaitingReportSessions(@Param userId Integer): return Set or List of memory_tracker ids where learning_session.user_id = userId AND learning_session.status = AWAITING_REPORT AND session_item links memory_tracker. Follow summarizeRecordedFeedbackByMemoryTrackerId JPQL style in same repository file.

    Inject SessionItemRepository into RecallService constructor. In getDueMemoryTrackers, before mapping dueCommissioned, load awaitingTrackerIds for user.getId(); filter getCommissionedMemoryTrackersNeedToRepeat stream to exclude ids in that set. Do not change toRepeat ordinary path.

    Add RecallsControllerTests scenario: Spanish notebook fixture with due commissioned tracker; assert dueCommissioned size 1; commission via LearningSessionController (or MakeMe session + items if faster); call controller.recalling again; assert dueCommissioned empty and toRepeat still 0. Use makeMe concise fixtures per unit-testing.mdc.

    Add two-user cross-leakage case for threat T-05-05: user A commissions tracker into AWAITING_REPORT session; user B has a separate due commissioned tracker; assert B's dueCommissioned still includes B's tracker (A's awaiting session must not exclude B's trackers). Use two MakeMe users with distinct notebooks/notes.

    No OpenAPI regen required if DueCommissionedMemoryTrackerLite shape unchanged — only list membership changes.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only -- --tests com.odde.doughnut.controllers.RecallsControllerTests</automated>
  </verify>
  <acceptance_criteria>
    - New RecallsControllerTests exclusion case passes
    - Two-user RecallsControllerTests case passes — user B's dueCommissioned unaffected by user A's awaiting-report session (T-05-05)
    - Existing shouldListDueCommissionedTrackersSeparatelyFromOrdinaryRecall still passes
    - pnpm backend:verify green
  </acceptance_criteria>
  <done>dueCommissioned omits trackers awaiting tutor report; recalling API reflects post-commission state.</done>
</task>

<task type="auto">
  <name>Graduate commission E2E and regression gate</name>
  <files>
    e2e_test/features/learning_session/commissioned_learning_session.feature
  </files>
  <read_first>
    e2e_test/features/learning_session/commissioned_learning_session.feature
    .planning/phases/05-commission-learning-session/05-VALIDATION.md (phase gate sampling)
    .planning/phases/05-commission-learning-session/05-01-SUMMARY.md (commission scenario wiring)
  </read_first>
  <action>
    Remove @wip tag from Scenario Commissioning a learning session produces a request for the tutor once Cypress green with backend exclusion from Task 1. Run full e2e_test/features/learning_session/commissioned_learning_session.feature — all four scenarios (assimilate commissioned, due await tutor, commission request, multi-notebook) must pass without @wip. Do not add Phase 6/7 scenarios. If commission scenario flaky on busy modal, confirm waitUntilAppIsNotBusy in recallPage commissionLearningSession after submit click.

    Update 05-VALIDATION.md task status rows to green if executor maintains that file (optional hygiene).

    Per COM-01/02/03: E2E is phase behavior gate; unit tests remain primary for edge cases per REQUIREMENTS REC-05 pattern.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature</automated>
  </verify>
  <acceptance_criteria>
    - commissioned_learning_session.feature has zero @wip tags
    - All scenarios in file pass in single Cypress run
    - Commission scenario assertions cover session items, learning status, expected content, rubric instruction, awaiting report
  </acceptance_criteria>
  <done>Phase 5 E2E gate green; commission behavior externally observable and CI-ready. Cypress ~120s is the intentional phase behavior gate; day-to-day feedback is JUnit in Task 1 (and Vitest from 05-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /api/recalls/recalling | dueCommissioned list scoped to current user session items only |
| Exclusion query | Must filter by user_id on learning_session to prevent cross-user tracker leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information disclosure | SessionItemRepository awaiting-report query | medium | mitigate | JPQL WHERE learning_session.user_id = :userId — test with two-user fixture that other user's awaiting items never exclude current user's trackers incorrectly |
| T-05-06 | Tampering | dueCommissioned filter logic | low | mitigate | Controller test asserts post-commission exclusion; ordinary toRepeat path untouched |
| T-05-07 | Elevation | Commission + recalling same session | low | accept | Same authenticated user throughout E2E |
| T-05-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this plan |
</threat_model>

<verification>
- CURSOR_DEV=true nix develop -c pnpm backend:test_only -- --tests com.odde.doughnut.controllers.RecallsControllerTests
- CURSOR_DEV=true nix develop -c pnpm backend:verify
- CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature
- CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/RecallProgressBar.spec.ts tests/components/recall/CommissionLearningSessionDialog.spec.ts (regression)
</verification>

<success_criteria>
- dueCommissioned exclusion active and tested
- commissioned_learning_session.feature fully green without @wip
- Phase 5 behavior complete: commission → Request + awaiting report with safe strip refresh
- COM-01, COM-02, COM-03 satisfied at E2E gate
</success_criteria>

<output>
Create `.planning/phases/05-commission-learning-session/05-02-SUMMARY.md` when done
</output>
