---
phase: 05-commission-learning-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/recall/CommissionLearningSessionDialog.vue
  - frontend/src/components/recall/RecallProgressBar.vue
  - frontend/tests/components/recall/CommissionLearningSessionDialog.spec.ts
  - frontend/tests/components/recall/RecallProgressBar.spec.ts
  - e2e_test/features/learning_session/commissioned_learning_session.feature
  - e2e_test/step_definitions/learning_session.ts
  - e2e_test/start/pageObjects/recallPage.ts
  - .cursor/rules/frontend-api.mdc
autonomous: true
requirements:
  - COM-01
  - COM-02
  - COM-03

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - User opens Commission dialog from recall progress bar potential-session row and commissions one notebook (COM-01 / Phase 1 CONTEXT dialog surface)
    - Commission response requestMarkdown displays in readonly textarea with CopyButton; lists Session Items, expected learning content, learning status, and 0–5 rubric substrings from backend (COM-02 / ADR 0005)
    - Post-commission dialog shows awaiting-report banner when status is AWAITING_REPORT (COM-03)
    - Commission uses apiCallWithLoading with blockUi message Commissioning learning session… and timezoneParam on SDK call
    - Potential-session row keeps Phase 3 glossary copy and data-test=potential-learning-session; explicit daisy-btn-primary Commission button per UI-SPEC
    - Populated strip one row per notebook; zero notebooks yields silent absence (UI-SPEC empty/populated)
    - Ordinary recall counts and ProgressBar math unchanged (Phase 3 D-05)
  artifacts:
    - path: frontend/src/components/recall/CommissionLearningSessionDialog.vue
      provides: Modal commission dialog with pre/post states, request textarea, awaiting banner
      contains: learning-session-request
    - path: frontend/src/components/recall/RecallProgressBar.vue
      provides: Commission button per potential-session row + dialog host
      contains: commission-learning-session
    - path: e2e_test/step_definitions/learning_session.ts
      provides: Commission + request assertion steps
    - path: e2e_test/features/learning_session/commissioned_learning_session.feature
      provides: Commission scenario tagged @wip until green
  key_links:
    - from: RecallProgressBar Commission button
      to: CommissionLearningSessionDialog
      via: notebookId + notebookName props; v-if open state
      pattern: commission-learning-session-dialog
    - from: CommissionLearningSessionDialog commission CTA
      to: LearningSessionController.commission
      via: apiCallWithLoading + timezoneParam
      pattern: LearningSessionController.commission
    - from: successful commission emit
      to: requestDueRecallsRefresh
      via: useRecallData in RecallProgressBar parent chain
      pattern: requestDueRecallsRefresh
  backstop:
    - statement: Long notebookName in potential-session row wraps with break-words; Commission button stays visible on same row (flex shrink-0)
      verification: RecallProgressBar.spec.ts asserts flex row layout with long title fixture; no ellipsis on glossary copy
---

<objective>
Tracer slice: learner commissions a Learning Session from the recall progress bar, receives a copyable Learning Session Request, and sees awaiting-report state — one notebook happy path wired dialog → API → E2E (@wip until green at phase gate).

Purpose: Deliver the single observable behavior commission → Request + awaiting report (ROADMAP Phase 5) using Phase 4 backend unchanged.
Output: CommissionLearningSessionDialog + promoted RecallProgressBar + Cypress commission scenario infrastructure.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-commission-learning-session/05-RESEARCH.md
@.planning/phases/05-commission-learning-session/05-PATTERNS.md
@.planning/phases/05-commission-learning-session/05-UI-SPEC.md
@docs/adrs/0005-commissioned-learning-session-protocol.md
@.planning/phases/01-commissioned-tracker-model/CONTEXT.md
@frontend/src/components/commons/AiRequestExportDialog.vue
@frontend/src/components/recall/RecallProgressBar.vue
@packages/generated/doughnut-backend-api/sdk.gen.ts
</context>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | commission → Request + awaiting report | 05-01 |
| GOAL | Dialog from recall progress bar | 05-01 |
| GOAL | Abandon prior unfinished sessions on recommission | 05-02 (backend already Phase 4; UX via exclusion) |
| REQ COM-01 | Commission from progress bar dialog | 05-01 Task 1 |
| REQ COM-02 | Request markdown with items, content, status, rubric | 05-01 Tasks 1–2 |
| REQ COM-03 | Session awaits tutor report after commission | 05-01 Tasks 1–2 |
| RESEARCH | CommissionLearningSessionDialog + RecallProgressBar promotion | 05-01 |
| RESEARCH | E2E step_definitions/learning_session.ts + recallPage helpers | 05-01 Task 1 |
| RESEARCH | dueCommissioned exclusion after commission | 05-02 |
| RESEARCH | frontend-api.mdc blocking inventory | 05-01 Task 3 |
| CONTEXT Phase 1 | Dialog on recall progress bar; lifecycle abandon | 05-01 (API); 05-02 (strip refresh) |
| CONTEXT Phase 3 D-03/D-04 | Display-only → commission interaction | 05-01 |
| CONTEXT Phase 3 D-05 | Ordinary recall unchanged | 05-01 + 05-02 regression |
| UI-SPEC | Copywriting, testability data-test attrs, DaisyUI accent | 05-01 |
| UI-SPEC backstop | Long notebook title wrap | 05-01 Task 2 |
| ADR 0005 | Request shape displayed verbatim from backend | 05-01 |

## Artifacts this phase produces

| Symbol | Kind | Plan |
|--------|------|------|
| `CommissionLearningSessionDialog.vue` | Vue component | 05-01 |
| `RecallProgressBar.vue` (commission row + dialog) | Vue component (modified) | 05-01 |
| `CommissionLearningSessionDialog.spec.ts` | Vitest component test | 05-01 |
| `RecallProgressBar.spec.ts` (commission affordance) | Vitest component test | 05-01 |
| `learning_session.ts` | Cypress step definitions | 05-01 |
| `recallPage.ts` commission helpers | E2E page object | 05-01 |
| `commissioned_learning_session.feature` commission scenario | E2E feature (@wip → green in 05-02) | 05-01 add; 05-02 graduate |
| `frontend-api.mdc` commission row | Blocking mutation inventory | 05-01 |
| `SessionItemRepository` awaiting-report query | JPA repository method | 05-02 |
| `RecallService` dueCommissioned filter | Backend service | 05-02 |
| `RecallsControllerTests` exclusion case | JUnit | 05-02 |

<tasks>

<task type="tracer">
  <name>End-to-end commission — Spanish notebook happy path</name>
  <files>
    frontend/src/components/recall/CommissionLearningSessionDialog.vue
    frontend/src/components/recall/RecallProgressBar.vue
    e2e_test/features/learning_session/commissioned_learning_session.feature
    e2e_test/step_definitions/learning_session.ts
    e2e_test/start/pageObjects/recallPage.ts
  </files>
  <read_first>
    frontend/src/components/commons/AiRequestExportDialog.vue (Modal + textarea + CopyButton pattern)
    frontend/src/components/commons/Modal.vue
    frontend/src/components/commons/CopyButton.vue
    frontend/src/components/recall/RecallProgressBar.vue (current potential-session strip, lines 60–72)
    frontend/src/components/recall/NoteRefinement.vue (dialog v-if wiring pattern)
    frontend/src/composables/useGoToNextAssimilation.ts (apiCallWithLoading + timezoneParam)
    frontend/src/composables/useRecallData.ts (requestDueRecallsRefresh)
    packages/generated/doughnut-backend-api/sdk.gen.ts (LearningSessionController.commission)
    packages/generated/doughnut-backend-api/types.gen.ts (LearningSessionCommissionResponse)
    .planning/phases/05-commission-learning-session/05-UI-SPEC.md (Copywriting + Testability Contract)
    .planning/phases/01-commissioned-tracker-model/commissioned_learning_session.feature (lines 28–36)
    e2e_test/step_definitions/recall.ts (step + page object delegation pattern)
    e2e_test/start/pageObjects/recallPage.ts (expectPotentialLearningSession)
    e2e_test/start/pageBase.ts (waitUntilAppIsNotBusy)
  </read_first>
  <action>
    Create CommissionLearningSessionDialog.vue per 05-UI-SPEC and 05-PATTERNS: props notebookId (number) and notebookName (string); emits close and commissioned. Root wrapper data-test=commission-learning-session-dialog. Pre-commission state: title Commission learning session, label Notebook: "{notebookName}", body copy per UI-SPEC, primary CTA data-test=commission-learning-session-submit with label Commission learning session. On CTA click call apiCallWithLoading(() => LearningSessionController.commission({ body: { notebookId }, query: { timezone: timezoneParam() } }), { blockUi: true, message: "Commissioning learning session…" }). On success bind response.requestMarkdown to readonly textarea data-test=learning-session-request (font-mono text-xs h-96); show CopyButton with test-id=copy-learning-session-request and aria-label Copy learning session request; when response.status === "AWAITING_REPORT" show banner data-test=learning-session-awaiting-report with copy This learning session is awaiting the tutor's report.; hide pre-commission CTA. On error leave pre-commission state (apiCallWithLoading toast — no inline error panel). Emit commissioned after success. Never v-html notebookName or markdown.

    Promote RecallProgressBar.vue potential-session rows per UI-SPEC row layout: flex gap-2 px-4 row with glossary copy flex-1 break-words left, daisy-btn daisy-btn-primary Commission button data-test=commission-learning-session right shrink-0. Keep data-test=potential-learning-session on row and canonical glossary sentence unchanged. Click Commission sets commissionDialogSession ref and mounts CommissionLearningSessionDialog v-if. onCommissioned calls requestDueRecallsRefresh from local useRecallData in RecallProgressBar (dialog host — not RecallPage). Dialog stays open post-commission per UI-SPEC; only refresh recall data.

    Add commission scenario to e2e_test/features/learning_session/commissioned_learning_session.feature with @wip tag (steps from draft feature lines 28–36). Create e2e_test/step_definitions/learning_session.ts with When/Then steps delegating to recallPage: commissionLearningSession(notebookTitle), expectLearningSessionRequestListsNotes, expectLearningSessionRequestIncludesLearningStatus(noteTitle), expectLearningSessionRequestIncludesContent(text), expectLearningSessionRequestIncludesRubric, expectLearningSessionAwaitingReport. Extend recallPage.ts: commissionLearningSession finds row containing notebook title, clicks data-test=commission-learning-session, clicks dialog submit, waitUntilAppIsNotBusy; assertion helpers read textarea data-test=learning-session-request val() for substrings ### {title}, Expected learning content:, not yet tutored or learning status line, score from 0 to 5 per item; awaiting uses data-test=learning-session-awaiting-report visible.

    Per COM-01: dialog opens only from progress bar Commission button on recall page. Per COM-02: display backend requestMarkdown only. Per COM-03: awaiting banner from response status.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature --env tags="@wip"</automated>
    <!-- E2E ~120s is intentional at tracer boundary; Vitest Task 2 provides faster per-task feedback (see 05-VALIDATION.md Sampling Rate). -->
  </verify>
  <acceptance_criteria>
    - Commission scenario steps execute without missing step-definition errors
    - After commission, textarea contains ### Hola and ### Gracias and Expected learning content: Hello
    - data-test=learning-session-awaiting-report visible after successful commission
    - Phase 3 scenarios in same feature file still pass when run together
  </acceptance_criteria>
  <done>Single notebook commission works end-to-end: progress bar → dialog → API → request display → awaiting banner; E2E commission scenario runs (@wip until 05-02 removes tag). Cypress ~120s is the intentional behavior gate; day-to-day feedback is Vitest in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Vitest — dialog request display and awaiting state</name>
  <files>
    frontend/tests/components/recall/CommissionLearningSessionDialog.spec.ts
    frontend/tests/components/recall/RecallProgressBar.spec.ts
  </files>
  <read_first>
    frontend/tests/components/recall/NoteRefinement.exportBreakdownRequest.spec.ts (mockSdkService + textarea assertion)
    frontend/tests/components/recall/RecallProgressBar.spec.ts (mountBar helper)
    frontend/tests/helpers/index.ts (mockSdkService)
    .planning/phases/05-commission-learning-session/05-UI-SPEC.md (UI Considerations backstop long-text row)
    backend/src/test/java/com/odde/doughnut/controllers/LearningSessionControllerTests.java (markdown substring contract)
  </read_first>
  <behavior>
    - Click dialog Commission learning session CTA → LearningSessionController.commission called with notebookId and timezone query
    - Post-commission textarea value contains # Learning Session Request and ### Hola and score from 0 to 5 per item
    - data-test=learning-session-awaiting-report present when status AWAITING_REPORT
    - RecallProgressBar shows Commission button on row with glossary copy for notebook fixture
    - Long notebookName (80+ chars) row still shows Commission button in same flex row without truncating glossary text with ellipsis
  </behavior>
  <action>
    Add CommissionLearningSessionDialog.spec.ts: mount with notebookId/notebookName props; mockSdkService(LearningSessionController, commission, { learningSessionId: 42, requestMarkdown: canonical ADR-shaped string with Hola/Hello/not yet tutored/rubric line, status: AWAITING_REPORT }); click data-test=commission-learning-session-submit; assert spy called with body.notebookId and query.timezone; assert textarea data-test=learning-session-request value substrings; assert awaiting banner visible. Error path: mock error → pre-commission CTA still visible (no swallow).

    Extend RecallProgressBar.spec.ts: mountBar with one potential session → Commission button data-test=commission-learning-session visible; click opens dialog in document.body; preserve existing glossary and potential-learning-session assertions. Add long-title fixture case for UI-SPEC backstop (break-words row + shrink-0 button).

    Per COM-02 and COM-03 at component boundary without re-asserting full rubric in every sibling test.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/CommissionLearningSessionDialog.spec.ts tests/components/recall/RecallProgressBar.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - CommissionLearningSessionDialog.spec.ts passes with mockSdkService commission spy
    - RecallProgressBar.spec.ts passes including new Commission affordance test
    - Long-title case asserts Commission button exists adjacent to full glossary string in DOM
  </acceptance_criteria>
  <done>Component tests lock COM-02 markdown display and COM-03 awaiting banner at stable Vitest boundaries.</done>
</task>

<task type="auto">
  <name>Blocking mutation inventory — frontend-api.mdc</name>
  <files>
    .cursor/rules/frontend-api.mdc
  </files>
  <read_first>
    .cursor/rules/frontend-api.mdc (Intentionally noncancelable table, lines 114–124)
    frontend/src/components/recall/CommissionLearningSessionDialog.vue
    .planning/phases/05-commission-learning-session/05-RESEARCH.md (Pitfall 6 blocking classification)
  </read_first>
  <action>
    Add row to frontend-api.mdc Intentionally noncancelable mutations table: User-facing label Commission learning session | blockUi message Commissioning learning session… | call site CommissionLearningSessionDialog.vue | options { blockUi: true } | classification Mutation. Ensures E2E waitUntilAppIsNotBusy pairing documented for executors. No production code changes unless audit reveals message string drift from UI-SPEC — align dialog message exactly.
  </action>
  <verify>
    <automated>test "$(grep -c "Commissioning learning session" .cursor/rules/frontend-api.mdc)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - frontend-api.mdc table includes commission mutation row with blockUi true
    - Message string matches CommissionLearningSessionDialog apiCallWithLoading message character-for-character
  </acceptance_criteria>
  <done>Commission mutation registered in blocking inventory per frontend-api.mdc contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/learning-sessions/commission | Untrusted notebookId from dialog props; auth session required (Phase 4) |
| Browser rendering | notebookName and requestMarkdown displayed as text only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of privilege | LearningSessionController.commission | high | mitigate | Backend assertLoggedIn + assertAuthorization(notebook) unchanged from Phase 4; FE never sends cross-user notebookId |
| T-05-02 | Tampering (XSS) | CommissionLearningSessionDialog notebookName / requestMarkdown | medium | mitigate | Text interpolation and textarea value only; never v-html per 05-UI-SPEC Security/XSS |
| T-05-03 | Information disclosure | Request textarea in dialog | low | accept | Markdown only shown after successful authorized commission |
| T-05-04 | Denial of service | Repeated commission clicks | low | mitigate | blockUi prevents double-submit during in-flight mutation |
| T-05-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this plan |
</threat_model>

<verification>
- CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/CommissionLearningSessionDialog.spec.ts tests/components/recall/RecallProgressBar.spec.ts
- CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature
- CURSOR_DEV=true nix develop -c pnpm backend:test_only -- --tests com.odde.doughnut.controllers.LearningSessionControllerTests (regression — no backend changes expected)
</verification>

<success_criteria>
- COM-01: User commissions from recall progress bar dialog
- COM-02: Copyable Request lists session items, content, status, rubric
- COM-03: Awaiting-report state visible after commission
- Vitest recall component specs green
- Commission E2E scenario executable (@wip acceptable at 05-01 boundary; graduate in 05-02)
</success_criteria>

<output>
Create `.planning/phases/05-commission-learning-session/05-01-SUMMARY.md` when done
</output>
