---
phase: 03-cancel-extraction-preview-generation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/tests/components/recall/noteRefinementExtractionTestSupport.ts
  - frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts
autonomous: true
requirements:
  - REFN-03
  - REFN-04
must_haves:
  truths:
    - statement: "While extraction-preview generation is pending, the global blocker shows AI is generating preview... and an accessible Cancel control."
      verification: "Vitest pending-gate asserts loadingModalMask, message text, and getByText(Cancel)."
    - statement: "Activating Cancel from Extract removes that blocker promptly, shows no error toast, does not open extraction-preview, keeps layout selections, and leaves Extract enabled."
      verification: "After Cancel: mask null; toast.error not called; extraction-preview absent; layoutCheckbox checked; extract-refinement-layout enabled; no contentUpdated."
    - statement: "Activating Cancel during Ask AI to retry keeps extraction-preview visible with prior field values and shows no error UI."
      verification: "After regenerate Cancel: expectExtractionPreviewVisible true; expectPreviewFields prior values; toast silence."
    - statement: "Late settlement after Extract cancel does not open preview or apply cancelled payload text."
      verification:
        backstop: true
        held_out: "resolve deferred mock after Cancel; preview still hidden; cancelled title string absent from body."
    - statement: "Late settlement after regenerate cancel does not change prior preview fields."
      verification:
        backstop: true
        held_out: "resolve deferred second call after Cancel; expectPreviewFields unchanged."
    - statement: "Create-note blocker remains noncancelable in this phase (not asserted cancelable here)."
      verification: "Scope-boundary cases deferred to Plan 03; this RED suite locks preview cancel only."
    - statement: "Do not expect a layout empty recovery panel after preview cancel — populated refinement-layout remains."
      verification: "Post-Extract-cancel: refinement-layout present; empty-layout recovery controls absent."
  artifacts:
    - path: frontend/tests/components/recall/noteRefinementExtractionTestSupport.ts
      provides: "Pending-preview mount helper that starts Extract with extractNotePreview held open."
    - path: frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts
      provides: "Failing observable cancel+retry product evidence for REFN-03/04."
  key_links:
    - from: NoteRefinement.extractionPreview.cancel.spec.ts
      to: noteRefinementExtractionTestSupport.ts
      via: "Deferred extractNotePreview gate after layout ready; Cancel via teleported overlay."
    - from: cancel spec
      to: noteRefinementLayoutLoadingTestSupport.ts
      via: "Reuse createDeferredGate, loadingModalMask, clickLoadingModalCancel."
---

## Phase Goal

**As a** note author using Refine note, **I want to** cancel AI extraction-preview generation without losing my layout selections, **so that** I can retry Extract from the same choices or keep my prior preview if I cancelled a regenerate.

## Artifacts this phase produces

| File / symbol | Role |
|---|---|
| `mountNoteRefinementPendingExtractionPreview` (or equivalent) | Layout ready → select → Extract with deferred `extractNotePreview`; no flush until caller asserts Cancel. |
| `NoteRefinement.extractionPreview.cancel.spec.ts` | Capability-named cancel suite (RED until Plan 02). |

<objective>
Author the failing Vitest browser suite and pending-preview helpers that lock Phase 3 observable cancel behavior before product code changes (D-01–D-10).

Purpose: Wave 0 / TDD RED for the single Behavior slice (extraction-preview cancel + preserved selection / prior preview). Per REFN-03 / REFN-04 and 03-CONTEXT.
Output: Failing focused cancel spec + extraction support helpers; no product file changes.
</objective>

<execution_context>
@/Users/terryyin/.cursor/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-cancel-extraction-preview-generation/03-RESEARCH.md
@.planning/phases/03-cancel-extraction-preview-generation/03-PATTERNS.md
@.planning/phases/03-cancel-extraction-preview-generation/03-VALIDATION.md
@.planning/phases/03-cancel-extraction-preview-generation/03-UI-SPEC.md
@.planning/phases/03-cancel-extraction-preview-generation/03-CONTEXT.md
@.planning/phases/02-cancel-refinement-layout-generation/02-01-SUMMARY.md
@frontend/tests/components/recall/noteRefinementExtractionTestSupport.ts
@frontend/tests/components/recall/noteRefinementLayoutLoadingTestSupport.ts
@frontend/tests/components/recall/noteRefinementTestSupport.ts
@frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
@frontend/tests/components/recall/NoteRefinement.extractNote.spec.ts
@frontend/src/components/recall/NoteRefinement.vue

<interfaces>
Reuse `createDeferredGate`, `loadingModalMask`, and `clickLoadingModalCancel` from `noteRefinementLayoutLoadingTestSupport.ts`. Reuse `mountNoteRefinementReady`, `selectRefinementLayoutItem`, `clickExtractRefinementLayout`, `layoutCheckbox`, `threePointLayoutTexts`, `sampleExtractionPreview`, and `setupNoteRefinementTests` from noteRefinementTestSupport. Reuse `expectExtractionPreviewVisible`, `expectPreviewFields`, `openExtractionPreview`, `clickRetryExtractionPreview`, and `labeledExtractionPreview` from extraction support. Pending helper: mount layout ready first, then install deferred `extractNotePreview` before Extract (mirror extractNote.spec pending-preview loading, not pending-layout mount). Product `runExtractionPreview` remains noncancelable composite until Plan 02 — this plan must stay RED.
</interfaces>

<planner_assumptions>
- A1: Collapsing fetchExtractionPreview into runExtractionPreview is Plan 02 discretion; RED suite only needs observable cancel outcomes.
- A2: Existing API-error force-show preview path stays out of cancel assertions.
- A3: No Cypress E2E required if Vitest browser coverage is thorough (RESEARCH).
</planner_assumptions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pending-preview mount helpers</name>
  <files>frontend/tests/components/recall/noteRefinementExtractionTestSupport.ts</files>
  <read_first>
    - .planning/phases/03-cancel-extraction-preview-generation/03-PATTERNS.md
    - .planning/phases/03-cancel-extraction-preview-generation/03-RESEARCH.md
    - frontend/tests/components/recall/noteRefinementExtractionTestSupport.ts
    - frontend/tests/components/recall/noteRefinementLayoutLoadingTestSupport.ts
    - frontend/tests/components/recall/NoteRefinement.extractNote.spec.ts
  </read_first>
  <behavior>
    - Pending helper mounts with layout flushed, selects an item, starts Extract with extractNotePreview held pending via createDeferredGate.
    - Helper returns without awaiting flushPromises (at most nextTick after Extract click).
    - Returns wrapper, resolve, gate, and optionally the extract spy for retry call-count cases.
  </behavior>
  <action>
    Extend `noteRefinementExtractionTestSupport.ts` only. Add `mountNoteRefinementPendingExtractionPreview` (name may match 03-PATTERNS) that (1) creates a deferred gate, (2) mocks `AiController.extractNotePreview` with `mockSdkServiceWithImplementation` awaiting the gate before returning a preview payload (default title suitable for late-apply negative asserts), (3) mounts via `mountNoteRefinementReady`, (4) selects the given item id, (5) clicks Extract, (6) awaits at most `nextTick` (not flushPromises), and (7) returns `{ wrapper, resolve, gate, extractSpy }` (or equivalent). Import deferred-gate / Cancel helpers from layout-loading support rather than duplicating them. Keep existing `openExtractionPreview` (flushing) helpers unchanged so extractNote.spec stays stable. Do not modify product Vue files. Do not add layout-empty retry helpers (Phase 2 only).
  </action>
  <acceptance_criteria>
    - New pending-preview helper is exported from noteRefinementExtractionTestSupport.ts.
    - Pending helper does not call flushPromises before returning.
    - Helper uses createDeferredGate from layout-loading support (no duplicate gate implementation).
    - No changes under frontend/src/.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/NoteRefinement.extractNote.spec.ts</automated>
  </verify>
  <done>Pending-preview helpers exist; existing extractNote specs still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author failing extraction-preview cancel suite</name>
  <files>frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts</files>
  <read_first>
    - .planning/phases/03-cancel-extraction-preview-generation/03-UI-SPEC.md
    - .planning/phases/03-cancel-extraction-preview-generation/03-VALIDATION.md
    - .planning/phases/03-cancel-extraction-preview-generation/03-PATTERNS.md
    - .planning/phases/03-cancel-extraction-preview-generation/03-CONTEXT.md
    - frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
    - frontend/tests/components/recall/noteRefinementExtractionTestSupport.ts
  </read_first>
  <behavior>
    - Pending Extract: mask + AI is generating preview... + Cancel (REFN-03, D-08, D-09).
    - Cancel from Extract: stay on layout; selection kept; preview hidden; Extract still enabled; silent toast; no contentUpdated; late resolve ignored (REFN-04, D-01–D-05).
    - Cancel during Ask AI to retry: stay on preview; prior expectPreviewFields; late resolve ignored (REFN-04, D-06).
    - Do not assert Phase 2 empty-layout recovery controls as the Extract-cancel landing.
  </behavior>
  <action>
    Create capability-named `NoteRefinement.extractionPreview.cancel.spec.ts`. Use `setupNoteRefinementTests`, toast mock pattern from layout cancel suite (`vue-toastification` useToast), pending-preview helper, `clickLoadingModalCancel`, `layoutCheckbox`, `expectExtractionPreviewVisible`, `expectPreviewFields`, `openExtractionPreview`, `clickRetryExtractionPreview`, and `labeledExtractionPreview`. Cover REFN-03 pending Cancel and REFN-04 Extract-cancel + regenerate-cancel with deterministic deferred gates (no fixed waits). For regenerate-cancel: complete first preview, hold second `extractNotePreview` call, Cancel, assert prior fields. Assert late `resolve()` does not apply cancelled payload (Extract path) or mutate fields (retry path). Run the file and confirm it fails for missing product cancelable opt-in (right reason: Cancel absent while preview pending, and/or cancel opens preview / clears selection), not harness typos. Do not implement NoteRefinement.vue changes here — RED only per MVP Wave 0.
  </action>
  <acceptance_criteria>
    - Spec file exists and imports pending-preview / Cancel helpers.
    - Assertions include message AI is generating preview..., Cancel, mask cleanup, selection preserve, preview hidden after Extract cancel, prior fields after regenerate cancel, toast silence, no contentUpdated, late non-apply.
    - Focused run exits non-zero (RED) with failures pointing at missing Cancel and/or wrong post-cancel landing.
    - No frontend/src product edits in this plan.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts</automated>
  </verify>
  <done>Cancel suite is authored and fails for the correct missing product behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Teleported Cancel control → selected loading identity | Test must click only the visible Cancel and must not invent a second abort path. |
| Mocked extractNotePreview → component state | Late mock resolve after cancel must not apply preview fields or open the panel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Integrity ambiguity | Cancel UX in tests | medium | mitigate | Specs assert client-only silent cancel; no assertion that server AI stopped. |
| T-03-02 | Tampering | Late preview apply after cancel | high | mitigate | Assert late deferred resolve does not open preview / change fields (UI-SPEC partial backstop). |
| T-03-03 | Denial of Service | Stale Cancel retarget | medium | accept | Shared Phase 1 identity-bound action already covered; product-level concurrent case optional in Plan 03. |
| T-03-04 | Tampering | Accidental cancelable mutation | high | mitigate | This plan does not opt create-note / remove into cancelable; only preview mocks. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this phase. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Cancel preview without losing layout choices | 03-01 (RED) + 03-02/03 (GREEN) | COVERED |
| REQ | REFN-03, REFN-04 | Observable cancel + preserve selection / prior preview | 03-01 locks via failing tests | COVERED |
| RESEARCH | Wave 0 gaps | Pending helpers + cancel spec | 03-01 tasks 1-2 | COVERED |
| CONTEXT | D-01–D-10 | Landing, silence, message, opt-in | cited in behaviors; product in 03-02 | COVERED |
| UI-SPEC | UI Considerations | lifted into must_haves truths | 03-01 frontmatter | COVERED |
</source_audit>

<verification>
Focused cancel spec fails RED for missing Cancel / wrong post-cancel landing. Existing extractNote specs remain green. No product src changes.
</verification>

<success_criteria>
- Pending-preview helpers exist without flushing the preview request.
- Cancel suite encodes REFN-03/04 including Extract-cancel, regenerate-cancel, silent cancel, and late non-apply.
- Suite fails for the right product gap before Plan 02.
</success_criteria>

<output>
Create `.planning/phases/03-cancel-extraction-preview-generation/03-01-SUMMARY.md` when done.
</output>
