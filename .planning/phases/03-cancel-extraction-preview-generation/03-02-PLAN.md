---
phase: 03-cancel-extraction-preview-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - frontend/src/components/recall/NoteRefinement.vue
autonomous: true
requirements:
  - REFN-03
  - REFN-04
must_haves:
  truths:
    - statement: "While extraction-preview generation is pending, the global blocker shows AI is generating preview... and an accessible Cancel control."
      verification: "Cancel suite pending assertions green."
    - statement: "Activating Cancel from Extract aborts only that browser request, promptly removes its loading state, produces no error toast, does not open extraction-preview, keeps selected layout items, and leaves Extract enabled for retry."
      verification: "Cancel suite Extract-cancel cases green (D-01–D-05)."
    - statement: "Activating Cancel during Ask AI to retry keeps extraction-preview with prior field values unchanged."
      verification: "Cancel suite regenerate-cancel cases green (D-06)."
    - statement: "runExtractionPreview uses literal blockUi true, cancelable true, message AI is generating preview..., passes AbortSignal into extractNotePreview, and narrows status before unwrapping result (D-08, D-10)."
      verification: "Cancel suite green; TypeScript compiles; cancelled branch returns without apply/createError/showPreview toggle."
    - statement: "Ordinary API failures keep existing createError + force-show preview path; accepted cancel never shares that path (D-03, D-05)."
      verification: "Cancelled early return before error/showPreview mutations; cancel path toast.error not called."
    - statement: "Create-note / remove / layout blockers are not changed in this plan."
      verification: "Diff touches only preview generation path (fetchExtractionPreview / runExtractionPreview collapse)."
    - statement: "Late settlement after cancel does not apply preview payload."
      verification:
        backstop: true
        held_out: "Cancel suite late-resolve assertions green."
  artifacts:
    - path: frontend/src/components/recall/NoteRefinement.vue
      provides: "Cancelable preview load opt-in; status-narrowed cancelled no-op for Extract and regenerate paths."
  key_links:
    - from: runExtractionPreview
      to: apiCallWithLoading cancelable overload
      via: "signal callback + CancelableApiResult status branch before preview apply."
    - from: openExtractionPreview / retryExtractionPreview
      to: runExtractionPreview
      via: "Thin wrappers; retry keeps unsaved-edit confirm (D-07)."
---

## Phase Goal

**As a** note author using Refine note, **I want to** cancel AI extraction-preview generation without losing my layout selections, **so that** I can retry Extract from the same choices or keep my prior preview if I cancelled a regenerate.

## Artifacts this phase produces

| File / symbol | Role |
|---|---|
| `runExtractionPreview` (updated) | Single cancelable `apiCallWithLoading` with `blockUi: true`, `cancelable: true`, message `AI is generating preview...`; early return on `status === "cancelled"`. |
| Removed composite path | No outer `runWithBlockingApiLoading` around preview; no separate thin-bar-only `fetchExtractionPreview` (collapse per discretion). |
| Existing Extract / Ask AI to retry | Post-cancel retry affordances — no new empty panel (D-04). |

<objective>
Deliver the thinnest vertical product slice: opt extraction-preview generation into the Phase 1 cancelable blocker with a cancelled domain no-op so Cancel + preserve-selection / keep-prior-preview work end-to-end (D-01–D-10).

Purpose: Make Plan 01 cancel suite go GREEN for REFN-03/04 core path without touching create-note or inventing a layout empty panel.
Output: Working cancelable preview load in NoteRefinement.vue only.
</objective>

<execution_context>
@/Users/terryyin/.cursor/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-cancel-extraction-preview-generation/03-01-PLAN.md
@.planning/phases/03-cancel-extraction-preview-generation/03-RESEARCH.md
@.planning/phases/03-cancel-extraction-preview-generation/03-PATTERNS.md
@.planning/phases/03-cancel-extraction-preview-generation/03-UI-SPEC.md
@.planning/phases/03-cancel-extraction-preview-generation/03-CONTEXT.md
@frontend/src/components/recall/NoteRefinement.vue
@frontend/src/managedApi/clientSetup.ts
@frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts
@.cursor/rules/frontend-api.mdc

<interfaces>
Adopt existing exported `CancelableApiLoadingOptions` / `CancelableApiResult` from `clientSetup.ts`. Callback form: `(signal) => AiController.extractNotePreview({ path: { note: props.note.id }, body: layoutSelectionBody(), signal })`. Options must be the literal object `{ blockUi: true, cancelable: true, message: "AI is generating preview..." }`. Do not invent a cancelable `runWithBlockingApiLoading`. Do not change create-note / remove / layout call sites. Do not clear selection, call settleLayout, resetExtractionPreview, emit contentUpdated, or toggle showExtractionPreview on cancelled. Keep `retryExtractionPreview` unsaved-edit confirm unchanged (D-07).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Opt runExtractionPreview into cancelable blocking</name>
  <files>frontend/src/components/recall/NoteRefinement.vue</files>
  <read_first>
    - .planning/phases/03-cancel-extraction-preview-generation/03-PATTERNS.md
    - .planning/phases/03-cancel-extraction-preview-generation/03-RESEARCH.md
    - .planning/phases/03-cancel-extraction-preview-generation/03-CONTEXT.md
    - frontend/src/components/recall/NoteRefinement.vue
    - frontend/src/managedApi/clientSetup.ts
    - frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts
  </read_first>
  <behavior>
    - Pending preview shows global blocker message AI is generating preview... and Cancel (REFN-03, D-08, D-09, D-10).
    - Cancel resolves cancelled; caller returns without applying preview, createError, showExtractionPreview toggle, selection clear, or contentUpdated (D-01–D-06).
    - Signal is passed into extractNotePreview Options.
  </behavior>
  <action>
    Replace the composite `fetchExtractionPreview` + outer `runWithBlockingApiLoading` preview path with one Phase 1 cancelable `apiCallWithLoading` overload per D-10 and 03-PATTERNS Pattern 1 / RESEARCH Pattern 1. Prefer collapsing into a single `runExtractionPreview(showPreviewOnSuccess)` that: clears createError at start; awaits cancelable call with message exactly `AI is generating preview...`; on `outcome.status === "cancelled"` returns immediately with no domain mutations (do not copy Phase 2 layout cancel settle/clearSelection); on completed, unwrap `outcome.result` only after status narrowing; on SDK error keep existing createError + force-show preview path unchanged; on success apply extractionPreview / lastAiExtractionResult and set showExtractionPreview only when showPreviewOnSuccess is true. Keep `openExtractionPreview` / `retryExtractionPreview` as thin wrappers; do not change unsaved-edit confirm (D-07). Leave create-note, remove, and layout cancelable load untouched. Prefer minimal Behavior change in this ~465-line file; defer cohesion extract to post-change-refactor if needed.
  </action>
  <acceptance_criteria>
    - runExtractionPreview uses cancelable apiCallWithLoading with blockUi true, cancelable true, and message AI is generating preview....
    - Signal is forwarded to extractNotePreview.
    - Cancelled branch returns before preview apply / createError / showExtractionPreview mutation.
    - No outer runWithBlockingApiLoading remains on the preview path.
    - create-note / remove / layout option lines unchanged.
    - Tasks 1–2 ship as one vertical GREEN commit: after both tasks, focused cancel suite core REFN-03/04 cases are green (same verify as Task 2).
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts</automated>
  </verify>
  <done>Cancelable preview path is in place; do not commit Task 1 alone — finish Task 2 in the same vertical GREEN commit so the cancel suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Green Extract-cancel and regenerate-cancel landings</name>
  <files>frontend/src/components/recall/NoteRefinement.vue</files>
  <read_first>
    - .planning/phases/03-cancel-extraction-preview-generation/03-UI-SPEC.md
    - .planning/phases/03-cancel-extraction-preview-generation/03-PATTERNS.md
    - frontend/src/components/recall/NoteRefinement.vue
    - frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts
  </read_first>
  <behavior>
    - After Extract cancel: refinement-layout still visible; selected checkboxes unchanged; extraction-preview absent; Extract still enabled (D-01, D-02, D-04).
    - After Ask AI to retry cancel: extraction-preview remains; prior fields unchanged (D-06).
    - Retry after Extract cancel is the existing Extract button (no new empty panel) (D-04).
  </behavior>
  <action>
    Make Plan 01 cancel suite core REFN-03/04 cases green. If any fail for a product reason after Task 1, apply the smallest fix inside the cancelled / completed branches only (for example ensuring cancelled never falls into the API-error force-show branch, or ensuring showPreviewOnSuccess only gates completed success). Do not add a layout empty-state or new retry control for preview cancel. Do not clear selection or reset preview on cancel. Confirm both Extract-from-layout and Ask-AI-to-retry share the same cancelable `runExtractionPreview` path. Prefer a single feat commit covering Tasks 1–2 as one vertical GREEN slice (mirror Phase 2).
  </action>
  <acceptance_criteria>
    - After Extract Cancel: mask gone; preview hidden; layout checkbox still checked; Extract enabled; no toast; no contentUpdated.
    - After regenerate Cancel: preview visible with prior fields; no toast.
    - Late resolve still does not apply cancelled payload / mutate fields.
    - Focused cancel suite core REFN-03/04 cases are green (edge/idempotent/create-note boundary may remain for Plan 03).
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts</automated>
  </verify>
  <done>Cancel leaves preserved selection on layout or prior preview in-panel; no new empty recovery surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User Cancel → AbortSignal → generated fetch | Browser abort only; does not stop server AI. |
| CancelableApiResult → NoteRefinement state | Status narrowing is the only gate before preview mutation. |
| Extract / Ask AI to retry → runExtractionPreview | Fresh opt-in cancelable request; same note id + current selection body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Integrity ambiguity | Loading message / Cancel | medium | mitigate | Keep message AI is generating preview... and Phase 1 Cancel copy; client-only abort (PROJECT constraint). |
| T-03-02 | Tampering | Late response after cancel | high | mitigate | Early cancelled return + shared latch; cancel suite late-resolve assertion. |
| T-03-03 | Denial of Service | Stale Cancel | high | mitigate | Use Phase 1 identity-bound Cancel; do not re-bind cancel in the caller. |
| T-03-04 | Tampering | Accidental cancelable mutation | high | mitigate | Do not set cancelable on create-note / remove in this plan. |
| T-03-SC | Tampering | Package installs | low | accept | No new packages. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Cancel preview without losing layout choices | 03-02 vertical slice | COVERED |
| REQ | REFN-03, REFN-04 | Core cancel + preserve selection / prior preview | 03-02 tasks 1-2 | COVERED |
| RESEARCH | Pattern 1 | Cancelable preview load + status branch | 03-02 | COVERED |
| CONTEXT | D-01–D-10 | Landing, silence, message, opt-in, no empty panel | 03-02 | COVERED |
| UI-SPEC | adopt Cancel + existing retry CTAs | Product surfaces | 03-02 | COVERED |
</source_audit>

<verification>
Focused cancel suite is green for pending Cancel, Extract-cancel preserve selection, regenerate-cancel keep fields, silent cancel, and late non-apply. Create-note / remove / layout option lines unchanged.
</verification>

<success_criteria>
- User can cancel extraction-preview generation from the global blocker.
- Extract cancel stays on layout with selections; regenerate cancel keeps prior preview.
- One coherent Behavior slice: Cancel UI, abort, cleanup, and retry via existing Extract / Ask AI to retry land together.
</success_criteria>

<output>
Create `.planning/phases/03-cancel-extraction-preview-generation/03-02-SUMMARY.md` when done.
</output>
