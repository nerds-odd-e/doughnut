---
phase: 03-cancel-extraction-preview-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts
  - frontend/src/components/recall/NoteRefinement.vue
  - .cursor/rules/frontend-api.mdc
autonomous: true
requirements:
  - REFN-03
  - REFN-04
must_haves:
  truths:
    - statement: "Activating Extract after Extract-path cancel starts a fresh cancelable preview request under the same blocker contract."
      verification: "Call count increments; pending Cancel appears again on retry."
    - statement: "Double Cancel is idempotent at the product seam (no throw, no toast, selection preserved)."
      verification: "Double-click Cancel assertions green."
    - statement: "If Ask AI to retry is interrupted, the same cancel contract applies without wiping prior preview."
      verification: "Cancel-during-regenerate + late resolve leave prior fields (may already be green from 03-02; reinforce if needed)."
    - statement: "Cancelling preview leaves any older concurrent loading state intact."
      verification: "Optional product-level concurrent survivor case green, or rely on Phase 1 identity binding with documented acceptance if suite already proves via shared tests."
    - statement: "Create-note pending still shows AI is creating note... without Cancel (D-10 / Phase 4 boundary)."
      verification: "extractNote create-loading case or cancel-spec boundary: message present; Cancel absent while create pending."
    - statement: "frontend-api.mdc documents extraction-preview as an allowed second cancelable opt-in alongside layout, still forbidding cancelable mutations and cancelable runWithBlockingApiLoading."
      verification: "Rule mentions extract/preview opt-in; mutation forbid remains."
    - statement: "Under frontend/src, cancelable true appears only in NoteRefinement.vue (layout + preview) and clientSetup.ts."
      verification: "rg exclusivity gate."
  artifacts:
    - path: frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts
      provides: "Green retry-fresh, idempotent, create-note noncancelable, and late/concurrent edge coverage."
    - path: .cursor/rules/frontend-api.mdc
      provides: "Documented second product cancelable opt-in (extraction preview)."
  key_links:
    - from: extract-refinement-layout after cancel
      to: extractNotePreview
      via: "Each Extract creates a new cancelable apiCallWithLoading invocation with a fresh signal."
    - from: preview Cancel
      to: older concurrent blocker
      via: "Exact-id finishLoading leaves the older state selected after cancel."
---

## Phase Goal

**As a** note author using Refine note, **I want to** cancel AI extraction-preview generation without losing my layout selections, **so that** I can retry Extract from the same choices or keep my prior preview if I cancelled a regenerate.

## Artifacts this phase produces

| File / symbol | Role |
|---|---|
| Cancel suite edge cases | Extract retry-fresh; double-cancel; create-note noncancelable; concurrent survivor if needed. |
| `.cursor/rules/frontend-api.mdc` preview mention | Documents extraction-preview as second safe cancelable opt-in. |
| Any minimal NoteRefinement.vue fixups | Only if edges reveal gaps (no Phase 4 create-note cancel). |

<objective>
Harden Phase 3 edges required by REFN-04 retry/idempotency probes, prove create-note remains noncancelable, and extend frontend-api.mdc for the second product cancelable opt-in.

Purpose: Close concurrency/idempotency/scope-boundary probes and RESEARCH open questions without expanding to REFN-05.
Output: Fully green cancel suite including retry-fresh + create-note boundary; updated frontend-api rule; layout cancel suite still green.
</objective>

<execution_context>
@/Users/terryyin/.cursor/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-cancel-extraction-preview-generation/03-02-PLAN.md
@.planning/phases/03-cancel-extraction-preview-generation/03-RESEARCH.md
@.planning/phases/03-cancel-extraction-preview-generation/03-VALIDATION.md
@.planning/phases/03-cancel-extraction-preview-generation/03-UI-SPEC.md
@.cursor/rules/frontend-api.mdc
@frontend/src/components/recall/NoteRefinement.vue
@frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts
@frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
@frontend/tests/components/recall/NoteRefinement.extractNote.spec.ts

<interfaces>
Shared exact-id cancellation already owns concurrent cleanup (Phase 1). Product work here only proves preview cancel does not clear other states, Extract after cancel re-enters `runExtractionPreview`, and create-note stays noncancelable. Rule docs must describe the literal overload already exported from `clientSetup.ts` — mention preview as a second allowed read-only opt-in; do not invent AbortError-name matching or a cancelable composite helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Green retry-fresh, idempotent, and create-note boundary edges</name>
  <files>frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts, frontend/src/components/recall/NoteRefinement.vue</files>
  <read_first>
    - .planning/phases/03-cancel-extraction-preview-generation/03-VALIDATION.md
    - .planning/phases/03-cancel-extraction-preview-generation/03-RESEARCH.md
    - frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts
    - frontend/tests/components/recall/NoteRefinement.extractNote.spec.ts
    - frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
    - frontend/src/components/recall/NoteRefinement.vue
  </read_first>
  <behavior>
    - After Extract cancel, clicking Extract again increments extractNotePreview and can show Cancel again (REFN-04 / D-04).
    - Double Cancel does not throw or toast; selection remains (idempotency).
    - Create-note pending shows AI is creating note... without Cancel (D-10).
    - Layout cancel suite remains green (preview work did not regress Phase 2).
    - Optional: older concurrent blocker survives preview Cancel (product-level identity proof).
  </behavior>
  <action>
    Extend the Plan 01 cancel suite with Extract retry-fresh call-count, idempotent double Cancel, and create-note noncancelable boundary (hold createExtractedNote pending and assert Cancel absent while message AI is creating note... is visible — mirror extractNote create-loading if already present, otherwise add a focused boundary case). Ensure late-resolve and regenerate-cancel cases remain green. If any fail for a product reason, apply the smallest NoteRefinement.vue fix without opting create-note into cancelable and without adding empty-layout recovery for preview. After green cancel suite, run extractNote.spec and layoutGeneration.cancel.spec regressions. Under frontend/src, cancelable true may appear only in NoteRefinement.vue and clientSetup.ts.
  </action>
  <acceptance_criteria>
    - Extract after cancel increases extractNotePreview invocation count by at least one.
    - Double Cancel leaves layout selection intact without toast.error.
    - Create-note pending: message AI is creating note... present; Cancel not shown for that blocker.
    - NoteRefinement.extractNote.spec.ts and NoteRefinement.layoutGeneration.cancel.spec.ts still pass.
    - Under frontend/src, cancelable true only in NoteRefinement.vue and clientSetup.ts.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c bash -lc 'set -euo pipefail; pnpm frontend:test tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts tests/components/recall/NoteRefinement.extractNote.spec.ts tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts; MATCHES=$(rg -n "cancelable:\\s*true" frontend/src || true); echo "$MATCHES"; echo "$MATCHES" | rg -q "NoteRefinement\\.vue"; echo "$MATCHES" | rg -q "clientSetup\\.ts"; ! echo "$MATCHES" | rg -v "NoteRefinement\\.vue|clientSetup\\.ts" | rg -q .'</automated>
  </verify>
  <done>Probe edges for retry/idempotency and create-note noncancelable scope boundary are green.</done>
</task>

<task type="auto">
  <name>Task 2: Document extraction-preview cancelable opt-in in frontend-api.mdc</name>
  <files>.cursor/rules/frontend-api.mdc</files>
  <read_first>
    - .cursor/rules/frontend-api.mdc
    - frontend/src/managedApi/clientSetup.ts
    - .planning/phases/03-cancel-extraction-preview-generation/03-RESEARCH.md
    - .planning/phases/03-cancel-extraction-preview-generation/03-CONTEXT.md
  </read_first>
  <action>
    Update the Cancelable blocking calls subsection of `.cursor/rules/frontend-api.mdc` to note that note-refinement extraction-preview generation is a second allowed read-only cancelable opt-in (alongside layout generation), using the same literal overload and status narrowing. Keep existing forbids: no cancelable mutations, no cancelable `runWithBlockingApiLoading`, no AbortError-name classification, client-only abort. Optionally show or mention the preview message `AI is generating preview...` as an example alongside layout. Do not hand-edit generated SDK docs.
  </action>
  <acceptance_criteria>
    - frontend-api.mdc mentions extraction preview (or extractNotePreview / generating preview) as an allowed cancelable opt-in.
    - Docs still require status narrowing and forbid cancelable mutations / cancelable runWithBlockingApiLoading.
    - Client-only abort guidance remains present.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c bash -lc 'set -euo pipefail; RULE=.cursor/rules/frontend-api.mdc; rg -n -i "preview|extractNotePreview|generating preview" "$RULE"; rg -n "cancelable:\\s*true" "$RULE"; rg -n -i "status narrowing|CancelableApiResult|narrow.*status" "$RULE"; rg -n -i "mutation|runWithBlockingApiLoading" "$RULE"; rg -n -i "client-only|does not.*(stop|promise).*server|server work" "$RULE"'</automated>
  </verify>
  <done>Rule docs match the shipped cancelable preview opt-in without inventing a parallel API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Preview Cancel → concurrent loading states | Exact-id removal must not clear unrelated blockers. |
| Docs → future callers | Rule text must not encourage cancelable mutations (especially create-note). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Integrity ambiguity | frontend-api.mdc | medium | mitigate | Explicitly keep browser abort does not stop server work; preview is read-only opt-in only. |
| T-03-02 | Tampering | Late apply on retry cancel | high | mitigate | Late-resolve + regenerate-cancel assertions remain green. |
| T-03-03 | Denial of Service | Concurrent cancel targeting | high | mitigate | Optional older-blocker survival assertion at product seam; Phase 1 identity binding remains. |
| T-03-04 | Tampering | Accidental mutation cancel | high | mitigate | Create-note Cancel-absent regression + rg gate; docs forbid cancelable mutations. |
| T-03-SC | Tampering | Package installs | low | accept | No new packages. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| REQ | REFN-04 | Retry from preserved selection | 03-03 task 1 | COVERED |
| REQ | REFN-03 | Cancel visible while pending | already 03-02; edges reinforce | COVERED |
| RESEARCH | Pitfall 6 | No create-note cancel | 03-03 task 1 | COVERED |
| RESEARCH | Open Q docs | frontend-api.mdc preview mention | 03-03 task 2 | COVERED |
| CONTEXT | D-04, D-10 | Extract retry; create-note noncancelable | 03-03 | COVERED |
| UI-SPEC | create-note non-surface | Boundary regression | 03-03 | COVERED |
</source_audit>

<verification>
Full cancel suite green including retry-fresh and create-note boundary. extractNote + layout cancel regressions green. frontend-api.mdc documents preview opt-in. Only NoteRefinement + clientSetup use cancelable true under frontend/src.
</verification>

<success_criteria>
- Extract after cancel starts a fresh cancelable preview request.
- Create-note remains noncancelable.
- Docs and scope boundary prevent unsafe copy-paste of cancelable onto mutations.
</success_criteria>

<output>
Create `.planning/phases/03-cancel-extraction-preview-generation/03-03-SUMMARY.md` when done.
</output>
