---
phase: 12-title-navigate-reopen-e2e-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
  - e2e_test/features/recall/accidental_match_reveal.feature
  - e2e_test/step_definitions/accidental_match.ts
autonomous: true
requirements:
  - AMR-05
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After Resolve → matched title navigate → history back, accidental-match alert and Resolve CTA remain; opening Resolve again lists the same match title(s) (D-01, D-02, D-03, D-06; AMR-05)"
    - "Return uses history back (cy.go back); product path does not rely on Main-menu Resume or full document remount (D-03)"
    - "Match list comes from live KeepAlive previousAnsweredQuestions matchedNotes — no OpenAPI/previouslyAnswered enrichment (D-04, D-05)"
    - "Existing open/dismiss path in expectAccidentalMatchReveal stays green; overlap_try_again stays green and uncoupled (D-07)"
    - "Capability-named E2E scenario and helpers — no phase numbers in product tests (D-06, D-08)"
  artifacts:
    - path: e2e_test/start/pageObjects/AnsweredQuestionPage.ts
      provides: "openResolveDialog / clickMatchedNoteTitle / returnToRecallViaHistoryBack / expectResolveAvailableAgainWithMatch helpers"
    - path: e2e_test/features/recall/accidental_match_reveal.feature
      provides: "Capability-named reopen-after-title-navigate scenario"
    - path: e2e_test/step_definitions/accidental_match.ts
      provides: "Thin When/Then wrappers for navigate + history back + reopen asserts"
  key_links:
    - from: AnsweredQuestionPage.clickMatchedNoteTitle
      to: NoteTitleWithLink router-link inside accidental-match-resolve-dialog
      via: "Real in-app navigation closes Modal; KeepAlive deactivates RecallPage (D-01)"
    - from: returnToRecallViaHistoryBack
      to: KeepAlive-activated RecallPage previousAnsweredQuestions + cursor
      via: "cy.go('back') + waitUntilAppIsNotBusy; CTA gated on matchedNotes (D-03, D-05)"
    - from: Manual Resolve CTA after return
      to: AccidentalMatchResolveDialog match list
      via: "User clicks resolve-accidental-match again — no auto-open (D-02)"
---

<objective>
Prove AMR-05 end-to-end: after title navigate from resolve and history-back return, manual Resolve reopen shows the same matches — via page objects + capability-named E2E (D-01..D-08).

Purpose: Behavior phase accepting reopen-after-navigate; research expects KeepAlive live session so product code stays untouched unless Plan 02 contingency fires.
Output: AnsweredQuestionPage reopen helpers, accidental_match_reveal scenario + thin steps, targeted Cypress green with overlap_try_again still green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-title-navigate-reopen-e2e-polish/12-CONTEXT.md
@.planning/phases/12-title-navigate-reopen-e2e-polish/12-RESEARCH.md
@.planning/phases/12-title-navigate-reopen-e2e-polish/12-PATTERNS.md
@.planning/phases/12-title-navigate-reopen-e2e-polish/12-UI-SPEC.md
@.planning/phases/12-title-navigate-reopen-e2e-polish/12-VALIDATION.md
@.cursor/rules/e2e-authoring.mdc
@.cursor/rules/planning.mdc
</context>

## Artifacts this phase produces

- E2E reopen-after-title-navigate path (AMR-05)
- Page-object history-back return (D-03) with manual Resolve reopen (D-02)

## UI Considerations (from 12-UI-SPEC)

No new chrome, copy, or testids. Reuse resolve-accidental-match / accidental-match-resolve-dialog / accidental-match-alert. After return assert alert + Resolve CTA then reopen list — dialog stays closed until CTA click.

## Multi-Source Coverage (this plan)

| SOURCE | ID | Item | Status |
|--------|-----|------|--------|
| GOAL | — | Return + reopen resolve with same matches; E2E polish | COVERED |
| REQ | AMR-05 | Reopen resolve after title navigate + return | COVERED |
| RESEARCH | — | E2E-first; KeepAlive; cy.go back; no OpenAPI | COVERED |
| CONTEXT | D-01..D-08 | Title leave, manual reopen, history back, no API enrich, E2E shape | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end reopen after matched title + history back</name>
  <reversibility rating="reversible">E2E + page-object only; undo is revert of helpers/scenario (D-06).</reversibility>
  <files>e2e_test/start/pageObjects/AnsweredQuestionPage.ts, e2e_test/features/recall/accidental_match_reveal.feature, e2e_test/step_definitions/accidental_match.ts</files>
  <read_first>
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/step_definitions/accidental_match.ts
    - .planning/phases/12-title-navigate-reopen-e2e-polish/12-PATTERNS.md
    - .planning/phases/12-title-navigate-reopen-e2e-polish/12-RESEARCH.md (Pattern 3 + recommended E2E helpers)
    - .planning/phases/12-title-navigate-reopen-e2e-polish/12-CONTEXT.md (D-01..D-08)
    - .planning/phases/12-title-navigate-reopen-e2e-polish/12-UI-SPEC.md (Interaction Contract)
  </read_first>
  <behavior>
    - After accidental-match reveal for sedation/sedition, open Resolve, click matched title link "sedation", leave recall to note show
    - History back returns to /recall with accidental-match alert + visible resolve-accidental-match CTA
    - Opening Resolve again shows the same matched title "sedation" in accidental-match-resolve-dialog
    - Dialog is closed after title navigate until CTA is clicked again (manual reopen per D-02)
  </behavior>
  <action>
    Per D-06/D-01/D-02/D-03, extend AnsweredQuestionPage with fluent helpers mirroring openResolveAndClickMatchedNoteCta style: openResolveDialog (click resolve-accidental-match, waitUntilAppIsNotBusy, assert accidental-match-resolve-dialog visible), clickMatchedNoteTitle(title) (within dialog cy.contains('a', title).click, waitUntilAppIsNotBusy, assert note-show URL), returnToRecallViaHistoryBack (cy.go('back'), waitUntilAppIsNotBusy, assert url includes /recall), and expectResolveAvailableAgainWithMatch(answer, matchedNoteTitle) (alert + visible Resolve CTA + openResolveDialog + dialog contains matched title). Prefer page-object over Gherkin churn. Per D-03 discretion use cy.go('back') — not Main-menu Resume (clears cursor) and not full-document reload of /recall. Per D-04/D-05 expect KeepAlive live matchedNotes; do not change backend or OpenAPI. Add one capability-named scenario to accidental_match_reveal.feature (reuse Background + sedition/sedation setup; no phase numbers), initially @wip if red: open resolve and navigate to matched note, return via history back, then assert resolve available again with same match. Wire thin When/Then in accidental_match.ts that only call the new page-object methods. Leave NoteTitleWithLink navigable (D-01). Single-match reopen satisfies AMR-05 (research discretion). Assume pnpm sut running.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature --env tags='@wip'</automated>
  </verify>
  <acceptance_criteria>
    - AnsweredQuestionPage exposes openResolveDialog, clickMatchedNoteTitle, returnToRecallViaHistoryBack, expectResolveAvailableAgainWithMatch
    - accidental_match_reveal.feature has capability-named reopen-after-title-navigate scenario
    - accidental_match.ts When/Then steps delegate only to those helpers
    - Tracer path exercises Resolve → title click → history back → Resolve CTA → same match title in dialog
  </acceptance_criteria>
  <done>Thin E2E path for AMR-05 exists end-to-end via page objects + scenario (D-01, D-02, D-03, D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Green reopen scenario and keep overlap uncoupled</name>
  <files>e2e_test/features/recall/accidental_match_reveal.feature, e2e_test/start/pageObjects/AnsweredQuestionPage.ts, e2e_test/step_definitions/accidental_match.ts</files>
  <read_first>
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/features/recall/overlap_try_again.feature
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
    - .planning/phases/12-title-navigate-reopen-e2e-polish/12-RESEARCH.md (Pitfalls 1–4; contingency order)
    - .planning/phases/12-title-navigate-reopen-e2e-polish/12-CONTEXT.md (D-07, D-08)
  </read_first>
  <behavior>
    - Reopen scenario passes without @wip
    - Existing Accidental match reveals scenario still opens/dismisses resolve via expectAccidentalMatchReveal
    - overlap_try_again.feature exits 0 and file content stays unchanged
  </behavior>
  <action>
    Per D-07/D-08, drive the reopen scenario green under KeepAlive (no product code in this plan). Remove @wip once green. Keep expectAccidentalMatchReveal open/dismiss asserts intact — extend helpers rather than rewriting the whole feature. Do not edit overlap_try_again.feature. If Cypress fails because Resolve CTA is missing after history back (remount / empty matchedNotes / previouslyAnswered path), stop product changes here: document remount symptoms in SUMMARY and leave Plan 02 contingency to harden KeepAlive name — still no OpenAPI (D-04). If green, SUMMARY must state KeepAlive path sufficient so Plan 02 can be skipped. Run targeted Cypress on accidental_match_reveal and overlap_try_again. Prefer page-object fixes over Gherkin text churn.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature,e2e_test/features/recall/overlap_try_again.feature</automated>
  </verify>
  <acceptance_criteria>
    - Reopen scenario is green without @wip
    - Targeted Cypress for accidental_match_reveal and overlap_try_again both exit 0
    - overlap_try_again.feature content unchanged
    - SUMMARY records whether KeepAlive path worked or remount symptoms require Plan 02
  </acceptance_criteria>
  <done>AMR-05 E2E green with open/dismiss preserved and overlap suite uncoupled (D-07, D-08); Plan 02 gated by SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress → app UI | E2E drives authenticated recall; asserts live graded matchedNotes after SPA history back |
| Client KeepAlive cache | Preserves previousAnsweredQuestions from authenticated answerSpelling response — not client-invented match IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | Match list after reopen | medium | mitigate | Assert only matches from live graded payload already returned by authenticated answer API; do not invent client-only match lists (D-04/D-05) |
| T-12-02 | Information Disclosure | History reload path | low | accept | previouslyAnswered remains lossy this phase; no enrichment that could widen history DTOs (D-04) |
| T-12-03 | Tampering | Match title rendering | medium | mitigate | Reuse existing NoteTitleWithLink text interpolation — no new HTML sinks |
| T-12-SC | Tampering | npm installs | high | accept | No package installs in this plan |
</threat_model>

<verification>
Targeted accidental_match_reveal + overlap_try_again Cypress with Nix prefix (pnpm sut assumed). Full E2E suite not required locally. No OpenAPI/backend changes.
</verification>

<success_criteria>
- Resolve → matched title → history back → manual Resolve shows same match title(s)
- Capability-named E2E without @wip; open/dismiss still covered
- overlap_try_again green and uncoupled
- SUMMARY states KeepAlive OK or remount → Plan 02
</success_criteria>

<output>
Create `.planning/phases/12-title-navigate-reopen-e2e-polish/12-01-SUMMARY.md` when done
</output>
