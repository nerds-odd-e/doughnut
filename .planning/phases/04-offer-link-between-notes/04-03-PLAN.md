---
phase: 04-offer-link-between-notes
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - e2e_test/features/recall/accidental_match_reveal.feature
  - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
  - e2e_test/start/pageObjects/noteTargetSearchDialog.ts
  - e2e_test/step_definitions/recall.ts
autonomous: false
requirements: [AM-04]
must_haves:
  truths:
    - "E2E: after an accidental-match reveal, the user can open Link to this note under the matched note and complete Add wiki link as a new property without leaving the recall result page (AM-04 property path)."
    - "E2E: from the same offer surface, the user can choose Add a new relationship note, pick a relation type, and remain on the accidental-match recall result after success (AM-04 relationship path + D-07)."
    - "E2E does not assert auto-created links: CTA opens choice; writes happen only after explicit choice/confirm."
    - "Feature remains capability-named accidental_match_reveal (no phase numbers in product/test artifact names)."
    - statement: "Visual CTA placement under each matched NoteShow feels intentional (UI-SPEC); confirmed by human spot-check."
      verification: backstop
  artifacts:
    - path: "e2e_test/features/recall/accidental_match_reveal.feature"
      provides: "Offer-link scenarios on accidental-match reveal"
      contains: "Link to this note"
    - path: "e2e_test/start/pageObjects/AnsweredQuestionPage.ts"
      provides: "Helpers to open matched-note link offer and assert stay-on-page"
      contains: "link-to-matched-note"
    - path: "e2e_test/step_definitions/recall.ts"
      provides: "Cucumber steps for offer-link flows"
      contains: "link"
  key_links:
    - from: "accidental_match_reveal.feature"
      to: "AnsweredSpellingQuestion + MatchedNoteLinkOffer"
      via: "accidental match → Link to this note → property or relationship → still on recall result"
      pattern: "Link to this note"
---

## Phase Goal

**As a** learner doing spelling recall, **I want to** build a link between the reviewed note and a matched note after an accidental match with the matched note already selected, **so that** I can turn recall confusion into a lasting connection without the system auto-writing a link.

<objective>
Prove AM-04 end-to-end through Cypress on the existing accidental_match_reveal capability feature: property and relationship offers from the matched-note CTA, with D-07 stay-on-page, then human visual spot-check.

Purpose: UI hint is yes — close the phase with observable E2E + human verify after unit-proven waves.
Output: Extended accidental_match_reveal.feature (+ page objects/steps); @wip until green; human approval checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offer-link-between-notes/04-CONTEXT.md
@.planning/phases/04-offer-link-between-notes/04-UI-SPEC.md
@.planning/phases/04-offer-link-between-notes/04-PATTERNS.md
@.planning/phases/04-offer-link-between-notes/04-VALIDATION.md
@.planning/phases/04-offer-link-between-notes/04-01-PLAN.md
@.planning/phases/04-offer-link-between-notes/04-02-PLAN.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-03-SUMMARY.md
e2e_test/features/recall/accidental_match_reveal.feature
e2e_test/start/pageObjects/AnsweredQuestionPage.ts
e2e_test/start/pageObjects/noteTargetSearchDialog.ts
e2e_test/step_definitions/recall.ts
</context>

<nyquist_validation>
Per 04-VALIDATION.md Wave 0: append E2E scenarios to accidental_match_reveal.feature; extend AnsweredQuestionPage with link helpers; reuse noteTargetSearchDialog button labels (Add wiki link as a new property / Add a new relationship note). Tag @wip until green, then untag. Phase gate: targeted E2E green before human verify.
</nyquist_validation>

<tasks>

<task type="auto" tdd="true">
  <name>E2E offer-link scenarios on accidental_match_reveal</name>
  <precondition>Plans 04-01 and 04-02 complete and frontend unit tests green; pnpm sut (or equivalent) serves the app for Cypress.</precondition>
  <reversibility rating="reversible">Test-only E2E artifacts; product behavior already shipped in prior waves.</reversibility>
  <files>e2e_test/features/recall/accidental_match_reveal.feature, e2e_test/start/pageObjects/AnsweredQuestionPage.ts, e2e_test/start/pageObjects/noteTargetSearchDialog.ts, e2e_test/step_definitions/recall.ts</files>
  <read_first>
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts (expectAccidentalMatchReveal)
    - e2e_test/start/pageObjects/noteTargetSearchDialog.ts (createRelationshipToTargetAs / button labels)
    - e2e_test/step_definitions/recall.ts
    - .planning/phases/04-offer-link-between-notes/04-UI-SPEC.md (CTA testid + choice copy)
    - .planning/phases/04-offer-link-between-notes/04-PATTERNS.md (E2E section)
    - .planning/phases/04-offer-link-between-notes/04-VALIDATION.md
  </read_first>
  <behavior>
    - After accidental-match reveal for spelling answer naming another note, Link to this note is visible under the matched note.
    - Property path: open CTA → see Link to: matched title (no note search field) → Add wiki link as a new property → dialog closes → accidental-match alert / matched-notes-section still visible (stay on result).
    - Relationship path: open CTA → Add a new relationship note → select a valid relation type → confirm → dialog closes → still on accidental-match recall result (URL/path still recall result, not note show of the new relationship note) (D-07).
  </behavior>
  <action>
    Extend the capability-named accidental_match_reveal feature (do not invent a parallel phase-numbered feature):

    1. Add scenario(s) tagged @wip until green covering property offer and relationship offer. Reuse Background seeding from the existing reveal scenario where possible; ensure reviewed note is writable by the logged-in user so CTAs appear (D-06).

    2. Extend AnsweredQuestionPage.expectAccidentalMatchReveal return (or sibling helpers) with e.g. openLinkToMatchedNote(matchedTitleOrId), linkMatchedNoteAsProperty(...), linkMatchedNoteAsRelationship(..., relationType) using data-testid link-to-matched-note-* and existing choice button roles/names from noteTargetSearchDialog.

    3. Wire Then/When steps in recall.ts. Assert stay-on-page after success (recall result chrome still present; do not land on the new relationship note page).

    4. Run: CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature. Fix selectors/seeding first; product only if a real gap remains after 04-01/04-02.

    5. When green, remove @wip from the new scenarios (CI ≤5 @wip cap).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature</automated>
  </verify>
  <acceptance_criteria>
    - accidental_match_reveal.feature includes offer-link coverage for property and relationship paths.
    - Page object helpers target link-to-matched-note-* and existing choice button labels.
    - Scenarios run without @wip and exit 0 on the targeted Cypress command.
    - No phase numbers in feature/file names.
  </acceptance_criteria>
  <done>Targeted E2E proves AM-04 offer-link property + relationship with stay-on-page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human spot-check: CTA placement + offer dialog</name>
  <what-built>
    Per-matched-note Link to this note (daisy-btn-secondary sm) under matched NoteShows; MatchedNoteLinkOffer with property + relationship; D-07 stay on accidental-match result; E2E green on accidental_match_reveal.
  </what-built>
  <how-to-verify>
    1. With local app running, trigger an accidental-match spelling answer so reviewed + matched notes reveal.
    2. Confirm each matched note has a clear Link to this note control directly under its NoteShow (mt-2, secondary small) — not competing with the alert.
    3. Open the CTA: dialog shows Link to: matched title; Insert as a wiki link is absent; property (accent) and relationship (secondary) options present.
    4. Complete a property link: dialog closes; still on the same recall result (both notes visible).
    5. Optionally complete a relationship link on another run: dialog closes; still on recall result (not navigated to the new relationship note).
    6. If possible, open a read-only reviewed notebook context and confirm CTAs are omitted while matches still show.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E browser → app | Exercises real session + existing write APIs; no new endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | E2E write paths | high | mitigate | Scenarios use the logged-in owner of writable notes; readonly omission covered in unit (04-01) and optional human check; server auth unchanged |
| T-04-SC | Tampering | package installs | high | accept | No new packages |
</threat_model>

<verification>
CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature
Human checkpoint approved.
</verification>

<success_criteria>
- AM-04 observable via E2E: offer existing add-link UI with matched pre-selected; property + relationship; never auto-write; D-07 stay on result.
- Human approves CTA visual placement / dialog chrome.
</success_criteria>

<output>
Create `.planning/phases/04-offer-link-between-notes/04-03-SUMMARY.md` when done
</output>
