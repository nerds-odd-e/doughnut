---
phase: 04-offer-link-between-notes
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/store/StoredApiCollection.ts
  - frontend/src/components/links/AddRelationshipFinalize.vue
  - frontend/src/components/recall/MatchedNoteLinkOffer.vue
  - frontend/tests/components/recall/MatchedNoteLinkOffer.spec.ts
  - frontend/tests/links/AddRelationship.spec.ts
autonomous: true
requirements: [AM-04]
must_haves:
  truths:
    - "From MatchedNoteLinkOffer, choosing Add a new relationship note shows AddRelationshipFinalize with the matched note as targetSearchResult (D-03 relationship path)."
    - "Confirming a relationship creates the relationship note via existing createRootNoteAtNotebook, then closes the dialog and leaves the user on the accidental-match recall result — no router navigation to the new note (D-07 resolved)."
    - "Toolbar / default AddRelationshipFinalize callers still navigate after create (default navigateOnSuccess true / skipNavigation unset)."
    - "Relationship create API is not called until the user confirms relation type in AddRelationshipFinalize (never auto-write)."
  artifacts:
    - path: "frontend/src/store/StoredApiCollection.ts"
      provides: "createRootNoteAtNotebook optional skipNavigation"
      contains: "skipNavigation"
    - path: "frontend/src/components/links/AddRelationshipFinalize.vue"
      provides: "Optional navigateOnSuccess prop (default true)"
      contains: "navigateOnSuccess"
    - path: "frontend/src/components/recall/MatchedNoteLinkOffer.vue"
      provides: "Two-stage choice → AddRelationshipFinalize with navigateOnSuccess false"
      contains: "AddRelationshipFinalize"
    - path: "frontend/tests/components/recall/MatchedNoteLinkOffer.spec.ts"
      provides: "Relationship path + D-07 no navigation assertions"
      contains: "relationship"
  key_links:
    - from: "MatchedNoteLinkOffer.vue"
      to: "AddRelationshipFinalize.vue"
      via: "chooseAddRelationship sets targetSearchResult; finalize @success → closeDialog"
      pattern: "AddRelationshipFinalize"
    - from: "AddRelationshipFinalize.vue"
      to: "StoredApiCollection.createRootNoteAtNotebook"
      via: "navigateOnSuccess false → options.skipNavigation true"
      pattern: "skipNavigation"
---

## Phase Goal

**As a** learner doing spelling recall, **I want to** build a link between the reviewed note and a matched note after an accidental match with the matched note already selected, **so that** I can turn recall confusion into a lasting connection without the system auto-writing a link.

<objective>
Complete AM-04’s relationship-note path on the recall offer, resolving D-07 vs AddRelationshipFinalize’s post-create navigation: stay on the accidental-match result after success while preserving toolbar navigate-after-create defaults.

Purpose: Property path already works (04-01); this wave adds relationship finalize without forking creation logic.
Output: skipNavigation option + navigateOnSuccess prop + MatchedNoteLinkOffer second stage + Vitest.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offer-link-between-notes/04-CONTEXT.md
@.planning/phases/04-offer-link-between-notes/04-RESEARCH.md
@.planning/phases/04-offer-link-between-notes/04-PATTERNS.md
@.planning/phases/04-offer-link-between-notes/04-01-PLAN.md
frontend/src/components/links/AddRelationshipFinalize.vue
frontend/src/store/StoredApiCollection.ts
</context>

<d07_resolution>
Locked D-07 requires remaining on the accidental-match recall result after successful link. Today createRootNoteAtNotebook always calls navigateToFocusedNote after create (used by AddRelationshipFinalize). Resolution for this phase (Claude’s Discretion, reversible):

1. Add optional `skipNavigation?: boolean` to createRootNoteAtNotebook options. When true: still refresh NoteStorage, undo history, and wiki-title cache refresh; return the focused NoteRealm without router.push / navigateToFocusedNote.
2. Add optional `navigateOnSuccess` prop on AddRelationshipFinalize (default `true`). When false, pass `skipNavigation: true` into createRootNoteAtNotebook.
3. MatchedNoteLinkOffer mounts AddRelationshipFinalize with `:navigate-on-success="false"` and on `@success` emits closeDialog (PopButton closer). Do not auto-advance recall cards.

Toolbar SearchForm continues to omit the prop (default navigate-after-create unchanged).
</d07_resolution>

<nyquist_validation>
Per 04-VALIDATION.md / RESEARCH Validation Architecture: extend MatchedNoteLinkOffer.spec.ts for relationship + D-07; keep existing AddRelationship.spec.ts green for default navigation.
</nyquist_validation>

<tasks>

<task type="auto" tdd="true">
  <name>skipNavigation + navigateOnSuccess for stay-on-page relationship create</name>
  <precondition>Plan 04-01 complete: MatchedNoteLinkOffer property path and LinkInsertionChoice relationship button exist; chooseAddRelationship may set a ref without finalize mounted yet.</precondition>
  <reversibility rating="costly">Additive options on StoredApiCollection + AddRelationshipFinalize shared by toolbar and recall; default preserves today’s navigate-after-create, but the new flag is a cross-call-site contract (D-07).</reversibility>
  <files>frontend/src/store/StoredApiCollection.ts, frontend/src/components/links/AddRelationshipFinalize.vue, frontend/tests/links/AddRelationship.spec.ts</files>
  <read_first>
    - frontend/src/store/StoredApiCollection.ts (createRootNoteAtNotebook, navigateToFocusedNote)
    - frontend/src/components/links/AddRelationshipFinalize.vue (createRootNoteAtNotebook call ~144–155)
    - frontend/tests/links/AddRelationship.spec.ts
    - .planning/phases/04-offer-link-between-notes/04-CONTEXT.md (D-07)
    - .planning/phases/04-offer-link-between-notes/04-PATTERNS.md (D-07 vs finalize navigation note)
  </read_first>
  <behavior>
    - createRootNoteAtNotebook without skipNavigation still navigates (existing AddRelationship / toolbar behavior).
    - createRootNoteAtNotebook with skipNavigation true creates note, refreshes storage/caches, does not call router.push / navigateToFocusedNote.
    - AddRelationshipFinalize with navigateOnSuccess false passes skipNavigation and still emits success after create.
  </behavior>
  <action>
    Implement D-07 stay-on-page plumbing without changing default toolbar behavior:

    1. StoredApiCollection.createRootNoteAtNotebook: extend options with skipNavigation?: boolean. After successful create + refreshWikiLinkCacheForNoteIds loop, if skipNavigation then return focus NoteRealm; else existing navigateToFocusedNote(router, focus).

    2. AddRelationshipFinalize: add navigateOnSuccess Boolean prop default true. When calling createRootNoteAtNotebook, pass skipNavigation: navigateOnSuccess === false. Keep emit("success") after successful create. Do not fork markdown/placement logic.

    3. Extend or add a focused unit assertion in AddRelationship.spec.ts (or a thin StoredApi test if that is the local pattern) proving default path still navigates / recall-flag path skips — prefer mocking router and asserting push call counts. Keep existing relationship create scenarios green.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/links/AddRelationship.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - createRootNoteAtNotebook options type includes skipNavigation.
    - AddRelationshipFinalize accepts navigateOnSuccess defaulting to true.
    - Existing AddRelationship.spec.ts (and any new skipNavigation assertion) exit 0.
    - Default callers without the new prop still navigate after create.
  </acceptance_criteria>
  <done>Shared create API can create a relationship note without navigating when requested.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire MatchedNoteLinkOffer → AddRelationshipFinalize (D-07)</name>
  <precondition>Task 1 skipNavigation / navigateOnSuccess landed; MatchedNoteLinkOffer from 04-01 exists.</precondition>
  <reversibility rating="reversible">Recall wrapper only; mirrors SearchForm two-ref stage machine (D-03).</reversibility>
  <files>frontend/src/components/recall/MatchedNoteLinkOffer.vue, frontend/tests/components/recall/MatchedNoteLinkOffer.spec.ts</files>
  <read_first>
    - frontend/src/components/recall/MatchedNoteLinkOffer.vue
    - frontend/src/components/links/SearchForm.vue (selectedSearchResult / targetSearchResult two-stage)
    - frontend/src/components/links/AddRelationshipFinalize.vue
    - frontend/tests/components/recall/MatchedNoteLinkOffer.spec.ts
    - frontend/tests/links/AddRelationship.spec.ts
    - .planning/phases/04-offer-link-between-notes/04-RESEARCH.md (Pattern diagram relationship branch)
  </read_first>
  <behavior>
    - After chooseAddRelationship, AddRelationshipFinalize renders with note=reviewed Note and targetSearchResult=matched; goBack returns to LinkInsertionChoice.
    - Completing relationship create: createNoteAtNotebookRoot called once; closeDialog emitted; router.push not called (D-07).
    - Mount / choice-only still does not call create API (never auto-write).
  </behavior>
  <action>
    Complete SearchForm-like second stage in MatchedNoteLinkOffer (D-03):

    1. On @choose-add-relationship set targetSearchResult to the preselected matched NoteSearchResult; v-if AddRelationshipFinalize when targetSearchResult && source note ready; bind note + targetSearchResult; :navigate-on-success="false"; @success → emit closeDialog; @go-back → clear targetSearchResult.

    2. Vitest: mock NotebookController.createNoteAtNotebookRoot; mock/spy router; assert create after user selects relation type and submits; assert no router navigation; assert closeDialog emitted; assert create not called on mount or on merely opening choice.

    Capability-named tests only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/MatchedNoteLinkOffer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - MatchedNoteLinkOffer.vue imports and mounts AddRelationshipFinalize with navigateOnSuccess false.
    - Spec proves relationship create + closeDialog without router navigation (D-07).
    - Spec proves create API call count stays 0 until finalize confirm.
    - Vitest file exits 0.
  </acceptance_criteria>
  <done>Recall offer supports property and relationship paths; relationship success stays on the recall result page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → NotebookController.createNoteAtNotebookRoot | Relationship note creation; existing server write auth |
| navigateOnSuccess flag | Client-only UX; must not weaken server authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | createRootNoteAtNotebook from recall | high | mitigate | Reuse existing createNoteAtNotebookRoot auth; CTA already gated in 04-01 (D-06); skipNavigation only suppresses client routing, not auth |
| T-04-05 | Tampering | skipNavigation misuse | low | accept | Default false/unset preserves toolbar navigation; recall passes explicitly; no security boundary change |
| T-04-SC | Tampering | package installs | high | accept | No new packages |
</threat_model>

<verification>
CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/MatchedNoteLinkOffer.spec.ts tests/links/AddRelationship.spec.ts
</verification>

<success_criteria>
- AM-04 relationship path works from recall with matched pre-selected.
- D-07 honored for relationship success (stay on recall result).
- Toolbar relationship create navigation unchanged.
</success_criteria>

<output>
Create `.planning/phases/04-offer-link-between-notes/04-02-SUMMARY.md` when done
</output>
