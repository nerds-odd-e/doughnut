---
phase: 04-offer-link-between-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/links/LinkInsertionChoice.vue
  - frontend/src/utils/noteContentPropertyRows.ts
  - frontend/src/components/notes/core/NoteEditableContent.vue
  - frontend/src/components/recall/MatchedNoteLinkOffer.vue
  - frontend/src/components/recall/AnsweredSpellingQuestion.vue
  - frontend/tests/components/recall/MatchedNoteLinkOffer.spec.ts
  - frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts
autonomous: true
requirements: [AM-04]
must_haves:
  truths:
    - "After ACCIDENTAL_MATCH with matchedNotes, each matched-note row shows a Link to this note control (data-testid link-to-matched-note-{id}) when the reviewed notebook is writable (D-01, D-06 / UI-SPEC)."
    - "Clicking Link to this note opens the existing choice UI already past search with Link to: showing the matched note title (D-03 preselection); bare Insert as a wiki link is hidden (D-05 bareWikiLinkAvailable=false)."
    - "Choosing Add wiki link as a new property writes a wiki-link property onto the reviewed note via updateTextField, then closes the dialog and leaves the user on the accidental-match recall result (D-02 source→target, D-07 property path)."
    - "Opening the offer and landing on choice does not call content or relationship create APIs until the user explicitly confirms a choice (AM-04 criterion 3)."
    - "When the reviewed notebookRealm.readonly is true, link CTAs are omitted while matched NoteShows still render (D-06)."
    - statement: "Long matched titles in Link to: wrap within existing NoteTitleComponent styling (UI-SPEC long-text backstop)."
      verification: backstop
  artifacts:
    - path: "frontend/src/components/recall/MatchedNoteLinkOffer.vue"
      provides: "Recall-scoped preselected LinkInsertionChoice + direct property-write path"
      contains: "MatchedNoteLinkOffer"
    - path: "frontend/src/components/links/LinkInsertionChoice.vue"
      provides: "Optional bareWikiLinkAvailable prop (default true)"
      contains: "bareWikiLinkAvailable"
    - path: "frontend/src/utils/noteContentPropertyRows.ts"
      provides: "Shared appendWikiLinkPropertyRow pure helper"
      contains: "appendWikiLinkPropertyRow"
    - path: "frontend/src/components/recall/AnsweredSpellingQuestion.vue"
      provides: "Per-matched-note Link to this note PopButton CTAs"
      contains: "link-to-matched-note"
    - path: "frontend/tests/components/recall/MatchedNoteLinkOffer.spec.ts"
      provides: "Vitest for preselection, property write, never-auto-write"
      contains: "MatchedNoteLinkOffer"
    - path: "frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts"
      provides: "CTA visibility / per-match count / readonly gate"
      contains: "link-to-matched-note"
  key_links:
    - from: "AnsweredSpellingQuestion.vue"
      to: "MatchedNoteLinkOffer.vue"
      via: "PopButton under matched-note-{id} → offer with reviewed source id + matched target id"
      pattern: "MatchedNoteLinkOffer"
    - from: "MatchedNoteLinkOffer.vue"
      to: "storedApi().updateTextField"
      via: "buildWikiLinkText → appendWikiLinkPropertyRow → updateTextField(sourceId, edit content, composed)"
      pattern: "updateTextField"
    - from: "MatchedNoteLinkOffer.vue"
      to: "LinkInsertionChoice.vue"
      via: "bareWikiLinkAvailable false; wikiPropertyOptionAvailable from parseNoteContentMarkdown(source).ok"
      pattern: "bareWikiLinkAvailable"
---

## Phase Goal

**As a** learner doing spelling recall, **I want to** build a link between the reviewed note and a matched note after an accidental match with the matched note already selected, **so that** I can turn recall confusion into a lasting connection without the system auto-writing a link.

<objective>
Ship the production-quality tracer for AM-04: per-matched-note Link to this note CTA → MatchedNoteLinkOffer (preselected target) → property wiki-link write via direct updateTextField, with bare wiki-insert hidden (D-01–D-06 property path; D-07 for property success).

Purpose: Prove the architecture end-to-end on the highest-value path (property link) without touching SearchForm’s cursor singleton and without expanding AnsweredQuestion.
Output: MatchedNoteLinkOffer + CTA wiring + bareWikiLinkAvailable + appendWikiLinkPropertyRow + Vitest coverage for CTA/property.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offer-link-between-notes/04-CONTEXT.md
@.planning/phases/04-offer-link-between-notes/04-RESEARCH.md
@.planning/phases/04-offer-link-between-notes/04-PATTERNS.md
@.planning/phases/04-offer-link-between-notes/04-UI-SPEC.md
@.planning/phases/04-offer-link-between-notes/04-VALIDATION.md
@.planning/phases/03-reveal-both-notes-after-accidental-match/03-03-SUMMARY.md
</context>

<nyquist_validation>
Per `.planning/phases/04-offer-link-between-notes/04-VALIDATION.md` and RESEARCH § Validation Architecture:
- Wave 0: create `MatchedNoteLinkOffer.spec.ts`; extend `AnsweredSpellingQuestion.spec.ts` CTA cases in this plan.
- Sampling: after each task commit run the touched Vitest file(s); after wave merge run `pnpm frontend:test`.
- Relationship path + E2E remain Wave 2–3 (04-02 / 04-03).
</nyquist_validation>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end property link offer — CTA → choice → updateTextField</name>
  <precondition>Phase 3 complete: AnsweredSpellingQuestion shows matched-notes-section with NoteShow per matchedNotes entry on ACCIDENTAL_MATCH; NoteStorage loads realms via those NoteShows.</precondition>
  <reversibility rating="reversible">Additive Vue wrapper + optional prop + pure helper; toolbar SearchForm path unchanged (D-03, D-05).</reversibility>
  <files>frontend/src/components/recall/MatchedNoteLinkOffer.vue, frontend/src/components/links/LinkInsertionChoice.vue, frontend/src/utils/noteContentPropertyRows.ts, frontend/src/components/notes/core/NoteEditableContent.vue, frontend/src/components/recall/AnsweredSpellingQuestion.vue, frontend/tests/components/recall/MatchedNoteLinkOffer.spec.ts, frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts</files>
  <read_first>
    - .planning/phases/04-offer-link-between-notes/04-CONTEXT.md (D-01–D-07)
    - .planning/phases/04-offer-link-between-notes/04-RESEARCH.md (Patterns 1–4, Pitfall 1–4, Architecture diagram)
    - .planning/phases/04-offer-link-between-notes/04-PATTERNS.md (MatchedNoteLinkOffer / SearchForm two-stage minus search / dead-link write)
    - .planning/phases/04-offer-link-between-notes/04-UI-SPEC.md (CTA copy, colors, testids, spacing)
    - frontend/src/components/links/SearchForm.vue (closeDialogThen, onLinkDeadLinkToNote updateTextField)
    - frontend/src/components/links/LinkInsertionChoice.vue
    - frontend/src/components/notes/core/NoteToolbar.vue (PopButton + SearchForm host)
    - frontend/src/components/commons/Popups/PopButton.vue (btnClass, closer slot)
    - frontend/src/components/recall/AnsweredSpellingQuestion.vue (matched-notes-section)
    - frontend/src/components/notes/core/NoteEditableContent.vue (registerWikiPropertyInserter.insert body to extract)
    - frontend/src/utils/noteContentPropertyRows.ts
    - frontend/src/utils/buildWikiLinkText.ts
    - frontend/tests/links/AddRelationship.spec.ts (withCleanStorage, refreshNoteRealm, mockSdkService)
    - frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts
  </read_first>
  <behavior>
    - MatchedNoteLinkOffer with reviewed + matched ids seeded in NoteStorage: renders LinkInsertionChoice showing Link to: matched title; primary Insert as a wiki link button absent when bareWikiLinkAvailable is false; Add wiki link as a new property present when wiki property parse is ok; Add a new relationship note absent in Wave 1 (relationshipOptionAvailable false — finalize lands in 04-02).
    - Clicking Add wiki link as a new property calls TextContentController.updateNoteContent (via updateTextField) once with composed content containing a wiki link to the matched note; emits closeDialog; no createNoteAtNotebookRoot call.
    - Mounting offer alone does not call updateNoteContent or createNoteAtNotebookRoot (never auto-write).
    - AnsweredSpellingQuestion ACCIDENTAL_MATCH with writable reviewed realm: one link-to-matched-note-{id} per matchedNotes entry; clicking opens modal containing Link to: for that match.
    - AnsweredSpellingQuestion with reviewed notebookRealm.readonly true: zero link-to-matched-note-* controls; matched-notes-section still present.
  </behavior>
  <action>
    Implement one production property-link path through every layer this phase touches (AM-04 / D-01–D-06; D-07 for property close). Do not expand AnsweredQuestion or matchedNotes wire contract (D-04).

    1. LinkInsertionChoice: add optional bareWikiLinkAvailable (default true) and optional relationshipOptionAvailable (default true). Hide the primary bare-wiki button when bareWikiLinkAvailable is false and there is no deadLinkDisplayText (D-05). Hide the secondary "Add a new relationship note" button when relationshipOptionAvailable is false (stop-safe interim for Wave 1 — no dead-end). Leave accent property button unchanged. Existing SearchForm callers stay on defaults true/true.

    2. Extract appendWikiLinkPropertyRow(content, linkText) → string | undefined into noteContentPropertyRows.ts (parse → sorted rows → propertyRowWithScalar empty key → validate → compose). Refactor NoteEditableContent registerWikiPropertyInserter.insert to call the helper, keeping caret/rich-editor side effects local (RESEARCH Pattern 2 / A2).

    3. New MatchedNoteLinkOffer.vue under recall/: props reviewedNoteId + matchedNoteId; load both via getNoteRealmRefAndLoadWhenNeeded (D-04 Pattern 1); build NoteSearchResult from matched realm (notebookId/name from realm, not topology); source Note from reviewed realm.note. Seed selectedSearchResult from matched (skip SearchForNoteAndFolder). Render LinkInsertionChoice with bareWikiLinkAvailable false, relationshipOptionAvailable false (Wave 1), and wikiPropertyOptionAvailable from parseNoteContentMarkdown(source.content).ok (not from the cursor-inserter canInsert helper — RESEARCH Pitfall 1). On chooseInsertWikiLinkAsProperty: buildWikiLinkText(target, { notebookId: source notebook id }) → appendWikiLinkPropertyRow → storedApi().updateTextField(source.id, "edit content", composed) using SearchForm closeDialogThen pattern; emit closeDialog (D-07 property path). Do not mount AddRelationshipFinalize in this wave. goBack from choice emits closeDialog (recall has no search step to return to). Drive property writes only via updateTextField + appendWikiLinkPropertyRow.

    4. AnsweredSpellingQuestion: inside each matched-note-{id} wrapper below NoteShow, when canOfferLink (currentUser present, reviewed realm loaded, reviewed notebookRealm.readonly !== true, matched realm loaded — D-06 / Pitfall 3), render PopButton with title/aria-label Link to this note, btn-class daisy-btn daisy-btn-secondary daisy-btn-sm, data-testid link-to-matched-note-{id}, mt-2, show-close-button false; slot default closer → MatchedNoteLinkOffer with reviewed id from recalledNote.noteTopology.id and matched.id, @close-dialog=closer (D-01, D-02, UI-SPEC).

    5. Tests first where practical (RED→GREEN): create MatchedNoteLinkOffer.spec.ts (AddRelationship.spec mocking patterns); extend AnsweredSpellingQuestion.spec.ts for CTA count, readonly omission, and that opening shows Link to: without a search field. Capability-named describes only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/MatchedNoteLinkOffer.spec.ts tests/components/recall/AnsweredSpellingQuestion.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - MatchedNoteLinkOffer.vue exists and mounts LinkInsertionChoice with preselected matched title (no SearchForNoteAndFolder).
    - LinkInsertionChoice.vue defines bareWikiLinkAvailable; recall offer passes false and primary bare-wiki control is absent in the offer test.
    - appendWikiLinkPropertyRow is exported from noteContentPropertyRows.ts and used by MatchedNoteLinkOffer property success path.
    - AnsweredSpellingQuestion renders link-to-matched-note-{id} under each matched row when writable, and omits them when reviewed notebook is readonly (D-06).
    - Property choice triggers exactly one updateNoteContent/updateTextField after click; zero create API calls on mount or on choice-open alone.
    - `rg -F 'composables/useContentCursorInserter' frontend/src/components/recall/MatchedNoteLinkOffer.vue` reports no matches.
    - Targeted Vitest files above exit 0.
  </acceptance_criteria>
  <done>User can open Link to this note under a matched reveal and confirm Add wiki link as a new property; dialog closes; recall result remains; Vitest green for CTA + property path.</done>
</task>

<task type="auto" tdd="true">
  <name>Harden CTA gate + multi-match CTA count Vitest</name>
  <precondition>Task 1 MatchedNoteLinkOffer + AnsweredSpellingQuestion CTA wiring exists.</precondition>
  <reversibility rating="reversible">Test + small gate polish only.</reversibility>
  <files>frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts, frontend/src/components/recall/AnsweredSpellingQuestion.vue</files>
  <read_first>
    - frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts
    - frontend/src/components/recall/AnsweredSpellingQuestion.vue
    - .planning/phases/04-offer-link-between-notes/04-UI-SPEC.md (zero-one-many, permission)
    - .planning/phases/04-offer-link-between-notes/04-VALIDATION.md
  </read_first>
  <behavior>
    - Two matchedNotes + writable reviewed realm → exactly two link-to-matched-note-* buttons with ids matching each matched.id (D-01).
    - Readonly reviewed realm → zero CTAs; matched-notes-section still exists.
    - CTA absent until reviewed and matched realms are available (loading gate — no spinner required).
  </behavior>
  <action>
    Fill any Wave 0 gaps left in AnsweredSpellingQuestion.spec.ts for multi-match CTA count and D-06 readonly omission (seed realms via useStorageAccessor().value.refreshNoteRealm; provide currentUser). Fix CTA v-if if a test reveals a race where CTA shows before realms resolve (Pitfall 3). Do not add relationship finalize here.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestion.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec asserts CTA count equals matchedNotes.length when writable.
    - Spec asserts zero CTAs when reviewed notebookRealm.readonly is true while matched-notes-section remains.
    - Vitest file exits 0.
  </acceptance_criteria>
  <done>D-01 multi-match CTA count and D-06 readonly gate are proven in Vitest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → existing write APIs | Property write uses TextContentController.updateNoteContent via updateTextField; server already authorizes notebook write |
| Client UX gate → server auth | D-06 readonly CTA hide is UX-only; must not replace server checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege / Tampering | Link CTA + updateTextField | high | mitigate | Show CTA only when reviewed notebookRealm.readonly !== true and currentUser present (D-06); rely on existing server auth on updateNoteContent; never invent client-only bypass |
| T-04-02 | Information Disclosure | matchedNotes targets | medium | accept | Matched notes already readability-filtered in Phase 2/3 findAllAccidentalMatches; Phase 4 adds no new read endpoint |
| T-04-03 | Tampering | NoteSearchResult.notebookId for wiki text | medium | mitigate | Build notebookId/name only from server-loaded NoteRealm in NoteStorage (D-04 Pattern 1), not from client-edited AnsweredQuestion fields |
| T-04-04 | Tampering | Property content compose | low | mitigate | Reuse validatePropertyRowsForRichEdit / appendWikiLinkPropertyRow; wiki text from buildWikiLinkText (machine-built) |
| T-04-SC | Tampering | package installs | high | accept | No new npm/pip/cargo packages this phase |
</threat_model>

<verification>
CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/MatchedNoteLinkOffer.spec.ts tests/components/recall/AnsweredSpellingQuestion.spec.ts
Optional wave merge: CURSOR_DEV=true nix develop -c pnpm frontend:test
</verification>

<success_criteria>
- Property-link offer works from accidental-match result with matched note pre-selected (AM-04 criteria 1–2 for property path).
- No auto-write on CTA open (criterion 3).
- Readonly reviewed notes omit CTAs (D-06).
- Product remains usable if later waves stop: property linking from recall works; relationship path deferred to 04-02.
</success_criteria>

<output>
Create `.planning/phases/04-offer-link-between-notes/04-01-SUMMARY.md` when done
</output>
