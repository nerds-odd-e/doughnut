---
phase: 04-learning-session-request-builder
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/src/main/java/com/odde/doughnut/services/LearningSessionService.java
  - backend/src/main/java/com/odde/doughnut/entities/repositories/LearningSessionRepository.java
  - backend/src/main/java/com/odde/doughnut/entities/repositories/SessionItemRepository.java
  - backend/src/main/java/com/odde/doughnut/services/LearningSessionRequestMarkdownBuilder.java
  - backend/src/test/java/com/odde/doughnut/controllers/LearningSessionControllerTests.java
  - backend/src/test/java/com/odde/doughnut/testability/builders/LearningSessionBuilder.java
  - backend/src/test/java/com/odde/doughnut/testability/builders/SessionItemBuilder.java
  - backend/src/test/java/com/odde/doughnut/testability/MakeMe.java
  - packages/generated/doughnut-backend-api/**
  - docs/database-erd.md
autonomous: true
requirements:
  - COM-01
  - COM-02
  - COM-03

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Re-commissioning the same notebook hard-deletes prior AWAITING_REPORT sessions and their session items before creating the new session
    - Learning status in Request markdown reflects prior recorded Feedback (not yet tutored vs N previous session(s), last on ISO date)
    - makeMe.aLearningSession() can seed recorded sessions for Phase 6 prep
    - OpenAPI client regenerated; existing learning_session Cypress spec still green (no user-visible regression)
  artifacts:
    - backend/src/test/java/com/odde/doughnut/testability/builders/LearningSessionBuilder.java
    - backend/src/test/java/com/odde/doughnut/testability/builders/SessionItemBuilder.java
    - packages/generated/doughnut-backend-api/** (regenerated)
    - docs/database-erd.md (refreshed)
  key_links:
    - LearningSessionService.abandonUnfinishedSessions → delete prior AWAITING_REPORT for user+notebook before insert
    - SessionItemRepository aggregate queries → LearningSessionRequestMarkdownBuilder learning status line
    - MakeMe.aLearningSession → Phase 6 record/amend test fixtures
---

<objective>
Expansion: abandon unfinished sessions per Phase 1 lifecycle, learning status aggregation from recorded history, MakeMe builders, OpenAPI regen, and Structure-phase regression proof (backend:verify + existing Cypress spec).

Purpose: Complete COM-* unlock backend contract and lock Structure SC1 (no user-visible change) before Phase 5 commission UI.
Output: Full commission lifecycle at API level + green regression gates.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-learning-session-request-builder/04-RESEARCH.md
@.planning/phases/04-learning-session-request-builder/04-PATTERNS.md
@.planning/phases/01-commissioned-tracker-model/CONTEXT.md (session lifecycle abandon)
@.planning/phases/04-learning-session-request-builder/04-01-SUMMARY.md
</context>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | Existing product behavior unchanged | 04-02 Task 3 |
| GOAL | Request includes learning status | 04-02 Task 2 |
| REQ COM-01 (unlock) | Commission service complete with lifecycle | 04-02 Task 1 |
| REQ COM-02 (unlock) | Learning status in markdown | 04-02 Task 2 |
| RESEARCH | Abandon unfinished sessions (Pattern 4) | 04-02 Task 1 |
| RESEARCH | Learning status query (prior recorded items) | 04-02 Task 2 |
| RESEARCH | MakeMe builders for Phase 6 prep | 04-02 Task 2 |
| RESEARCH | OpenAPI regen + ERD refresh | 04-02 Task 3 |
| CONTEXT Phase 1 | Old sessions without Feedback abandoned on new commission | 04-02 Task 1 |

## Artifacts this phase produces

| Symbol | Kind | Plan |
|--------|------|------|
| `LearningSessionService.abandonUnfinishedSessions` | private/service method | 04-02 |
| `LearningSessionRepository` abandon queries | delete/find AWAITING_REPORT by user+notebook | 04-02 |
| `SessionItemRepository.countRecordedByMemoryTrackerId` | JPQL aggregate | 04-02 |
| `SessionItemRepository.findLastRecordedDate` | JPQL date query | 04-02 |
| `LearningSessionRequestMarkdownBuilder` learning status | dynamic status line per tracker | 04-02 |
| `LearningSessionBuilder` | MakeMe builder | 04-02 |
| `SessionItemBuilder` | MakeMe builder | 04-02 |
| `MakeMe.aLearningSession()` / `MakeMe.aSessionItem()` | test fixture API | 04-02 |
| Generated TypeScript SDK commission types | OpenAPI output | 04-02 |

<tasks>

<task type="auto" tdd="true">
  <name>Abandon prior unfinished sessions on re-commission</name>
  <files>
    backend/src/main/java/com/odde/doughnut/services/LearningSessionService.java
    backend/src/main/java/com/odde/doughnut/entities/repositories/LearningSessionRepository.java
    backend/src/main/java/com/odde/doughnut/entities/repositories/SessionItemRepository.java
    backend/src/test/java/com/odde/doughnut/controllers/LearningSessionControllerTests.java
  </files>
  <read_first>
    .planning/phases/01-commissioned-tracker-model/CONTEXT.md (session lifecycle table — abandoned deleted)
    backend/src/main/java/com/odde/doughnut/services/QuestionGenerationBatchRetentionService.java (delete loop pattern)
    backend/src/main/java/com/odde/doughnut/services/LearningSessionService.java
    backend/src/main/java/com/odde/doughnut/entities/repositories/LearningSessionRepository.java
    backend/src/main/java/com/odde/doughnut/entities/repositories/SessionItemRepository.java
  </read_first>
  <behavior>
    - First commission for notebook creates one AWAITING_REPORT learning_session
    - Second commission same user+notebook deletes the first session and its items (hard delete), leaving exactly one AWAITING_REPORT row
    - Recorded sessions (status RECORDED or recordedAt set) are not deleted on re-commission
  </behavior>
  <action>
    Implement LearningSessionService.abandonUnfinishedSessions(user, notebook) called at start of commission: find and hard-delete all LearningSession rows for user_id + notebook_id where status AWAITING_REPORT (or recorded_at IS NULL per schema — use status enum as primary key). Rely on ON DELETE CASCADE for session_item children or explicit deleteByLearningSession_Id if needed. Add LearningSessionRepository methods (e.g. findByUser_IdAndNotebook_IdAndStatus or deleteByUser_IdAndNotebook_IdAndStatus). Add LearningSessionControllerTests: commission twice same notebook → assert only one AWAITING_REPORT session exists and new response id differs from abandoned id; add test with makeMe-recorded session (after builders land in next task, or inline persist RECORDED session) proving recorded session survives re-commission. Per Phase 1 CONTEXT lifecycle decision.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only -- --tests com.odde.doughnut.controllers.LearningSessionControllerTests</automated>
  </verify>
  <acceptance_criteria>
    - Double-commission test passes: count of AWAITING_REPORT rows for user+notebook equals 1 after second call
    - Abandoned session id is not findable after second commission
    - RECORDED session for same notebook is not removed when commissioning again (test added or deferred to Task 2 once builders exist — must be green before plan close)
  </acceptance_criteria>
  <done>Re-commission abandons prior awaiting-report sessions per locked lifecycle; no duplicate awaiting sessions per notebook.</done>
</task>

<task type="auto" tdd="true">
  <name>Learning status aggregation + MakeMe builders</name>
  <files>
    backend/src/main/java/com/odde/doughnut/entities/repositories/SessionItemRepository.java
    backend/src/main/java/com/odde/doughnut/services/LearningSessionRequestMarkdownBuilder.java
    backend/src/test/java/com/odde/doughnut/testability/builders/LearningSessionBuilder.java
    backend/src/test/java/com/odde/doughnut/testability/builders/SessionItemBuilder.java
    backend/src/test/java/com/odde/doughnut/testability/MakeMe.java
    backend/src/test/java/com/odde/doughnut/controllers/LearningSessionControllerTests.java
  </files>
  <read_first>
    docs/adrs/0005-commissioned-learning-session-protocol.md (learning status examples, lines 78–84)
    backend/src/main/java/com/odde/doughnut/services/RecallStatsAggregator.java (ISO_LOCAL_DATE pattern)
    backend/src/test/java/com/odde/doughnut/testability/builders/QuestionGenerationBatchBuilder.java
    backend/src/test/java/com/odde/doughnut/testability/builders/QuestionGenerationBatchRequestBuilder.java
    backend/src/test/java/com/odde/doughnut/testability/MakeMe.java (aQuestionGenerationBatch registration)
    backend/src/main/java/com/odde/doughnut/services/LearningSessionRequestMarkdownBuilder.java
  </read_first>
  <behavior>
    - Tracker with no prior recorded Feedback → learning status: not yet tutored
    - Tracker with one prior recorded session item (feedback_score set on RECORDED session) → 1 previous session, last on {ISO date}
    - Tracker with N>1 prior recorded items → N previous sessions, last on {ISO date} (pluralize session/s per RESEARCH A1)
  </behavior>
  <action>
    Add SessionItemRepository JPQL: countRecordedByMemoryTrackerId (items with non-null feedback on sessions with status RECORDED); findLastRecordedDate(memoryTrackerId) converted to LocalDate in user ZoneId via DateTimeFormatter.ISO_LOCAL_DATE. Wire into LearningSessionRequestMarkdownBuilder per-item status line.

    Create LearningSessionBuilder (forNotebook, by user, status, commissionedAt, withSessionItems trackers) and SessionItemBuilder (learningSession, memoryTracker, noteTitle, feedbackScore, feedbackRecordedAt) following QuestionGenerationBatch builder patterns; register makeMe.aLearningSession() and makeMe.aSessionItem() on MakeMe.

    Add LearningSessionControllerTests: seed a RECORDED session with feedback on Hola tracker via builders, then commission fresh due session — assert requestMarkdown for Hola contains previous session line with ISO date and Gracias still not yet tutored. Per COM-02 unlock and ADR 0005 learning status contract.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only -- --tests com.odde.doughnut.controllers.LearningSessionControllerTests</automated>
  </verify>
  <acceptance_criteria>
    - Learning status test passes with 1 previous session, last on YYYY-MM-DD substring for Hola
    - makeMe.aLearningSession().forNotebook(...).by(...).withSessionItems(...).please() persists parent and children
    - Builder defaults throw IllegalStateException when required fields missing (forUser/forNotebook pattern)
  </acceptance_criteria>
  <done>Request markdown reflects tutoring history; MakeMe can seed sessions for Phase 6.</done>
</task>

<task type="auto">
  <name>OpenAPI regen, ERD refresh, Structure regression gate</name>
  <files>
    packages/generated/doughnut-backend-api/**
    docs/database-erd.md
  </files>
  <read_first>
    .agents/skills/generate-api-client/SKILL.md
    .agents/skills/database-erd/SKILL.md
    .planning/phases/04-learning-session-request-builder/04-VALIDATION.md
    e2e_test/features/learning_session/commissioned_learning_session.feature
  </read_first>
  <action>
    Regenerate frontend TypeScript API client via CURSOR_DEV=true nix develop -c pnpm generateTypeScript after LearningSessionController and DTOs from Plan 04-01 — never hand-edit packages/generated/. Refresh docs/database-erd.md via database-erd skill (pnpm export:database-erd) to include learning_session and session_item tables.

    Run Structure-phase regression gate: CURSOR_DEV=true nix develop -c pnpm backend:verify (full backend suite) and CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature. Do not remove @wip from commission scenario; do not modify frontend recall paths. Per Structure SC1: existing Phase 2–3 scenarios remain green; no user-visible behavior change.

    Confirm RecallService / dueCommissioned code untouched — if accidental edits from 04-01, revert before closing plan.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:verify && CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature</automated>
  </verify>
  <acceptance_criteria>
    - packages/generated/doughnut-backend-api includes learning-sessions commission operation types
    - docs/database-erd.md documents learning_session and session_item tables
    - backend:verify exits 0
    - commissioned_learning_session.feature Cypress run exits 0 (commission scenario may remain @wip-skipped if tagged — all non-wip scenarios green)
  </acceptance_criteria>
  <done>API client and ERD synced; full backend verify and targeted E2E regression green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → DB abandon delete | Must scope delete to current user + notebook only |
| Aggregate learning status queries | Must not leak other users' session history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | abandonUnfinishedSessions | high | mitigate | Repository queries filter by user.id and notebook.id from authorized commission path only |
| T-04-06 | Information disclosure | SessionItemRepository aggregates | medium | mitigate | Scope count/date queries by memory_tracker_id on trackers already in user's commission set |
| T-04-07 | Repudiation | Hard delete abandoned sessions | low | accept | Phase 1 CONTEXT mandates hard delete; audit trail deferred |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages; generateTypeScript uses existing toolchain |
</threat_model>

<verification>
- All LearningSessionControllerTests green including abandon and learning-status cases
- `CURSOR_DEV=true nix develop -c pnpm backend:verify` green
- `CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature` green
- No frontend/src changes in this phase
</verification>

<success_criteria>
- Abandon lifecycle tested (one awaiting session per notebook)
- Learning status reflects recorded history per ADR 0005
- MakeMe builders registered
- OpenAPI + ERD updated; Structure regression gate green
</success_criteria>

<output>
Create `.planning/phases/04-learning-session-request-builder/04-02-SUMMARY.md` when done
</output>
