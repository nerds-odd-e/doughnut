---
phase: 09-resolve-preview-before-pull-story-2
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - e2e_test/features/cli/cli_sync_dry_run.feature
autonomous: true
requirements:
  - EXP-02
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CLI /sync --dry-run integration shows at least one explicit action label beyond unlabeled content overwrite (D-02, D-09)"
    - "CLI dry-run integration shows at least one reserved, duplicate, or invalid-mapping reject finding clearly (D-04, D-05, D-09)"
    - "Existing Rule The preview leaves nothing behind still passes; extended if new scenarios touch the filesystem (D-06)"
    - "New E2E scenarios are capability-named with no phase numbers and are not left @wip when green (D-09, planning.mdc)"
    - "Targeted cli_sync_dry_run.feature is green; full E2E suite not required (planning.mdc)"
  artifacts:
    - path: e2e_test/features/cli/cli_sync_dry_run.feature
      provides: "Integration proof for Story 2 preview taxonomy + non-mutation"
      contains: "dry-run"
  key_links:
    - from: "/sync --dry-run"
      to: previewPull report string
      via: "syncSlashCommand → past CLI assistant messages"
      pattern: "sync --dry-run"
    - from: Plan 01 unit contract
      to: E2E assertions
      via: "same action/reject vocabulary visible in assistant output"
      pattern: "(create|update|move|reject)"
  assumptions:
    - "SUT (`pnpm sut`) already running per repo contract; do not restart"
    - "Plan 01 wording choices are the E2E assertion source — update scenarios to match shipped labels"
  prohibitions:
    - statement: "Story 3 apply module and mutate pull feature stay out of this plan's diff (D-07)"
      status: active
      verification: "git diff --name-only does not list cli/src/sync/applyPull.ts or e2e_test/features/cli/cli_sync_pull.feature"
    - statement: "Do not run the full Cypress suite; targeted spec only"
      status: active
      verification: "verify command uses --spec e2e_test/features/cli/cli_sync_dry_run.feature"
---

<objective>
Prove Story 2 acceptance through targeted CLI E2E: action labels, reserved/duplicate/invalid reporting, and non-mutation via `cli_sync_dry_run.feature` (D-06, D-09, EXP-02).

Purpose: Close the integration half of EXP-02 after Plan 01 units lock the report contract.
Output: Extended capability-named E2E scenarios green with `@wip` removed from any newly added scenarios.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-resolve-preview-before-pull-story-2/09-CONTEXT.md
@.planning/phases/09-resolve-preview-before-pull-story-2/09-RESEARCH.md
@.planning/phases/09-resolve-preview-before-pull-story-2/09-VALIDATION.md
@.planning/phases/09-resolve-preview-before-pull-story-2/09-01-PLAN.md
@.planning/notes/2026-07-24-portable-notebook-workspace.md
@.cursor/rules/planning.mdc
@.cursor/rules/cli.mdc
@e2e_test/features/cli/cli_sync_dry_run.feature
@cli/src/sync/previewPull.ts
</context>

## Phase Goal

Story 2 preview-before-pull is healthy against acceptance end-to-end (EXP-02).

This plan delivers the integration proof on the existing dry-run capability feature.

## Artifacts this phase produces

| Symbol / artifact | Path | Role |
|-------------------|------|------|
| Dry-run E2E (extended) | `e2e_test/features/cli/cli_sync_dry_run.feature` | Action labels + diagnostics + non-mutation |
| Units (Plan 01) | `cli/tests/previewPull.test.ts` | Taxonomy edge cases |
| Preview classify (Plan 01) | `cli/src/sync/previewPull.ts` (+ helpers) | Production dry-run path |

<tasks>

<task type="tracer" tdd="true">
  <name>E2E tracer — action label visible on /sync --dry-run</name>
  <reversibility rating="costly">D-02/D-09: E2E assertions lock the Story 2 preview wording contract used for EXP-02 proof.</reversibility>
  <files>e2e_test/features/cli/cli_sync_dry_run.feature</files>
  <read_first>
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-CONTEXT.md (D-02, D-09)
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-01-SUMMARY.md (if present — shipped label wording)
    - e2e_test/features/cli/cli_sync_dry_run.feature
    - cli/tests/previewPull.test.ts (expected label strings from Plan 01)
  </read_first>
  <behavior>
    - Existing "Preview one changed note" (or sibling) asserts the shipped update (or create) action label in past CLI assistant messages, not only the path and hunk (D-02)
    - Summary line may include counts by action; still proves the note would change (D-02)
    - Capability-named scenario titles only — no phase numbers in feature/scenario names (D-09)
  </behavior>
  <action>
    After Plan 01, read the green unit expectations for exact action label strings. Update `cli_sync_dry_run.feature` so at least one integration scenario asserts an explicit create or update label for `/sync --dry-run` (D-02, D-09). Tag new scenarios `@wip` only while red; remove `@wip` once green (planning.mdc). Prefer extending existing Rules over inventing a parallel feature file. Do not touch Story 3 apply E2E. Assume SUT is already running. Exact wording matches Plan 01 discretion.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_sync_dry_run.feature</automated>
  </verify>
  <acceptance_criteria>
    - Feature asserts at least one explicit create or update action label in past CLI assistant messages (D-02, D-09)
    - Scenario/feature names contain no phase-number tokens like `09` or `phase-9` (D-09)
    - Targeted dry-run feature run is green for the action-label scenario(s)
  </acceptance_criteria>
  <done>Integration path shows labeled create/update for dry-run; targeted E2E green for this tracer.</done>
</task>

<task type="auto" tdd="true">
  <name>E2E expand — reject finding + non-mutation Rule</name>
  <reversibility rating="costly">D-04/D-05/D-06: first-class rejects and non-mutation Rule are the Story 2 acceptance oracle surface.</reversibility>
  <files>e2e_test/features/cli/cli_sync_dry_run.feature</files>
  <read_first>
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-CONTEXT.md (D-04, D-05, D-06, D-09)
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-VALIDATION.md (Wave 0 E2E gaps)
    - e2e_test/features/cli/cli_sync_dry_run.feature
    - cli/tests/previewPull.test.ts
  </read_first>
  <behavior>
    - At least one scenario shows a reserved, duplicate, or invalid-mapping reject finding clearly in past CLI assistant messages (D-04, D-05)
    - Rule `The preview leaves nothing behind` still passes; extend it if new scenarios write workspace fixtures (D-06)
    - Existing no-diff scenario still sees the clean no-op only when there are no create/update/move/reject findings (D-05)
    - No `@wip` left on scenarios that now pass (planning.mdc)
  </behavior>
  <action>
    Add or extend scenarios for one reserved/duplicate/invalid reject path that the running SUT + workspace fixtures can produce (prefer reserved basename or an invalid mapping the export/compare surface already exposes — follow Plan 01 unit fixtures for which case is easiest end-to-end). Assert reject visibility (path + reason fragment) in past CLI assistant messages (D-04, D-05). Keep/extend Rule `The preview leaves nothing behind` so new filesystem-touching setups still prove no writes and no extra files (D-06). Update existing overwrite/no-diff assertions if summary wording changed in Plan 01. Remove `@wip` from green scenarios. Capability names only (D-09). Leave Story 3 apply feature untouched. Run only the targeted dry-run spec.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_sync_dry_run.feature</automated>
  </verify>
  <acceptance_criteria>
    - Feature includes a scenario asserting reserved, duplicate, or invalid reject output (D-04, D-05, D-09)
    - Rule `The preview leaves nothing behind` scenarios pass (D-06)
    - No `@wip` tags remain on scenarios that are green in this feature
    - `git diff --name-only` does not list `cli/src/sync/applyPull.ts` or `e2e_test/features/cli/cli_sync_pull.feature`
  </acceptance_criteria>
  <done>Reject diagnostics and non-mutation are integration-proven; EXP-02 E2E gate green on the targeted feature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Interactive CLI → workspace directory | E2E must prove dry-run does not write |
| Export zip → preview report | Reject paths must surface unsafe/reserved names without applying |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | dry-run E2E reject path | high | mitigate | Assert invalid/reserved reject visibility; Rule proves no workspace write (Plan 01 implements classify) |
| T-09-02 | Tampering | sync metadata | high | mitigate | Non-mutation Rule: preview adds no files; no `.doughnut-sync` writes from dry-run |
| T-09-03 | Information disclosure | assistant messages | low | accept | Assert paths/actions only; scenarios do not print tokens |
| T-09-04 | Elevation of privilege | `/sync --dry-run` vs apply | high | mitigate | Scenarios use `--dry-run` only; apply feature out of scope |
| T-09-SC | Tampering | package installs | high | accept | No installs in this plan |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_sync_dry_run.feature` green
- Plan 01 units still green if report strings were adjusted during E2E wording sync
- HYG-02: diff scoped to the dry-run feature (and only Plan 01 CLI files if tiny label sync needed — prefer feature-only)
</verification>

<success_criteria>
EXP-02 satisfied for Story 2: preview reports paths/actions including rejects; preview mutates nothing; Phase 7 strengthen verdict applied; Terry/YS work untouched.
</success_criteria>

<output>
Create `.planning/phases/09-resolve-preview-before-pull-story-2/09-02-SUMMARY.md` when done
</output>
