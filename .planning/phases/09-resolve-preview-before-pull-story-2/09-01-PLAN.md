---
phase: 09-resolve-preview-before-pull-story-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/src/sync/previewPull.ts
  - cli/src/sync/diffReport.ts
  - cli/src/sync/previewPullActions.ts
  - cli/tests/previewPull.test.ts
  - cli/tests/previewPush.test.ts
autonomous: true
requirements:
  - EXP-02
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Dry-run report labels relevant paths with create, update, move, or reject actions (D-01, D-02)"
    - "Move appears only when the same doughnut_id is at different workspace vs export paths; missing id stays path-keyed create/update (D-03, D-08)"
    - "Reserved basenames index.md and log.md, paths under .doughnut-sync/, duplicate/case-clash export paths, and unsafe/empty-segment paths are reported as reject findings with path + short reason (D-04, D-05)"
    - "A rejects-only dry-run still shows reject findings and does not return only the clean no-op sentinel (D-05)"
    - "previewPull only reads workspace + export zip in memory; no workspace writes and no sync-metadata writes (D-06, D-07)"
    - "NoteDiffStatus for push dry-run remains pull|push|conflict; pull actions use a parallel type (RESEARCH Pattern 2)"
  artifacts:
    - path: cli/src/sync/previewPull.ts
      provides: "Orchestrates classify → report for /sync --dry-run"
      contains: "previewPull"
    - path: cli/src/sync/previewPullActions.ts
      provides: "Optional cohesion helper: PreviewPullAction classify + path safety/reserved/duplicate"
      contains: "create"
    - path: cli/src/sync/diffReport.ts
      provides: "Report assembly that can render pull action labels without breaking push statuses"
      contains: "renderDiffReport"
    - path: cli/tests/previewPull.test.ts
      provides: "Unit proofs for taxonomy + diagnostics + non-write"
      contains: "reject"
  key_links:
    - from: syncSlashCommand dry-run branch
      to: previewPull
      via: "dryRun ? previewPull : applyPull (apply path frozen)"
      pattern: "previewPull\\("
    - from: previewPull
      to: classify helpers + diffReport
      via: "create/update/move/reject rows → renderDiffReport"
      pattern: "renderDiffReport\\("
    - from: export zip entries
      to: doughnut_id extract
      via: "frontmatter line extract for move inference"
      pattern: "doughnut_id"
  assumptions:
    - "Reserved index.md/log.md are reject (not create/update), prefer reject when reserved and content differs (CONTEXT discretion / RESEARCH A1)"
    - "Invalid/unsafe paths are reject rows (non-throwing) so dry-run still returns a string report; unreadable zip still throws (RESEARCH A2)"
    - "Case-clash via lowercased path keys is enough for unit fixtures (RESEARCH A3)"
  prohibitions:
    - statement: "Story 3 apply module and its mutate E2E/units stay out of this phase's diff (D-07)"
      status: active
      verification: "git diff --name-only does not list cli/src/sync/applyPull.ts, cli/tests/applyPull.test.ts, or e2e_test/features/cli/cli_sync_pull.feature"
    - statement: "Do not install new npm packages; reuse yaml/vitest already in cli (RESEARCH Package Legitimacy)"
      status: active
      verification: "cli/package.json dependencies unchanged for new packages"
    - statement: "HYG-02 — leave Terry Yin / Tan Yeong Sheng authored hunks alone; strengthen only Story 2 preview surface"
      status: active
      verification: "diff scoped to previewPull/report helpers/tests listed in files_modified"
---

<objective>
Strengthen `previewPull` + report helpers so `/sync --dry-run` reports create/update/move/reject with reserved/duplicate/invalid diagnostics, proven by Vitest units (D-01..D-08, EXP-02). Leave Story 3 apply frozen (D-07).

Purpose: Close both TRIAGE Story 2 gaps at the CLI compare/report surface before integration E2E (Plan 02).
Output: Production classify+report path with green `cli/tests/previewPull.test.ts` (and `previewPush` regression if shared report surface changes).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-resolve-preview-before-pull-story-2/09-CONTEXT.md
@.planning/phases/09-resolve-preview-before-pull-story-2/09-RESEARCH.md
@.planning/phases/09-resolve-preview-before-pull-story-2/09-PATTERNS.md
@.planning/phases/09-resolve-preview-before-pull-story-2/09-VALIDATION.md
@.planning/phases/07-publish-triage-decisions/TRIAGE.md
@.planning/notes/2026-07-24-portable-notebook-workspace.md
@.cursor/rules/planning.mdc
@.cursor/rules/cli.mdc
@cli/src/sync/previewPull.ts
@cli/src/sync/diffReport.ts
@cli/src/sync/previewPush.ts
@cli/src/sync/writeNotebookExport.ts
@cli/src/lint/lintWorkspace.ts
@cli/tests/previewPull.test.ts
@cli/tests/zipFixture.ts
</context>

## Phase Goal

Story 2 preview-before-pull participant work is healthy against acceptance: exact paths/actions reported; reserved/duplicate/invalid clear; preview mutates nothing (strengthen path for EXP-02).

This plan delivers the unit-proven classify + report contract.

## Artifacts this phase produces

| Symbol / artifact | Path | Role |
|-------------------|------|------|
| `previewPull` (strengthened) | `cli/src/sync/previewPull.ts` | Dry-run orchestrate + classify entry |
| `PreviewPullAction` classify | `cli/src/sync/previewPullActions.ts` (or cohesive equivalent inside previewPull) | create/update/move/reject |
| Report extension | `cli/src/sync/diffReport.ts` | Action labels / reject rows; push statuses untouched |
| Unit proofs | `cli/tests/previewPull.test.ts` | Taxonomy + diagnostics + non-write |
| E2E proofs (Plan 02) | `e2e_test/features/cli/cli_sync_dry_run.feature` | Integration labels + non-mutation |

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end dry-run create + update labels through previewPull report</name>
  <reversibility rating="costly">D-01/D-02/D-07: shipping create/update labels in the dry-run report locks the Story 2 proof surface and keeps classify in preview (not apply).</reversibility>
  <files>cli/src/sync/previewPull.ts, cli/src/sync/diffReport.ts, cli/src/sync/previewPullActions.ts, cli/tests/previewPull.test.ts, cli/tests/previewPush.test.ts</files>
  <read_first>
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-CONTEXT.md (D-01, D-02, D-06, D-07)
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-RESEARCH.md (Patterns 1–2; Pitfall 2)
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-PATTERNS.md (previewPull + diffReport + previewPush analogs)
    - cli/src/sync/previewPull.ts
    - cli/src/sync/diffReport.ts
    - cli/src/sync/previewPush.ts
    - cli/tests/previewPull.test.ts
  </read_first>
  <behavior>
    - Remote-only `.md` path (absent in workspace) appears in the report with an explicit create action label (D-02)
    - Same-path content difference appears with an explicit update action label (D-02)
    - Unchanged notes stay out of the per-path list; summary remains concise (counts by action allowed) (D-02)
    - Existing unit cases that asserted unlabeled headings are updated so they expect the new labels and stay green
    - Push `NoteDiffStatus` union and `(push)` / `(CONFLICT)` labeling behavior remain intact (RESEARCH Pattern 2)
  </behavior>
  <action>
    RED first: extend `cli/tests/previewPull.test.ts` so remote-only notes assert a create label and content diffs assert an update label (existing create-as-all-added case today lacks that label — fail for the right reason). GREEN: introduce a pull-only action type (`PreviewPullAction` with at least create|update for this tracer) parallel to `NoteDiffStatus` — do not overload the push status union (D-07, RESEARCH Pattern 2). Replace the content-inequality-only classify body in `previewPull` with classify-then-report: missing workspace path → create; same path content differs → update; omit equals. Extend `diffReport` carefully so pull rows can show path + action (and still use `renderNoteDiff` hunks for create/update content). Prefer a small `previewPullActions.ts` helper if cohesion needs it; otherwise keep classify private next to `previewPull`. Touch `syncSlashCommand` / `syncArgument` only if the report string cannot surface otherwise (D-07 expects usually none). Leave the Story 3 apply module untouched. After shared report changes, run `cli/tests/previewPush.test.ts`. Exact wording/order of labels is Claude's Discretion as long as create and update are explicit and E2E-ready.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C cli exec vitest run tests/previewPull.test.ts tests/previewPush.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `previewPull` report for a remote-only note includes an explicit create label on that path (D-02)
    - `previewPull` report for same-path content diff includes an explicit update label (D-02)
    - Existing multi-note / folder-path / nothing-changed unit cases still pass after label updates
    - `NoteDiffStatus` remains only the push-oriented union; pull actions are a separate type or field (D-07)
    - `git diff --name-only` does not list `cli/src/sync/applyPull.ts`
  </acceptance_criteria>
  <done>One production-quality dry-run path labels create and update end-to-end; previewPull and previewPush units green.</done>
</task>

<task type="auto" tdd="true">
  <name>Expand taxonomy — identity move and missing-id path-keyed only</name>
  <reversibility rating="reversible">D-03/D-08: move is preview-only inference; Phase 10 can refine apply without forcing label redesign if labels stay id-based.</reversibility>
  <files>cli/src/sync/previewPull.ts, cli/src/sync/previewPullActions.ts, cli/tests/previewPull.test.ts</files>
  <read_first>
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-CONTEXT.md (D-03, D-08)
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-RESEARCH.md (Pattern 3; Pitfall 4)
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-PATTERNS.md (doughnutId extract analog)
    - cli/src/sync/previewPull.ts
    - cli/tests/previewPull.test.ts
    - cli/tests/zipFixture.ts
  </read_first>
  <behavior>
    - Same `doughnut_id` at different workspace vs export paths → report includes move for that identity (preview only) (D-03, D-08)
    - Export note without `doughnut_id` never produces a move; only path-keyed create/update (D-08)
    - Move does not invent filesystem writes; workspace files unchanged after preview (D-06)
  </behavior>
  <action>
    RED: add unit fixtures where workspace file holds frontmatter `doughnut_id: N` at path A and export holds the same id at path B — expect move in the report; add a second fixture missing the id line that looks like a rename by path alone — expect create/update only, never move (D-03, D-08). GREEN: extract `doughnut_id` with a lightweight case-insensitive line match aligned with backend `DOUGHNUT_ID_LINE` (prefer line extract over full YAML for every note; existing `yaml` package allowed only if already natural). Build id→path maps from workspace + export; emit move when same id differs in path. Do not invent moves from title/path heuristics. Destination occupied by a different id: prefer reject on the destination with a short reason, optionally still report move for the id if informative (CONTEXT/RESEARCH discretion). Keep classify inside the dry-run path only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C cli exec vitest run tests/previewPull.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Unit proves same `doughnut_id` at different paths yields a move label (D-03, D-08)
    - Unit proves missing `doughnut_id` yields no move inference (D-08)
    - Non-write assertion still holds after move-preview scenarios (D-06)
  </acceptance_criteria>
  <done>Move preview is identity-gated; missing-id stays path-keyed; units green.</done>
</task>

<task type="auto" tdd="true">
  <name>Expand diagnostics — reserved, duplicate, invalid, rejects-only</name>
  <reversibility rating="costly">D-04/D-05: reserved vocabulary and first-class reject output become the shared Story 2/11 surface; silent skips would reopen EXP-02.</reversibility>
  <files>cli/src/sync/previewPull.ts, cli/src/sync/previewPullActions.ts, cli/src/sync/diffReport.ts, cli/tests/previewPull.test.ts</files>
  <read_first>
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-CONTEXT.md (D-04, D-05, D-06)
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-RESEARCH.md (Pitfalls 1, 3; Security Domain; Wave 0)
    - .planning/phases/09-resolve-preview-before-pull-story-2/09-PATTERNS.md (unsafe path mirror; reserved lint vocabulary; unzip duplicate collapse)
    - cli/src/sync/writeNotebookExport.ts (assertSafeEntryPath)
    - cli/src/lint/lintWorkspace.ts (index.md / log.md)
    - cli/src/sync/unzip.ts
    - cli/tests/zipFixture.ts (buildZip)
  </read_first>
  <behavior>
    - Basename `index.md` or `log.md` → reject with path + short actionable reason; prefer reject over update when reserved and content differs (D-04)
    - Any path under `.doughnut-sync/` → reject as sync metadata, never a pull target (D-04)
    - Two export entries resolving to the same path (or case-clash) → reject; detect before Map collapse using `buildZip` duplicate fixtures (D-04; RESEARCH Pitfall 1)
    - Unsafe paths (leading `/`, `\`, `..` segment, empty segment) → reject rows without writing the workspace (D-04; T-09-01)
    - Rejects-only report still shows rejects and is not solely the clean no-op sentinel string (D-05)
    - previewPull still only reads the workspace (D-06)
  </behavior>
  <action>
    RED: add Wave 0 unit cases from 09-VALIDATION — reserved basename reject reason visible; duplicate zip names via `buildZip([{name, content}, {name, content}])` not `zipOfNotes` Record; unsafe path reject without workspace mutation; rejects-only report visibly includes reject findings rather than only the nothing-to-pull sentinel (D-04, D-05). GREEN: classify order — collect export `.md` entries with duplicate/case-clash detection before trusting collapsed Maps; then unsafe / `.doughnut-sync/**` / reserved basename → reject; then move/create/update as prior tasks. Mirror `assertSafeEntryPath` criteria plus empty segments as non-throwing reject reasons so dry-run still returns a string (RESEARCH A2). Do not change `unzipToEntries` Map semantics for shared apply/export callers — use preview-local scan or a new list-names helper that preserves duplicates. Reject rows may omit unified hunks (lintReport-style path + reason is fine; wording discretionary). Ensure first-class reject visibility in the assembled report (D-05). Preserve existing non-write unit. Leave Story 3 apply frozen.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C cli exec vitest run tests/previewPull.test.ts tests/previewPush.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Unit: reserved `log.md` or `index.md` yields reject with a short reason (D-04)
    - Unit: duplicate export paths via `buildZip` yield reject (D-04)
    - Unit: unsafe path yields reject and workspace file contents unchanged (D-04, D-06, T-09-01)
    - Unit: rejects-only report includes reject text and is not equal to only `No changes to pull.` (D-05)
    - Unit: `.doughnut-sync/` path yields reject (D-04, T-09-02)
    - `git diff --name-only` does not list `cli/src/sync/applyPull.ts`
  </acceptance_criteria>
  <done>Reserved/duplicate/invalid/rejects-only diagnostics are unit-green; dry-run remains non-mutating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Export zip bytes → preview classify | Untrusted entry names/contents cross into report classification |
| Dry-run CLI → filesystem | Must remain read-only for workspace and sync metadata |
| Dry-run CLI → Doughnut API | Export download only; no notebook mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | preview classify / unsafe paths | high | mitigate | Mirror safe-path rules; emit reject; never write on dry-run (task 3) |
| T-09-02 | Tampering | `.doughnut-sync/**` entries | high | mitigate | Reject sync-metadata paths as pull targets; no baseline/sync writes in previewPull (D-06) |
| T-09-03 | Information disclosure | diff report body | low | accept | Report only note paths/content already in workspace/export; do not dump session tokens |
| T-09-04 | Elevation of privilege | syncSlashCommand dry-run branch | high | mitigate | Dry-run continues to call only `previewPull`; leave apply module frozen this phase (D-07) |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — legitimacy audit N/A for installs |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c pnpm -C cli exec vitest run tests/previewPull.test.ts tests/previewPush.test.ts` green
- Diff excludes Story 3 apply module and mutate pull E2E
- Wave 0 unit gaps from 09-VALIDATION closed for create/update/move/reject/reserved/duplicate/invalid/rejects-only/missing-id
</verification>

<success_criteria>
Unit-proven Story 2 taxonomy + diagnostics on `previewPull` with non-mutation preserved and apply frozen (EXP-02 partial: units). Plan 02 completes integration proof.
</success_criteria>

<output>
Create `.planning/phases/09-resolve-preview-before-pull-story-2/09-01-SUMMARY.md` when done
</output>
