---
phase: 01-health-lint-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java
  - backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java
  - backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java
  - backend/src/main/java/com/odde/doughnut/services/health/HealthRuleRunner.java
  - backend/src/main/java/com/odde/doughnut/services/NotebookHealthService.java
  - backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "HealthRule interface + HealthRuleRunner + NotebookHealthService.lint exist under the locked package layout (SC-2)"
    - "Runner with zero registered rules returns NotebookHealthLintReport with empty groups (SC-3 / D-02)"
    - "No HealthRule @Service beans registered; runner never mutates notebook data (D-11)"
    - "Existing backend unit suite remains green (SC-1); no E2E @wip; no generateTypeScript (D-15, D-16)"
  artifacts:
    - path: backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java
      provides: "HealthRule interface: id, title, severity, autoFixable, evaluate (D-09)"
    - path: backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java
      provides: "Empty extensible HealthRunContext placeholder (D-12)"
    - path: backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java
      provides: "Stable snake_case rule id constants empty_folders, readme_only_folders, dead_wiki_links (D-10)"
    - path: backend/src/main/java/com/odde/doughnut/services/health/HealthRuleRunner.java
      provides: "List<HealthRule> injection runner → NotebookHealthLintReport (D-11)"
    - path: backend/src/main/java/com/odde/doughnut/services/NotebookHealthService.java
      provides: "Public lint orchestration skeleton for Phase 2 controller (D-01, D-13)"
  key_links:
    - from: "NotebookHealthService.lint"
      to: "HealthRuleRunner.run"
      via: "constructor injection"
      pattern: "Phase 2 controller call site"
    - from: "HealthRuleRunner"
      to: "List<HealthRule>"
      via: "Spring multi-bean injection"
      pattern: "AssimilationServiceFactory List pattern; zero beans → empty list"
    - from: "HealthRule.evaluate"
      to: "HealthFindingGroup"
      via: "return type"
      pattern: "one group per rule"
---

<objective>
Wire the Health rule registry skeleton: `HealthRule`, empty `HealthRunContext`, reserved `HealthRuleIds`, Spring `List&lt;HealthRule&gt;` `HealthRuleRunner`, and public `NotebookHealthService.lint` — verified by zero-rules → empty `groups` (D-01, D-02, D-09..D-13, D-15).

Purpose: Complete the Structure contract so Phase 2 can register `EmptyFolderHealthRule` and call `NotebookHealthService.lint` without reshaping the model.
Output: `services/health/*` contract types, `NotebookHealthService`, and green empty-registry unit coverage; full backend suite green (SC-1).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-health-lint-contract/01-CONTEXT.md
@.planning/phases/01-health-lint-contract/01-RESEARCH.md
@.planning/phases/01-health-lint-contract/01-VALIDATION.md
@.planning/phases/01-health-lint-contract/01-01-PLAN.md
@.cursor/rules/planning.mdc
@.cursor/rules/backend-code.mdc
@.cursor/rules/backend-testing.mdc
@.cursor/skills/post-change-refactor/SKILL.md
@.cursor/skills/execute-plan/SKILL.md
@backend/src/main/java/com/odde/doughnut/services/AssimilationServiceFactory.java
@backend/src/main/java/com/odde/doughnut/entities/Notebook.java
</context>

## Phase Goal

**Structure only:** Rule interface + runner + orchestration skeleton ready for Phase 2 empty-folder rule plug-in. No HTTP, no concrete rules, no fix applicator (D-01, D-03, D-11, D-16).

## Artifacts this phase produces

| Symbol | Path |
|--------|------|
| `HealthSeverity` | `backend/src/main/java/com/odde/doughnut/controllers/dto/HealthSeverity.java` *(01-01)* |
| `HealthFindingItem` | `backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingItem.java` *(01-01)* |
| `HealthFindingGroup` | `backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingGroup.java` *(01-01)* |
| `NotebookHealthLintReport` | `backend/src/main/java/com/odde/doughnut/controllers/dto/NotebookHealthLintReport.java` *(01-01)* |
| `HealthRule` | `backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java` |
| `HealthRunContext` | `backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java` |
| `HealthRuleIds` | `backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java` |
| `HealthRuleRunner` | `backend/src/main/java/com/odde/doughnut/services/health/HealthRuleRunner.java` |
| `NotebookHealthService` | `backend/src/main/java/com/odde/doughnut/services/NotebookHealthService.java` |
| `HealthRuleRunnerTest` | `backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java` |

This plan creates the five service-layer symbols and extends the test class with the empty-rules case (D-02).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add zero-rules empty report test (RED)</name>
  <files>backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java</files>
  <read_first>
    - .planning/phases/01-health-lint-contract/01-CONTEXT.md (D-02, D-09, D-11, D-15)
    - .planning/phases/01-health-lint-contract/01-RESEARCH.md (Zero-rules unit test example)
    - backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java
    - .cursor/rules/backend-testing.mdc
  </read_first>
  <behavior>
    - Construct `HealthRuleRunner` with `List.of()` (no rule beans)
    - Call `run` with a plain `Notebook` instance and a new `HealthRunContext`
    - Assert returned `NotebookHealthLintReport.getGroups()` is empty (D-02 / SC-3)
    - Prefer direct construction over `@SpringBootTest` for this contract test
    - Must fail until runner/service types exist (RED before Task 2)
  </behavior>
  <action>
    Extend `HealthRuleRunnerTest` with one behavior-named method proving zero registered rules → empty `groups` (D-02, D-15). Do not register any `HealthRule` Spring beans. Do not add controller or E2E coverage. Keep the nested DTO construction test from plan 01-01. Use readme domain language only (D-14).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - Empty-rules assertion exists in `HealthRuleRunnerTest`
    - Suite fails for missing runner/types (or equivalent RED), not unrelated flake
    - No `HealthRule` implementing `@Service` classes added in this task
  </acceptance_criteria>
  <done>RED empty-registry test is in place for SC-3.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HealthRule runner and NotebookHealthService (GREEN)</name>
  <files>backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java, backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java, backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java, backend/src/main/java/com/odde/doughnut/services/health/HealthRuleRunner.java, backend/src/main/java/com/odde/doughnut/services/NotebookHealthService.java</files>
  <read_first>
    - backend/src/main/java/com/odde/doughnut/services/AssimilationServiceFactory.java
    - .planning/phases/01-health-lint-contract/01-RESEARCH.md (HealthRule interface + Pattern 1 + Discretion table)
    - backend/src/main/java/com/odde/doughnut/controllers/dto/NotebookHealthLintReport.java
    - backend/src/main/java/com/odde/doughnut/entities/Notebook.java
  </read_first>
  <action>
    Per D-13 package layout: create `services/health/HealthRule` interface with `id()`, `title()`, `severity()`, `autoFixable()`, and `evaluate(Notebook, HealthRunContext) → HealthFindingGroup` (D-09). Create empty public `HealthRunContext` class with no fields yet (D-12) — do not invent lint-request option DTOs (D-03). Add `HealthRuleIds` final constants class with `EMPTY_FOLDERS = "empty_folders"`, `README_ONLY_FOLDERS = "readme_only_folders"`, `DEAD_WIKI_LINKS = "dead_wiki_links"` (D-10); no rule beans for these ids. Implement `@Service HealthRuleRunner` constructor-injecting `List&lt;HealthRule&gt;` (copy-of for immutability), mapping each rule’s `evaluate` into report `groups` in injection order; never mutate notebook or call delete/purge APIs (D-11). Rely on Spring empty multi-element injection when zero beans exist (D-02); skip `@Order` until Phase 2+ (discretion). Implement `@Service NotebookHealthService` at `services/NotebookHealthService.java` with a **public** `lint(Notebook, HealthRunContext)` that delegates to the runner (D-01, research discretion). Register **zero** `HealthRule` implementations in this phase (D-11). No controller routes, no OpenAPI regen / `generateTypeScript` (D-16). After green: run post-change-refactor on the uncommitted Health change; do not leave speculative fix/UI code.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - Empty-rules test and nested DTO test both pass
    - Full backend unit suite green (SC-1)
    - No `@RestController` Health mappings; no Flyway findings table; no frontend diffs; no TS client regen
    - No class implementing `HealthRule` is annotated as a Spring bean in this phase
    - `NotebookHealthService.lint` is public and callable from a future controller without package hacks
  </acceptance_criteria>
  <done>SC-2/SC-3 satisfied: runner + service skeleton with zero rules → empty report; SC-1 suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-process service → DTOs | Lint orchestration returns ephemeral report objects; no network yet |
| Future client → Health API | Not introduced in Phase 1 — auth threats deferred to Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | HealthRuleRunner | medium | mitigate | Runner only maps `evaluate` → groups; no repository deletes / no fix applicator (D-03, D-11) |
| T-01-05 | Information Disclosure | NotebookHealthService | low | mitigate | Ephemeral DTOs only; no findings persistence (D-08) |
| T-01-06 | Elevation of Privilege | future RestNotebookHealthController | high | accept | Deferred Phase 2: must call `AuthorizationService.assertAuthorization(notebook)` before lint; Phase 1 adds no routes (D-01) |
| T-01-07 | Spoofing | client-only lint | medium | accept | Backend-authoritative design prepared by this contract; HTTP surface deferred to Phase 2 |
| T-01-SC | Tampering | package installs | low | accept | No new npm/pip/cargo packages in this phase |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c pnpm backend:test_only` green (SC-1 + new contract tests)
- Confirm zero `HealthRule` implementing `@Service` beans
- Confirm no new Health controller mappings and no `generateTypeScript` run
- Phase wrap-up (execute-plan): Jidoka → post-change-refactor → update plan/STATE → commit → push
</verification>

<success_criteria>
- SC-1: Existing backend unit tests pass
- SC-2: `HealthRule` + runner + DTO shapes + `NotebookHealthService` exist under locked layout
- SC-3: Zero rules → `groups: []`
- Contract sufficient for Phase 2 to add one rule bean and return typed report without reworking shared model
</success_criteria>

<output>
Create `.planning/phases/01-health-lint-contract/01-02-SUMMARY.md` when done
</output>
