---
phase: 01-health-lint-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/odde/doughnut/controllers/dto/HealthSeverity.java
  - backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingItem.java
  - backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingGroup.java
  - backend/src/main/java/com/odde/doughnut/controllers/dto/NotebookHealthLintReport.java
  - backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "OpenAPI-ready findings DTO types exist: report with groups, recursive group with items/children, item identity fields, severity enum error|warning|info (SC-2 shape)"
    - "A focused unit test can construct a nested HealthFindingGroup (items + children) without errors (SC-3b)"
    - "No HTTP routes, no frontend, no Flyway findings table, no TypeScript client regen in this plan"
  artifacts:
    - path: backend/src/main/java/com/odde/doughnut/controllers/dto/HealthSeverity.java
      provides: "HealthSeverity enum (error, warning, info) per D-07"
    - path: backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingItem.java
      provides: "HealthFindingItem DTO with optional folderId, noteId, label, message, wikiLinkToken per D-06"
    - path: backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingGroup.java
      provides: "HealthFindingGroup with ruleId, title, severity, autoFixable, items, children per D-05"
    - path: backend/src/main/java/com/odde/doughnut/controllers/dto/NotebookHealthLintReport.java
      provides: "NotebookHealthLintReport with groups list per D-04"
    - path: backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java
      provides: "Nested group construction test (runner empty-rules test added in 01-02)"
  key_links:
    - from: "NotebookHealthLintReport.groups"
      to: "List<HealthFindingGroup>"
      via: "field type"
      pattern: "one group per rule later"
    - from: "HealthFindingGroup.children"
      to: "List<HealthFindingGroup>"
      via: "recursive nesting"
      pattern: "Phase 4 dead links by note"
    - from: "HealthFindingGroup.severity"
      to: "HealthSeverity"
      via: "enum field"
      pattern: "reserved for later rules"
---

<objective>
Deliver OpenAPI-ready Health findings DTO shapes (report, recursive groups, items, severity) so Phase 2 can return a typed lint report without inventing a second model (D-04, D-05, D-06, D-07).

Purpose: Structure-only foundation for the Health milestone; no user-facing behavior, no HTTP surface (D-01, D-08, D-16).
Output: Four DTO/enum types under `controllers/dto/` plus a unit test proving nested group construction.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-health-lint-contract/01-CONTEXT.md
@.planning/phases/01-health-lint-contract/01-RESEARCH.md
@.planning/phases/01-health-lint-contract/01-VALIDATION.md
@.cursor/rules/planning.mdc
@.cursor/rules/backend-code.mdc
@.cursor/rules/backend-testing.mdc
@backend/src/main/java/com/odde/doughnut/controllers/dto/FolderCreationRequest.java
@backend/src/main/java/com/odde/doughnut/controllers/dto/AttachBookLayoutNodeRequest.java
@backend/src/main/java/com/odde/doughnut/controllers/dto/Randomization.java
</context>

## Phase Goal

**Structure only (not a user-facing MVP slice):** Shared Health lint findings contract ready so Phase 2 can plug in one rule and return a typed nested report without reworking the model.

## Artifacts this phase produces

| Symbol | Path |
|--------|------|
| `HealthSeverity` | `backend/src/main/java/com/odde/doughnut/controllers/dto/HealthSeverity.java` |
| `HealthFindingItem` | `backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingItem.java` |
| `HealthFindingGroup` | `backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingGroup.java` |
| `NotebookHealthLintReport` | `backend/src/main/java/com/odde/doughnut/controllers/dto/NotebookHealthLintReport.java` |
| `HealthRule` | `backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java` *(plan 01-02)* |
| `HealthRunContext` | `backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java` *(plan 01-02)* |
| `HealthRuleIds` | `backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java` *(plan 01-02)* |
| `HealthRuleRunner` | `backend/src/main/java/com/odde/doughnut/services/health/HealthRuleRunner.java` *(plan 01-02)* |
| `NotebookHealthService` | `backend/src/main/java/com/odde/doughnut/services/NotebookHealthService.java` *(plan 01-02)* |
| `HealthRuleRunnerTest` | `backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java` |

This plan (01) creates the four DTO/enum types and starts `HealthRuleRunnerTest` with nested-group coverage. Plan 01-02 adds the service/runner stack and the empty-rules test on the same test class.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add nested findings DTO construction test (RED)</name>
  <files>backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java</files>
  <read_first>
    - .planning/phases/01-health-lint-contract/01-CONTEXT.md (D-04..D-07, D-15)
    - .planning/phases/01-health-lint-contract/01-RESEARCH.md (DTO shape + Validation Architecture)
    - .cursor/rules/backend-testing.mdc
  </read_first>
  <behavior>
    - Construct a `HealthFindingGroup` with non-null `items` containing a `HealthFindingItem` that sets optional `folderId`, `noteId`, `label`, `message`, and `wikiLinkToken`
    - Nest another `HealthFindingGroup` under `children` on the parent group
    - Wrap groups in a `NotebookHealthLintReport` and assert parent group retains both `items` and `children`
    - Assert `HealthSeverity` values used are `error`, `warning`, or `info` (D-07)
    - Test must fail to compile or fail until DTO types exist (RED before Task 2)
  </behavior>
  <action>
    Create `HealthRuleRunnerTest` under `services/health` test package (capability-named; no phase numbers in class name). Write one focused test method for nested report/group construction covering SC-3b fields locked in D-04/D-05/D-06/D-07. Do not implement production DTOs in this task — RED first per D-15. Do not add E2E or `@wip` scenarios. Do not invent fix DTOs (D-03). Use readme domain language only in any strings/comments (D-14).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - `HealthRuleRunnerTest` exists and contains a nested group/item construction assertion
    - Running the backend unit suite fails for the right reason (missing types or failing assertion), not env/typo noise
    - No new controller mappings, Flyway migrations, or frontend files
  </acceptance_criteria>
  <done>Failing (or non-compiling) nested DTO construction test is in place for SC-3b.</done>
</task>

<task type="auto">
  <name>Task 2: Implement OpenAPI-ready findings DTOs (GREEN)</name>
  <files>backend/src/main/java/com/odde/doughnut/controllers/dto/HealthSeverity.java, backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingItem.java, backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingGroup.java, backend/src/main/java/com/odde/doughnut/controllers/dto/NotebookHealthLintReport.java</files>
  <read_first>
    - backend/src/main/java/com/odde/doughnut/controllers/dto/FolderCreationRequest.java
    - backend/src/main/java/com/odde/doughnut/controllers/dto/AttachBookLayoutNodeRequest.java
    - backend/src/main/java/com/odde/doughnut/controllers/dto/Randomization.java
    - .planning/phases/01-health-lint-contract/01-RESEARCH.md (Pattern 2 + Discretion recommendations)
  </read_first>
  <action>
    Implement Lombok `@Getter`/`@Setter` DTO classes (not records) under `controllers/dto/` per D-01/D-13 and research discretion: `HealthSeverity` as public enum with lowercase constants `error`, `warning`, `info` (D-07; mirror `Randomization.RandomStrategy` style); `HealthFindingItem` with optional Integer `folderId`, Integer `noteId`, String `label`, String `message`, String `wikiLinkToken` — omit item-level `autoFixable` (D-06); `HealthFindingGroup` with required-shaped fields `ruleId`, `title`, `severity`, `autoFixable`, plus optional `items` and recursive `children` (D-05); `NotebookHealthLintReport` with `groups: List&lt;HealthFindingGroup&gt;` (D-04). Mark required fields with `@Schema(requiredMode = Schema.RequiredMode.REQUIRED)` lightly like `FolderCreationRequest`. Findings remain ephemeral DTOs only — no JPA entity, no migration, no SARIF (D-08). Do not regenerate TypeScript client (D-16). Do not add controller routes (D-01). Domain language: readme / readmeContent only (D-14).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - All four types compile and the nested construction test from Task 1 passes
    - `HealthFindingGroup` exposes both `items` and `children`
    - No RestController Health endpoints added; no `pnpm generateTypeScript`; no DB migration for findings
  </acceptance_criteria>
  <done>DTO contract types exist and nested construction test is green (SC-2 shape / SC-3b).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 1 plan 01 adds in-process DTOs only; no HTTP, no persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | findings DTOs | low | mitigate | Ephemeral DTOs only — no findings table / history persistence (D-08) |
| T-01-02 | Elevation of Privilege | future Health HTTP | medium | accept | Deferred to Phase 2: authorize via existing `AuthorizationService` before lint; no routes in this plan (D-01) |
| T-01-03 | Tampering | future lint path | medium | accept | Deferred: runner must never mutate; fix path is Phase 7 (D-03/D-11) — not introduced here |
</threat_model>

<verification>
- Nested DTO construction test green via `CURSOR_DEV=true nix develop -c pnpm backend:test_only`
- Grep new Health DTO paths: domain uses readme vocabulary; recursive `children` present on group type
- Diff contains no controller mapping changes and no Flyway files
</verification>

<success_criteria>
- SC-2 (partial): OpenAPI-ready report/group/item/severity shapes exist
- SC-3b: Nested `items` + `children` constructible in unit test
- SC-1 deferred to plan 01-02 full suite after runner lands (existing suite still expected green if only DTOs + one test added)
</success_criteria>

<output>
Create `.planning/phases/01-health-lint-contract/01-01-SUMMARY.md` when done
</output>
