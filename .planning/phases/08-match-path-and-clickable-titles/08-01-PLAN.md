---
phase: 08-match-path-and-clickable-titles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/recall/AccidentalMatchResolveRow.vue
  - frontend/src/components/recall/AccidentalMatchResolveDialog.vue
  - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
  - frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts
autonomous: true
requirements:
  - AMR-04
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After opening Resolve, each match row shows a clickable title (NoteTitleWithLink toward noteShow) immediately from NoteTopology (D-03, D-06, D-08; AMR-04)"
    - "When that row's NoteRealm is present (seeded or hydrated), a BreadcrumbWithCircle path appears under the title via notebookRealm + ancestorFolders (D-02, D-04, D-08, D-09; AMR-04)"
    - "Dialog lists all current matches with title + path together; no note body / peek inside the dialog (D-03; UI populated/partial/empty covered)"
    - "matchedNotes stays NoteTopology[]; path comes from client hydrate getNoteRealmRefAndLoadWhenNeeded — no OpenAPI / topology widen (D-01, D-02)"
    - "Progressive per-row path: titles never wait on the slowest realm; path wrapper omitted until realm present; no list-level spinner/Suspense (D-08, D-09; UI loading/partial)"
  artifacts:
    - path: frontend/src/components/recall/AccidentalMatchResolveRow.vue
      provides: "Per-match row: hydrate + NoteTitleWithLink + conditional BreadcrumbWithCircle; testids resolve-match-row-{id} / resolve-match-path-{id}"
    - path: frontend/src/components/recall/AccidentalMatchResolveDialog.vue
      provides: "Thin list host v-for → AccidentalMatchResolveRow; keeps accidental-match-resolve-dialog testid"
    - path: frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
      provides: "Boundary Vitest: title link href + distinct notebook path text; no dialog body peek (D-10)"
    - path: frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts
      provides: "Optional helper or documented seedRealms with distinct notebookRealm.notebook.name for path asserts"
  key_links:
    - from: AccidentalMatchResolveRow setup
      to: StoredApiCollection.getNoteRealmRefAndLoadWhenNeeded(matched.id)
      via: "useStorageAccessor → storedApi(); hydrate starts when dialog Modal mounts (PopButton v-if)"
    - from: AccidentalMatchResolveRow template
      to: NoteTitleWithLink + BreadcrumbWithCircle
      via: ":note-topology=matched; breadcrumb only when matchRealmRef present"
    - from: AccidentalMatchResolveDialog
      to: AccidentalMatchResolveRow
      via: "v-for matchedNotes; dialog stays list host (D-05)"
---

<objective>
Prove AMR-04 end-to-end at the Vitest boundary: extract AccidentalMatchResolveRow that hydrates realm and renders clickable title + BreadcrumbWithCircle under it; wire the Phase 7 dialog list host to that row (D-01..D-10).

Purpose: Deliver the one Behavior of Phase 8 (path + clickable titles) stop-safely before E2E — unit evidence first.
Output: AccidentalMatchResolveRow.vue; dialog list uses rows; AccidentalMatch Vitest asserts title link + path identity with seeded realms.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-match-path-and-clickable-titles/08-CONTEXT.md
@.planning/phases/08-match-path-and-clickable-titles/08-RESEARCH.md
@.planning/phases/08-match-path-and-clickable-titles/08-PATTERNS.md
@.planning/phases/08-match-path-and-clickable-titles/08-UI-SPEC.md
@.planning/phases/08-match-path-and-clickable-titles/08-VALIDATION.md
@.cursor/rules/frontend-testing.mdc
@.cursor/rules/unit-testing.mdc
</context>

## Artifacts this phase produces

- `frontend/src/components/recall/AccidentalMatchResolveRow.vue` — hydrate + title link + conditional path breadcrumb
- Dialog list host updated to mount rows (titles-only interim replaced)
- Vitest boundary coverage for path identity + clickable titles (capability-named; no phase numbers in product/test names)

## UI Considerations (from 08-UI-SPEC)

Lifted into must_haves / tasks: empty CTA omit (unchanged Phase 7); loading = progressive path omit until realm; error = title-only row if realm missing; populated = title + breadcrumb; partial = independent per-row hydrate; overflow = existing Modal scroll / breadcrumb wrap; zero-one-many = same list chrome; long-text = wrap backstop (no truncate).

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: resolve row title link + path breadcrumb</name>
  <reversibility rating="reversible">Row extract and client hydrate compose existing seams; undo is local component swap back to titles-only list (D-05).</reversibility>
  <files>frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts, frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts, frontend/src/components/recall/AccidentalMatchResolveRow.vue, frontend/src/components/recall/AccidentalMatchResolveDialog.vue</files>
  <read_first>
    - frontend/src/components/recall/AccidentalMatchResolveDialog.vue
    - frontend/src/components/recall/MatchedNoteLinkOffer.vue
    - frontend/src/components/notes/NoteTitleWithLink.vue
    - frontend/src/components/toolbars/BreadcrumbWithCircle.vue
    - frontend/src/store/StoredApiCollection.ts (getNoteRealmRefAndLoadWhenNeeded)
    - frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts
    - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
    - frontend/tests/components/recall/MatchedNoteLinkOffer.spec.ts (notebook name mutate pattern)
    - .planning/phases/08-match-path-and-clickable-titles/08-UI-SPEC.md (row visual spec + testids)
    - .planning/phases/08-match-path-and-clickable-titles/08-CONTEXT.md (D-01..D-10)
  </read_first>
  <behavior>
    - Canonical ACCIDENTAL_MATCH with two matches: open resolve dialog; each resolve-match-row-{id} contains an anchor whose href targets that match id (noteShow / note id in path)
    - With seedRealms whose notebookRealm.notebook.name are distinct (e.g. Notebook Alpha / Notebook Beta), resolve-match-path-{id} text contains that notebook name under the title
    - Dialog still lists both matches; path wrappers appear after flushPromises when realms are seeded
    - Inside accidental-match-resolve-dialog there is no matched note body / NoteShow content for match ids — identity chrome only
  </behavior>
  <action>
    Per D-10, rewrite the open-dialog Vitest case first (RED) for clickable titles + path identity: mount via mountAnsweredSpellingQuestion with withRouter true and seedRealms for matched A/B after mutating distinct notebookRealm.notebook.name (builder defaults name to title — mutate after please(), same pattern as MatchedNoteLinkOffer.spec). Prefer one canonical case for title + path + link; keep sibling cases as deltas only. Then implement AccidentalMatchResolveRow under recall/ (D-05): prop matched NoteTopology; useStorageAccessor → getNoteRealmRefAndLoadWhenNeeded(matched.id) in setup (D-02, D-08 — no prefetch before dialog open; PopButton Modal v-if already gates mount). Always render NoteTitleWithLink with note-topology matched (D-06); when matchRealmRef is truthy, render a wrapper data-testid resolve-match-path-{id} containing BreadcrumbWithCircle with ancestorFolders (default []) and notebookRealm (D-03, D-04, D-09). Root li uses resolve-match-row-{id} and flex flex-col gap-2 (UI-SPEC). Update AccidentalMatchResolveDialog to v-for AccidentalMatchResolveRow with :matched; keep ul accidental-match-resolve-dialog and matchedNotes NoteTopology[] prop unchanged (D-01). Do not widen NoteTopology or grade API. Do not add list-level Suspense/spinner. Do not implement AMR-05 reopen (D-07). Leave AnsweredSpellingQuestion / MatchedNoteLinkOffer / grading / SRS untouched. Capability-named testids only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - AccidentalMatchResolveRow.vue exists and calls getNoteRealmRefAndLoadWhenNeeded with matched.id
    - AccidentalMatchResolveDialog.vue mounts AccidentalMatchResolveRow per matched note; plain {{ matched.title }} list text is gone
    - Vitest: after open + flushPromises with seeded distinct notebook names, resolve-match-row-10 has an a[href] matching /10/; resolve-match-path-10 text contains the seeded notebook name
    - Vitest file green; no OpenAPI / NoteTopology type changes in this plan
  </acceptance_criteria>
  <done>Happy path AMR-04 at Vitest: dialog rows show NoteTitleWithLink + BreadcrumbWithCircle path from client hydrate (D-01..D-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Expand fixtures: multi-row path deltas + support helper</name>
  <files>frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts, frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts</files>
  <read_first>
    - frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts
    - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
    - frontend/tests/notes/sidebar/sidebarTestSupport.ts (mockShowNoteForRealms fallback only if seedRealms insufficient)
    - .planning/phases/08-match-path-and-clickable-titles/08-RESEARCH.md (Pitfall 1 fixture determinism)
  </read_first>
  <behavior>
    - Two-match path disambiguation: row 10 path contains Notebook Alpha; row 20 path contains Notebook Beta (or equivalent distinct names) — assert only the delta across rows, not full payload twice
    - Existing dismiss / empty-omit / compact CTA cases remain green without requiring path asserts
    - If hydrate still hits showNote after seed, keyed mock or stronger seed ensures deterministic notebook names (no generic random realm wipe)
  </behavior>
  <action>
    Per D-10 and RESEARCH pitfall 1, add a concise test-support helper (or document mutate-before-seed in accidentalMatchWithTwoMatchedNotes callers) that returns matched realms with distinct notebookRealm.notebook.name ready for seedRealms — prefer helper in answeredSpellingQuestionTestSupport over extending NoteRealmBuilder unless mutation is awkward. Ensure beforeEach generic showNote mock does not erase path identity when seedRealms is used (seed cache first; keyed mockShowNoteForRealms only if needed). Keep small-test style: drive AnsweredSpellingQuestion boundary; focused asserts; no dedicated AccidentalMatchResolveRow.spec unless the boundary test cannot express the path delta. Still exclude AMR-05 navigate-and-return coverage (D-07).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Support path for distinct notebook names is reusable (helper or factory return) without repeating field soup in every test
    - Spec asserts both match rows can show different notebook path text when seeded accordingly
    - AccidentalMatch Vitest file fully green including prior Phase 7 dismiss/empty cases
  </acceptance_criteria>
  <done>Vitest fixtures make multi-row path identity deterministic; AMR-04 unit side complete for Plan 01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grade response → dialog | matchedNotes NoteTopology[] already authorized for this answer |
| Dialog row → showNote hydrate | Authenticated StoredApi load of NoteRealm for path chrome |
| Title / breadcrumb → vue-router | Navigation to noteShow / folder routes; Modal closes on route change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering / XSS | AccidentalMatchResolveRow title + BreadcrumbWithCircle | medium | mitigate | Reuse NoteTitleWithLink / BreadcrumbWithCircle text interpolation only; never v-html for titles or path names (per D-03 / UI-SPEC a11y) |
| T-08-02 | Information Disclosure | resolve dialog path chrome | low | accept | Only display topologies already returned on the graded answer; hydrate uses existing authenticated showNote |
| T-08-03 | Elevation of Privilege | NoteTitleWithLink → noteShow | low | accept | Backend note/page authz unchanged; client does not invent deep links past API |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — reuse-only; Package Legitimacy N/A |
</threat_model>

<verification>
AccidentalMatch Vitest file green with Nix prefix. Manual visual check optional; automated path+link asserts are the gate for Plan 01.
</verification>

<success_criteria>
- AccidentalMatchResolveRow hydrates and shows title link + BreadcrumbWithCircle when realm present
- Dialog list host uses rows; NoteTopology[] unchanged
- Vitest proves AMR-04 identity at the answered-question boundary (D-01..D-10)
</success_criteria>

<output>
Create `.planning/phases/08-match-path-and-clickable-titles/08-01-SUMMARY.md` when done
</output>
