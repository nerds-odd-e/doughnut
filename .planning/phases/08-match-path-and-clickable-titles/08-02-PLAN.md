---
phase: 08-match-path-and-clickable-titles
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
  - e2e_test/features/recall/accidental_match_reveal.feature
autonomous: true
requirements:
  - AMR-04
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Accidental-match E2E open-resolve flow asserts notebook path identity (English practice) inside the dialog alongside the matched title (D-11; AMR-04)"
    - "Matched title inside the dialog is a visible clickable link (a / router-link) toward the note — without navigating away and reopening (D-06, D-07, D-11)"
    - "Overlap E2E stays uncoupled; no AMR-05 navigate-and-return scenario added this phase (D-07, D-11)"
  artifacts:
    - path: e2e_test/start/pageObjects/AnsweredQuestionPage.ts
      provides: "expectAccidentalMatchReveal asserts path text + clickable title inside accidental-match-resolve-dialog"
    - path: e2e_test/features/recall/accidental_match_reveal.feature
      provides: "Optional wording for titles + path; no new AMR-05 scenario"
  key_links:
    - from: AnsweredQuestionPage.expectAccidentalMatchReveal
      to: accidental-match-resolve-dialog + resolve-match-row / title link
      via: "cy.findByTestId after Resolve click; assert English practice + contains a for matched title; dismiss without noteShow round-trip"
---

<objective>
Extend accidental-match E2E / page object so the resolve dialog proves notebook path identity and clickable title in Cypress — without AMR-05 reopen coverage (D-11).

Purpose: Close AMR-04 observable Behavior with targeted E2E after Plan 01 Vitest.
Output: Updated AnsweredQuestionPage assert; optional feature wording; green targeted cypress run.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-match-path-and-clickable-titles/08-CONTEXT.md
@.planning/phases/08-match-path-and-clickable-titles/08-RESEARCH.md
@.planning/phases/08-match-path-and-clickable-titles/08-PATTERNS.md
@.planning/phases/08-match-path-and-clickable-titles/08-UI-SPEC.md
@.planning/phases/08-match-path-and-clickable-titles/08-01-PLAN.md
@.cursor/rules/e2e-authoring.mdc
</context>

## Artifacts this phase produces

- Page-object asserts for path + clickable title in resolve dialog (AMR-04 E2E)
- Feature file wording aligned if needed (prefer page-object change over Gherkin rewrite)

## UI Considerations (from 08-UI-SPEC)

E2E covers populated path+title in same-notebook fixture; empty/partial loading are Vitest/UI-SPEC; do not add long-text or navigate-away scenarios here.

<tasks>

<task type="auto">
  <name>E2E page object: path identity + clickable title</name>
  <reversibility rating="reversible">Page-object assert extension only; undo is revert of expectAccidentalMatchReveal block (D-11).</reversibility>
  <files>e2e_test/start/pageObjects/AnsweredQuestionPage.ts</files>
  <read_first>
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/step_definitions/recall.ts
    - .planning/phases/08-match-path-and-clickable-titles/08-PATTERNS.md (E2E extension section)
    - .planning/phases/07-compact-result-resolve-dialog-shell/07-02-SUMMARY.md
  </read_first>
  <action>
    Per D-11, extend expectAccidentalMatchReveal (or equivalent dialog block) so after clicking resolve-accidental-match and seeing accidental-match-resolve-dialog with matchedNoteTitle, also assert the dialog contains notebook path text English practice (Background notebook — single-notebook fixture is enough; do not require a second notebook). Within the dialog, assert the matched title is inside a visible anchor (cy.contains('a', matchedNoteTitle) or find a under the dialog) without clicking through to noteShow or returning to reopen (D-07 / Phase 12 owns that). Keep dismiss via .close-button and assert dialog gone. Leave openLinkToMatchedNote and other link helpers untouched for Phase 9 @wip scenarios. Prefer page-object behavior change over new Gherkin steps.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature</automated>
  </verify>
  <acceptance_criteria>
    - expectAccidentalMatchReveal (or dialog helper) asserts English practice text inside accidental-match-resolve-dialog
    - Helper asserts matched title appears in a visible a tag inside the dialog
    - Helper still dismisses dialog and does not click the title link through to noteShow
    - Targeted accidental_match_reveal Cypress run exits 0 (assume pnpm sut already running)
  </acceptance_criteria>
  <done>E2E prove AMR-04 path + clickable title in resolve dialog without AMR-05 round-trip (D-11).</done>
</task>

<task type="auto">
  <name>Align feature wording; keep overlap uncoupled</name>
  <files>e2e_test/features/recall/accidental_match_reveal.feature</files>
  <read_first>
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/features/recall/overlap_try_again.feature
    - .planning/phases/08-match-path-and-clickable-titles/08-CONTEXT.md (D-07, D-11)
  </read_first>
  <action>
    Per D-11, optionally update Feature/Scenario description text from titles-only to titles + notebook path if it still claims titles only — prefer minimal wording; do not rewrite step text if page object already carries asserts. Do not add navigate-away-and-reopen scenarios (AMR-05 / Phase 12). Do not touch overlap_try_again.feature or un-@wip Phase 9 link scenarios. Capability names only — no phase numbers in feature titles.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature</automated>
  </verify>
  <acceptance_criteria>
    - accidental_match_reveal.feature does not describe or exercise navigate-and-reopen resolve
    - @wip link-from-result scenarios remain @wip
    - Targeted Cypress run for accidental_match_reveal still green
  </acceptance_criteria>
  <done>Feature wording matches path+title Behavior; overlap and AMR-05 stay out of scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress → app UI | E2E drives authenticated recall session; asserts only what Plan 01 already renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering / XSS | Dialog title/path in E2E fixture | medium | mitigate | Relies on Plan 01 text-interpolation components; E2E asserts visible text only |
| T-08-04 | Information Disclosure | E2E path assert | low | accept | Fixture notebook name already visible to the learning user in-app |
| T-08-SC | Tampering | npm installs | high | accept | No package installs in this plan |
</threat_model>

<verification>
Targeted accidental_match_reveal Cypress green with Nix prefix. Overlap suite not required unless regression suspected.
</verification>

<success_criteria>
- E2E dialog shows English practice path + clickable matched title
- No AMR-05 reopen scenario; overlap uncoupled
- AMR-04 Behavior fully evidenced (Vitest Plan 01 + E2E Plan 02)
</success_criteria>

<output>
Create `.planning/phases/08-match-path-and-clickable-titles/08-02-SUMMARY.md` when done
</output>
