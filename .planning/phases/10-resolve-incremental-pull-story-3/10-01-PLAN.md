---
phase: 10-resolve-incremental-pull-story-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/src/sync/applyPull.ts
  - cli/tests/applyPull.test.ts
  - e2e_test/features/cli/cli_sync_pull.feature
  - cli/src/commands/notebook/syncSlashCommand.tsx
autonomous: false
requirements:
  - EXP-03
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Mutating /sync applies remote create, update, and identity move via the same classify taxonomy as dry-run (D-01, D-02, D-07)"
    - "Reject paths are reported and never written; local-only files stay untouched; unchanged intersecting files are not rewritten (D-03, D-04)"
    - "After at least one successful create/update/move, .doughnut-sync/baseline.json is updated via savePushBaseline with agreed content for touched paths (D-05)"
    - "No-op and rejects-only pulls leave sync metadata untouched — no irrelevant baseline churn (D-06)"
    - "Anti-create unit and E2E contracts are inverted to create (and unit move) proofs; update, local-only, idempotent re-pull, and @perfSync stay green (D-08, D-09)"
    - "Terry-authored classify module is import-only (HYG-02)"
  artifacts:
    - path: cli/src/sync/applyPull.ts
      provides: "Classify → apply create/update/move + reject report + gated baseline"
      contains: "classifyPreviewPullNotes"
    - path: cli/tests/applyPull.test.ts
      provides: "Unit proofs for create, move, reject, baseline mutate/no-op, update, local-only, perf"
      contains: "scrum.md"
    - path: e2e_test/features/cli/cli_sync_pull.feature
      provides: "Capability E2E for create flip, update, local-only, no-op, baseline, @perfSync"
      contains: "pull into the workspace"
  key_links:
    - from: syncSlashCommand non-dry-run
      to: applyPull
      via: "dryRun ? previewPull : applyPull"
      pattern: "applyPull\\("
    - from: applyPull
      to: classifyPreviewPullNotes
      via: "import classify; drive disk writes from ClassifiedPullNote"
      pattern: "classifyPreviewPullNotes\\("
    - from: applyPull mutations
      to: savePushBaseline
      via: "load → patch touched paths → save only when mutation count ≥ 1"
      pattern: "savePushBaseline\\("
  assumptions:
    - "Baseline merge = loadPushBaseline prior map + set applied create/update paths; on move delete fromPath then set path; when prior empty, mutate-path-only map is enough (RESEARCH A1)"
    - "Move on disk = write new path + unlinkSync(fromPath) only (RESEARCH A2)"
    - "E2E move is optional; identity move proven in units (RESEARCH A3 / Pitfall 8)"
    - "Summary may count all mutations as updated notes plus reject lines via renderRejectFinding (discretion A4)"
  prohibitions:
    - statement: "Do not edit Terry-authored previewPullActions.ts — import classify only (HYG-02 / D-07)"
      status: active
      verification: "git diff --name-only excludes cli/src/sync/previewPullActions.ts"
    - statement: "Do not install new npm packages"
      status: active
      verification: "cli/package.json dependencies unchanged for new packages"
    - statement: "Do not remote-delete local-only Markdown absent from export (D-04)"
      status: active
      verification: "units cover local-only preserved; no unlink of paths outside classified move fromPath"
    - statement: "Do not weaken Phase 9 previewPull non-mutation"
      status: active
      verification: "diff does not regress previewPull.ts unless required; dry-run units stay green if touched"
---

<objective>
Strengthen mutating `/sync` → `applyPull` so Story 3 / EXP-03 closes both TRIAGE gaps: apply create/update/move via shared Phase 9 classify, and write `.doughnut-sync/baseline.json` only after successful mutating pulls — proven by `applyPull.test.ts` and `cli_sync_pull.feature` (D-01..D-10).

Purpose: One coarse Behavior plan (D-10) that lands classify→disk→baseline and inverts the anti-create contract without a separate micro E2E plan.
Output: Production `applyPull` + green units + green targeted pull E2E; import-only reuse of Terry classify.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-resolve-incremental-pull-story-3/10-CONTEXT.md
@.planning/phases/10-resolve-incremental-pull-story-3/10-RESEARCH.md
@.planning/phases/10-resolve-incremental-pull-story-3/10-PATTERNS.md
@.planning/phases/10-resolve-incremental-pull-story-3/10-COVERAGE.md
@.planning/phases/09-resolve-preview-before-pull-story-2/09-CONTEXT.md
@.planning/phases/07-publish-triage-decisions/TRIAGE.md
@.planning/notes/2026-07-24-portable-notebook-workspace.md
@.cursor/rules/planning.mdc
@.cursor/rules/cli.mdc
@cli/src/sync/applyPull.ts
@cli/src/sync/previewPull.ts
@cli/src/sync/previewPullActions.ts
@cli/src/sync/pushBaseline.ts
@cli/src/sync/diffReport.ts
@cli/src/commands/notebook/syncSlashCommand.tsx
@cli/tests/applyPull.test.ts
@cli/tests/previewPullDiagnostics.test.ts
@e2e_test/features/cli/cli_sync_pull.feature
</context>

## Phase Goal

Story 3 incremental pull participant work is healthy against acceptance: unchanged files undisturbed; create/update/move applied; re-pull idempotent; sync metadata only after mutating success (strengthen path for EXP-03).

This plan delivers the full strengthen in one coarse wave (D-10).

## Artifacts this phase produces

| Symbol / artifact | Path | Role |
|-------------------|------|------|
| `applyPull` (strengthened) | `cli/src/sync/applyPull.ts` | Classify → create/update/move writes; reject report; gated baseline |
| Unit proofs | `cli/tests/applyPull.test.ts` | Create, move, reject, baseline, update, local-only, perf |
| Capability E2E | `e2e_test/features/cli/cli_sync_pull.feature` | Create flip + baseline / no-op / update / local-only / `@perfSync` |
| Optional help polish | `cli/src/commands/notebook/syncSlashCommand.tsx` | Description matches create/update/move (discretion) |
| API coverage decision | `10-COVERAGE.md` | Declares no new external API integration |

## Source coverage audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|-----|---------------------|------|--------|-------|
| GOAL | — | Story 3 incremental pull healthy or gone | 01 | COVERED | Strengthen via apply+units+E2E |
| REQ | EXP-03 | Incremental pull matches story 3 | 01 | COVERED | Both TRIAGE gaps |
| RESEARCH | — | Classify→apply + baseline gate | 01 | COVERED | Tasks 2–3 |
| RESEARCH | — | Invert anti-create unit+E2E | 01 | COVERED | Checkpoint + tasks 2–3 |
| CONTEXT | D-01 | Close create/move + metadata gaps | 01 | COVERED | Task 2 |
| CONTEXT | D-02 | Reuse classify create/update/move | 01 | COVERED | Task 2 |
| CONTEXT | D-03 | Rejects reported, not written | 01 | COVERED | Task 2 |
| CONTEXT | D-04 | No remote-driven local deletes | 01 | COVERED | Task 2 |
| CONTEXT | D-05 | Baseline after mutating success | 01 | COVERED | Tasks 2–3 |
| CONTEXT | D-06 | No baseline on no-op/rejects-only | 01 | COVERED | Tasks 2–3 |
| CONTEXT | D-07 | applyPull primary; import classify | 01 | COVERED | Task 2 |
| CONTEXT | D-08 | Invert anti-create contracts | 01 | COVERED | Checkpoint + tasks 2–3 |
| CONTEXT | D-09 | Prove via feature + applyPull tests | 01 | COVERED | Tasks 2–3 |
| CONTEXT | D-10 | One coarse plan / larger tasks | 01 | COVERED | This plan shape |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm one-way invert of anti-create pull contract (D-08)</name>
  <decision>Replace the published anti-create unit and E2E contracts with create (and unit move) proofs so mutating pull matches Story 3 strengthen.</decision>
  <context>
    D-08 is one-way: `cli_sync_pull.feature` and `applyPull.test.ts` currently lock “remote-only notes are never created.” Inverting that is the Story 3 proof surface for EXP-03. Undoing later means rewriting the published capability E2E again and drifting from the oracle. Locked CONTEXT already chose invert; this gate records human confirmation before the agent walks through the door (REVERSIBILITY_GATES).
  </context>
  <options>
    <option id="invert-create">
      <name>Invert anti-create → create/move proofs</name>
      <pros>Matches TRIAGE strengthen, D-01/D-08, and Story 3 oracle; unlocks EXP-03 in one Behavior phase</pros>
      <cons>Published E2E/unit contract for pull create changes permanently</cons>
    </option>
    <option id="keep-anti-create">
      <name>Keep anti-create (abort strengthen)</name>
      <pros>No public contract flip</pros>
      <cons>Leaves both Story 3 gaps open; contradicts locked D-01/D-08 — not a valid EXP-03 completion</cons>
    </option>
  </options>
  <resume-signal>Select: invert-create (expected per locked D-08) or keep-anti-create</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>End-to-end classify→apply create/update/move + baseline gate (units)</name>
  <reversibility rating="one-way">D-08: inverting the anti-create unit (and enabling create/move on disk) changes the published Story 3 contract; paired with the checkpoint above.</reversibility>
  <precondition>Checkpoint resolved with invert-create (or explicit human override documented); Phase 9 classifyPreviewPullNotes available to import</precondition>
  <files>cli/src/sync/applyPull.ts, cli/tests/applyPull.test.ts</files>
  <read_first>
    - .planning/phases/10-resolve-incremental-pull-story-3/10-CONTEXT.md (D-01..D-09)
    - .planning/phases/10-resolve-incremental-pull-story-3/10-RESEARCH.md (Patterns 1–3; Pitfalls 1–6; Wave 0; A1/A2)
    - .planning/phases/10-resolve-incremental-pull-story-3/10-PATTERNS.md (applyPull compose; baseline merge; move unlink)
    - cli/src/sync/applyPull.ts
    - cli/src/sync/previewPull.ts (classify loop + listZipFileNames)
    - cli/src/sync/previewPullActions.ts (ClassifiedPullNote shape — import only)
    - cli/src/sync/pushBaseline.ts (loadPushBaseline / savePushBaseline)
    - cli/src/sync/diffReport.ts (renderRejectFinding)
    - cli/tests/applyPull.test.ts
    - cli/tests/previewPullDiagnostics.test.ts (move/reject fixtures)
  </read_first>
  <behavior>
    - Remote-only safe `.md` is written with export content; return summary is not the clean no-op sentinel (D-02, D-08)
    - Intersecting content diff still overwrites on disk (keep existing update behavior) (D-02)
    - Same doughnut_id at different paths: write new path, remove classified fromPath, preserve local-only files (D-02, D-04)
    - Reserved/duplicate/unsafe rejects are not written; summary includes reject path/reason; safe actions in the same run may still apply (D-03)
    - Rejects-only does not create or rewrite `.doughnut-sync/baseline.json` (D-06)
    - After ≥1 create/update/move: baseline exists with notebookId + agreed content for touched paths (move drops fromPath) (D-05, A1)
    - Content-matching no-op returns NOTHING_TO_PULL and does not introduce baseline when none existed (D-06)
    - Unchanged intersecting files are not rewritten; local-only untouched; 1000-note perf budget stays under 5s (D-04, D-09)
  </behavior>
  <action>
    After invert-create confirmation (D-08): RED first — rewrite the intentional remote-only unit into a create proof (file on disk + non-no-op summary); add move fixture mirroring preview diagnostics (doughnut_id path change); add reject (reserved/duplicate via listZipFileNames/buildZip, unsafe) and rejects-only cases that assert no write + reject text; add baseline assertions after mutate success and after no-op/rejects-only; keep update, local-only, matching-content no-op, and perf cases green (D-09). GREEN — rewrite `applyPull` to mirror preview’s classify loop: `readWorkspace` + `exportNotebookAsZip` + `listZipFileNames` + `unzipToEntries` + `classifyPreviewPullNotes` (D-02, D-07). Apply create/update with mkdir + writeFileSync; apply move as write new path then unlinkSync only on classified fromPath (A2); skip rejects while collecting `renderRejectFinding` lines (D-03). Never unlink local-only or remote-absent paths (D-04). When mutation count ≥ 1: `const next = new Map(loadPushBaseline(...))`, set applied create/update paths to exportContent, on move delete fromPath then set path, then `savePushBaseline` (D-05, A1). When mutation count is 0 (including rejects-only): skip baseline entirely (D-06). Extend summary to count applied create+update+move (wording discretionary; may keep “N note(s) updated.”) and append reject lines when present — rejects-only must not equal only the clean no-op sentinel (align Phase 9). Import classify from the existing Phase 9 module; put all apply/baseline/summary logic in `applyPull.ts` (HYG-02). Do not change backend zip. Prefer one coherent commit for this unit-green slice (D-10).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C cli exec vitest run tests/applyPull.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Unit: remote-only note is created with remote content; summary ≠ NOTHING_TO_PULL alone (D-08, D-02)
    - Unit: identity move writes destination, removes fromPath, preserves local-only (D-02, D-04)
    - Unit: reserved/duplicate/unsafe reject paths are absent on disk; summary contains reject (D-03)
    - Unit: rejects-only leaves no new `.doughnut-sync/baseline.json` (D-06)
    - Unit: mutate success writes `.doughnut-sync/baseline.json` with notebookId and touched-path content; move drops old path key (D-05)
    - Unit: matching-content no-op returns NOTHING_TO_PULL without creating baseline when none existed (D-06)
    - Unit: update, local-only, and 1000-note &lt;5s cases still pass (D-09)
    - `git diff --name-only` excludes `cli/src/sync/previewPullActions.ts` (HYG-02)
  </acceptance_criteria>
  <done>Production classify→apply→gated-baseline path is unit-green for create/update/move/reject/baseline; anti-create unit inverted.</done>
</task>

<task type="auto" tdd="true">
  <name>Capability E2E — create flip, baseline gate, keep green scenarios</name>
  <reversibility rating="one-way">D-08: published cli_sync_pull.feature create scenario flips from never-create to create proof.</reversibility>
  <precondition>Task 2 applyPull units green; invert-create still in force</precondition>
  <files>e2e_test/features/cli/cli_sync_pull.feature, cli/src/commands/notebook/syncSlashCommand.tsx, e2e_test/step_definitions/cli_sync_pull.ts</files>
  <read_first>
    - .planning/phases/10-resolve-incremental-pull-story-3/10-CONTEXT.md (D-05, D-06, D-08, D-09)
    - .planning/phases/10-resolve-incremental-pull-story-3/10-RESEARCH.md (Pitfall 8; Wave 0 E2E; A3/A5)
    - .planning/phases/10-resolve-incremental-pull-story-3/10-PATTERNS.md (cli_sync_pull + baseline hold-only patterns)
    - e2e_test/features/cli/cli_sync_pull.feature
    - e2e_test/features/cli/cli_push_dry_run.feature (baseline path listing analog)
    - e2e_test/step_definitions/cli_workspace.ts
    - cli/src/commands/notebook/syncSlashCommand.tsx
  </read_first>
  <behavior>
    - After pull with remote-only note removed locally then present remotely: workspace contains that note with remote content; less.md undisturbed; assistant is not only the clean no-op (D-08, D-02)
    - After a mutating pull: `.doughnut-sync/baseline.json` is present (or content assertable via existing workspace steps) (D-05)
    - After no-op pull on zip-seeded workspace without prior baseline: no irrelevant baseline file introduced (D-06)
    - Existing update, local-only, no-op message, and `@perfSync` scenarios stay green (D-09)
  </behavior>
  <action>
    Invert the remote-only E2E scenario into a capability-named create proof: after pull, `scrum.md` holds the remote body; `less.md` unchanged; assistant confirms a mutation (not only NOTHING_TO_PULL) (D-08, D-09). Add or extend scenarios so a mutating pull leaves `.doughnut-sync/baseline.json` in the workspace (reuse `should hold only` / contain path patterns from push dry-run E2E — prefer existing `cli_workspace` steps over fat new defs). Ensure the no-op scenario still shows NOTHING_TO_PULL and does not introduce baseline churn on a zip-seeded workspace that had no sync metadata (D-06). Do not add an E2E move unless a title→path rename step already exists cheaply — unit move is the “when feasible” proof (A3). Optionally update `/sync` help description so it no longer claims only existing files are updated (A5). Tag new scenarios `@wip` only while red; remove `@wip` before phase close. Prefer grouping E2E+help polish in one commit with the feature green (D-10). Run post-change-refactor on the uncommitted change before closing (execute-plan wrap-up).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_sync_pull.feature</automated>
  </verify>
  <acceptance_criteria>
    - Feature no longer asserts remote-only notes stay absent after pull; create proof passes (D-08)
    - Mutating pull E2E shows `.doughnut-sync/baseline.json` present after success (D-05)
    - No-op E2E still sees NOTHING_TO_PULL and does not add irrelevant baseline on empty-metadata seed (D-06)
    - Update, local-only, and `@perfSync` scenarios pass without `@wip` (D-09)
    - Capability names only — no phase numbers in scenario titles (D-09 / planning.mdc)
  </acceptance_criteria>
  <done>Targeted cli_sync_pull.feature proves create + baseline gate + preserved green behaviors for EXP-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Export zip bytes → apply classify/write | Untrusted entry names/contents cross into filesystem writes |
| applyPull → workspace filesystem | Mutating writes under workspacePath only |
| applyPull → `.doughnut-sync/baseline.json` | Sync merge-base rewrite gated on mutations |
| CLI → Doughnut API | Existing export download only; no new third-party API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | apply write / unsafe zip paths | high | mitigate | Trust classify rejects; never write reject paths; reuse safe-path rejects from Phase 9 (task 2) |
| T-10-02 | Tampering | `.doughnut-sync/**` as pull targets | high | mitigate | Classify rejects sync-metadata paths; only savePushBaseline after successful mutations (D-05/D-06) |
| T-10-03 | Tampering | move unlink | medium | mitigate | Unlink only classified fromPath; never delete local-only or remote-absent paths (D-04) |
| T-10-04 | Tampering | baseline on no-op | medium | mitigate | Skip savePushBaseline when mutation count is 0 including rejects-only (D-06) |
| T-10-05 | Information disclosure | apply summary | low | accept | Report note paths/content already in workspace/export; do not dump session tokens |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — legitimacy audit N/A for installs |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c pnpm -C cli exec vitest run tests/applyPull.test.ts` green
- `CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_sync_pull.feature` green (assume `pnpm sut` running)
- Diff excludes Terry-authored `previewPullActions.ts` (HYG-02)
- Both TRIAGE Story 3 gaps closed: create/rename/move applied + baseline after mutate only
- No `@wip` left on new pull scenarios; post-change-refactor + commit+push per execute-plan / local wrap-up
</verification>

<success_criteria>
EXP-03 satisfied via strengthen: `/sync` apply creates/updates/moves per shared classify, rejects clearly, baselines only on mutating success, idempotent no-op without VCS churn, proven by units + targeted `cli_sync_pull.feature`, with D-08 contract invert confirmed.
</success_criteria>

<output>
Create `.planning/phases/10-resolve-incremental-pull-story-3/10-01-SUMMARY.md` when done
</output>
