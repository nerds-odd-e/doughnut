---
phase: 03-potential-learning-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/odde/doughnut/controllers/dto/DueCommissionedMemoryTrackerLite.java
  - backend/src/main/java/com/odde/doughnut/controllers/dto/DueMemoryTrackers.java
  - backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java
  - backend/src/main/java/com/odde/doughnut/services/RecallService.java
  - backend/src/main/java/com/odde/doughnut/services/UserService.java
  - backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java
  - packages/generated/doughnut-backend-api
  - packages/doughnut-test-fixtures/src/DueMemoryTrackersBuilder.ts
  - frontend/src/composables/useRecallData.ts
  - frontend/src/composables/useRecallPageLoading.ts
  - frontend/src/components/toolbars/MainMenu.vue
  - frontend/src/components/recall/RecallProgressBar.vue
  - frontend/src/pages/RecallPage.vue
  - frontend/tests/pages/recallPageTestSupport.ts
  - frontend/tests/pages/RecallPage.spec.ts
  - frontend/tests/components/recall/RecallProgressBar.spec.ts
  - e2e_test/features/learning_session/commissioned_learning_session.feature
  - e2e_test/start/pageObjects/recallPage.ts
  - e2e_test/start/testability.ts
  - e2e_test/step_definitions
autonomous: true
requirements:
  - TRK-03
  - POT-01
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With only due COMMISSIONED trackers, ordinary recall count / toRepeat / nav badge stay 0 (TRK-03 / D-05)."
    - "Recall progress-bar area shows one potential learning session row for a notebook with due commissioned trackers, using glossary copy and notebook title (POT-01 / D-03)."
    - "Potential session is derived in the frontend by grouping dueCommissioned by notebookId — no Potential Learning Session persistence (D-02)."
    - "Phase 3 rows are display-only — no commission dialog / Learning Session create (D-04)."
    - "dueCommissioned arrives on the existing recalling / DueMemoryTrackers round-trip alongside ordinary toRepeat (D-01)."
    - "Empty potential-session list renders nothing (silent absence); no strip-local skeleton or false empty on load failure (UI-SPEC empty/loading/error)."
    - "Populated strip: one row per notebook with canonical copy; zero = hidden; one = single row (UI-SPEC zero-one-many / populated)."
  artifacts:
    - path: "backend/src/main/java/com/odde/doughnut/controllers/dto/DueCommissionedMemoryTrackerLite.java"
      provides: "Companion lite with memoryTrackerId, notebookId, notebookName"
      contains: "notebookName"
    - path: "backend/src/main/java/com/odde/doughnut/controllers/dto/DueMemoryTrackers.java"
      provides: "Additive dueCommissioned list on recalling DTO"
      contains: "dueCommissioned"
    - path: "frontend/src/components/recall/RecallProgressBar.vue"
      provides: "Sibling display-only potential-session strip under ordinary progress bar"
      contains: "potential-learning-session"
    - path: "frontend/src/composables/useRecallData.ts"
      provides: "dueCommissioned store + potentialLearningSessions computed group-by notebookId"
      contains: "potentialLearningSessions"
    - path: "e2e_test/features/learning_session/commissioned_learning_session.feature"
      provides: "Graduated Phase 3 scenario for Tutor-await / ordinary-empty"
      contains: "Due commissioned trackers await a Tutor rather than ordinary recall"
  key_links:
    - from: "GET /api/recalls/recalling"
      to: "DueMemoryTrackers.dueCommissioned"
      via: "RecallService.getDueMemoryTrackers + byUserIdCommissionedFrom sibling"
      pattern: "dueCommissioned"
    - from: "useRecallPageLoading / MainMenu recallStatus"
      to: "useRecallData.setDueCommissioned"
      via: "same response as toRepeat; never shuffle into quiz queue"
      pattern: "setDueCommissioned"
    - from: "potentialLearningSessions computed"
      to: "RecallProgressBar data-test=potential-learning-session"
      via: "group by notebookId; glossary copy with notebookName"
      pattern: "potential-learning-session"
---

<objective>
Deliver the tracer Behavior slice: due COMMISSIONED trackers feed `dueCommissioned` on recalling, the recall progress bar shows a display-only potential learning session by notebook, and ordinary recall stays empty of commissioned work (TRK-03, POT-01; D-01–D-06).

Purpose: First user-visible potential-session path on one notebook — stop-safe if Phase 3 stops after this plan.
Output: Additive DTO + sibling query (no Flyway), OpenAPI client regen, FE group-by-notebook strip per UI-SPEC, first graduated E2E scenario green without `@wip`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-potential-learning-sessions/03-CONTEXT.md
@.planning/phases/03-potential-learning-sessions/03-RESEARCH.md
@.planning/phases/03-potential-learning-sessions/03-PATTERNS.md
@.planning/phases/03-potential-learning-sessions/03-UI-SPEC.md
@.planning/phases/03-potential-learning-sessions/03-VALIDATION.md
@.planning/phases/02-assimilate-as-commissioned/02-CONTEXT.md
@.planning/phases/01-commissioned-tracker-model/commissioned_learning_session.feature
@docs/adrs/0001-ubiquitous-language.md

@backend/src/main/java/com/odde/doughnut/controllers/dto/DueMemoryTrackers.java
@backend/src/main/java/com/odde/doughnut/controllers/dto/MemoryTrackerLite.java
@backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java
@backend/src/main/java/com/odde/doughnut/services/RecallService.java
@backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java
@frontend/src/composables/useRecallData.ts
@frontend/src/composables/useRecallPageLoading.ts
@frontend/src/components/recall/RecallProgressBar.vue
@frontend/src/pages/RecallPage.vue
@.agents/skills/generate-api-client/SKILL.md
@.agents/skills/adr-awareness/SKILL.md
</context>

## Source coverage (this plan)

| Source | IDs | Covered here |
|--------|-----|--------------|
| GOAL | Potential sessions visible / ordinary empty of commissioned | Tracer + E2E |
| REQ | TRK-03, POT-01 | Both tasks |
| CONTEXT | D-01, D-02, D-03, D-04, D-05, D-06 (first scenario) | Tracer + E2E |
| RESEARCH | Flat dueCommissioned lites; sibling COMMISSIONED query; FE group; UI strip; OpenAPI regen | Tracer |
| UI-SPEC | Sibling strip, glossary copy, data-test, display-only, empty/loading/error | Tracer |

**Deferred / other plans:** POT-02 multi-notebook E2E + long-title wrap backstop → `03-02`. Commission dialog → Phase 4–5.

**Schema gate:** No Flyway / no PLS table — additive DTO/API only (D-01/D-02).

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end one-notebook potential learning session</name>
  <reversibility rating="reversible">Additive dueCommissioned field + FE strip; undo by removing field and UI — no Flyway / no PLS entity (per D-01, D-02).</reversibility>
  <files>backend/src/main/java/com/odde/doughnut/controllers/dto/DueCommissionedMemoryTrackerLite.java, backend/src/main/java/com/odde/doughnut/controllers/dto/DueMemoryTrackers.java, backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java, backend/src/main/java/com/odde/doughnut/services/RecallService.java, backend/src/main/java/com/odde/doughnut/services/UserService.java, backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java, packages/generated/doughnut-backend-api, packages/doughnut-test-fixtures/src/DueMemoryTrackersBuilder.ts, frontend/src/composables/useRecallData.ts, frontend/src/composables/useRecallPageLoading.ts, frontend/src/components/toolbars/MainMenu.vue, frontend/src/components/recall/RecallProgressBar.vue, frontend/src/pages/RecallPage.vue, frontend/tests/pages/recallPageTestSupport.ts, frontend/tests/pages/RecallPage.spec.ts, frontend/tests/components/recall/RecallProgressBar.spec.ts, e2e_test/features/learning_session/commissioned_learning_session.feature, e2e_test/start/pageObjects/recallPage.ts, e2e_test/start/testability.ts, e2e_test/step_definitions</files>
  <read_first>
    - `.planning/phases/03-potential-learning-sessions/03-RESEARCH.md` (Primary recommendation; Patterns 1–4; Pitfalls 1–8; Security Domain)
    - `.planning/phases/03-potential-learning-sessions/03-PATTERNS.md` (DTO companion, byUserIdCommissionedFrom sibling, RecallService map, useRecallData group, UI-SPEC strip, E2E graduate shape)
    - `.planning/phases/03-potential-learning-sessions/03-UI-SPEC.md` (sibling strip, copy template, data-test, display-only, empty/loading/error)
    - `.planning/phases/03-potential-learning-sessions/03-CONTEXT.md` (D-01–D-06)
    - `.planning/phases/01-commissioned-tracker-model/commissioned_learning_session.feature` (graduate only Scenario: Due commissioned trackers await a Tutor rather than ordinary recall — D-06)
    - `docs/adrs/0001-ubiquitous-language.md` §3 (potential learning session glossary)
    - `.agents/skills/generate-api-client/SKILL.md` (regenerate after DTO change; never hand-edit generated client)
  </read_first>
  <behavior>
    - RecallsController.recalling with only due COMMISSIONED trackers: toRepeat empty, dueCommissioned lists memoryTrackerId + notebookId + notebookName matching the note’s notebook (TRK-03 / D-01 / D-05).
    - Existing Phase 1 exclusion test remains green — byUserIdFrom still excludes COMMISSIONED (do not edit that fragment).
    - useRecallData: potentialLearningSessions groups dueCommissioned by notebookId; toRepeatCount ignores dueCommissioned (D-02 / D-05).
    - RecallProgressBar: when one notebook has due commissioned trackers, one row with data-test="potential-learning-session" shows `{n} potential learning session to commission for notebook "{notebookName}"` (singular “session”; D-03 / UI-SPEC).
    - Zero dueCommissioned → no potential-session rows rendered (UI-SPEC empty).
    - Row is display-only (role="status" or plain div; no commission click handler — D-04).
    - notebookName bound via Vue text interpolation only (mitigate T-03-03).
    - E2E Wave 0: graduate first Phase 3 scenario under learning_session feature tagged @wip until Task 2 greens it (D-06).
  </behavior>
  <action>
    Implement one production-quality vertical slice for TRK-03 / POT-01 / D-01–D-05 (first E2E scenario scaffold per D-06):

    Backend (no Flyway): Add companion DTO `DueCommissionedMemoryTrackerLite` with required `memoryTrackerId`, `notebookId`, `notebookName` (OpenAPI Schema REQUIRED; mirror MemoryTrackerLite Lombok style — RESEARCH A1 / PATTERNS). Add Getter/Setter list field `dueCommissioned` of type DueCommissionedMemoryTrackerLite on `DueMemoryTrackers` — leave `toRepeat` / `totalAssimilatedCount` semantics unchanged (D-01, D-05). Add sibling repository fragment `byUserIdCommissionedFrom` with `type = 'COMMISSIONED'` plus `findAllCommissionedByUserAndNextRecallAtLessThanEqual…` using the same `next_recall_at` cutoff as ordinary due; leave `byUserIdFrom` literal intact (Pitfall 1 / T-03-01 user_id scoped). Extend `RecallService.getDueMemoryTrackers` (and optional UserService mirror) to map due COMMISSIONED trackers to lites via `mt.getNote().getNotebook()` inside the transactional recalling path; setDueCommissioned on the same DTO. Mitigate T-03-02: only notebooks reachable via the current user’s due COMMISSIONED trackers.

    Tests: Extend `RecallsControllerTests` at controller boundary — only-commissioned-due → toRepeat empty AND dueCommissioned carries notebookName/notebookId (small-test style; keep Phase 1 exclusion test as canonical ordinary shape). Prefer `makeMe.aMemoryTrackerFor(note).commissioned().nextRecallAt(...).please()`.

    OpenAPI: Run generate-api-client skill (`CURSOR_DEV=true nix develop -c pnpm generateTypeScript`); do not hand-edit `packages/generated/doughnut-backend-api/**`.

    Frontend: Extend `DueMemoryTrackersBuilder` with fluent `dueCommissioned([...])` defaulting to `[]`. In `useRecallData`, add dueCommissioned ref + setDueCommissioned + `potentialLearningSessions` computed grouping by notebookId (D-02). Keep toRepeatCount derived solely from toRepeat (D-05). In `useRecallPageLoading` and MainMenu recallStatus hydrate: call `setDueCommissioned` **only** on successful recalling / recallStatus payloads (`!error && response`); on load failure leave prior `dueCommissioned` unchanged — never clear to `[]` (UI-SPEC error ≠ false empty; PATTERNS Shared Patterns). Still set dueCommissioned from a successful early empty-toRepeat return. Never shuffle commissioned into quiz queue (Pitfall 5/8). Pass potentialLearningSessions into RecallProgressBar from RecallPage.

    UI (UI-SPEC): Sibling strip below ProgressBar inside RecallProgressBar / GlobalBar column — not inside progress fill, not daisy-btn-primary. One row per session: `data-test="potential-learning-session"`, role="status" preferred, copy exactly `{n} potential learning session to commission for notebook "{notebookName}"` with singular “session”. Empty → render nothing. No @click commission path (D-04). Spacing: gap-2 between rows, px-4 horizontal inset; text-base / 400 / base-content. Bind notebookName with Vue mustache text interpolation only (T-03-03). Capability names only — no phase numbers in product/test files.

    Vitest: Mount recall progress / page with dueCommissioned fixture → assert potential-learning-session text includes notebook title; assert toRepeatCount unchanged when only commissioned due. Extend recallPageTestSupport mock with dueCommissioned / potentialLearningSessions.

    E2E scaffold (D-06): Append only Scenario “Due commissioned trackers await a Tutor rather than ordinary recall” from the draft feature into `e2e_test/features/learning_session/commissioned_learning_session.feature`, tag `@wip` (keep existing feature tags). Add testability `assimilateNoteAsCommissioned` via assimilateAsCommissioned true; Given step for bulk “notes … assimilated as commissioned on day N”; Then step + recallPage page-object `expectPotentialLearningSession(count, notebookTitle)` that navigates/asserts progress-bar strip (not sidebar-only — Pitfall 5). Ordinary “0 notes to recall” uses existing expectCount.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only -- --tests com.odde.doughnut.controllers.RecallsControllerTests &amp;&amp; CURSOR_DEV=true nix develop -c pnpm frontend:test tests/pages/RecallPage.spec.ts tests/components/recall/RecallProgressBar.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - DueMemoryTrackers OpenAPI/Java field dueCommissioned present; DueCommissionedMemoryTrackerLite has notebookName.
    - RecallsControllerTests: only-commissioned-due → empty toRepeat + non-empty dueCommissioned with notebook identity.
    - byUserIdFrom fragment still excludes COMMISSIONED (unchanged Phase 1 filter).
    - RecallProgressBar renders data-test="potential-learning-session" with glossary copy when fixtures provide due commissioned for one notebook.
    - toRepeatCount / ProgressBar math unchanged by dueCommissioned.
    - No new Flyway migration under backend db migration paths for this plan.
    - First Phase 3 E2E scenario exists under learning_session feature tagged @wip; steps/page-object/testability helpers present.
    - Generated client includes dueCommissioned (regenerated, not hand-edited).
  </acceptance_criteria>
  <done>One-notebook potential session visible end-to-end in unit tests; ordinary recall empty of commissioned; E2E scenario graduated @wip; no schema push.</done>
</task>

<task type="auto">
  <name>Graduate first Phase 3 E2E scenario to green</name>
  <files>e2e_test/features/learning_session/commissioned_learning_session.feature, e2e_test/start/pageObjects/recallPage.ts, e2e_test/start/testability.ts, e2e_test/step_definitions</files>
  <read_first>
    - `.planning/phases/03-potential-learning-sessions/03-VALIDATION.md` (sampling; Wave 0 E2E)
    - `.planning/phases/03-potential-learning-sessions/03-CONTEXT.md` (D-06 — only two Phase 3 scenarios; this task greens the first)
    - `scripts/check_wip_tags.sh` (MAX_WIP=5)
    - `e2e_test/step_definitions/recall.ts` (ordinary notes-to-recall step)
  </read_first>
  <action>
    Drive Scenario “Due commissioned trackers await a Tutor rather than ordinary recall” to green with targeted Cypress only (`--spec e2e_test/features/learning_session/commissioned_learning_session.feature`). Assume `pnpm sut` running. Assert both: 0 ordinary notes to recall (TRK-03 / D-05) and 1 potential learning session for notebook “Spanish conversation” (POT-01 / D-03). After green, remove `@wip` from that scenario only. Do not graduate the multi-notebook scenario or commission/record/amend draft scenarios yet (D-06 remainder → 03-02). Confirm `scripts/check_wip_tags.sh` under cap.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature &amp;&amp; CURSOR_DEV=true nix develop -c bash scripts/check_wip_tags.sh</automated>
  </verify>
  <acceptance_criteria>
    - Scenario “Due commissioned trackers await a Tutor rather than ordinary recall” passes without @wip.
    - Feature still contains Phase 2 assimilate scenario green.
    - WIP tag count within MAX_WIP.
  </acceptance_criteria>
  <done>First Phase 3 E2E scenario green without @wip; ordinary empty + one potential session asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `GET /api/recalls/recalling` (and menu recallStatus) | Authenticated session; response includes dueCommissioned notebook titles |
| RecallService → `memory_tracker` native queries | User-scoped reads only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege (IDOR) | MemoryTrackerRepository commissioned due query | high | mitigate | Keep `user_id = :userId` on byUserIdCommissionedFrom; never accept foreign userId from client; reuse assertLoggedIn on recalling |
| T-03-02 | Information Disclosure | dueCommissioned.notebookName | medium | mitigate | Emit only notebooks reachable via current user’s due COMMISSIONED trackers; do not join unrelated notebooks |
| T-03-03 | Tampering / XSS | RecallProgressBar notebookName render | high | mitigate | Bind notebookName with Vue text interpolation only; never HTML-bind notebook titles |
| T-03-04 | Spoofing | RecallsController.recalling | high | mitigate | Keep existing authorizationService.assertLoggedIn(); no anonymous recalling path |
| T-03-SC | Tampering | Package installs | high | accept | No new npm/pip/cargo packages this phase (Package Legitimacy N/A) |
</threat_model>

<verification>
RecallsControllerTests + RecallPage/RecallProgressBar Vitest green; learning_session Cypress feature green for first Phase 3 scenario without `@wip`; no Flyway added; `check_wip_tags.sh` OK.
</verification>

<success_criteria>
ROADMAP SC1–SC2 for Phase 3 (single-notebook path): ordinary recall 0 with only due commissioned; progress bar shows potential learning session for that notebook.
</success_criteria>

## Artifacts this phase produces (this plan)

- `DueCommissionedMemoryTrackerLite` + `DueMemoryTrackers.dueCommissioned`
- Sibling commissioned due repository query (no schema change)
- Regenerated OpenAPI TS client
- FE `potentialLearningSessions` + RecallProgressBar sibling strip
- Graduated E2E scenario “Due commissioned trackers await a Tutor rather than ordinary recall”

<output>
Create `.planning/phases/03-potential-learning-sessions/03-01-SUMMARY.md` when done.
After plan: local wrap-up — Jidoka, post-change-refactor, update plan status, commit, push (execute-plan / gsd-coexistence).
</output>
