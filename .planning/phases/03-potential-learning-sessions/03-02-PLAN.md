---
phase: 03-potential-learning-sessions
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - frontend/src/components/recall/RecallProgressBar.vue
  - frontend/tests/components/recall/RecallProgressBar.spec.ts
  - e2e_test/features/learning_session/commissioned_learning_session.feature
  - e2e_test/start/pageObjects/recallPage.ts
  - e2e_test/start/testability.ts
  - e2e_test/step_definitions
autonomous: true
requirements:
  - POT-02
  - TRK-03
  - POT-01
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Due commissioned trackers from two notebooks yield two potential learning session rows — one per notebook title (POT-02 / D-02 / D-03)."
    - "Multi-notebook case still keeps ordinary recall count at 0 when only COMMISSIONED work is due (TRK-03 / D-05 regression)."
    - "Many notebooks: rows stack vertically with gap-2; zero/one/many behavior remains (UI-SPEC overflow / zero-one-many)."
    - "Partial/missing notebookName should not occur when API contract holds; backend continues to send notebookName (UI-SPEC partial)."
  artifacts:
    - path: "e2e_test/features/learning_session/commissioned_learning_session.feature"
      provides: "Graduated multi-notebook Phase 3 scenario"
      contains: "Notes from different notebooks are commissioned as separate learning sessions"
    - path: "frontend/src/components/recall/RecallProgressBar.vue"
      provides: "Long notebook title wraps without ellipsis truncation that breaks E2E title match"
      contains: "potential-learning-session"
  key_links:
    - from: "dueCommissioned lites with distinct notebookId"
      to: "two data-test=potential-learning-session rows"
      via: "potentialLearningSessions group-by notebookId"
      pattern: "notebookId"
    - from: "E2E Given assimilate as commissioned across notebooks"
      to: "Then assert Spanish conversation + Kanji potential sessions"
      via: "recallPage.expectPotentialLearningSession"
      pattern: "potential learning session to commission for notebook"
  backstops:
    - statement: "Long notebook titles wrap within the strip (break-words / normal wrap); no ellipsis truncation that would break E2E substring match on full title."
      verification: backstop
---

<objective>
Prove multi-notebook potential learning sessions (POT-02) and polish UI-SPEC backstops / overflow so Phase 3 Behavior is fully observable (D-02, D-03, D-06 second scenario).

Purpose: Stop-safe completion of Phase 3 — two notebooks → two potential sessions without commission UI.
Output: Second graduated E2E scenario green; long-title wrap coverage; both Phase 3 scenarios without `@wip`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-potential-learning-sessions/03-CONTEXT.md
@.planning/phases/03-potential-learning-sessions/03-RESEARCH.md
@.planning/phases/03-potential-learning-sessions/03-PATTERNS.md
@.planning/phases/03-potential-learning-sessions/03-UI-SPEC.md
@.planning/phases/03-potential-learning-sessions/03-VALIDATION.md
@.planning/phases/03-potential-learning-sessions/03-01-SUMMARY.md

@frontend/src/components/recall/RecallProgressBar.vue
@frontend/src/composables/useRecallData.ts
@e2e_test/features/learning_session/commissioned_learning_session.feature
@.planning/phases/01-commissioned-tracker-model/commissioned_learning_session.feature
</context>

## Source coverage (this plan)

| Source | IDs | Covered here |
|--------|-----|--------------|
| GOAL | Two notebooks → two potential sessions (SC3) | Both tasks |
| REQ | POT-02 (primary); TRK-03 / POT-01 regression | Task 1–2 |
| CONTEXT | D-02, D-03, D-05, D-06 (second scenario) | Task 1 |
| UI-SPEC | overflow, zero-one-many, long-text backstop | Task 2 |
| RESEARCH | Flat lites carry notebookId/Name; group key = notebook | Task 1 |

**Deferred:** Commission dialog / Learning Session create (D-04 / Phases 4–5). No schema.

<tasks>

<task type="auto" tdd="true">
  <name>Multi-notebook potential sessions E2E + unit delta</name>
  <reversibility rating="reversible">FE grouping already shipped in 03-01; this locks POT-02 observability only.</reversibility>
  <files>e2e_test/features/learning_session/commissioned_learning_session.feature, e2e_test/start/pageObjects/recallPage.ts, e2e_test/start/testability.ts, e2e_test/step_definitions, frontend/tests/components/recall/RecallProgressBar.spec.ts, frontend/src/composables/useRecallData.ts</files>
  <read_first>
    - `.planning/phases/03-potential-learning-sessions/03-CONTEXT.md` (D-06 second scenario; D-02 FE group)
    - `.planning/phases/01-commissioned-tracker-model/commissioned_learning_session.feature` (Scenario: Notes from different notebooks are commissioned as separate learning sessions — Phase 3 asserts potential sessions only)
    - `.planning/phases/03-potential-learning-sessions/03-PATTERNS.md` (E2E multi-notebook Given/Then; page-object)
    - `.planning/phases/03-potential-learning-sessions/03-01-SUMMARY.md` (what 03-01 shipped for dueCommissioned / steps)
  </read_first>
  <behavior>
    - Two notebooks each with due COMMISSIONED trackers → two potentialLearningSessions entries (distinct notebookId) and two progress-bar rows with correct notebook titles (POT-02 / D-02 / D-03).
    - Ordinary toRepeatCount / “0 notes to recall” still holds when only commissioned due (TRK-03 / D-05).
    - E2E: graduate Scenario “Notes from different notebooks are commissioned as separate learning sessions” asserting potential sessions for “Spanish conversation” and “Kanji” (D-06) — no commission action.
  </behavior>
  <action>
    Per D-02 / POT-02: confirm `potentialLearningSessions` groups by notebookId (fix only if 03-01 grouping regresses). Add Vitest delta: fixture with two notebookIds → two `data-test="potential-learning-session"` rows with respective titles; do not re-assert full single-notebook copy shape already covered in 03-01.

    E2E (D-06): Append the draft multi-notebook scenario into `commissioned_learning_session.feature` (Kanji notebook + Spanish notes assimilated as commissioned). Tag `@wip` until green. Reuse assimilate-as-commissioned helpers from 03-01; add notebook-scoped Given if draft wording requires it. Assert both potential-session rows via page object; keep ordinary count 0 if the scenario implies only commissioned due. Do not graduate commission/record/amend scenarios. Capability-named steps only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/RecallProgressBar.spec.ts &amp;&amp; CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature</automated>
  </verify>
  <acceptance_criteria>
    - Vitest: two notebook dueCommissioned fixtures → two potential-learning-session rows with distinct titles.
    - E2E multi-notebook scenario present and runnable under learning_session feature (may still be @wip until Task 2).
    - No commission dialog / Learning Session create steps added.
  </acceptance_criteria>
  <done>POT-02 covered in unit + E2E scaffold; TRK-03 ordinary-empty still asserted where applicable.</done>
</task>

<task type="auto">
  <name>Polish wrap backstop and graduate multi-notebook E2E</name>
  <files>frontend/src/components/recall/RecallProgressBar.vue, frontend/tests/components/recall/RecallProgressBar.spec.ts, e2e_test/features/learning_session/commissioned_learning_session.feature</files>
  <read_first>
    - `.planning/phases/03-potential-learning-sessions/03-UI-SPEC.md` (UI Considerations long-text backstop; overflow stacking)
    - `.planning/phases/03-potential-learning-sessions/03-VALIDATION.md` (phase gate; remove @wip when both scenarios pass)
    - `scripts/check_wip_tags.sh`
  </read_first>
  <action>
    UI-SPEC long-text backstop: ensure potential-session row text wraps long notebook titles (`break-words` or equivalent Tailwind wrap) so the full title remains in the DOM for E2E substring match — avoid ellipsis truncation of the quoted title. Add a Vitest fixture with a long notebookName and assert the full title string is present in the row text.

    Overflow: keep vertical stack with gap-2 for many rows (already in UI-SPEC; adjust only if missing).

    Drive multi-notebook E2E scenario to green with targeted Cypress `--spec`; then remove `@wip` from that scenario. Confirm both Phase 3 scenarios (Tutor-await + multi-notebook) pass without `@wip`. Confirm `scripts/check_wip_tags.sh` under cap. No Flyway. No commission UI (D-04).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/RecallProgressBar.spec.ts &amp;&amp; CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature &amp;&amp; CURSOR_DEV=true nix develop -c bash scripts/check_wip_tags.sh</automated>
  </verify>
  <acceptance_criteria>
    - Long-title unit fixture: full notebookName visible in potential-learning-session text (backstop).
    - Multi-notebook E2E scenario green without @wip.
    - Both Phase 3 scenarios from D-06 pass; WIP under MAX_WIP.
  </acceptance_criteria>
  <done>POT-02 E2E green; long-title wrap backstop covered; Phase 3 Behavior complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → recalling / progress-bar strip | Same as 03-01; multi-notebook titles in response |
| E2E → assimilateAsCommissioned fixtures | Testability only; still user-scoped session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege (IDOR) | Commissioned due query | high | mitigate | Unchanged from 03-01 — user_id scoped; no new endpoints |
| T-03-02 | Information Disclosure | Multi-notebook notebookName rows | medium | mitigate | Still only user’s due COMMISSIONED notebooks; E2E must not leak other users’ titles |
| T-03-03 | Tampering / XSS | Long / adversarial notebook titles in strip | high | mitigate | Keep mustache text binding for notebookName; wrap CSS must not switch to HTML injection |
| T-03-SC | Tampering | Package installs | high | accept | No new packages |
</threat_model>

<verification>
RecallProgressBar Vitest (multi-notebook + long title) green; learning_session Cypress feature green for both Phase 3 scenarios without `@wip`; `check_wip_tags.sh` OK.
</verification>

<success_criteria>
ROADMAP SC3: two notebooks with due commissioned trackers yield two potential sessions; SC1 ordinary empty still holds; UI-SPEC long-text backstop verified.
</success_criteria>

## Artifacts this phase produces (this plan)

- Graduated E2E scenario “Notes from different notebooks are commissioned as separate learning sessions”
- Long-title wrap / break-words coverage on potential-session rows
- Phase 3 Behavior complete when both D-06 scenarios are green without `@wip`

<output>
Create `.planning/phases/03-potential-learning-sessions/03-02-SUMMARY.md` when done.
After plan: local wrap-up — Jidoka, post-change-refactor, update plan status, commit, push (execute-plan / gsd-coexistence).
</output>
