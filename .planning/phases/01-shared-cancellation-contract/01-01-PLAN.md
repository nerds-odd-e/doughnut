---
phase: 01-shared-cancellation-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/managedApi/ApiStatusHandler.ts
  - frontend/src/managedApi/clientSetup.ts
  - frontend/tests/managedApi/clientSetup.loading.spec.ts
  - frontend/tests/managedApi/clientSetup.spec.ts
autonomous: true
requirements:
  - COHE-01
flagged_assumptions:
  - requirement_id: COHE-01
    category: unclassified
    status: unresolved
    verification: null
    note: "The SPEC-less deterministic edge probe returned unclassified; this remains flagged and is neither dismissed nor converted to a backstop."
must_haves:
  truths:
    - "Existing noncancelable apiCallWithLoading callers keep their raw SDK-shaped return type, synchronous loading, ordinary error handling, and exact cleanup."
    - "An opted-in operation receives one operation-owned AbortSignal and resolves only as completed-with-result or cancelled-with-status."
    - "Accepted cancellation promptly aborts and removes only its own loading identity while other nested or concurrent states remain active."
    - "Accepted cancellation wins a same-turn completion race, and late fulfillment or rejection cannot revive completion, toast, log, or leak an unhandled rejection."
  prohibitions:
    - statement: "The Phase 1 shared contract must not be adopted by a product call site, especially a transactional mutation, before its domain-specific safe cancelled outcome is delivered."
      status: unresolved
      verification: null
  artifacts:
    - path: frontend/src/managedApi/ApiStatusHandler.ts
      provides: "Identity-bound optional cancel action and exact-removal acceptance result."
    - path: frontend/src/managedApi/clientSetup.ts
      provides: "Opt-in abort ownership and discriminated cancellation outcome without widening default callers."
    - path: frontend/tests/managedApi/clientSetup.loading.spec.ts
      provides: "Deterministic contract, concurrency, prompt-settlement, stale-action, and race evidence."
    - path: frontend/tests/managedApi/clientSetup.spec.ts
      provides: "Silent accepted-cancellation and unchanged ordinary-error evidence."
  key_links:
    - from: frontend/src/managedApi/clientSetup.ts
      to: frontend/src/managedApi/ApiStatusHandler.ts
      via: "apiCallWithLoading creates one state whose id-bound action is the only cancellation linearization point."
    - from: frontend/src/managedApi/clientSetup.ts
      to: "future generated controller invocation in Phase 2"
      via: "The opt-in callback receives AbortSignal; no generated file or Phase 1 product caller changes."
---

<objective>
Create the dormant shared cancellation contract that Phase 2 can opt into, while every existing caller remains behaviorally and type-compatible.

Purpose: Satisfy COHE-01 and locked decisions D-01 through D-06 and D-10 through D-13 at the shared loading seam only.
Output: A tested overload, state-bound abort lifecycle, deterministic terminal outcome, and silent late-settlement handling.
</objective>

<execution_context>
@/Users/terryyin/.codex/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shared-cancellation-contract/01-CONTEXT.md
@.planning/phases/01-shared-cancellation-contract/01-RESEARCH.md
@.planning/phases/01-shared-cancellation-contract/01-PATTERNS.md
@.planning/phases/01-shared-cancellation-contract/01-VALIDATION.md
@.planning/phases/01-shared-cancellation-contract/01-UI-SPEC.md
@frontend/src/managedApi/ApiStatusHandler.ts
@frontend/src/managedApi/clientSetup.ts
@frontend/tests/managedApi/clientSetup.loading.spec.ts
@frontend/tests/managedApi/clientSetup.spec.ts

<interfaces>
Preserve `ApiLoadingOptions`, `ApiStatus`, `currentBlockingApiState`, `setupGlobalClient`, `runWithBlockingApiLoading`, and the existing noncancelable `apiCallWithLoading(() => Promise<T>, options?) => Promise<T>` contract. Add exported `CancelableApiLoadingOptions` and `CancelableApiResult<T>` plus a literal `cancelable: true` overload whose callback accepts `AbortSignal`. Extend `ApiLoadingState` with an omitted-by-default state-bound `cancel` action, and make `finishLoading(state)` report whether that exact active identity was removed.
</interfaces>
</context>

<structure_contract>
ROADMAP marks this phase `Mode: mvp` and `Type: Structure`. Per `.cursor/rules/planning.mdc` and `.cursor/rules/gsd-coexistence.mdc`, the Structure grammar wins: do not invent a user story, do not add a product call-site slice, and prepare only Phase 2's immediately following layout-generation behavior. The phase boundary must remain externally unchanged.
</structure_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the typed identity-bound cancellation contract</name>
  <files>frontend/src/managedApi/ApiStatusHandler.ts, frontend/src/managedApi/clientSetup.ts, frontend/tests/managedApi/clientSetup.loading.spec.ts</files>
  <read_first>
    - .planning/phases/01-shared-cancellation-contract/01-CONTEXT.md
    - .planning/phases/01-shared-cancellation-contract/01-RESEARCH.md
    - .planning/phases/01-shared-cancellation-contract/01-PATTERNS.md
    - frontend/src/managedApi/ApiStatusHandler.ts
    - frontend/src/managedApi/clientSetup.ts
    - frontend/tests/managedApi/clientSetup.loading.spec.ts
  </read_first>
  <behavior>
    - Default calls still synchronously create state, return the raw SDK result, and expose no cancel action.
    - Literal opt-in yields `{ status: "completed", result } | { status: "cancelled" }`; `result` is inaccessible until status narrowing.
    - The callback receives one AbortSignal; accepting the state-bound action aborts it, removes only that state, and resolves cancelled before the request settles.
    - Repeating a captured action is a no-op and cannot remove a replacement state.
  </behavior>
  <action>Use RED→GREEN→REFACTOR. First add focused runtime and `expectTypeOf`/`@ts-expect-error` tests, run them, and confirm failure for the missing opt-in contract. Then implement exported `CancelableApiLoadingOptions` and `CancelableApiResult&lt;T&gt;`, narrow overloads, one private AbortController per opted-in operation, an optional `ApiLoadingState.cancel`, and boolean exact-id removal. Enforce D-01, D-02, and D-03 without widening existing destructuring callers; enforce D-04, D-06, and D-10 with an identity-bound, idempotent action. Keep `runWithBlockingApiLoading`, all product callers, generated files, `LoadingThinBar`, and backend code unchanged per D-12 and the Structure boundary. Consolidate overlapping assertions if needed so touched files remain cohesive and comply with the 250-line refactor rule.</action>
  <acceptance_criteria>
    - Default calls retain `Promise&lt;T&gt;` and have no cancel action.
    - Opted-in calls require `blockUi: true` and `cancelable: true`, receive AbortSignal, and require status narrowing.
    - Accepted cancel removes exactly one active id, aborts its signal, resolves promptly, and is idempotent.
    - No product or generated file changes.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/managedApi/clientSetup.loading.spec.ts</automated>
  </verify>
  <done>The typed opt-in path and exact state ownership are green while every default-call regression remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Linearize cancellation ahead of late completion and errors</name>
  <files>frontend/src/managedApi/clientSetup.ts, frontend/tests/managedApi/clientSetup.loading.spec.ts, frontend/tests/managedApi/clientSetup.spec.ts</files>
  <read_first>
    - .planning/phases/01-shared-cancellation-contract/01-CONTEXT.md
    - .planning/phases/01-shared-cancellation-contract/01-VALIDATION.md
    - frontend/src/managedApi/clientSetup.ts
    - frontend/tests/managedApi/clientSetup.loading.spec.ts
    - frontend/tests/managedApi/clientSetup.spec.ts
  </read_first>
  <behavior>
    - Resolving the request and accepting Cancel in the same synchronous turn resolves cancelled.
    - Cancel-then-late-resolve and cancel-then-late-reject remain cancelled and create neither success handling, toast, log, nor unhandled rejection.
    - Ordinary non-cancellation SDK failures still follow existing toast/error behavior.
  </behavior>
  <action>Use RED→GREEN→REFACTOR. Add controlled-promise tests with no timers for resolve-then-cancel, cancel-then-resolve, cancel-then-reject, prompt outer settlement, and unchanged ordinary errors. Confirm each new assertion fails for the missing race policy. Implement one synchronous accepted-cancellation latch as the terminal linearization point, set it before abort/settlement, and attach fulfillment and rejection observers to the losing request before racing it. Gate completed classification and `handleSdkError` on that latch so D-05 wins deterministically; keep the public cancelled value status-only per D-01/D-02, silent per D-11, domain-neutral per D-12, and free of console/telemetry per D-13.</action>
  <acceptance_criteria>
    - Same-turn accepted cancellation always returns `{ status: "cancelled" }`.
    - Late resolve/reject is consumed and cannot toast or escape as an unhandled rejection.
    - Ordinary errors still toast exactly as before.
    - Focused tests use deterministic promise control and no fixed waits.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/managedApi/clientSetup.loading.spec.ts tests/managedApi/clientSetup.spec.ts</automated>
  </verify>
  <done>Cancellation precedence and silence are deterministic, and the existing error contract remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Global modal action → shared loading state | A user action may affect only the exact operation represented by its rendered state identity. |
| Shared wrapper → generated browser request | AbortSignal affects browser request consumption; it does not prove server work stopped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Denial of Service | `ApiLoadingState.cancel` | high | mitigate | Bind an idempotent closure to one state id; deterministic repeated/stale-action tests prove it cannot retarget. |
| T-01-02 | Tampering / Denial of Service | `finishLoading` | high | mitigate | Remove exactly one matching id and test that concurrent/nested survivors remain present. |
| T-01-03 | Tampering | `apiCallWithLoading` terminal result | high | mitigate | An accepted latch wins same-turn races; observed late settlement cannot produce completed handling. |
| T-01-04 | Repudiation / integrity ambiguity | Browser abort semantics | medium | mitigate | Keep the result named `cancelled`, expose no server-stop result/reason, and add no product copy or call-site adoption in this plan. |
</threat_model>

<probe_fallback>
The deterministic SPEC-less edge probe row for COHE-01 remains `unclassified / unresolved / verification:null` in frontmatter `flagged_assumptions`; it is not auto-backstopped. The prohibition recall→precision pass dropped routine race/cleanup/type-safety items to ordinary tests and kept the product-safety prohibition in `must_haves.prohibitions` descriptor-less and flagged-unverified. No `check_*` fields are fabricated.
</probe_fallback>

## Artifacts this phase produces

| File | New or changed public surface |
|---|---|
| `frontend/src/managedApi/ApiStatusHandler.ts` | `ApiLoadingState.cancel?`; `finishLoading(state)` reports exact-id removal acceptance. |
| `frontend/src/managedApi/clientSetup.ts` | Exported `CancelableApiLoadingOptions`, exported `CancelableApiResult&lt;T&gt;`, and a literal opt-in `apiCallWithLoading` overload; default overload remains stable. |
| `frontend/tests/managedApi/clientSetup.loading.spec.ts` | Contract/race/concurrency/type evidence; no production export. |
| `frontend/tests/managedApi/clientSetup.spec.ts` | Cancellation-silence regression evidence; no production export. |

<source_audit>
| Source | ID | Item | Plan/task | Status |
|---|---|---|---|---|
| GOAL | — | Shared abort ownership, cancelled outcome, and conditional control for immediate Phase 2 only | 01-01 + 01-02 | COVERED |
| REQ | COHE-01 | One shared abort, cleanup, and cancelled-outcome contract; caller owns domain state | 01-01 tasks 1-2 | COVERED |
| RESEARCH | — | Overload/default compatibility, accepted latch, observed losing promise, no generated changes | 01-01 tasks 1-2 | COVERED |
| RESEARCH | — | Selected-state modal path and approved UI contract | 01-02 tasks 1-2 | COVERED |
| CONTEXT | D-01 | Two-way discriminated result | 01-01 task 1 | COVERED |
| CONTEXT | D-02 | Cancelled variant is status-only | 01-01 tasks 1-2 | COVERED |
| CONTEXT | D-03 | Narrow before result access | 01-01 task 1 | COVERED |
| CONTEXT | D-04 | Abort/remove only exact operation | 01-01 task 1 | COVERED |
| CONTEXT | D-05 | Accepted cancellation wins late completion | 01-01 task 2 | COVERED |
| CONTEXT | D-06 | Prompt cancelled resolution | 01-01 task 1 | COVERED |
| CONTEXT | D-07 | Visible control targets selected blocker | 01-02 task 2 | COVERED |
| CONTEXT | D-08 | Reveal older blocker without remount | 01-02 task 2 | COVERED |
| CONTEXT | D-09 | Hidden cancelability never leaks | 01-02 task 2 | COVERED |
| CONTEXT | D-10 | Identity-bound idempotent action | 01-01 task 1 + 01-02 task 2 | COVERED |
| CONTEXT | D-11 | Silent cancellation feedback | 01-01 task 2 + 01-02 task 1 | COVERED |
| CONTEXT | D-12 | Shared layer stays domain-neutral | 01-01 task 1 | COVERED |
| CONTEXT | D-13 | No cancellation logging/telemetry | 01-01 task 2 | COVERED |
</source_audit>

<verification>
Run the Wave 0/focused commands after each task. At plan completion run `CURSOR_DEV=true nix develop -c pnpm frontend:build` and `CURSOR_DEV=true nix develop -c pnpm frontend:test tests/managedApi/clientSetup.loading.spec.ts tests/managedApi/clientSetup.spec.ts`.
</verification>

<success_criteria>
- COHE-01 and D-01 through D-06/D-10 through D-13 have deterministic focused evidence.
- Existing callers compile and behave unchanged; no product caller can display Cancel in Phase 1.
- No generated, backend, schema, `runWithBlockingApiLoading`, or thin-bar change exists.
</success_criteria>

<output>
Create `.planning/phases/01-shared-cancellation-contract/01-01-SUMMARY.md` when done.
</output>
