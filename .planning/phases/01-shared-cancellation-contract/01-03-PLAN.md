---
phase: 01-shared-cancellation-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - frontend/src/components/commons/Overlay.vue
  - frontend/tests/components/commons/LoadingModal.spec.ts
autonomous: true
requirements:
  - COHE-01
gap_closure: true
gap_ids: [G-01-3]
must_haves:
  truths:
    - "At a real Chromium viewport of 320x568, the exact long-message LoadingModal starts with its spinner fully reachable, provides vertical scrolling to the Cancel control, and creates no horizontal overflow."
    - "When the LoadingModal stack fits the viewport, its spinner, message, and optional control remain centered with the approved 16px gaps and unchanged typography."
    - "The gap fix changes only layout overflow behavior: existing blocker selection, identity-bound Cancel action, silent feedback, and noncancelable product defaults remain intact per D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, and D-13."
  artifacts:
    - path: frontend/src/components/commons/Overlay.vue
      provides: "Overflow-aware safe vertical centering and the scroll container used by LoadingModal."
    - path: frontend/tests/components/commons/LoadingModal.spec.ts
      provides: "Real-browser wide/narrow viewport regression using the diagnosed long message and live Cancel control."
  key_links:
    - from: frontend/src/components/commons/LoadingModal.vue
      to: frontend/src/components/commons/Overlay.vue
      via: "LoadingModal's existing `centered` Overlay prop selects the overflow-safe centering rule without new props or wrappers."
    - from: frontend/tests/components/commons/LoadingModal.spec.ts
      to: frontend/src/components/commons/Overlay.vue
      via: "The mounted teleported LoadingModal is measured at 1280x720 and 320x568 through Vitest browser `page.viewport`."
---

<objective>
Close G-01-3 by keeping an oversized long-message LoadingModal usable at 320x568 while preserving the ordinary centered layout and all existing Phase 1 cancellation behavior.

Purpose: Repair the one failed UI-SPEC overflow truth with a test-first browser regression and the smallest layout change proven by the diagnosis.
Output: A real-Chromium viewport regression plus overflow-aware safe centering and vertical scrolling on the existing Overlay surface.
</objective>

<execution_context>
@/Users/terryyin/.codex/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.codex/gsd-core/templates/summary.md
@.cursor/skills/execute-plan/SKILL.md
@.cursor/skills/post-change-refactor/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.cursor/agent-map.md
@.cursor/rules/planning.mdc
@.cursor/rules/gsd-coexistence.mdc
@.cursor/rules/frontend-component.mdc
@.cursor/rules/frontend-testing.mdc
@.cursor/rules/linting_formating.mdc
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-shared-cancellation-contract/01-CONTEXT.md
@.planning/phases/01-shared-cancellation-contract/01-RESEARCH.md
@.planning/phases/01-shared-cancellation-contract/01-UI-SPEC.md
@.planning/phases/01-shared-cancellation-contract/01-UAT.md
@.planning/phases/01-shared-cancellation-contract/01-02-SUMMARY.md
@.planning/debug/loading-modal-long-message-overflow.md
@frontend/src/components/commons/Overlay.vue
@frontend/src/components/commons/LoadingModal.vue
@frontend/tests/components/commons/LoadingModal.spec.ts
@frontend/vitest.config.ts

<interfaces>
Use the installed Vitest 4.1.10 browser API exactly as `page.viewport(width, height)` from `vitest/browser`. Keep `LoadingModal`'s existing `centered` Overlay usage and `ApiLoadingCancelControl` interface unchanged.
</interfaces>
</context>

<structure_contract>
This remains a stop-safe Phase 1 Structure repair. It restores the approved overflow contract without opting in a product caller, changing cancellation state/action semantics, altering message typography, adding UI copy, or preparing behavior beyond immediate Phase 2. The sole observable correction is reachability of the existing spinner/message/control stack when that stack exceeds the viewport.
</structure_contract>

<preflight_gate name="jidoka_before_change">
Read the current worktree status before editing and preserve every pre-existing change. The diagnosis has already selected the narrow layout solution; stop for developer judgment only if implementation reveals a value/design fork, missing credential, ambiguity about the Structure boundary, or an unrelated failure that cannot be diagnosed. Do not treat routine test/CSS choices as a stop.
</preflight_gate>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the long-message blocker reachable at a narrow browser viewport</name>
  <files>frontend/tests/components/commons/LoadingModal.spec.ts, frontend/src/components/commons/Overlay.vue</files>
  <read_first>
    - .planning/phases/01-shared-cancellation-contract/01-UAT.md
    - .planning/debug/loading-modal-long-message-overflow.md
    - .planning/phases/01-shared-cancellation-contract/01-UI-SPEC.md
    - .cursor/rules/frontend-component.mdc
    - .cursor/rules/frontend-testing.mdc
    - frontend/tests/components/commons/LoadingModal.spec.ts
    - frontend/src/components/commons/LoadingModal.vue
    - frontend/src/components/commons/Overlay.vue
    - frontend/vitest.config.ts
  </read_first>
  <behavior>
    - At 1280x720, the exact 20-repeat long message with Cancel remains centered when the complete stack fits.
    - At 320x568 and initial scroll position, the overlay begins at the viewport top, the spinner is fully visible, and content does not overflow horizontally.
    - At 320x568 after scrolling the overlay to its end, the same Cancel button is fully visible and still invokes its identity-bound action once.
    - The existing computed-style assertions continue to prove unchanged message typography, wrapping mode, and 16px stack gaps.
  </behavior>
  <action>Use RED→GREEN→REFACTOR. First extract the existing 20-repeat message into one file-local constant shared by the current typography test and a new browser-viewport regression. Import `page` from `vitest/browser`; capture the original browser dimensions, render the real teleported `LoadingModal` with the exact long message and a spy-backed Cancel control, and restore the original viewport in `finally`. At 1280x720 assert the fitting content remains vertically centered. Then resize the same rendered modal to 320x568 and assert from live DOM rectangles and scroll metrics that the spinner is fully visible at scroll start, overlay `scrollHeight` exceeds `clientHeight`, horizontal scroll extent does not exceed the client width, and the Cancel control becomes fully visible and clickable after setting the overlay to its maximum vertical scroll position. Run the focused spec and confirm this new case fails for the diagnosed negative top/unreachable endpoints before editing production CSS.

Then change only `.overlay--centered` in `Overlay.vue`: set `align-items: safe center` and add `overflow-y: auto` to the fixed-height flex surface. Keep `justify-content: center`, fixed positioning, dimensions, background, z-index plumbing, transition, `LoadingModal.vue`, typography, 16px gaps, Cancel styling/copy, and every cancellation prop/callback untouched. Do not add a wrapper, max-height, message-width rule, truncation, new prop, product caller opt-in, or dependency. This is the counterfactual proven in the debug artifact and preserves D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, and D-13.</action>
  <acceptance_criteria>
    - The new regression fails against the pre-fix layout for G-01-3's diagnosed off-screen endpoints, then passes after the two layout declarations are added.
    - The exact long-message stack is usable at 320x568 without horizontal overflow, typography change, gap change, truncation, or a new visual element.
    - A fitting stack remains centered, so ordinary blocker presentation is unchanged.
    - `LoadingModal.vue`, application callers, managed API code, generated code, and backend code are unchanged.
    - `LoadingModal.spec.ts` remains at or below the repository's 250-line file-size gate; if the cohesive regression cannot fit, stop and re-plan rather than make an unrelated split.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/commons/LoadingModal.spec.ts</automated>
  </verify>
  <done>Real Chromium proves the ordinary stack stays centered and the oversized 320x568 stack exposes both endpoints through one vertical scroll path.</done>
</task>

<task type="auto">
  <name>Task 2: Close the gap through the repository delivery gates</name>
  <files>frontend/src/components/commons/Overlay.vue, frontend/tests/components/commons/LoadingModal.spec.ts, .planning/phases/01-shared-cancellation-contract/01-03-PLAN.md, .planning/phases/01-shared-cancellation-contract/01-03-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>With Task 1 green, run the repository's post-change-refactor skill over only this plan's uncommitted product/test diff and require its `## REFACTOR COMPLETE` handoff; the refactor must remain Structure-only, retain the two-file implementation scope, check both touched files against the 250-line rule, and rerun the focused modal spec. Then run the required formatting and CI-style lint gates through Nix (`pnpm format:all`, followed non-interactively by `CI=true pnpm lint:all`), rerun the focused browser spec and frontend build after any formatter edit, and run the whitespace checker through the repository script. Do not run full Cypress because no product call site or end-to-end behavior changes.

Perform post-phase Jidoka after all checks: stop if the work revealed a value/design fork, unsafe cancellation adoption, credential need, or unexplained unrelated failure. Otherwise update this plan as completed, create `01-03-SUMMARY.md` with the RED/GREEN evidence and G-01-3 linkage, and apply the normal GSD STATE/ROADMAP completion updates. Commit only this plan's two implementation files and its normal gap-closure planning artifacts; preserve and exclude every unrelated or pre-existing worktree change, including debug evidence not owned by execution. Push the resulting phase commits before handoff. Git commands run directly; all repository tooling runs through `CURSOR_DEV=true nix develop -c`.</action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/commons/LoadingModal.spec.ts &amp;&amp; CURSOR_DEV=true nix develop -c pnpm frontend:build &amp;&amp; CURSOR_DEV=true nix develop -c pnpm diff:check</automated>
  </verify>
  <done>Focused regression, build, formatting, lint, whitespace, refactor, plan-update, Jidoka, scoped commit, and push gates are complete, with no unrelated worktree content captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Viewport → fixed loading overlay | Browser dimensions and content length determine whether the blocker endpoints are reachable. |
| Layout surface → identity-bound Cancel control | Scrolling may expose the existing action but must not replace, duplicate, or retarget its callback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-05 | Denial of Service | `Overlay.vue` narrow-viewport layout | high | mitigate | Use safe vertical centering plus automatic vertical overflow and prove both endpoints reachable in real Chromium at 320x568. |
| T-01-06 | Tampering | `LoadingModal` control/state binding | medium | mitigate | Leave `LoadingModal.vue` and managed state untouched; click the same identity-bound control after scrolling and retain the existing replacement/stale-action regression suite. |
| T-01-07 | Repudiation / integrity ambiguity | Existing Cancel presentation | low | accept | No copy, feedback, or action semantics change; the already-approved fixed `Cancel` label continues to mean abandoning the browser wait only. |
</threat_model>

<source_coverage_audit>

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 1 | Shared abort/cleanup/outcome plus conditional modal control without product adoption | 01-01, 01-02, 01-03 | COVERED | 01-03 restores the approved modal overflow truth without expanding Phase 1 behavior. |
| REQ | COHE-01 | One shared cancellation contract; callers own only domain-local outcomes | 01-01, 01-02, 01-03 | COVERED | The gap plan changes layout/test only and protects the existing shared contract. |
| RESEARCH | Architecture | Use the one global blocker seam; no component-local controller or generated/backend change | 01-01, 01-02, 01-03 | COVERED | `Overlay.vue` is the only current product Overlay consumer; `LoadingModal.vue` remains unchanged. |
| RESEARCH | Validation | Vitest Playwright Chromium, focused modal spec, build, format, and no full E2E for this Structure phase | 01-03 | COVERED | Uses the pinned `page.viewport` API and Nix-prefixed commands. |
| RESEARCH | Packages | Install no new package | 01-03 | COVERED | Existing Vitest/Playwright versions provide the regression seam. |
| CONTEXT | D-01, D-02, D-03 | Completed/cancelled type contract and narrowing | 01-01; preserved by 01-03 | COVERED | No managed API/type edit. |
| CONTEXT | D-04, D-05, D-06 | Exact prompt cancellation, precedence, and late-settlement handling | 01-01; preserved by 01-03 | COVERED | No loading lifecycle edit. |
| CONTEXT | D-07, D-08, D-09, D-10 | Newest-state selection, older revelation, hidden action, stale-action safety | 01-02; regression-protected by 01-03 | COVERED | Same button is exercised after scrolling; existing selection tests stay green. |
| CONTEXT | D-11, D-12, D-13 | Silent generic feedback and caller-owned post-cancel state | 01-02; preserved by 01-03 | COVERED | No copy, telemetry, toast, or caller change. |
| UAT | G-01-3 | Spinner, message, and control usable without clipping or horizontal overflow | 01-03 | COVERED | Test-first wide/narrow real-browser regression plus diagnosed safe-centering/scroll fix. |

Deferred server cooperation and mutation-safe cancellation remain excluded exactly as recorded in STATE/REQUIREMENTS; this plan does not introduce either.
</source_coverage_audit>

<verification>
1. RED evidence: `CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/commons/LoadingModal.spec.ts` fails the new 320x568 reachability assertions before the CSS edit.
2. Focused GREEN: rerun the same command after the CSS edit and after post-change-refactor.
3. Static/build gate: `CURSOR_DEV=true nix develop -c pnpm frontend:build`.
4. Repository quality gates: `CURSOR_DEV=true nix develop -c pnpm format:all`, `CI=true CURSOR_DEV=true nix develop -c pnpm lint:all`, and `CURSOR_DEV=true nix develop -c pnpm diff:check`.
5. Scope gate: inspect `git status --short` and the scoped diff before commit; the only product/test paths for G-01-3 are `Overlay.vue` and `LoadingModal.spec.ts`, and pre-existing changes remain unstaged.
</verification>

<success_criteria>
- G-01-3's exact 320x568 failure is reproduced test-first and closed in real Chromium.
- Oversized content uses a vertical scroll path with no negative-start loss or horizontal overflow; fitting content remains centered.
- UI-SPEC typography, 16px gaps, spinner, message, Cancel styling/copy, and identity-bound action remain unchanged.
- Phase 1 remains a stop-safe Structure phase: every product call site stays noncancelable and existing blocker/cancellation behavior is preserved.
- Required Jidoka, post-change-refactor, plan update, scoped commit, and push gates complete before handoff.
</success_criteria>

<output>
Create `.planning/phases/01-shared-cancellation-contract/01-03-SUMMARY.md` when done, retaining `gap_ids: [G-01-3]` linkage in the plan for UAT reconciliation.
</output>
