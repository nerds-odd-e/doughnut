---
phase: 09-build-a-link-from-resolve-dialog
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
  - e2e_test/features/recall/accidental_match_reveal.feature
autonomous: true
requirements:
  - AMR-06
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Accidental-match E2E link scenarios open Resolve then Build a link and complete property/relationship link while remaining on the accidental-match result (D-09; AMR-06)"
    - "Page object openLinkToMatchedNote uses Resolve → link-to-matched-note-* → existing Link to: helpers — not the removed stacked matched-notes section (D-05, D-09)"
    - "After link success, expectStillOnAccidentalMatchResult asserts alert + recall result chrome (and prefer resolve dialog/list still available); overlap_try_again stays uncoupled (D-04, D-09)"
    - "Both wiki-property and relationship link scenarios run without @wip once green (D-09)"
  artifacts:
    - path: e2e_test/start/pageObjects/AnsweredQuestionPage.ts
      provides: "openLinkToMatchedNote + expectStillOnAccidentalMatchResult updated for resolve-dialog Build a link path"
    - path: e2e_test/features/recall/accidental_match_reveal.feature
      provides: "Two link scenarios untagged @wip when green"
  key_links:
    - from: AnsweredQuestionPage.openLinkToMatchedNote
      to: resolve-accidental-match → link-to-matched-note-* → Link to:
      via: "waitUntilAppIsNotBusy after Resolve; Build a link label; existing property/relationship helpers (D-09)"
    - from: expectStillOnAccidentalMatchResult
      to: accidental-match-alert + Resolve CTA / optional dialog reopen
      via: "Stay-on-result after link; no stacked matched-notes section assert (D-04, D-09)"
---

<objective>
Close AMR-06 with targeted E2E: update page objects for Resolve → Build a link, then untag the two @wip accidental-match link scenarios once green (D-09).

Purpose: Restore link-from-result E2E acceptance after Plan 01 Vitest wiring.
Output: Updated AnsweredQuestionPage helpers; both link scenarios green without @wip.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-build-a-link-from-resolve-dialog/09-CONTEXT.md
@.planning/phases/09-build-a-link-from-resolve-dialog/09-RESEARCH.md
@.planning/phases/09-build-a-link-from-resolve-dialog/09-PATTERNS.md
@.planning/phases/09-build-a-link-from-resolve-dialog/09-UI-SPEC.md
@.planning/phases/09-build-a-link-from-resolve-dialog/09-01-PLAN.md
@.cursor/rules/e2e-authoring.mdc
</context>

## Artifacts this phase produces

- Page-object path: Resolve → Build a link → existing link helpers (AMR-06 E2E)
- Untagged wiki-property and relationship stay-on-result scenarios

## UI Considerations (from 09-UI-SPEC)

E2E covers populated Build a link + stay-on-result; AMR-07 gates remain Vitest-only; do not add Add as overlapped or AMR-05 reopen here.

## Multi-Source Coverage (this plan)

| SOURCE | ID | Item | Status |
|--------|-----|------|--------|
| REQ | AMR-06 | E2E property + relationship stay | COVERED |
| CONTEXT | D-09 | Untag @wip via page-object | COVERED |
| CONTEXT | D-04 / D-05 | Stay + preserve testids | COVERED |

<tasks>

<task type="auto">
  <name>E2E page object: Resolve then Build a link</name>
  <reversibility rating="reversible">Page-object path rewrite only; undo is revert of openLinkToMatchedNote / expectStillOnAccidentalMatchResult (D-09).</reversibility>
  <files>e2e_test/start/pageObjects/AnsweredQuestionPage.ts</files>
  <read_first>
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/step_definitions/recall.ts
    - .planning/phases/09-build-a-link-from-resolve-dialog/09-PATTERNS.md (E2E page-object section)
    - .planning/phases/09-build-a-link-from-resolve-dialog/09-RESEARCH.md (Pitfall 4; E2E sketch)
    - .planning/phases/08-match-path-and-clickable-titles/08-02-SUMMARY.md
  </read_first>
  <action>
    Per D-09 / D-05, rewrite openLinkToMatchedNote so it clicks resolve-accidental-match, waitUntilAppIsNotBusy, asserts accidental-match-resolve-dialog contains matchedNoteTitle, clicks visible link-to-matched-note-* labeled Build a link, then asserts Link to: parent contains matchedNoteTitle and continues with existing search-placeholder / helper flow used by property and relationship scenarios. Rewrite expectStillOnAccidentalMatchResult to assert accidental-match alert + recall result chrome (Resolve CTA visible; optionally reopen dialog and see match title) — stop asserting stay via the removed stacked matched-notes section helper. Prefer page-object-only changes; leave Gherkin step text in recall.ts as thin delegates. Keep overlap helpers and expectAccidentalMatchReveal path/title asserts intact.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature</automated>
  </verify>
  <acceptance_criteria>
    - openLinkToMatchedNote opens Resolve then clicks Build a link via link-to-matched-note-* before Link to: asserts
    - expectStillOnAccidentalMatchResult asserts accidental-match alert / result chrome without requiring the stacked matched-notes section
    - Existing non-@wip accidental_match_reveal scenarios still green under targeted Cypress (assume pnpm sut running)
  </acceptance_criteria>
  <done>Page object drives Build a link from resolve dialog; stay-on-result asserts match Phase 7+ chrome (D-04, D-05, D-09).</done>
</task>

<task type="auto">
  <name>Untag @wip on accidental-match link E2E scenarios</name>
  <files>e2e_test/features/recall/accidental_match_reveal.feature</files>
  <read_first>
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/features/recall/overlap_try_again.feature
    - .planning/phases/09-build-a-link-from-resolve-dialog/09-CONTEXT.md (D-09)
  </read_first>
  <action>
    Per D-09, after page-object path is green against the two link scenarios (run locally with tags including @wip if needed), remove @wip from the wiki-property and relationship accidental-match link scenarios only. Do not rewrite scenario step text if page object already carries the flow. Do not touch overlap_try_again.feature. Capability names only — no phase numbers in feature titles. Confirm both link scenarios pass in the targeted accidental_match_reveal Cypress run without @wip.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature</automated>
  </verify>
  <acceptance_criteria>
    - accidental_match_reveal.feature wiki-property and relationship link scenarios have no @wip tag
    - Targeted Cypress run for accidental_match_reveal exits 0 including both link scenarios
    - overlap_try_again.feature unchanged
  </acceptance_criteria>
  <done>AMR-06 E2E complete: both link scenarios green without @wip (D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress → app UI | E2E drives authenticated recall; asserts Plan 01 Build a link + stay chrome |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering / Spoofing | E2E Resolve → offer path | high | mitigate | Relies on Plan 01 single-Modal host; E2E opens one Resolve dialog then Build a link |
| T-09-02 | Elevation of Privilege | E2E link writes | high | mitigate | Scenarios use writable learner notebooks; readonly gate covered in Plan 01 Vitest |
| T-09-SC | Tampering | npm installs | high | accept | No package installs in this plan |
</threat_model>

<verification>
Targeted accidental_match_reveal Cypress green with Nix prefix (pnpm sut assumed running). Overlap suite not required unless regression suspected.
</verification>

<success_criteria>
- Page object opens Resolve → Build a link → existing helpers
- Both link E2E scenarios green without @wip
- AMR-06 fully evidenced (Vitest Plan 01 + E2E Plan 02); AMR-07 evidenced in Plan 01
</success_criteria>

<output>
Create `.planning/phases/09-build-a-link-from-resolve-dialog/09-02-SUMMARY.md` when done
</output>
