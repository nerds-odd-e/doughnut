---
phase: 09-build-a-link-from-resolve-dialog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/recall/AnsweredSpellingQuestion.vue
  - frontend/src/components/recall/AccidentalMatchResolveDialog.vue
  - frontend/src/components/recall/AccidentalMatchResolveRow.vue
  - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
autonomous: true
requirements:
  - AMR-06
  - AMR-07
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "From an opened Resolve dialog, each eligible match row shows a Build a link CTA with data-testid link-to-matched-note-{id} when realms are writable and loaded (D-03, D-05, D-06, D-08; AMR-06, AMR-07)"
    - "Clicking Build a link swaps the same Resolve Modal body to MatchedNoteLinkOffer — no second PopButton or Modal around the offer (D-01, D-02, D-05; AMR-06)"
    - "Offer closeDialog (go-back or link success) returns to the match list inside the same Modal; accidental-match alert remains on the result (D-04, D-08; AMR-06)"
    - "Build a link is omitted (not disabled-looking) when reviewed notebook is readonly or reviewed/matched realms required for the offer are not loaded; titles and path still show when available (D-06, D-07, D-08; AMR-07)"
    - "reviewedNoteId is passed from the answered recalled note into AccidentalMatchResolveDialog; AnsweredSpellingQuestion stays Resolve CTA + single PopButton only (D-02)"
  artifacts:
    - path: frontend/src/components/recall/AccidentalMatchResolveDialog.vue
      provides: "list|link step state; reviewedNoteId; host-level reviewed hydrate + canOfferBuildLink; MatchedNoteLinkOffer mount; closeDialog → returnToList"
    - path: frontend/src/components/recall/AccidentalMatchResolveRow.vue
      provides: "Gated Build a link CTA under title/path; emit buildLink; preserves link-to-matched-note-{id}"
    - path: frontend/src/components/recall/AnsweredSpellingQuestion.vue
      provides: "Passes reviewed-note-id from recalled noteTopology.id into resolve dialog"
    - path: frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
      provides: "Boundary Vitest: writable CTAs, readonly/unload omit, same-Modal step, stay-on-result after link (D-08)"
  key_links:
    - from: AccidentalMatchResolveDialog step=link
      to: MatchedNoteLinkOffer
      via: "reviewedNoteId + matchedNoteId props; @close-dialog=returnToList (not PopButton closer) per D-04, D-05"
    - from: AccidentalMatchResolveRow Build a link
      to: AccidentalMatchResolveDialog.openLinkOffer
      via: "@build-link with matched.id when canBuildLink true (D-03)"
    - from: canOfferBuildLink
      to: getNoteRealmRefAndLoadWhenNeeded + notebookRealm.readonly
      via: "currentUser inject + host reviewedRealm + matched realm presence (D-06, D-07)"
---

<objective>
Prove AMR-06 and AMR-07 at the Vitest boundary: single-Modal Build a link step from resolve rows, stay on accidental-match result after offer close/success, and hide Build a link under readonly/unload gates (D-01..D-08).

Purpose: Deliver Phase 9 Behavior stop-safely before E2E — unit evidence first (same wave shape as Phases 7–8).
Output: Dialog step host + gated row CTA + reviewedNoteId wiring; AccidentalMatch Vitest green for link + gates.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-build-a-link-from-resolve-dialog/09-CONTEXT.md
@.planning/phases/09-build-a-link-from-resolve-dialog/09-RESEARCH.md
@.planning/phases/09-build-a-link-from-resolve-dialog/09-PATTERNS.md
@.planning/phases/09-build-a-link-from-resolve-dialog/09-UI-SPEC.md
@.planning/phases/09-build-a-link-from-resolve-dialog/09-VALIDATION.md
@.cursor/rules/frontend-testing.mdc
@.cursor/rules/unit-testing.mdc
</context>

## Artifacts this phase produces

- AccidentalMatchResolveDialog list↔link step host mounting MatchedNoteLinkOffer in the same Modal
- Per-row gated **Build a link** CTA on AccidentalMatchResolveRow
- Vitest boundary coverage for Build a link + AMR-07 gates (capability-named; no phase numbers in product/test names)

## UI Considerations (from 09-UI-SPEC)

Lifted into must_haves / tasks: empty = omit Build a link when gated (no unavailable copy); loading = progressive hide until realms loaded (no skeleton); error = no gate retry chrome; populated = list with CTAs → offer step; partial = path and CTA may appear independently; overflow = existing Modal scroll; zero-one-many = same per-row chrome; long-text = fixed **Build a link** label + Phase 8 title wrap backstop.

## Multi-Source Coverage (this plan)

| SOURCE | ID | Item | Status |
|--------|-----|------|--------|
| GOAL | — | Build link from dialog; stay on result; gates | COVERED (with 09-02 for E2E) |
| REQ | AMR-06 | Build a link + stay on result | COVERED (Vitest; E2E in 09-02) |
| REQ | AMR-07 | Hide when readonly / unloaded | COVERED |
| CONTEXT | D-01..D-08 | Step host, CTA, gates, Vitest | COVERED |
| CONTEXT | D-09 | Untag @wip E2E | Deferred to 09-02 |

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: Build a link step in same Resolve Modal</name>
  <reversibility rating="costly">D-01 single-Modal step swap — undoing toward a nested PopButton around MatchedNoteLinkOffer would reintroduce Pitfall 3 (focus/close fights) across resolve and offer hosts.</reversibility>
  <files>frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts, frontend/src/components/recall/AnsweredSpellingQuestion.vue, frontend/src/components/recall/AccidentalMatchResolveDialog.vue, frontend/src/components/recall/AccidentalMatchResolveRow.vue</files>
  <read_first>
    - frontend/src/components/recall/AnsweredSpellingQuestion.vue
    - frontend/src/components/recall/AccidentalMatchResolveDialog.vue
    - frontend/src/components/recall/AccidentalMatchResolveRow.vue
    - frontend/src/components/recall/MatchedNoteLinkOffer.vue
    - frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts
    - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
    - frontend/tests/components/recall/MatchedNoteLinkOffer.spec.ts
    - .planning/phases/09-build-a-link-from-resolve-dialog/09-CONTEXT.md (D-01..D-08)
    - .planning/phases/09-build-a-link-from-resolve-dialog/09-PATTERNS.md (step host + CTA + gate)
    - .planning/phases/09-build-a-link-from-resolve-dialog/09-UI-SPEC.md (row CTA + offer host sketch)
    - .planning/phases/09-build-a-link-from-resolve-dialog/09-RESEARCH.md (Pattern 1–3; Vitest restore list)
  </read_first>
  <behavior>
    - Canonical ACCIDENTAL_MATCH with two matches, currentUser + seedRealms for reviewed + matched A/B: open Resolve; document.body shows two link-to-matched-note-* buttons labeled Build a link (D-03, D-05, D-08)
    - Click link-to-matched-note for match id 10: body shows Link to: and that match title; accidental-match-resolve-dialog list testid is gone while on offer step; still a single resolve Modal (no nested link PopButton) (D-01, D-05)
    - After property success (mock TextContentController.updateNoteContent to resolve): list / accidental-match-resolve-dialog visible again; wrapper still has accidental-match-alert (D-04, D-08; AMR-06)
    - Prefer one canonical case for writable CTAs + step + stay; sibling gate cases land in the next task
  </behavior>
  <action>
    Per D-08, write failing Vitest at the AnsweredSpellingQuestion boundary first (RED): mount with currentUser and seedRealms via mountAnsweredSpellingQuestion; open resolve-accidental-match; assert two Build a link CTAs; click one; assert offer chrome in the same Modal; mock updateNoteContent then assert return to list with alert still present. Then implement per D-01..D-05: pass reviewed-note-id from answeredQuestion.recalledNote.noteTopology.id into AccidentalMatchResolveDialog (D-02); extend dialog with ResolveStep list|link ref, openLinkOffer / returnToList, host-level reviewed hydrate via getNoteRealmRefAndLoadWhenNeeded(reviewedNoteId), canOfferBuildLink port of v1.1 canOfferLinkToMatched (currentUser + reviewedRealm + readonly !== true + matched realm present) (D-06, D-07); v-if list of AccidentalMatchResolveRow vs MatchedNoteLinkOffer with reviewedNoteId + step.matchedNoteId and @close-dialog=returnToList — never wire offer closeDialog to PopButton closer (D-04); reuse MatchedNoteLinkOffer as-is including navigate-on-success=false (D-05). On AccidentalMatchResolveRow: prop canBuildLink, emit buildLink, button face/title/aria-label Build a link, data-testid link-to-matched-note-{id}, daisy-btn daisy-btn-secondary daisy-btn-sm under title/path (UI-SPEC / D-03). Keep AnsweredSpellingQuestion as single Resolve PopButton only. Do not add Add as overlapped note (D-03 deferred Phase 11). Do not nest PopButton around the offer (D-01). Do not change ACCIDENTAL_MATCH / OVERLAP grading or SRS. Capability-named testids only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - AccidentalMatchResolveDialog owns list|link step and mounts MatchedNoteLinkOffer on link step with @close-dialog mapped to returnToList
    - AnsweredSpellingQuestion passes reviewed-note-id into the dialog; still one Resolve PopButton
    - AccidentalMatchResolveRow renders gated Build a link with link-to-matched-note-{id}
    - Vitest: writable+seeded open dialog shows two Build a link CTAs; click opens Link to: in same Modal; after mocked property success list + accidental-match-alert remain
    - Vitest file green for this tracer case; MatchedNoteLinkOffer.vue left as-is
  </acceptance_criteria>
  <done>Happy path AMR-06 at Vitest: Build a link is a same-Modal step; offer close returns to list; alert stays (D-01..D-05, D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Expand Vitest: AMR-07 readonly and unload gates</name>
  <files>frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts, frontend/src/components/recall/AccidentalMatchResolveDialog.vue</files>
  <read_first>
    - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
    - frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts
    - frontend/src/components/recall/AccidentalMatchResolveDialog.vue
    - .planning/phases/09-build-a-link-from-resolve-dialog/09-RESEARCH.md (Pattern 2; Pitfall 3)
    - .planning/phases/09-build-a-link-from-resolve-dialog/09-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <behavior>
    - Readonly delta: reviewedRealm.notebookRealm.readonly = true with currentUser + seedRealms; open Resolve; zero link-to-matched-note-* CTAs; titles/path may still show (D-06, D-08; AMR-07)
    - Unloaded delta: never-settling showNote mock and no seedRealms; open Resolve; zero Build a link CTAs (D-06, D-07, D-08; AMR-07)
    - Assert only gate deltas — do not re-assert full writable payload in each sibling case
  </behavior>
  <action>
    Per D-08 / AMR-07, add focused Vitest deltas (RED then green) adapting pre–Phase 7 gate cases: (1) mutate reviewedRealm.notebookRealm.readonly = true before seed/mount; open Resolve; expect zero link-to-matched-note-* on document.body; (2) mockSdkServiceWithImplementation NoteController.showNote never-settling Promise and omit seedRealms; open Resolve; expect zero Build a link CTAs. Prefer hide via v-if canBuildLink (D-06) — do not render disabled-looking CTAs. Confirm canOfferBuildLink at dialog host uses host-level reviewed hydrate once (discretion) plus per-row matched realm presence (D-07). Keep overlap try-again cases untouched. Small-test style: AnsweredSpellingQuestion boundary; focused asserts; capability-named tests.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec has a readonly case with zero link-to-matched-note-* after open Resolve
    - Spec has an unloaded-realm case with zero Build a link CTAs after open Resolve
    - AccidentalMatch Vitest file fully green including Phase 7–8 dismiss/path cases and Plan 01 tracer stay/step cases
  </acceptance_criteria>
  <done>AMR-07 unit side complete: Build a link omitted when readonly or realms unloaded (D-06..D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Result chrome → Resolve Modal | Authenticated learner opens dialog over graded ACCIDENTAL_MATCH matches |
| Dialog gate → Build a link CTA | Client uses currentUser + notebookRealm.readonly + realm load presence before offering mutate UI |
| Offer step → content / relationship APIs | Existing MatchedNoteLinkOffer writes; backend still enforces notebook authz |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering / Spoofing | Resolve Modal + MatchedNoteLinkOffer host | high | mitigate | Single-Modal step swap only (D-01); never nest PopButton/Modal; Vitest asserts offer appears without a second link PopButton |
| T-09-02 | Elevation of Privilege | Build a link CTA / canOfferBuildLink | high | mitigate | Hide CTA when readonly or realms unloaded (D-06); port currentUser check; server-side write authz unchanged on updateNoteContent / relationship create |
| T-09-03 | Tampering / XSS | Match titles / offer Link to: | medium | mitigate | Reuse NoteTitleWithLink + offer text interpolation; no v-html for titles |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — reuse-only; Package Legitimacy N/A |
</threat_model>

<verification>
AccidentalMatch Vitest file green with Nix prefix. Manual visual optional; automated Build-a-link + gate asserts are the Plan 01 gate.
</verification>

<success_criteria>
- Same-Modal Build a link step works; offer close returns to list; alert stays
- Build a link omitted for readonly and unloaded-realm cases
- Vitest proves AMR-06 + AMR-07 at the answered-question boundary (D-01..D-08)
</success_criteria>

<output>
Create `.planning/phases/09-build-a-link-from-resolve-dialog/09-01-SUMMARY.md` when done
</output>
