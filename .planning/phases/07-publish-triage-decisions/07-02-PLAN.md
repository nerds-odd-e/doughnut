---
phase: 07-publish-triage-decisions
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .planning/phases/07-publish-triage-decisions/TRIAGE.md
autonomous: true
requirements:
  - TRIAGE-01
  - TRIAGE-02
must_haves:
  truths:
    - "Stories 2, 3, and 4 each have exactly one Verdict: keep | strengthen | remove (TRIAGE-01)"
    - "Each of stories 2–4 cites every matching acceptance bullet from the portable-workspace note in oracle order with match|gap|N/A + proof (TRIAGE-02)"
    - "Each story 2–4 dossier includes Capability entrypoints + Delete/keep file set + Whole participant-touched inventory (D-01, D-02)"
    - "Overlapping paths among stories 1–4 appear under every related story dossier tagged shared (D-03; probe: adjacency — duplicate, do not merge dossiers)"
    - "Story order in TRIAGE.md remains 1 then 2 then 3 then 4 (probe: ordering); Summary table verdicts match dossiers"
    - "Evidence for stories 2–4 excludes Terry Yin / Tan Yeong Sheng / terryyin as triage basis (TRIAGE-01)"
    - "Strengthen/remove dossiers include WIP/gap labels with one concrete proof each (D-04)"
    - "Empty participant work for a story still publishes a verdict with empty-inventory evidence (probe: empty)"
  artifacts:
    - path: .planning/phases/07-publish-triage-decisions/TRIAGE.md
      provides: "Complete dossiers for stories 2–4 plus D-03 shared tags with story 1 overlaps"
      contains: "Story 2:|Story 3:|Story 4:"
  key_links:
    - from: "Story 2 dossier"
      to: "cli_sync_dry_run.feature + previewPull path"
      via: "PATTERNS story 2 map"
      pattern: "dry-run|previewPull"
    - from: "Story 3 dossier"
      to: "cli_sync_pull.feature + applyPull path"
      via: "PATTERNS story 3 map"
      pattern: "applyPull|cli_sync_pull"
    - from: "Story 4 dossier"
      to: "cli_lint_workspace.feature + lintSlashCommand"
      via: "PATTERNS story 4 map"
      pattern: "lintSlashCommand|lintWorkspace"
  assumptions:
    - "Stories 5–6 remain Pending until plan 03"
    - "Idempotent edit: overwrite story 2–4 sections in place without changing Story 1 schema (probe: idempotency)"
    - "Do not parallel-edit TRIAGE.md with plan 03 (depends_on enforces concurrency probe)"
  prohibitions:
    - statement: "must NOT base stories 2–4 verdicts on Terry/YS-only work"
      status: resolved
      verification: "Author filter evidence per dossier"
    - statement: "must NOT omit acceptance citations for stories 2–4"
      status: resolved
      verification: "Bullet counts: story 2 = 3, story 3 = 5, story 4 = 4"
    - statement: "must NOT list a shared path under only one overlapping story"
      status: resolved
      verification: "Every path tagged shared appears in each related story inventory"
    - statement: "must NOT modify product code or apply keep/remove in the tree"
      status: resolved
      verification: "git diff limited to .planning/"
    - statement: "must NOT invent acceptance criteria outside the oracle"
      status: resolved
      verification: "Citations quote oracle only"
---

<objective>
Publish complete actionable dossiers for stories 2 (preview-before-pull), 3 (incremental pull), and 4 (workspace lint), including D-03 shared-path duplication with story 1 and among 2–3.

Purpose: Expand tracer format to sync/lint capabilities without touching product code.
Output: Updated `TRIAGE.md` with stories 2–4 complete; Summary rows 2–4 filled.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-publish-triage-decisions/07-CONTEXT.md
@.planning/phases/07-publish-triage-decisions/07-PATTERNS.md
@.planning/phases/07-publish-triage-decisions/07-01-SUMMARY.md
@.planning/phases/07-publish-triage-decisions/TRIAGE.md
@.planning/notes/2026-07-24-portable-notebook-workspace.md
</context>

## Phase Goal

Continue Phase 7 Behavior: publish keep/strengthen/remove for stories 2–4 with citations and inventories so Phases 9–11 can act from TRIAGE.md alone.

## Artifacts this phase produces

| Symbol / artifact | Path | Plan |
|-------------------|------|------|
| `TRIAGE.md` stories 2–4 dossiers | `.planning/phases/07-publish-triage-decisions/TRIAGE.md` | 02 |

<tasks>

<task type="auto">
  <name>Task 1: Publish Story 2 preview-before-pull dossier</name>
  <files>.planning/phases/07-publish-triage-decisions/TRIAGE.md</files>
  <reversibility rating="costly">Phase 9 treats Story 2 dossier as sole action source (D-01).</reversibility>
  <read_first>
    - .planning/phases/07-publish-triage-decisions/TRIAGE.md
    - .planning/phases/07-publish-triage-decisions/07-CONTEXT.md (D-01..D-04)
    - .planning/phases/07-publish-triage-decisions/07-PATTERNS.md (story 2 map; sync dry-run branch)
    - .planning/notes/2026-07-24-portable-notebook-workspace.md (story 2 Acceptance examples — 3 bullets)
    - .planning/PROJECT.md (decision bar)
    - cli/src/commands/notebook/syncSlashCommand.tsx
    - e2e_test/features/cli/cli_sync_dry_run.feature
  </read_first>
  <action>
    Audit Story 2 only with author filter (exclude Terry Yin, Tan Yeong Sheng, terryyin) per D-01..D-04. Surface: /sync --dry-run, previewPull, syncArgument, cli_sync_dry_run.feature. Fill complete dossier: Verdict keep|strengthen|remove; all 3 oracle acceptance bullets in order with match|gap|N/A + proof; Capability entrypoints; Delete/keep file set; participant-touched inventory; WIP/gap proofs if strengthen/remove. Update Summary row 2. Leave Story 3 for the next task (do not fill Story 3 dossier yet). Note likely shared sync candidates for D-03 but defer shared-tag writes that require Story 3 inventory until Task 2. Do not modify product code.
  </action>
  <verify>
    <automated>T=.planning/phases/07-publish-triage-decisions/TRIAGE.md; rg -q "^## Story 2:" "$T"; rg -n "^### Verdict$|Acceptance citations|Capability entrypoints|Delete / keep|Participant-touched" "$T"; test "$(python3 -c "
from pathlib import Path
import re
t=Path('$T').read_text()
m=re.search(r'^## Story 2:.*?(?=^## Story |\Z)', t, re.M|re.S)
sec=m.group(0) if m else ''
print(len(re.findall(r'—\s*(match|gap|N/A)\b', sec, re.I)))
")" -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - Story 2 has exactly one of keep|strengthen|remove
    - Story 2 cites all 3 oracle acceptance bullets in order with match|gap|N/A + proof
    - Entrypoints, delete/keep set, and participant inventory present
    - Summary row 2 matches dossier Verdict
    - No product file changes
  </acceptance_criteria>
  <done>Story 2 actionable dossier published.</done>
</task>

<task type="auto">
  <name>Task 2: Publish Story 3 incremental-pull dossier with D-03 shared sync tagging</name>
  <files>.planning/phases/07-publish-triage-decisions/TRIAGE.md</files>
  <reversibility rating="costly">Phase 10 treats Story 3 dossier as sole action source (D-01).</reversibility>
  <read_first>
    - .planning/phases/07-publish-triage-decisions/TRIAGE.md
    - .planning/phases/07-publish-triage-decisions/07-CONTEXT.md (D-01..D-04, especially D-03)
    - .planning/phases/07-publish-triage-decisions/07-PATTERNS.md (story 3 map; sync mutate branch; D-03)
    - .planning/notes/2026-07-24-portable-notebook-workspace.md (story 3 Acceptance examples — 5 bullets)
    - .planning/PROJECT.md (decision bar)
    - cli/src/commands/notebook/syncSlashCommand.tsx
    - e2e_test/features/cli/cli_sync_pull.feature
  </read_first>
  <action>
    Audit Story 3 with author filter (exclude Terry Yin, Tan Yeong Sheng, terryyin) per D-01..D-04. Surface: /sync non-dry-run, applyPull, readWorkspace, .doughnut-sync baseline, cli_sync_pull.feature. Fill complete dossier: Verdict keep|strengthen|remove; all 5 oracle acceptance bullets in order with match|gap|N/A + proof; Capability entrypoints; Delete/keep file set; participant-touched inventory; WIP/gap proofs if strengthen/remove. Apply D-03: any path used by both Story 2 and Story 3 (and overlaps with Story 1 such as syncSlashCommand.tsx, applyPull, shared sync helpers) must appear in every related story inventory tagged shared — update Story 1 and Story 2 inventory rows if needed to add shared tags without changing those Verdicts unless new evidence requires Jidoka stop. If audit finds no overlap, state no shared overlap with proof under Story 3. Update Summary row 3. Do not modify product code.
  </action>
  <verify>
    <automated>T=.planning/phases/07-publish-triage-decisions/TRIAGE.md; rg -q "^## Story 3:" "$T"; rg -n "Story 2:|Story 3:|### Verdict|shared" "$T"; test "$(python3 -c "
from pathlib import Path
import re
t=Path('$T').read_text()
m=re.search(r'^## Story 3:.*?(?=^## Story |\Z)', t, re.M|re.S)
sec=m.group(0) if m else ''
print(len(re.findall(r'—\s*(match|gap|N/A)\b', sec, re.I)))
")" -eq 5</automated>
  </verify>
  <acceptance_criteria>
    - Story 3 has exactly one of keep|strengthen|remove
    - Story 3 cites all 5 oracle acceptance bullets in order with match|gap|N/A + proof
    - Entrypoints, delete/keep set, and participant inventory present
    - Overlapping sync paths appear under every related story tagged shared, or dossier states no shared overlap with proof (D-03)
    - Story 1 and Story 2 each still have exactly one verdict
    - Summary row 3 matches dossier Verdict
    - No product file changes
  </acceptance_criteria>
  <done>Story 3 dossier published with D-03 shared tagging across related sync stories.</done>
</task>

<task type="auto">
  <name>Task 3: Publish Story 4 workspace lint dossier</name>
  <files>.planning/phases/07-publish-triage-decisions/TRIAGE.md</files>
  <reversibility rating="costly">Phase 11 treats Story 4 dossier as sole action source (D-01).</reversibility>
  <read_first>
    - .planning/phases/07-publish-triage-decisions/TRIAGE.md
    - .planning/phases/07-publish-triage-decisions/07-PATTERNS.md (story 4 map; /lint outside notebook stage)
    - .planning/notes/2026-07-24-portable-notebook-workspace.md (story 4 Acceptance examples — 4 bullets)
    - .planning/PROJECT.md
    - cli/src/commands/lintSlashCommand.ts
    - e2e_test/features/cli/cli_lint_workspace.feature
  </read_first>
  <action>
    Audit Story 4 (/lint workspace compatibility check) with author filter. Entrypoints: lintSlashCommand.ts, lint/lintWorkspace.ts and okf* modules, cli_lint_workspace.feature. Fill complete dossier: Verdict; all 4 oracle bullets cited in order with match|gap|N/A + proof; entrypoints; delete/keep set; participant-touched inventory; WIP/gap proofs if needed. Tag any paths that also appear under stories 1–3 as shared under every related dossier (D-03). Update Summary row 4. Leave stories 5–6 Pending. No product code changes.
  </action>
  <verify>
    <automated>T=.planning/phases/07-publish-triage-decisions/TRIAGE.md; rg -n "Story 4:|lint|### Verdict" "$T"; test "$(python3 -c "
from pathlib import Path
import re
t=Path('$T').read_text()
m=re.search(r'^## Story 4:.*?(?=^## Story |\Z)', t, re.M|re.S)
sec=m.group(0) if m else ''
print(len(re.findall(r'—\s*(match|gap|N/A)\b', sec, re.I)))
")" -eq 4</automated>
  </verify>
  <acceptance_criteria>
    - Story 4 Verdict is exactly one of keep|strengthen|remove
    - All 4 story-4 oracle acceptance bullets cited in order
    - Entrypoints include /lint (or lintSlashCommand) and cli_lint_workspace.feature
    - Delete/keep set and participant inventory present (empty inventory allowed with explicit evidence)
    - Summary row 4 matches dossier Verdict; rows 5–6 still Pending
    - No product file changes
  </acceptance_criteria>
  <done>Story 4 dossier published.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auditor → TRIAGE.md | Docs-only; expands dossiers 2–4 |
| TRIAGE.md → Phases 9–11 | Sole action source for sync/lint resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | Stories 2–4 verdicts | high | mitigate | Author filter + full acceptance citations + PROJECT bar; no tree mutation |
| T-07-05 | Tampering | Shared-path omission (D-03) | high | mitigate | Duplicate shared paths under every related story; verify shared tags across 1–4 |
| T-07-06 | Information Disclosure | TRIAGE.md | high | mitigate | No secrets/tokens in inventories — paths and scenario names only |
| T-07-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- Stories 2–4 complete in TRIAGE.md with citations and inventories
- D-03 shared tags present for overlapping sync/lint paths
- Product tree untouched
- Wrap-up: Jidoka → plan update → commit → push
</verification>

<success_criteria>
- TRIAGE-01/02 satisfied for stories 2–4
- Stop-safe: stories 1–4 actionable; 5–6 still Pending
</success_criteria>

<output>
Create `.planning/phases/07-publish-triage-decisions/07-02-SUMMARY.md` when done
</output>
