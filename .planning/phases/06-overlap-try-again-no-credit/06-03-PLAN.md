---
phase: 06-overlap-try-again-no-credit
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java
  - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java
  - frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts
autonomous: true
requirements: [OVL-01]
must_haves:
  truths:
    - "Declarations that do not resolve, or resolved targets that do not matchAnswer the spelling, grade CORRECT with credit (D-01)."
    - "Unreadable overlap partners are skipped by resolve readability; if no readable dual-match remains, grade CORRECT (T-06-01)."
    - "Self-referential overlap wiki-link tokens do not fire OVERLAP for that token (RESEARCH open Q2)."
    - "Wrong-path accidental-match / skip-when-correct regressions stay green (D-02)."
    - "OVERLAP UI never shows matched-notes-section even if matchedNotes props leak (D-07 / UI-SPEC)."
  artifacts:
    - path: "backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java"
      provides: "Capability-named edge proofs for dead/unreadable/self/AM regression"
      contains: "OverlapTryAgain"
    - path: "frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts"
      provides: "Matched-notes absent on OVERLAP even with leaked matchedNotes"
      contains: "overlap-try-again-alert"
  key_links:
    - from: "overlapWikiLinkTokensFromNoteContent"
      to: "CORRECT path when dual-match fails"
      via: "resolve miss or target !matchAnswer"
      pattern: "D-01"
    - from: "showMatchedNotesSection"
      to: "ACCIDENTAL_MATCH only"
      via: "outcome gate"
      pattern: "D-07"
---

## Phase Goal

**As a** learner in spelling recall, **I want to** be asked to try a more specific answer when my answer is correct but non-distinguishing because of declared overlap, **so that** I get no SRS credit and can retry the same review without mutating note data.

<objective>
Expand OVL-01 grading and UI gates beyond the tracer happy path: dead/unreadable/self tokens stay CORRECT, accidental-match path stays wrong-path only (D-02), and OVERLAP UI refuses matched-notes (D-07).

Purpose: Stop-safe after 06-01/06-02 already delivers try-again; this plan closes D-01 edge semantics so credit is not blocked by non-resolving declarations.
Output: Controller edge suite + Vitest matched-notes absence proof.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-overlap-try-again-no-credit/06-CONTEXT.md
@.planning/phases/06-overlap-try-again-no-credit/06-RESEARCH.md
@.planning/phases/06-overlap-try-again-no-credit/06-UI-SPEC.md
@.planning/phases/06-overlap-try-again-no-credit/06-PATTERNS.md
@.planning/phases/06-overlap-try-again-no-credit/06-01-SUMMARY.md
@.planning/phases/06-overlap-try-again-no-credit/06-02-SUMMARY.md
@.planning/phases/06-overlap-try-again-no-credit/06-VALIDATION.md
</context>

<nyquist_validation>
Per 06-VALIDATION.md:
- Wave 0: additional OverlapTryAgain nested cases + AnsweredSpellingQuestion leaked-matchedNotes case.
- Sampling: backend:test_only after task 1; Vitest AnsweredSpellingQuestion after task 2.
</nyquist_validation>

## Artifacts this plan produces

- Dead / unreadable / self-token CORRECT proofs
- AM regression still green under D-02 order
- OVERLAP UI matched-notes absence under leaked props

<tasks>

<task type="auto" tdd="true">
  <name>Grade CORRECT when dual-match cannot fire</name>
  <reversibility rating="reversible">Edge predicates on the existing dual-match helper; easy to widen later.</reversibility>
  <files>backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java, backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java</files>
  <read_first>
    - .planning/phases/06-overlap-try-again-no-credit/06-CONTEXT.md (D-01 dead targets; D-02 order)
    - .planning/phases/06-overlap-try-again-no-credit/06-RESEARCH.md (Pitfall 2 brackets, Open Q2 self-ref, Security readability)
    - .planning/phases/06-overlap-try-again-no-credit/06-PATTERNS.md (controller fixture analogs)
    - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java (Phase 5 overlap fixture; AccidentalMatch nested)
  </read_first>
  <behavior>
    - Wiki-link declaration to a non-existent title → CORRECT with credit when reviewed matchAnswer true (D-01).
    - Partner note in an unreadable notebook for the answering user → that token skipped; if no other dual-match, CORRECT (T-06-01).
    - Self-referential [[Reviewed Title]] token excluded; CORRECT when no other dual-match (RESEARCH Q2).
    - Existing skip accidental-match when matchAnswer true remains green; wrong-path AM still fires when matchAnswer false (D-02).
  </behavior>
  <action>
    Extend OverlapTryAgain (or sibling) controller tests for dead target, unreadable partner, and self-token. Confirm dual-match helper already strips brackets (Pitfall 2), skips self ids, and relies on resolveWikiLinkToken readability. Fix only if any case incorrectly returns OVERLAP. Re-run existing AccidentalMatch skip-when-correct and AM positive cases.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <done>
    Non-firing dual-match cases credit as CORRECT; D-02 AM regressions green (D-01, D-02).
  </done>
</task>

<task type="auto" tdd="true">
  <name>OVERLAP UI refuses matched-notes even under leaked props</name>
  <files>frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts, frontend/src/components/recall/AnsweredSpellingQuestion.vue</files>
  <read_first>
    - .planning/phases/06-overlap-try-again-no-credit/06-UI-SPEC.md (D-07 matched notes table; must_haves)
    - .planning/phases/06-overlap-try-again-no-credit/06-PATTERNS.md (AnsweredSpellingQuestion ACCIDENTAL_MATCH gate)
    - frontend/src/components/recall/AnsweredSpellingQuestion.vue
    - frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts
  </read_first>
  <behavior>
    - Fixture outcome OVERLAP with overlap true and a non-empty matchedNotes array still: shows overlap-try-again-alert; matched-notes-section absent; no offer-link CTAs (D-07 / UI-SPEC).
    - ACCIDENTAL_MATCH fixture still shows matched-notes-section (regression).
  </behavior>
  <action>
    Add Vitest case that intentionally supplies matchedNotes on an OVERLAP fixture and asserts the section stays gated to ACCIDENTAL_MATCH only. Adjust showMatchedNotesSection (or equivalent) if needed so OVERLAP never opens that section.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestion.spec.ts</automated>
  </verify>
  <done>
    OVERLAP result never mounts matched-notes / offer-link UI (D-07).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resolve → AnsweredQuestion | Unreadable partners must not influence grade or UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | resolveWikiLinkToken / UI | medium | mitigate | Skip unreadable partners in dual-match; UI gate keeps matched-notes off OVERLAP even if props leak (D-07) |
| T-06-03 | Tampering | answerSpelling order | medium | mitigate | Keep overlap on correct path only; prove AM regressions (D-02) |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages |
</threat_model>

<verification>
- Dead / unreadable / self → CORRECT
- AM skip-when-correct + AM positive green
- OVERLAP Vitest matched-notes absence green
</verification>

<success_criteria>
Overlap fires only for true dual-match with readable other notes; accidental-match and matched-notes UI remain unchanged for non-OVERLAP outcomes.
</success_criteria>

<output>
Create `.planning/phases/06-overlap-try-again-no-credit/06-03-SUMMARY.md` when done
</output>
