---
phase: 06-overlap-try-again-no-credit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java
  - backend/src/main/java/com/odde/doughnut/controllers/dto/AnsweredQuestion.java
  - backend/src/main/java/com/odde/doughnut/controllers/RecallPromptController.java
  - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java
  - frontend/src/components/recall/AnsweredSpellingQuestion.vue
  - frontend/src/pages/RecallPage.vue
  - frontend/src/components/recall/Quiz.vue
  - frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts
  - frontend/tests/pages/RecallPage.spec.ts
autonomous: true
requirements: [OVL-01]
must_haves:
  truths:
    - "Dual-match (reviewed matchAnswer + resolved overlap target also matchAnswer) grades OVERLAP with correct=false, outcome=OVERLAP, AnsweredQuestion.overlap=true, matchedNotes empty (D-01, D-03, D-07)."
    - "OVERLAP path leaves recallCount, forgetting curve, and nextRecallAt unchanged — no markAsRecalled / markAsAccidentalMatch (D-03)."
    - "User sees daisy-alert-warning copy 'Correct, but we're looking for another answer — try again.' with data-testid overlap-try-again-alert (D-06 / UI-SPEC)."
    - "OVERLAP does not call moveToNextMemoryTracker or getThresholdExceeded; Try again (overlap-try-again) remounts spelling for the same tracker via remount nonce (D-05 / UI-SPEC)."
    - "Distinguishing plain alias on a note that also declares [[Partner]] still grades CORRECT with credit (D-01)."
  artifacts:
    - path: "backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java"
      provides: "Correct-path dual-match OVERLAP branch before markAsRecalled"
      contains: "OVERLAP"
    - path: "frontend/src/components/recall/AnsweredSpellingQuestion.vue"
      provides: "Warning try-again alert + Try again emit retry"
      contains: "overlap-try-again-alert"
    - path: "frontend/src/pages/RecallPage.vue"
      provides: "OVERLAP stay-and-retry + spellingRetryNonce"
      contains: "OVERLAP"
    - path: "frontend/src/components/recall/Quiz.vue"
      provides: "Spelling key includes remount nonce"
      contains: "spellingRetryNonce"
  key_links:
    - from: "FrontmatterAliases.overlapWikiLinkTokensFromNoteContent"
      to: "WikiLinkResolver.resolveWikiLinkToken(inner, reviewed, user)"
      via: "INNER_LINK_PATTERN strip then dual matchAnswer"
      pattern: "D-01"
    - from: "RecallPage.onAnswered"
      to: "AnsweredSpellingQuestion @retry"
      via: "no moveToNext; remount nonce"
      pattern: "D-05"
---

## Phase Goal

**As a** learner in spelling recall, **I want to** be asked to try a more specific answer when my answer is correct but non-distinguishing because of declared overlap, **so that** I get no SRS credit and can retry the same review without mutating note data.

<objective>
Ship the production-quality tracer for OVL-01: dual-match OVERLAP grading in `MemoryTrackerService.answerSpelling` (D-01–D-03), `AnsweredQuestion.overlap=true` with empty matchedNotes (D-07), warning try-again UI (D-06), and stay-and-retry with remount nonce (D-05). Prove distinguishing plain alias still earns CORRECT.

Purpose: Prove the full stack path (grade → DTO → warning UI → same-tracker retry) before edge-case / Flyway / E2E expansions.
Output: Backend OVERLAP branch + frontend try-again stay/retry + controller/Vitest happy-path proofs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-overlap-try-again-no-credit/06-CONTEXT.md
@.planning/phases/06-overlap-try-again-no-credit/06-RESEARCH.md
@.planning/phases/06-overlap-try-again-no-credit/06-UI-SPEC.md
@.planning/phases/06-overlap-try-again-no-credit/06-PATTERNS.md
@.planning/phases/06-overlap-try-again-no-credit/06-VALIDATION.md
</context>

<nyquist_validation>
Per 06-VALIDATION.md and RESEARCH § Validation Architecture:
- Wave 0 in this plan: OverlapTryAgain controller nested tests; AnsweredSpellingQuestion + RecallPage Vitest cases for alert / stay / remount.
- Sampling: after tracer run targeted backend:test_only + both Vitest files; after task 2 re-run controller nested.
- Durable D-04 Flyway, dead-target edges, and E2E remain later plans.
</nyquist_validation>

## Artifacts this plan produces

- Correct-path OVERLAP third outcome in `answerSpelling` (mirror AM early-return pattern; zero mark path)
- `AnsweredQuestion.overlap=true` when outcome is OVERLAP
- Warning alert + Try again CTA per UI-SPEC
- Recall queue stay + Quiz spelling remount nonce

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end overlap try-again — grade → warning UI → stay/retry</name>
  <precondition>Phase 5 FrontmatterAliases.overlapWikiLinkTokensFromNoteContent and Phase 1 AnswerOutcome.OVERLAP / AnsweredQuestion.overlap exist; answerSpelling has ACCIDENTAL_MATCH wrong-path only.</precondition>
  <reversibility rating="reversible">Grading predicate and UI queue branch are local swaps; no schema or public contract shape change in this plan.</reversibility>
  <files>backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java, backend/src/main/java/com/odde/doughnut/controllers/dto/AnsweredQuestion.java, backend/src/main/java/com/odde/doughnut/controllers/RecallPromptController.java, backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java, frontend/src/components/recall/AnsweredSpellingQuestion.vue, frontend/src/pages/RecallPage.vue, frontend/src/components/recall/Quiz.vue, frontend/tests/components/recall/AnsweredSpellingQuestion.spec.ts, frontend/tests/pages/RecallPage.spec.ts</files>
  <read_first>
    - .planning/phases/06-overlap-try-again-no-credit/06-CONTEXT.md (D-01–D-03, D-05–D-07)
    - .planning/phases/06-overlap-try-again-no-credit/06-RESEARCH.md (Patterns 1–3, Pitfalls 2–5, Discretion Recommendations)
    - .planning/phases/06-overlap-try-again-no-credit/06-UI-SPEC.md (alert copy, testids, retry remount, eye-path)
    - .planning/phases/06-overlap-try-again-no-credit/06-PATTERNS.md (MemoryTrackerService third path, RecallPage stay, Quiz key, controller test analogs)
    - backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java (answerSpelling SpellingAnswerResult)
    - backend/src/main/java/com/odde/doughnut/algorithms/FrontmatterAliases.java
    - backend/src/main/java/com/odde/doughnut/algorithms/WikiLinkMarkdown.java (INNER_LINK_PATTERN)
    - backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java (resolveWikiLinkToken)
    - frontend/src/components/recall/AnsweredSpellingQuestion.vue
    - frontend/src/pages/RecallPage.vue (onAnswered)
    - frontend/src/components/recall/Quiz.vue
  </read_first>
  <behavior>
    - Controller: reviewed note with plain alias + live partner wiki-link declaration; spelling that matches both titles → answer.correct=false, outcome=OVERLAP, overlap=true, matchedNotes null/empty; memoryTracker recallCount / forgettingCurveIndex / nextRecallAt unchanged vs pre-answer snapshot (D-01, D-03, D-07).
    - Evaluation order: matchAnswer true → dual-match overlap → OVERLAP or CORRECT; matchAnswer false → existing accidental-match / wrong (D-02). Strip [[…]] via INNER_LINK_PATTERN group(1) before resolveWikiLinkToken; exclude self id; any one dual-match fires (RESEARCH Pattern 2).
    - Vitest AnsweredSpellingQuestion: outcome OVERLAP → daisy-alert-warning, text Exact ROADMAP sentence, data-testid overlap-try-again-alert; matched-notes-section absent; Try again button data-testid overlap-try-again emits retry (D-06, D-07 / UI-SPEC).
    - Vitest RecallPage: on OVERLAP answered, currentIndex unchanged; getThresholdExceeded not invoked; clicking Try again clears pause cursor, bumps remount nonce, spelling key includes nonce so SpellingQuestionDisplay remounts (D-05).
  </behavior>
  <action>
    Write failing controller + Vitest proofs first (Wave 0), then implement the vertical slice.

    Backend (per D-01, D-02, D-03): In `MemoryTrackerService.answerSpelling(RecallPrompt, …)`, after attaching/saving the Answer and when `note.matchAnswer(spellingAnswer)` is true, run dual-match using `FrontmatterAliases.overlapWikiLinkTokensFromNoteContent`, strip to inner token, `wikiLinkResolver.resolveWikiLinkToken(inner, note, user)`, skip self, require target `matchAnswer`. On hit: set answer correct=false and outcome OVERLAP; return `SpellingAnswerResult(prompt, List.of())` without calling markAsRecalled or markAsAccidentalMatch. Otherwise fall through to existing CORRECT `markAsRecalled(true)`. Leave wrong-path AM unchanged.

    DTO (D-07): When assembling AnsweredQuestion (controller after `from(…, matches)` or extend from overload), if answer outcome is OVERLAP set overlap=true and leave matchedNotes unset/empty.

    Frontend (D-05, D-06, D-07 / UI-SPEC): Branch OVERLAP before plain `!correct` error styling. Alert: `daisy-alert daisy-alert-warning`, copy `Correct, but we're looking for another answer — try again.`, testid `overlap-try-again-alert`. Add secondary `Try again` button (`daisy-btn daisy-btn-secondary daisy-btn-sm`, testid `overlap-try-again`, mt-6 after reviewed NoteShow) emitting `retry`. Keep matched-notes gated to ACCIDENTAL_MATCH only. In RecallPage.onAnswered, when outcome is OVERLAP or overlap===true: push result, viewLastAnsweredQuestion, return without moveToNextMemoryTracker and without getThresholdExceeded. On retry: clear previousAnsweredQuestionCursor, increment spellingRetryNonce, pass nonce into Quiz so spelling `:key` is `` `spelling-${id}-${nonce}` ``.

    Capability-named tests only (e.g. OverlapTryAgain nested class). No OpenAPI regen unless a gap is discovered. Durable threshold DB exclusion is plan 06-02 (frontend skip is enough for this tracer).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only && CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestion.spec.ts tests/pages/RecallPage.spec.ts</automated>
  </verify>
  <done>
    Dual-match shared-title answer returns OVERLAP with zero SRS mutation and overlap=true; warning try-again UI shows; queue stays; Try again remounts a fresh spelling prompt for the same tracker (D-01–D-03, D-05–D-07).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Prove distinguishing plain alias still earns CORRECT credit</name>
  <files>backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java, backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java</files>
  <read_first>
    - .planning/phases/06-overlap-try-again-no-credit/06-CONTEXT.md (D-01 rationale — color beside [[Other Note]])
    - .planning/phases/06-overlap-try-again-no-credit/06-PATTERNS.md (Phase 5 overlap frontmatter fixture)
    - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java (existing overlap aliases fixture)
  </read_first>
  <behavior>
    - Same reviewed note with aliases color + "[[Other Note]]" and live Other Note: spelling "color" → CORRECT (correct=true), outcome not OVERLAP, overlap null/false, markAsRecalled success path (recallCount bumps) (D-01).
    - Dead/missing partner is covered in plan 06-03; this task only locks the distinguishing alias happy path that proves dual-match is required.
  </behavior>
  <action>
    Add/extend controller test with live partner note and mixed aliases. Assert spelling the plain distinguishing alias grades CORRECT with credit (per D-01). Adjust dual-match implementation only if the tracer incorrectly treated any declaration as OVERLAP.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <done>
    Plain distinguishing alias on an overlap-declaring note earns CORRECT with SRS credit (D-01).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → `POST …/answer-spelling` | Authenticated spelling string; grading must not leak unreadable partner notes |
| Resolve focus notebook → other notebooks | Wiki-link resolve crosses notebooks only when viewer may read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Dual-match resolve / AnsweredQuestion | medium | mitigate | Reuse `resolveWikiLinkToken` readability filter; leave matchedNotes empty for OVERLAP (D-07); never return partner topologies on this path |
| T-06-02 | Tampering | Wrong-count / threshold | medium | accept | Frontend skips getThresholdExceeded on OVERLAP in this plan; durable DB exclusion deferred to 06-02 (D-04) |
| T-06-03 | Elevation of Privilege | Grade path | low | mitigate | Keep D-02 order: overlap only on matchAnswer true; AM remains wrong-path only |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this phase |
</threat_model>

<verification>
- Controller OverlapTryAgain dual-match + distinguishing alias green
- Vitest warning alert + stay/retry remount green
- Existing AM skip-when-correct regression remains green
</verification>

<success_criteria>
User can submit a non-distinguishing dual-match answer, see the warning try-again message with no credit, stay on the same tracker, and retry; distinguishing alias still credits.
</success_criteria>

<output>
Create `.planning/phases/06-overlap-try-again-no-credit/06-01-SUMMARY.md` when done
</output>
