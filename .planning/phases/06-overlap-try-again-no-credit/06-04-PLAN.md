---
phase: 06-overlap-try-again-no-credit
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - e2e_test/features/recall/overlap_try_again.feature
  - e2e_test/step_definitions/recall.ts
  - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
autonomous: true
requirements: [OVL-01]
must_haves:
  truths:
    - "E2E: shared dual-match spelling shows overlap-try-again-alert and withholds credit; queue stays on the same review (D-01, D-03, D-05, D-06)."
    - "E2E: Try again (overlap-try-again) yields a fresh spelling prompt; distinguishing plain alias then credits as correct (D-05, D-01)."
    - "E2E: matched-notes-section and accidental-match-alert are absent on the OVERLAP path (D-07)."
  artifacts:
    - path: "e2e_test/features/recall/overlap_try_again.feature"
      provides: "Capability-named observable OVL-01 scenarios"
      contains: "overlap try again"
    - path: "e2e_test/start/pageObjects/AnsweredQuestionPage.ts"
      provides: "expectOverlapTryAgain helpers"
      contains: "overlap-try-again-alert"
  key_links:
    - from: "overlap_try_again.feature"
      to: "AnsweredQuestionPage overlap helpers"
      via: "recall.ts steps"
      pattern: "OVL-01"
---

## Phase Goal

**As a** learner in spelling recall, **I want to** be asked to try a more specific answer when my answer is correct but non-distinguishing because of declared overlap, **so that** I get no SRS credit and can retry the same review without mutating note data.

<objective>
Prove OVL-01 end-to-end through the live recall UI: dual-match try-again, stay-and-retry remount, distinguishing alias credit, no matched-notes (UI hint: yes).

Purpose: ROADMAP success criteria 1–3 are user-observable; controller/Vitest are necessary but not sufficient for the UI hint.
Output: Capability-named `overlap_try_again.feature` green without `@wip`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-overlap-try-again-no-credit/06-CONTEXT.md
@.planning/phases/06-overlap-try-again-no-credit/06-RESEARCH.md
@.planning/phases/06-overlap-try-again-no-credit/06-UI-SPEC.md
@.planning/phases/06-overlap-try-again-no-credit/06-PATTERNS.md
@.planning/phases/06-overlap-try-again-no-credit/06-01-SUMMARY.md
@.planning/phases/06-overlap-try-again-no-credit/06-02-SUMMARY.md
@.planning/phases/06-overlap-try-again-no-credit/06-03-SUMMARY.md
@.planning/phases/06-overlap-try-again-no-credit/06-VALIDATION.md
</context>

<precondition>
Assume `pnpm sut` may already be running (frontend :5173 + MySQL). Do not restart unless healthcheck fails. Run tooling with `CURSOR_DEV=true nix develop -c …`.
</precondition>

<nyquist_validation>
Per 06-VALIDATION.md:
- Wave 0: create feature `@wip`, steps, page-object helpers mirroring accidental_match_reveal.
- Phase gate: targeted cypress `--spec` green; remove `@wip`; no full E2E suite.
</nyquist_validation>

## Artifacts this plan produces

- `e2e_test/features/recall/overlap_try_again.feature`
- Thin recall.ts steps + AnsweredQuestionPage overlap helpers

<tasks>

<task type="auto">
  <name>Add capability-named overlap try-again E2E scaffold</name>
  <files>e2e_test/features/recall/overlap_try_again.feature, e2e_test/step_definitions/recall.ts, e2e_test/start/pageObjects/AnsweredQuestionPage.ts</files>
  <read_first>
    - .planning/phases/06-overlap-try-again-no-credit/06-UI-SPEC.md (copy, testids, interaction contract)
    - .planning/phases/06-overlap-try-again-no-credit/06-PATTERNS.md (E2E analog accidental_match_reveal)
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
    - e2e_test/step_definitions/recall.ts
  </read_first>
  <action>
    Create capability-named `overlap_try_again.feature` (no phase numbers in the name). Tag scenarios `@wip` until green. Background: logged-in user; notebook with reviewed note that has plain distinguishing alias + `[[Partner]]` wiki-link overlap declaration; live Partner note with shared title semantics for dual-match; remember spelling enabled on the reviewed note.

    Scenarios (minimum):
    1. Shared non-distinguishing answer → overlap-try-again-alert with UI-SPEC copy; matched-notes-section absent; user remains able to retry same review.
    2. Click overlap-try-again → fresh spelling prompt → answer distinguishing plain alias → correct credit / advance.

    Add page-object helpers (e.g. expectOverlapTryAgainAlert) using testids `overlap-try-again-alert` and `overlap-try-again`. Thin step wrappers in recall.ts. Pair busy UI with waitUntilAppIsNotBusy. Mirror accidental_match_reveal tags (`@mockBrowserTime`, `@disableOpenAiService`) as needed.
  </action>
  <verify>
    <automated>test -f e2e_test/features/recall/overlap_try_again.feature && rg -n "overlap-try-again-alert|overlap-try-again" e2e_test/start/pageObjects/AnsweredQuestionPage.ts e2e_test/step_definitions/recall.ts e2e_test/features/recall/overlap_try_again.feature</automated>
  </verify>
  <done>
    Feature file + steps + page object exist; scenarios may still be `@wip` if red for the right product reason.
  </done>
</task>

<task type="auto">
  <name>Make overlap try-again E2E green and drop @wip</name>
  <files>e2e_test/features/recall/overlap_try_again.feature, e2e_test/step_definitions/recall.ts, e2e_test/start/pageObjects/AnsweredQuestionPage.ts</files>
  <read_first>
    - .planning/phases/06-overlap-try-again-no-credit/06-01-SUMMARY.md
    - .planning/phases/06-overlap-try-again-no-credit/06-02-SUMMARY.md
    - .planning/phases/06-overlap-try-again-no-credit/06-03-SUMMARY.md
    - e2e_test/features/recall/overlap_try_again.feature
  </read_first>
  <action>
    Run the targeted Cypress spec. Fix product or test gaps until both scenarios pass for the right reason (dual-match try-again, remount retry, distinguishing credit, no matched-notes). Remove `@wip` from passing scenarios. Do not run the full E2E suite. If remount fails with already-answered, verify spellingRetryNonce wiring from 06-01 (RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/overlap_try_again.feature</automated>
  </verify>
  <done>
    Targeted overlap_try_again E2E green without `@wip`; ROADMAP success criteria 1–3 observable in the browser (OVL-01).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser E2E → app | Exercises authenticated recall only; no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | E2E assertions | low | mitigate | Assert matched-notes-section absent on OVERLAP path (D-07) |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages |
</threat_model>

<verification>
- Targeted `overlap_try_again.feature` green
- `@wip` removed from passing scenarios
</verification>

<success_criteria>
Live recall shows try-again with no credit, stay-and-retry works, distinguishing alias credits, matched-notes stay off OVERLAP.
</success_criteria>

<output>
Create `.planning/phases/06-overlap-try-again-no-credit/06-04-SUMMARY.md` when done
</output>
