---
phase: 02-assimilate-as-commissioned
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/odde/doughnut/controllers/dto/AssimilationRequestDTO.java
  - backend/src/main/java/com/odde/doughnut/services/MemoryTrackerAssimilation.java
  - backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java
  - frontend/src/composables/useAssimilateUnit.ts
  - frontend/src/components/recall/AssimilationButtons.vue
  - frontend/src/components/recall/AssimilationSettings.vue
  - frontend/src/components/recall/AssimilationPanel.vue
  - frontend/src/components/notes/NoteInfoMemoryTracker.vue
  - frontend/tests/components/recall/AssimilationPanel.spec.ts
  - frontend/tests/components/notes/NoteInfoMemoryTracker.spec.ts
  - packages/doughnut-test-fixtures/src/MemoryTrackerBuilder.ts
  - packages/generated/doughnut-backend-api
  - e2e_test/features/learning_session/commissioned_learning_session.feature
  - e2e_test/start/pageObjects/assimilationPage
  - e2e_test/step_definitions
autonomous: true
requirements:
  - TRK-01
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "User can open a note-level caret next to Assimilate and choose Assimilate as commissioned (not offered on property rows)."
    - "After commissioned assimilate, assimilation settings show a memory tracker whose Type is Commissioned for that note."
    - "After commissioned assimilate the UI stays on the current note (assimilation queue does not advance)."
    - "Primary Assimilate remains usable when only a COMMISSIONED tracker exists."
  artifacts:
    - path: "backend/src/main/java/com/odde/doughnut/controllers/dto/AssimilationRequestDTO.java"
      provides: "Optional assimilateAsCommissioned Boolean on assimilate request"
      contains: "assimilateAsCommissioned"
    - path: "backend/src/main/java/com/odde/doughnut/services/MemoryTrackerAssimilation.java"
      provides: "Early branch creating only note-level COMMISSIONED"
      contains: "COMMISSIONED"
    - path: "frontend/src/components/recall/AssimilationButtons.vue"
      provides: "Note-level daisy-join caret + Assimilate as commissioned menu item"
      contains: "assimilate-as-commissioned"
    - path: "frontend/src/components/notes/NoteInfoMemoryTracker.vue"
      provides: "Type cell label Commissioned for COMMISSIONED trackers"
      contains: "Commissioned"
    - path: "e2e_test/features/learning_session/commissioned_learning_session.feature"
      provides: "Graduated Phase 2 E2E scenario"
      contains: "Assimilating a note with a tutor creates a commissioned memory tracker"
  key_links:
    - from: "AssimilationButtons assimilate-as-commissioned"
      to: "POST /api/assimilation body.assimilateAsCommissioned"
      via: "AssimilationPanel → useAssimilateUnit → AssimilationController.assimilate"
      pattern: "assimilateAsCommissioned"
    - from: "MemoryTrackerAssimilation commissioned branch"
      to: "memory_tracker type=COMMISSIONED"
      via: "createNoteLevelTracker(..., MemoryTrackerType.COMMISSIONED)"
      pattern: "MemoryTrackerType.COMMISSIONED"
    - from: "NoteInfoMemoryTracker Type cell"
      to: "MemoryTracker.type COMMISSIONED"
      via: "trackerTypeLabel returns Commissioned"
      pattern: "Commissioned"
---

<objective>
Deliver the user-visible path to create a note-level commissioned memory tracker from the Assimilate caret (TRK-01; D-01, D-04, D-06, D-07, D-08) and harden primary Assimilate / menu visibility (D-03, D-05).

Purpose: First Behavior phase of the MVP — opt-in commissioned create without advancing the assimilation queue.
Output: Extended assimilate API flag, DaisyUI split control, Commissioned type label, graduated E2E scenario green without `@wip`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-assimilate-as-commissioned/02-CONTEXT.md
@.planning/phases/02-assimilate-as-commissioned/02-RESEARCH.md
@.planning/phases/02-assimilate-as-commissioned/02-PATTERNS.md
@.planning/phases/02-assimilate-as-commissioned/02-VALIDATION.md
@.planning/phases/01-commissioned-tracker-model/commissioned_learning_session.feature
@docs/adrs/0001-ubiquitous-language.md

@backend/src/main/java/com/odde/doughnut/controllers/dto/AssimilationRequestDTO.java
@backend/src/main/java/com/odde/doughnut/services/MemoryTrackerAssimilation.java
@backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java
@frontend/src/composables/useAssimilateUnit.ts
@frontend/src/components/recall/AssimilationButtons.vue
@frontend/src/components/recall/AssimilationSettings.vue
@frontend/src/components/recall/AssimilationPanel.vue
@frontend/src/components/notes/NoteInfoMemoryTracker.vue
@.agents/skills/generate-api-client/SKILL.md
@.agents/skills/adr-awareness/SKILL.md
</context>

## Source coverage (this plan)

| Source | IDs | Covered here |
|--------|-----|--------------|
| GOAL | User creates commissioned tracker from assimilate caret | Tracer + E2E |
| REQ | TRK-01 | All tasks |
| CONTEXT | D-01, D-03, D-04, D-05, D-06, D-07, D-08 | Tracer + expansion |
| RESEARCH | assimilateAsCommissioned flag; daisy-join; stay on note; Commissioned label; refuse property+commissioned | Tracer + expansion |

**Deferred / other plans:** D-02 coexistence entry when ordinary already exists → `02-02`; TRK-02 full SC → `02-02`.

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end assimilate as commissioned happy path</name>
  <reversibility rating="reversible">Create-path Boolean flag on existing assimilate DTO/endpoint; undo by removing the field and branch — no Flyway (per D-01).</reversibility>
  <files>backend/src/main/java/com/odde/doughnut/controllers/dto/AssimilationRequestDTO.java, backend/src/main/java/com/odde/doughnut/services/MemoryTrackerAssimilation.java, backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java, packages/generated/doughnut-backend-api, frontend/src/composables/useAssimilateUnit.ts, frontend/src/components/recall/AssimilationButtons.vue, frontend/src/components/recall/AssimilationSettings.vue, frontend/src/components/recall/AssimilationPanel.vue, frontend/src/components/notes/NoteInfoMemoryTracker.vue, frontend/tests/components/recall/AssimilationPanel.spec.ts, frontend/tests/components/notes/NoteInfoMemoryTracker.spec.ts, packages/doughnut-test-fixtures/src/MemoryTrackerBuilder.ts, e2e_test/features/learning_session/commissioned_learning_session.feature, e2e_test/start/pageObjects/assimilationPage, e2e_test/step_definitions</files>
  <read_first>
    - `.planning/phases/02-assimilate-as-commissioned/02-RESEARCH.md` (Primary recommendation; Patterns 1–3; Pitfalls 1–8; discretionary data-test ids)
    - `.planning/phases/02-assimilate-as-commissioned/02-PATTERNS.md` (DTO, MemoryTrackerAssimilation early branch, daisy-join + AutoCollapseDropdown, NoteInfoMemoryTracker label, E2E graduate shape)
    - `.planning/phases/01-commissioned-tracker-model/commissioned_learning_session.feature` (graduate only Scenario: Assimilating a note with a tutor creates a commissioned memory tracker — D-08)
    - `docs/adrs/0001-ubiquitous-language.md` §3 (menu copy Assimilate as commissioned; Type label Commissioned)
    - `.agents/skills/generate-api-client/SKILL.md` (regenerate after DTO change; never hand-edit generated client)
  </read_first>
  <behavior>
    - AssimilationController.assimilate with assimilateAsCommissioned true on a note with no COMMISSIONED tracker returns a single note-level tracker of type COMMISSIONED and does not create UNDERSTANDING/SPELLING in that call (D-01).
    - Same request when propertyKey is non-empty returns an empty list (no create) — backend refuse of property+commissioned (RESEARCH Pitfall 6 / T-02-04).
    - NoteInfoMemoryTracker with type COMMISSIONED shows Type text Commissioned (D-07).
    - AssimilationPanel: choosing Assimilate as commissioned posts body with assimilateAsCommissioned true, does not call goToNextAssimilation, does not open spelling verification (D-06; Pitfalls 3 and 5).
    - E2E scenario (tagged @wip until Task 3): assimilating note Hola → assimilate as commissioned → open assimilation settings → see commissioned memory tracker for Hola (D-08).
  </behavior>
  <action>
    Implement one production-quality vertical slice for TRK-01 / D-01 / D-04 / D-06 / D-07 / D-08:

    Backend: Add optional `Boolean assimilateAsCommissioned` on `AssimilationRequestDTO` (same style as `skipMemoryTracking` — RESEARCH A1). In `MemoryTrackerAssimilation`, early-branch when `Boolean.TRUE.equals(request.assimilateAsCommissioned)`: if propertyKey non-empty return empty list; if note-level COMMISSIONED already exists return empty list; else `createNoteLevelTracker(..., MemoryTrackerType.COMMISSIONED)` only — do not also create UNDERSTANDING/SPELLING (D-01). Leave ordinary/property branches unchanged. Extend `AssimilationControllerTests` with controller-boundary tests (stable boundary + makeMe); one behavior per test; mitigate T-02-01 via existing logged-in assimilate path (no new endpoint).

    OpenAPI: Run generate-api-client skill (`pnpm generateTypeScript` via Nix) so generated types include the new field; do not hand-edit `packages/generated/doughnut-backend-api/**`.

    Frontend: Note-level only — pass a show-commissioned-option into `AssimilationButtons` from note-level `AssimilationSettings` instance (never from property rows — D-04 / TRK-01). Wrap primary Assimilate + caret in DaisyUI `daisy-join`; caret uses existing `AutoCollapseDropdown` / `DropdownMenu*` / `dropdownMenuButtonClass`. Menu copy exactly Assimilate as commissioned. Recommended data-test ids: `assimilate-as-commissioned-caret` and `assimilate-as-commissioned` (prefer `data-test` to match assimilate controls). Wire emit → panel handler that never enters spelling verification → `useAssimilateUnit` with assimilateAsCommissioned true: on success skip `goToNextAssimilation`, do not increment ordinary assimilate counters, still reload note info / refresh due as needed (D-06; Pitfall 4). Extend `trackerTypeLabel` so COMMISSIONED → Commissioned before spelling/normal (D-07).

    Fixtures/tests: Add TS `MemoryTrackerBuilder.commissioned()` (mirror Java). Vitest: Commissioned label; panel posts assimilateAsCommissioned and does not navigate.

    E2E Wave 0: Create `e2e_test/features/learning_session/` and graduate only the Phase 2 scenario from the draft feature (D-08), tag `@wip` with existing `@disableOpenAiService @mockBrowserTime`. Add page-object helpers (note-level caret filter via `isNoteLevelAssimilationControl`) and steps `I assimilate it as commissioned` / `I should see a commissioned memory tracker for {string}` asserting Type Commissioned. Do not graduate later draft scenarios.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only -- --tests com.odde.doughnut.controllers.AssimilationControllerTests &amp;&amp; CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/AssimilationPanel.spec.ts tests/components/notes/NoteInfoMemoryTracker.spec.ts</automated>
  </verify>
  <done>Controller creates only COMMISSIONED when flag true; UI caret+menu on note-level only; Type shows Commissioned; stay-on-note path wired; E2E feature exists under learning_session/ tagged @wip; generated client includes assimilateAsCommissioned.</done>
</task>

<task type="auto" tdd="true">
  <name>Keep primary Assimilate usable and hide commissioned menu when already commissioned</name>
  <files>frontend/src/components/recall/AssimilationPanel.vue, frontend/src/components/recall/AssimilationSettings.vue, frontend/src/components/recall/AssimilationButtons.vue, frontend/tests/components/recall/AssimilationPanel.spec.ts</files>
  <read_first>
    - `.planning/phases/02-assimilate-as-commissioned/02-RESEARCH.md` (Pitfall 1 D-03; Open Question 2 — hide caret when COMMISSIONED exists)
    - `frontend/src/components/recall/AssimilationPanel.vue` (`hasNoteLevelMemoryTrackers` / `assimilateDisabled`)
    - `frontend/src/components/recall/AssimilationSettings.vue` (note-level vs property AssimilationButtons)
  </read_first>
  <behavior>
    - With only a note-level COMMISSIONED tracker in note recall info, primary Assimilate is enabled (D-03) — delta from existing “disables when ordinary note-level trackers exist” test.
    - When a note-level COMMISSIONED tracker exists, the commissioned caret/menu is not shown (D-05; RESEARCH: hide caret).
    - Property assimilation rows still have no commissioned caret (D-04 regression).
  </behavior>
  <action>
    Per D-03: change ordinary note-level existence used for `assimilateDisabled` so COMMISSIONED trackers are ignored the same way Phase 1 backend create ignores them (exclude `type === 'COMMISSIONED'` while keeping the spelling-only exception). Per D-05 / RESEARCH Open Question 2: hide the caret entirely when any note-level COMMISSIONED already exists (do not offer a second create). Property rows remain caret-free (D-04). Add Vitest coverage for commissioned-only → Assimilate enabled and caret absent when COMMISSIONED present. Capability-named tests only (no phase numbers in product/test names).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/AssimilationPanel.spec.ts</automated>
  </verify>
  <done>Commissioned-only notes keep Assimilate enabled; caret hidden once COMMISSIONED exists; property rows unchanged.</done>
</task>

<task type="auto">
  <name>Graduate Phase 2 E2E scenario to green</name>
  <files>e2e_test/features/learning_session/commissioned_learning_session.feature, e2e_test/start/pageObjects/assimilationPage, e2e_test/step_definitions</files>
  <read_first>
    - `.planning/phases/02-assimilate-as-commissioned/02-VALIDATION.md` (sampling; WIP cap)
    - `e2e_test/start/pageObjects/assimilationPage/shared.ts` / `assimilationFlow.ts`
    - `scripts/check_wip_tags.sh` (MAX_WIP=5)
  </read_first>
  <action>
    Run the graduated learning_session feature locally until green (assume `pnpm sut` running). Use targeted Cypress only — not the full E2E suite. After green, remove the `@wip` tag from the Phase 2 scenario only (D-08). Confirm `scripts/check_wip_tags.sh` still under cap. Do not add Phase 3+ scenarios from the draft feature.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature &amp;&amp; CURSOR_DEV=true nix develop -c bash scripts/check_wip_tags.sh</automated>
  </verify>
  <done>Phase 2 E2E scenario passes without @wip; WIP tag count within MAX_WIP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `POST /api/assimilation` | Untrusted body (`noteId`, flags, propertyKey); session auth required |
| Assimilation service → `memory_tracker` | Persistence scoped to `currentUser` only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | AssimilationController.assimilate | high | mitigate | Keep `authorizationService.assertLoggedIn()` on existing assimilate; no anonymous create path |
| T-02-02 | Elevation of Privilege | MemoryTrackerAssimilation initialize | high | mitigate | Tracker always `setUser(currentUser)` on create; never accept userId from request body |
| T-02-03 | Tampering | Duplicate COMMISSIONED create | medium | mitigate | Pre-check existing note-level COMMISSIONED → empty list; UK on (user, note, type, property) remains |
| T-02-04 | Tampering / Bypass | propertyKey + assimilateAsCommissioned | medium | mitigate | Backend early-branch returns empty list when propertyKey non-empty with commissioned flag; UI never sends both |
| T-02-05 | Information Disclosure | Assimilation response | low | accept | Same as today’s assimilate payload; no new sensitive fields |
| T-02-SC | Tampering | Package installs | high | accept | No new npm/pip/cargo packages this phase (Package Legitimacy N/A) |
</threat_model>

<verification>
Targeted AssimilationControllerTests + AssimilationPanel/NoteInfoMemoryTracker Vitest green; learning_session Cypress feature green without `@wip`; `check_wip_tags.sh` OK.
</verification>

<success_criteria>
ROADMAP SC1–SC2 for Phase 2: caret assimilate-as-commissioned works; settings show Commissioned tracker; stay on note; primary Assimilate still usable when only COMMISSIONED exists.
</success_criteria>

<output>
Create `.planning/phases/02-assimilate-as-commissioned/02-01-SUMMARY.md` when done.
After plan: local wrap-up — Jidoka, post-change-refactor, update plan status, commit, push (execute-plan / gsd-coexistence).
</output>
