---
phase: 02-assimilate-as-commissioned
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java
  - frontend/tests/components/recall/AssimilationPanel.spec.ts
  - frontend/tests/components/notes/NoteInfoMemoryTracker.spec.ts
  - e2e_test/features/learning_session/commissioned_learning_session.feature
  - e2e_test/start/pageObjects/assimilationPage
  - e2e_test/step_definitions
autonomous: true
requirements:
  - TRK-02
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "User can assimilate as commissioned when ordinary note-level trackers already exist; both remain visible in assimilation settings."
    - "Assimilate as commissioned menu remains available whenever no note-level COMMISSIONED tracker exists yet, even if UNDERSTANDING/SPELLING already exist."
    - "Creating commissioned does not remove or replace existing ordinary trackers for the same note."
  artifacts:
    - path: "backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java"
      provides: "Controller proof: commissioned create coexists with UNDERSTANDING"
      contains: "COMMISSIONED"
    - path: "e2e_test/features/learning_session/commissioned_learning_session.feature"
      provides: "Observable coexistence in the single graduated Phase 2 scenario (or equivalent Given)"
      contains: "commissioned memory tracker"
  key_links:
    - from: "assimilateAsCommissioned create"
      to: "existing UNDERSTANDING/SPELLING rows"
      via: "UK includes type; commissioned branch does not delete ordinary trackers"
      pattern: "createNoteLevelTracker"
    - from: "Assimilation settings Memory Trackers table"
      to: "both Type normal (or spelling) and Commissioned rows"
      via: "NoteInfoMemoryTracker labels"
      pattern: "Commissioned"
---

<objective>
Prove commissioned and ordinary memory trackers coexist on the same note after assimilate-as-commissioned (TRK-02; D-02; ROADMAP SC3).

Purpose: Stop-safe completion of Phase 2 Behavior — coexistence is user-observable, not only UK-enforced.
Output: Controller + UI/E2E evidence that ordinary trackers remain when commissioned is added (and menu available before COMMISSIONED exists).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-assimilate-as-commissioned/02-CONTEXT.md
@.planning/phases/02-assimilate-as-commissioned/02-RESEARCH.md
@.planning/phases/02-assimilate-as-commissioned/02-PATTERNS.md
@.planning/phases/02-assimilate-as-commissioned/02-01-SUMMARY.md

@backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java
@frontend/src/components/notes/NoteInfoMemoryTracker.vue
@e2e_test/features/learning_session/commissioned_learning_session.feature
</context>

## Source coverage (this plan)

| Source | IDs | Covered here |
|--------|-----|--------------|
| GOAL | Coexistence with ordinary trackers (SC3) | Both tasks |
| REQ | TRK-02 | Both tasks |
| CONTEXT | D-02 | Task 1–2 |
| RESEARCH | Open Question 1 — one E2E scenario + controller coexistence test | Both tasks |

<tasks>

<task type="auto" tdd="true">
  <name>Controller: commissioned create coexists with ordinary trackers</name>
  <files>backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java</files>
  <read_first>
    - `.planning/phases/02-assimilate-as-commissioned/02-RESEARCH.md` (TRK-02; Open Question 1 recommendation)
    - `.planning/phases/02-assimilate-as-commissioned/02-PATTERNS.md` (AssimilationControllerTests CreateAssimilationPoint patterns)
    - Existing Phase 1 test `assimilatingCommissionedOnlyNoteCreatesUnderstandingAndLeavesCommissioned` (inverse order already covered — add ordinary-then-commissioned)
  </read_first>
  <behavior>
    - Given a note that already has UNDERSTANDING (and optionally SPELLING), assimilate with assimilateAsCommissioned true returns COMMISSIONED and leaves the ordinary tracker(s) in the repository for that user/note (TRK-02 / D-01 create-only).
    - Second assimilateAsCommissioned on the same note returns empty list (idempotent; D-05 backend) — assert empty result only, not full payload shape.
    - Menu precondition D-02 is API-side: create succeeds when ordinary exists and COMMISSIONED does not.
  </behavior>
  <action>
    Extend `AssimilationControllerTests` at the controller boundary with makeMe fixtures (`.commissioned()` already exists on Java builder). Prefer ordinary-then-commissioned scenario as the TRK-02 delta (Phase 1 already proved commissioned-then-ordinary). Capability-named methods only. Do not add a dedicated assimilate controller or Flyway. Confirm production create path from 02-01 already creates COMMISSIONED without deleting ordinary rows — this task locks the observable with tests; change production only if a regression appears.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only -- --tests com.odde.doughnut.controllers.AssimilationControllerTests</automated>
  </verify>
  <done>Ordinary-then-commissioned create proven at AssimilationController; duplicate commissioned create returns empty list.</done>
</task>

<task type="auto" tdd="true">
  <name>Settings and E2E show ordinary plus Commissioned together</name>
  <files>frontend/tests/components/recall/AssimilationPanel.spec.ts, frontend/tests/components/notes/NoteInfoMemoryTracker.spec.ts, e2e_test/features/learning_session/commissioned_learning_session.feature, e2e_test/start/pageObjects/assimilationPage, e2e_test/step_definitions</files>
  <read_first>
    - `.planning/phases/02-assimilate-as-commissioned/02-CONTEXT.md` (Specifics: coexistence when ordinary already existed)
    - `.planning/phases/02-assimilate-as-commissioned/02-RESEARCH.md` (Open Question 1: keep one scenario name; add Given ordinary already assimilated)
    - `e2e_test/start/pageObjects/assimilationPage/propertyMemoryTrackerExpectations.ts` (expectMemoryTrackerInfo pattern)
  </read_first>
  <behavior>
    - After ordinary assimilate then assimilate as commissioned (same note), assimilation settings / memory tracker table shows both an ordinary Type (normal or spelling) and Commissioned (TRK-02 / D-07).
    - Note-level caret for Assimilate as commissioned is present when ordinary trackers exist but COMMISSIONED does not (D-02).
  </behavior>
  <action>
    Per D-02 and RESEARCH Open Question 1: keep a single graduated E2E scenario name from D-08; enrich its Given/When so ordinary assimilation already happened (or perform ordinary assimilate then commissioned in-scenario) and Then asserts both tracker types remain visible. Supplement with Vitest if faster for the caret-available-when-ordinary-exists case. Do not graduate additional draft scenarios. Targeted Cypress only after changes. Capability naming — no phase numbers in feature/file names.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/AssimilationPanel.spec.ts tests/components/notes/NoteInfoMemoryTracker.spec.ts &amp;&amp; CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/learning_session/commissioned_learning_session.feature</automated>
  </verify>
  <done>User-visible coexistence: ordinary and Commissioned trackers both shown; caret available before COMMISSIONED exists even when ordinary trackers present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `POST /api/assimilation` | Same as 02-01; coexistence does not widen auth surface |
| Create path → existing trackers | Must not delete or reassign another user’s trackers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | Coexistence create | medium | mitigate | Commissioned branch only inserts COMMISSIONED; never deletes UNDERSTANDING/SPELLING; UK prevents duplicate COMMISSIONED |
| T-02-07 | Elevation of Privilege | Cross-user note assimilate | high | mitigate | Inherit 02-01 / existing assimilate authorization posture (logged-in user owns created tracker); no new ownership bypass |
| T-02-03 | Tampering | Duplicate COMMISSIONED | medium | mitigate | Idempotent empty list covered by Task 1 (same as 02-01 T-02-03) |
| T-02-SC | Tampering | Package installs | high | accept | No new packages |
</threat_model>

<verification>
AssimilationControllerTests coexistence + idempotent cases green; frontend specs green; learning_session feature still green with coexistence assertions.
</verification>

<success_criteria>
ROADMAP SC3: ordinary trackers for the same note still exist when present after commissioned assimilate; D-02 menu availability when only ordinary trackers exist.
</success_criteria>

<output>
Create `.planning/phases/02-assimilate-as-commissioned/02-02-SUMMARY.md` when done.
After plan: local wrap-up — Jidoka, post-change-refactor, update plan status, commit, push.
</output>
