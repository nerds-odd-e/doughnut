---
phase: 06-user-level-defaults
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - frontend/src/components/notebook/NotebookHealthPanel.vue
  - frontend/tests/components/notebook/NotebookHealthPanel.spec.ts
  - e2e_test/features/notebooks/notebook_health.feature
  - e2e_test/start/pageObjects/notebookPage.ts
  - e2e_test/step_definitions/notebook.ts
autonomous: true
requirements:
  - DFLT-01
  - DFLT-02
must_haves:
  truths:
    - "Opening Health prefills Remove empty folders from currentUser.healthRemoveEmptyFoldersDefault without calling lint or mutating notebook data (DFLT-02, D-04, D-05, D-14)"
    - "Unset or missing preference prefills unchecked (false) (D-02, D-04)"
    - "After prefill, checkbox is run-scoped local state; toggle alone does not PATCH (D-06, D-07)"
    - "Save as defaults on the Health action bar PATCHes updateUser with full UserDTO-shaped body (name, dailyAssimilationCount, spaceIntervals, healthRemoveEmptyFoldersDefault) via apiCallWithLoading and updates injected currentUser (DFLT-01, D-07, D-09, D-14)"
    - "Save does not call lint, Fix, or mutate notebook content (D-10, D-14)"
    - "Save control is ghost/sm labeled Save as defaults with notebook-health-save-defaults; primary stays on Run lint; no UserProfileForm Health fields (D-08, UI-SPEC)"
    - "Cross-notebook E2E: save on notebook A, open Health on notebook B, checkbox matches; lint stays bodyless (D-11, D-12, D-15)"
  artifacts:
    - path: frontend/src/components/notebook/NotebookHealthPanel.vue
      provides: "Prefill on mount + Save as defaults (D-04..D-10)"
      contains: "notebook-health-save-defaults"
    - path: frontend/tests/components/notebook/NotebookHealthPanel.spec.ts
      provides: "Prefill, Save body, no lint on open/save (D-14)"
    - path: e2e_test/features/notebooks/notebook_health.feature
      provides: "Cross-notebook defaults scenario (D-15)"
    - path: e2e_test/start/pageObjects/notebookPage.ts
      provides: "saveAsDefaults + checkbox checked expectation"
  key_links:
    - from: "NotebookHealthPanel onMounted"
      to: "inject currentUser.healthRemoveEmptyFoldersDefault"
      via: "seed removeEmptyFolders local ref"
      pattern: "healthRemoveEmptyFoldersDefault"
    - from: "NotebookHealthPanel saveAsDefaults"
      to: "UserController.updateUser"
      via: "apiCallWithLoading full UserDTO body then currentUser.value = data"
      pattern: "UserController.updateUser"
    - from: "notebook_health.feature"
      to: "notebookPage.saveAsDefaults"
      via: "data-testid notebook-health-save-defaults"
      pattern: "notebook-health-save-defaults"
  prohibitions:
    - statement: "must NOT auto-save on checkbox toggle"
      status: resolved
      verification: automated
    - statement: "must NOT add Health defaults to UserProfileForm"
      status: resolved
      verification: judgment
    - statement: "must NOT call lint or Fix on Health open or Save"
      status: resolved
      verification: automated
    - statement: "must NOT send defaults on lint request body"
      status: resolved
      verification: automated
    - statement: "must NOT PATCH with only the new boolean (omit name or other prefs)"
      status: resolved
      verification: automated
    - statement: "must NOT add Fix/purge or phase numbers in product/E2E names"
      status: resolved
      verification: judgment
    - statement: "must NOT run the full E2E suite"
      status: resolved
      verification: judgment
---

<objective>
Deliver Health UI prefill and explicit Save as defaults so preferences apply across notebooks without lint/fix on open or save (DFLT-01, DFLT-02, D-04..D-12, D-14, D-15).

Purpose: Complete the Phase 6 Behavior stop-safe slice — preferences persist across notebooks.
Output: NotebookHealthPanel prefill + Save + green unit specs + targeted notebook_health E2E green (remove @wip when passing).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-level-defaults/06-CONTEXT.md
@.planning/phases/06-user-level-defaults/06-RESEARCH.md
@.planning/phases/06-user-level-defaults/06-PATTERNS.md
@.planning/phases/06-user-level-defaults/06-UI-SPEC.md
@.planning/phases/06-user-level-defaults/06-VALIDATION.md
@.planning/phases/06-user-level-defaults/06-01-PLAN.md
@.cursor/rules/planning.mdc
@.cursor/rules/frontend-api.mdc
@.cursor/rules/frontend-testing.mdc
@.cursor/rules/e2e-authoring.mdc
@frontend/src/components/notebook/NotebookHealthPanel.vue
@frontend/src/components/toolbars/UserProfileForm.vue
@frontend/tests/components/notebook/NotebookHealthPanel.spec.ts
@frontend/tests/toolbars/UserProfileForm.spec.ts
@e2e_test/features/notebooks/notebook_health.feature
@e2e_test/start/pageObjects/notebookPage.ts
@e2e_test/step_definitions/notebook.ts
@packages/generated/doughnut-backend-api/sdk.gen.ts
</context>

## Phase Goal

**As a** notebook owner, **I want to** save Remove empty folders as my Health default and see it prefilled on any notebook Health tab, **so that** my run options persist across notebooks without applying fixes on open or save.

This plan completes Save + prefill UI and cross-notebook E2E after 06-01 persistence.

## Artifacts this plan produces

| Symbol | Path |
|--------|------|
| Health panel Save/prefill | `frontend/src/components/notebook/NotebookHealthPanel.vue` |
| Panel unit tests | `NotebookHealthPanel.spec.ts` |
| Cross-notebook E2E | `notebook_health.feature` + notebookPage + notebook steps |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prefill and Save as defaults on Health panel</name>
  <files>frontend/tests/components/notebook/NotebookHealthPanel.spec.ts, frontend/src/components/notebook/NotebookHealthPanel.vue</files>
  <read_first>
    - .planning/phases/06-user-level-defaults/06-CONTEXT.md (D-04..D-10, D-12, D-14)
    - .planning/phases/06-user-level-defaults/06-UI-SPEC.md (Save control, interaction, data-testid, copywriting)
    - .planning/phases/06-user-level-defaults/06-PATTERNS.md (NotebookHealthPanel, UserProfileForm PATCH, withCurrentUser)
    - .planning/phases/06-user-level-defaults/06-RESEARCH.md (Pattern 2–3, Pitfall 2 full body, Pitfall 5 inject update)
    - .cursor/rules/frontend-api.mdc
    - .cursor/rules/frontend-testing.mdc
    - frontend/src/components/notebook/NotebookHealthPanel.vue
    - frontend/src/components/toolbars/UserProfileForm.vue
    - frontend/tests/components/notebook/NotebookHealthPanel.spec.ts
    - frontend/tests/helpers/index.ts
  </read_first>
  <behavior>
    - Prefill: withCurrentUser healthRemoveEmptyFoldersDefault true → checkbox checked on mount; lint spy not called (D-04, D-05, D-14)
    - Missing/false preference → checkbox unchecked; lint not called (D-02, D-04)
    - Save as defaults: notebook-health-save-defaults present; click calls UserController.updateUser once with path user id and body including name, dailyAssimilationCount, spaceIntervals, and healthRemoveEmptyFoldersDefault matching checkbox; lint spy not called (D-07, D-09, D-10, D-14)
    - After successful Save, injected currentUser ref updated to returned User (D-09)
    - Toggle checkbox alone does not call updateUser (D-06, D-07)
    - Existing idle / Run bodyless / no Fix cases remain green (D-12)
    - RED failing tests first, then GREEN implementation in this task
  </behavior>
  <action>
    Write failing NotebookHealthPanel.spec.ts cases first (helper.withCurrentUser + mockSdkService for UserController.updateUser and existing lint spy). Assert prefill, no lint on mount/save, Save PATCH body is full UserDTO-shaped (name + dailyAssimilationCount + spaceIntervals + healthRemoveEmptyFoldersDefault), inject updated after success, toggle without Save does not PATCH. Prefer data-testid selectors. Capability-named only.

    Then implement in NotebookHealthPanel only (D-04..D-10, UI-SPEC): inject currentUser; onMounted seed removeEmptyFolders from healthRemoveEmptyFoldersDefault ?? false (panel remounts via Health tab v-else-if — prefer onMounted; getUserProfile fallback only if field missing on inject). Add Save as defaults button daisy-btn daisy-btn-ghost daisy-btn-sm after the checkbox, data-testid notebook-health-save-defaults, label Save as defaults. saveAsDefaults uses apiCallWithLoading → UserController.updateUser with full body from current profile + checkbox value; on !error assign currentUser.value = data. Do not auto-save on toggle. Do not call lint on mount/save. Do not change UserProfileForm. Do not send defaults on lint. Do not add Fix/purge. Primary accent stays on Run lint.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/notebook/NotebookHealthPanel.spec.ts</automated>
  </verify>
  <done>Health panel prefills from currentUser and Save as defaults PATCHes full UserDTO body without lint/mutation; unit tests green (DFLT-01 UI, DFLT-02, D-14).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-notebook defaults E2E</name>
  <files>e2e_test/features/notebooks/notebook_health.feature, e2e_test/start/pageObjects/notebookPage.ts, e2e_test/step_definitions/notebook.ts</files>
  <read_first>
    - .planning/phases/06-user-level-defaults/06-CONTEXT.md (D-11, D-15)
    - .planning/phases/06-user-level-defaults/06-RESEARCH.md (E2E acceptance sketch)
    - .planning/phases/06-user-level-defaults/06-PATTERNS.md (notebook_health feature, page object, steps)
    - .planning/phases/06-user-level-defaults/06-UI-SPEC.md (notebook-health-save-defaults)
    - .cursor/rules/e2e-authoring.mdc
    - e2e_test/features/notebooks/notebook_health.feature
    - e2e_test/start/pageObjects/notebookPage.ts
    - e2e_test/step_definitions/notebook.ts
  </read_first>
  <behavior>
    - Scenario: two notebooks; open Health on A; check Remove empty folders; Save as defaults; open Health on B; checkbox checked (D-11, D-15)
    - Tag @wip while red; remove @wip when scenario passes (D-15)
    - Existing Health Run / AFIX-01 scenarios remain green
    - Capability name notebook_health only; no phase numbers in feature/file names
  </behavior>
  <action>
    Extend notebookPage with saveAsDefaults (click notebook-health-save-defaults, wait not loading) and expectRemoveEmptyFoldersChecked (checkbox should be checked). Wire Cucumber When/Then steps for save notebook health options as defaults and Remove empty folders checked assertion. Add the cross-notebook scenario from RESEARCH/PATTERNS to notebook_health.feature, tagged @wip until green. Implement until targeted cy:run passes, then remove @wip. Do not run the full E2E suite — only this feature. Assume pnpm sut already running. Do not add Fix/purge or UserProfileForm paths.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cy:run --spec e2e_test/features/notebooks/notebook_health.feature</automated>
  </verify>
  <done>Targeted notebook_health E2E proves cross-notebook Save/prefill without @wip (DFLT-01, DFLT-02, D-15).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → updateUser | Session user Save; must not open elevation path |
| Health UI → lint API | Unchanged bodyless report-only; defaults must not hitchhike |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | Save as defaults → updateUser | high | mitigate | Call existing updateUser with path current user id only; backend assertAuthorization remains from 06-01 |
| T-06-02 | Tampering | Prefill/Save side effects | high | mitigate | Unit asserts lint not called on mount/save; no Fix; checkbox not in lint body (D-05, D-10, D-12, D-14) |
| T-06-05 | Tampering | Partial PATCH wipes prefs | high | mitigate | Save body always includes name, dailyAssimilationCount, spaceIntervals, healthRemoveEmptyFoldersDefault; unit asserts full body (D-09, Research Pitfall 2) |
| T-06-06 | Repudiation | Silent auto-save | medium | mitigate | Explicit Save only; toggle alone does not PATCH (D-07) |
| T-06-04 | Information Disclosure | Prefill from currentUser | low | accept | Same session User already loaded at app start |
</threat_model>

<verification>
- NotebookHealthPanel.spec.ts green via nix-prefixed frontend:test
- Targeted cy:run --spec notebook_health.feature green without @wip on new scenario
- Diff does not modify UserProfileForm for Health defaults
- Lint remains bodyless; no Fix/purge added
</verification>

<success_criteria>
- DFLT-02: Opening Health prefills Remove empty folders from saved user default
- DFLT-01: Explicit Save as defaults persists across notebooks
- Success criterion 3: open/save does not apply fixes or mutate notebook data
- D-15: capability-named E2E green; no full suite
</success_criteria>

<output>
Create `.planning/phases/06-user-level-defaults/06-02-SUMMARY.md` when done
</output>
