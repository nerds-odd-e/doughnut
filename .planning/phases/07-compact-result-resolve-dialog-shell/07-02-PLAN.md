---
phase: 07-compact-result-resolve-dialog-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
  - e2e_test/features/recall/accidental_match_reveal.feature
  - e2e_test/step_definitions/recall.ts
autonomous: true
requirements:
  - AMR-01
  - AMR-02
  - AMR-03
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "E2E accidental-match reveal asserts Resolve CTA under alert, opens dialog with matched title, can dismiss, reviewed note stays primary (D-09; AMR-01..AMR-03)"
    - "Link-from-result E2E scenarios are tagged @wip until Phase 9 restores Build a link (D-09)"
    - "overlap_try_again E2E stays green; accidental-match resolve chrome does not appear on OVERLAP (D-09)"
  artifacts:
    - path: e2e_test/start/pageObjects/AnsweredQuestionPage.ts
      provides: "Reveal helpers for resolve-accidental-match + dialog titles; overlap helper asserts no resolve CTA"
    - path: e2e_test/features/recall/accidental_match_reveal.feature
      provides: "Scenario 1 asserts compact resolve shell; scenarios 2–3 tagged @wip"
  key_links:
    - from: AccidentalMatchReveal Gherkin steps
      to: AnsweredQuestionPage.expectAccidentalMatchReveal
      via: "e2e_test/step_definitions/recall.ts thin wrappers"
    - from: "@wip link scenarios"
      to: "CI skip via e2e_test/config/ci.ts not @wip"
      via: "Tag only scenarios 2–3; keep feature runnable"
---

<objective>
Align accidental-match E2E and page objects with the compact Resolve dialog shell: rewrite reveal asserts for CTA + titles + dismiss; @wip link-from-result scenarios until Phase 9; keep overlap_try_again green (D-09).

Purpose: Lock product-visible Behavior at Cypress boundary so Phase 7 does not leave CI red after stack removal.
Output: Updated AnsweredQuestionPage + accidental_match_reveal.feature (+ step defs only if Gherkin wording must change).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-compact-result-resolve-dialog-shell/07-01-PLAN.md
@.planning/phases/07-compact-result-resolve-dialog-shell/07-CONTEXT.md
@.planning/phases/07-compact-result-resolve-dialog-shell/07-UI-SPEC.md
@.planning/phases/07-compact-result-resolve-dialog-shell/07-PATTERNS.md
@e2e_test/start/pageObjects/AnsweredQuestionPage.ts
@e2e_test/features/recall/accidental_match_reveal.feature
@e2e_test/features/recall/overlap_try_again.feature
</context>

<tasks>

<task type="auto">
  <name>Rewrite reveal page object + Scenario 1 for Resolve dialog</name>
  <precondition>Plan 07-01 complete: Resolve CTA and AccidentalMatchResolveDialog exist in AnsweredSpellingQuestion with testids resolve-accidental-match / accidental-match-resolve-dialog</precondition>
  <files>e2e_test/start/pageObjects/AnsweredQuestionPage.ts, e2e_test/features/recall/accidental_match_reveal.feature, e2e_test/step_definitions/recall.ts</files>
  <read_first>
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/step_definitions/recall.ts
    - frontend/src/components/recall/AnsweredSpellingQuestion.vue
    - .planning/phases/07-compact-result-resolve-dialog-shell/07-UI-SPEC.md
    - .planning/phases/07-compact-result-resolve-dialog-shell/07-CONTEXT.md (D-09)
  </read_first>
  <action>
    Per D-09, update AnsweredQuestionPage expectAccidentalMatchReveal / matched-note helpers to assert: accidental-match-alert → resolve-accidental-match visible under alert → click opens dialog → matched title text visible (accidental-match-resolve-dialog / resolve-match-row or body text) → dismiss via .close-button (or ESC) → still on accidental-match result with reviewed note title; do not assert matched-notes-section or stacked NoteShow bodies (AMR-01..AMR-03). Prefer keeping Gherkin step wording and changing page-object behavior; update Feature blurb to compact optional resolve (capability language, no phase numbers). Touch recall.ts step defs only if step text must change. Assume pnpm sut already running; do not restart nag.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature</automated>
  </verify>
  <acceptance_criteria>
    - expectAccidentalMatchReveal (or successor) uses resolve-accidental-match and dialog title asserts; matched-notes-section not required for pass
    - Feature scenario Accidental match reveals reviewed and matched notes passes under new helpers
    - Feature description no longer claims stacked full-body reveal as the product promise
  </acceptance_criteria>
  <done>Reveal E2E Scenario 1 green for CTA + dialog titles + dismiss (D-09; AMR-01..AMR-03).</done>
</task>

<task type="auto">
  <name>@wip link scenarios; keep overlap E2E uncoupled</name>
  <files>e2e_test/features/recall/accidental_match_reveal.feature, e2e_test/start/pageObjects/AnsweredQuestionPage.ts</files>
  <read_first>
    - e2e_test/features/recall/accidental_match_reveal.feature
    - e2e_test/features/recall/overlap_try_again.feature
    - e2e_test/start/pageObjects/AnsweredQuestionPage.ts
    - e2e_test/config/ci.ts
    - .planning/phases/07-compact-result-resolve-dialog-shell/07-CONTEXT.md (D-09)
  </read_first>
  <action>
    Per D-09, tag only the two link-from-result scenarios (wiki property + relationship) with @wip — do not @wip the whole feature. Keep link page-object helpers callable for Phase 9 rather than deleting them. Extend expectNoMatchedNotesOrAccidentalMatch (or equivalent overlap helper) to also assert resolve-accidental-match absent. Run targeted overlap_try_again.feature and confirm green / no accidental-match resolve chrome bleed. Respect @wip cap of 5 (repo starts at 0). Capability-named only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/recall/accidental_match_reveal.feature,e2e_test/features/recall/overlap_try_again.feature</automated>
  </verify>
  <acceptance_criteria>
    - Both link scenarios carry @wip; Scenario 1 does not
    - Non-@wip Cypress for reveal + overlap_try_again exits 0
    - Overlap helper asserts resolve CTA absent when used by overlap flow
    - Repo @wip scenario count remains ≤ 5
  </acceptance_criteria>
  <done>Link E2E paused via @wip; overlap try-again stays green and uncoupled from resolve CTA (D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress → app UI | E2E drives authenticated recall UI; no new trust boundary beyond existing session |
| Test selectors → DOM | data-testid contracts for resolve CTA/dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Information Disclosure | E2E fixtures | low | accept | Uses existing test notebooks; no production secrets |
| T-07-05 | Tampering (XSS regression) | Dialog title asserts | low | mitigate | E2E asserts visible title text nodes from fixture titles (complements unit XSS text-interpolation rule) |
| T-07-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs this phase |
</threat_model>

<verification>
Targeted Cypress: accidental_match_reveal (non-@wip) + overlap_try_again green. Link scenarios tagged @wip only.
</verification>

<success_criteria>
D-09 complete: reveal E2E covers compact resolve shell; link scenarios @wip; OVERLAP E2E unchanged and green.
</success_criteria>

<output>
Create `.planning/phases/07-compact-result-resolve-dialog-shell/07-02-SUMMARY.md` when done
</output>
