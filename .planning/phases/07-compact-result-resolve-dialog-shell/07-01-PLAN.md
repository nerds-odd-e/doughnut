---
phase: 07-compact-result-resolve-dialog-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/recall/AccidentalMatchResolveDialog.vue
  - frontend/src/components/recall/AnsweredSpellingQuestion.vue
  - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
  - frontend/tests/components/recall/AnsweredSpellingQuestionOverlap.spec.ts
autonomous: true
requirements:
  - AMR-01
  - AMR-02
  - AMR-03
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "On ACCIDENTAL_MATCH with matches, result shows Resolve accidental match under the alert and no stacked matched NoteShows / matched-notes-section (D-01, D-02, D-07; AMR-01, AMR-02)"
    - "Opening the Resolve CTA shows a Modal listing matched note titles only from matchedNotes (D-03, D-05, D-06; AMR-02)"
    - "User can dismiss the Modal via existing close control and remain on the accidental-match result (D-04; AMR-03)"
    - "Empty matchedNotes omits the Resolve CTA; OVERLAP (even with leaked matchedNotes) shows no Resolve CTA and keeps try-again (D-08)"
  artifacts:
    - path: frontend/src/components/recall/AccidentalMatchResolveDialog.vue
      provides: "Presentational title-only match list (data-testid accidental-match-resolve-dialog / resolve-match-row-{id})"
    - path: frontend/src/components/recall/AnsweredSpellingQuestion.vue
      provides: "Gated Resolve PopButton under alert; stacked match section and MatchedNoteLinkOffer CTAs removed"
    - path: frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
      provides: "Boundary unit coverage for compact result + dialog titles + dismiss + empty omit"
    - path: frontend/tests/components/recall/AnsweredSpellingQuestionOverlap.spec.ts
      provides: "Leak assert: no resolve CTA on OVERLAP"
  key_links:
    - from: AnsweredSpellingQuestion Resolve PopButton
      to: AccidentalMatchResolveDialog via PopButton Modal #body slot
      via: "v-if showResolveAccidentalMatchCta; :matched-notes from answeredQuestion.matchedNotes"
    - from: showResolveAccidentalMatchCta
      to: "outcome === ACCIDENTAL_MATCH && matchedNotes.length > 0"
      via: "computed gate (never length-alone)"
---

<objective>
Ship the compact accidental-match result shell: drop stacked matched NoteShows and link CTAs; add gated **Resolve accidental match** PopButton→Modal listing titles only; cover with rewritten Vitest boundary tests (D-01..D-08; AMR-01..AMR-03 unit side).

Purpose: Restore reviewed-note primary focus while preserving optional match identity via dialog — stop-safe Behavior for Phase 7 before path/link/overlap work.
Output: AccidentalMatchResolveDialog.vue; modified AnsweredSpellingQuestion.vue; updated AccidentalMatch + Overlap unit specs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-compact-result-resolve-dialog-shell/07-CONTEXT.md
@.planning/phases/07-compact-result-resolve-dialog-shell/07-RESEARCH.md
@.planning/phases/07-compact-result-resolve-dialog-shell/07-PATTERNS.md
@.planning/phases/07-compact-result-resolve-dialog-shell/07-UI-SPEC.md
@.cursor/rules/frontend-testing.mdc
@.cursor/rules/unit-testing.mdc
</context>

## Artifacts this phase produces

- `frontend/src/components/recall/AccidentalMatchResolveDialog.vue` — title-only presentational list
- Compact Resolve CTA chrome in `AnsweredSpellingQuestion.vue` (no matched-notes section)
- Rewritten AccidentalMatch / Overlap Vitest coverage for resolve shell

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: compact result + Resolve CTA opens title list</name>
  <reversibility rating="costly">Removes stacked matched NoteShows and MatchedNoteLinkOffer CTAs from the result surface (D-07); restoring link-from-result without Phase 9 means reintroducing stacked bodies or a parallel link surface.</reversibility>
  <files>frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts, frontend/src/components/recall/AccidentalMatchResolveDialog.vue, frontend/src/components/recall/AnsweredSpellingQuestion.vue</files>
  <read_first>
    - frontend/src/components/recall/AnsweredSpellingQuestion.vue
    - frontend/src/components/commons/Popups/PopButton.vue
    - frontend/src/components/commons/Modal.vue
    - frontend/tests/components/recall/answeredSpellingQuestionTestSupport.ts
    - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
    - frontend/src/components/notebook/NotebookHealthFindings.vue (list analog)
    - .planning/phases/07-compact-result-resolve-dialog-shell/07-UI-SPEC.md
    - .planning/phases/07-compact-result-resolve-dialog-shell/07-CONTEXT.md (D-01..D-07)
  </read_first>
  <behavior>
    - Canonical ACCIDENTAL_MATCH with two matches: no matched-notes-section; no NoteShow stubs for matched ids 10/20; reviewed NoteShow path still present for reviewed id
    - Resolve CTA data-testid resolve-accidental-match is present with accessible name Resolve accidental match
    - Clicking CTA opens Modal; document.body/dialog shows titles Matched A and Matched B inside accidental-match-resolve-dialog / resolve-match-row-{id}
    - Do not leave failing link-offer unit cases; remove or replace obsolete link-to-matched-note asserts from this spec
  </behavior>
  <action>
    Per D-08, rewrite AccidentalMatch unit tests first (RED) for compact chrome + open dialog titles using mountAnsweredSpellingQuestion / accidentalMatchWithTwoMatchedNotes; query Modal content on document.body (attachTo already set). Then implement: create presentational AccidentalMatchResolveDialog under recall/ with props matchedNotes: NoteTopology[], vertical ul flex flex-col gap-2, text interpolation for titles only (no v-html), testids per UI-SPEC (D-03, D-05, D-06). In AnsweredSpellingQuestion: delete matched-notes-section and all MatchedNoteLinkOffer / link-to-matched-note PopButtons (D-07); insert PopButton immediately under alert before NoteUnderQuestion with title/aria-label Resolve accidental match, btn-class daisy-btn daisy-btn-secondary daisy-btn-sm mt-2, data-testid resolve-accidental-match, default showCloseButton (D-01, D-02, D-04); gate with renamed showResolveAccidentalMatchCta using same ACCIDENTAL_MATCH + length check as today’s showMatchedNotesSection (never length alone). Slot body without closer; pass matchedNotes. Keep MatchedNoteLinkOffer.vue file on disk unused. Drop link-only helpers/imports (canOfferLinkToMatched, inject currentUser, useStorageAccessor, reviewedRealm) if unused after removal. Do not change OVERLAP try-again, grading, or SRS (ADR 0003). Capability-named testids only — no phase numbers in product or test names.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - AccidentalMatchResolveDialog.vue exists and renders titles via text nodes keyed by matched.id
    - AnsweredSpellingQuestion has Resolve PopButton under alert when ACCIDENTAL_MATCH with matches; matched-notes-section and link-to-matched-note-* absent from template
    - AccidentalMatch.spec: no matched-notes-section; CTA present; click shows Matched A / Matched B; Vitest file green
    - MatchedNoteLinkOffer.vue file still present in repo
  </acceptance_criteria>
  <done>Happy path: compact result + Resolve CTA → title-only Modal works under AccidentalMatch Vitest (AMR-01, AMR-02; D-01..D-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Expand unit edges: empty omit, OVERLAP leak, dismiss</name>
  <files>frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts, frontend/tests/components/recall/AnsweredSpellingQuestionOverlap.spec.ts</files>
  <read_first>
    - frontend/tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts
    - frontend/tests/components/recall/AnsweredSpellingQuestionOverlap.spec.ts
    - frontend/tests/commons/modalTestSupport.ts
    - frontend/tests/commons/popButtonTestSupport.ts
    - .planning/phases/07-compact-result-resolve-dialog-shell/07-CONTEXT.md (D-04, D-08)
  </read_first>
  <behavior>
    - Empty matchedNotes on ACCIDENTAL_MATCH: resolve-accidental-match absent (delta-only assert)
    - OVERLAP with leaked matchedNotes: resolve-accidental-match absent; overlap-try-again still works (existing emit test unchanged)
    - Light dismiss: open Resolve → click .close-button → dialog gone; AccidentalMatch alert still present (AMR-03)
  </behavior>
  <action>
    Per D-08, add focused AccidentalMatch deltas for empty matchedNotes (no CTA) and one open→close dismiss path using modalTestSupport / popButtonTestSupport helpers (D-04; AMR-03) — do not duplicate full Modal.spec suite. Update Overlap leak test to assert no resolve-accidental-match (and no resolve dialog entry) instead of only matched-notes-section / Link to this note copy. Prefer AnsweredSpellingQuestion boundary; do not add a thin AccidentalMatchResolveDialog-only spec unless list logic grows. Keep OVERLAP try-again emit scenario green and uncoupled from resolve gate.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/components/recall/AnsweredSpellingQuestionAccidentalMatch.spec.ts tests/components/recall/AnsweredSpellingQuestionOverlap.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Empty matchedNotes case asserts resolve CTA absent
    - Overlap leak case asserts resolve CTA absent; try-again emit test still passes
    - At least one AccidentalMatch test closes dialog via .close-button and confirms dialog absent afterward
    - Both Vitest files green
  </acceptance_criteria>
  <done>Unit edges cover AMR-03 dismiss and D-08 empty/OVERLAP gates; no resolve chrome on OVERLAP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grade API → Vue result UI | Authenticated AnsweredQuestion.matchedNotes titles rendered in dialog |
| User → Modal dismiss controls | Untrusted click/ESC/backdrop closes dialog only; no mutate in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering (XSS) | AccidentalMatchResolveDialog title rows | low | mitigate | Render `{{ matched.title }}` text interpolation only; never `v-html` (UI-SPEC / RESEARCH) |
| T-07-02 | Elevation of Privilege | Resolve dialog body | low | accept | No write/link/overlap actions in Phase 7; mutations deferred to Phase 9+ |
| T-07-03 | Denial of Service | Nested modal / focus trap | low | mitigate | Single PopButton→Modal; no nested PopButton; reuse existing Modal showModal/ESC |
| T-07-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs this phase (Package Legitimacy N/A) |
</threat_model>

<verification>
AccidentalMatch + Overlap Vitest files green. Result chrome has Resolve CTA + title Modal and no stacked matched NoteShows. OVERLAP unchanged aside from no-resolve leak assert.
</verification>

<success_criteria>
AMR-01/AMR-02/AMR-03 observable at unit boundary: compact result, optional resolve title list, dismissible Modal; D-01..D-08 implemented in Vue + Vitest.
</success_criteria>

<output>
Create `.planning/phases/07-compact-result-resolve-dialog-shell/07-01-SUMMARY.md` when done
</output>
