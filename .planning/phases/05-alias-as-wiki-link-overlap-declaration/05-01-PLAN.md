---
phase: 05-alias-as-wiki-link-overlap-declaration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/odde/doughnut/algorithms/FrontmatterAliases.java
  - backend/src/test/java/com/odde/doughnut/algorithms/FrontmatterAliasesTest.java
  - backend/src/test/java/com/odde/doughnut/controllers/TextContentControllerTests.java
  - frontend/src/utils/authoredAliasesValidation.ts
  - frontend/tests/utils/authoredAliasesValidation.spec.ts
  - frontend/tests/utils/noteContentPropertyRows.spec.ts
autonomous: true
requirements: [OVL-02, OVL-03]
must_haves:
  truths:
    - "Authored aliases lists may include well-formed whole-item wiki-link tokens ([[Title]], [[Notebook:Title]], pipe-display forms); overlap uses the target segment per D-01 / D-04 (OVL-02)."
    - "Mixed plain+wiki-link aliases list: plain items index/match; wiki-link items declare overlap only (D-02 / EDGE)."
    - "Empty aliases list yields no aliases; wiki-link-only aliases list yields no plain aliases from fromNoteContent / fromFrontmatter and returns overlap tokens from overlapWikiLinkTokensFrom* (EDGE)."
    - "Soft parse skips invalid items; authored validation rejects malformed wiki-link items such as bare open brackets (D-04 / EDGE)."
    - "overlapWikiLinkTokensFromNoteContent / FromFrontmatter return raw authored [[…]] token strings for Phase 6; grading paths remain untouched (D-03)."
    - "Frontend authoredAliasesValidation accepts the same well-formed wiki-link forms the backend accepts and rejects the same malformed cases (RESEARCH parity)."
  prohibitions:
    - "Must NOT wire MemoryTrackerService OVERLAP grading / AnsweredQuestion.overlap in this phase"
    - "Must NOT index wiki-link alias strings into note_alias_index"
    - "Must NOT change Flyway schema unless research proves necessary (prefer no migration)"
  artifacts:
    - path: "backend/src/main/java/com/odde/doughnut/algorithms/FrontmatterAliases.java"
      provides: "Plain-only from* + overlapWikiLinkTokensFrom* + authored accept for whole-item wiki-links"
      contains: "overlapWikiLinkTokensFromNoteContent"
    - path: "backend/src/test/java/com/odde/doughnut/algorithms/FrontmatterAliasesTest.java"
      provides: "Capability-named unit proofs for accept/segregate/overlap/malformed"
      contains: "overlapWikiLinkTokensFromFrontmatter"
    - path: "frontend/src/utils/authoredAliasesValidation.ts"
      provides: "Client parity for authored wiki-link alias items"
      contains: "AUTHORED_ALIASES_MESSAGE"
    - path: "frontend/tests/utils/authoredAliasesValidation.spec.ts"
      provides: "Vitest accept/reject parity for wiki-link alias items"
      contains: "authoredAliasesValidationErrorForPropertyValue"
  key_links:
    - from: "FrontmatterAliases.authoredValidationError*"
      to: "isAcceptableAuthoredAliasItem (plain ∪ wiki-link)"
      via: "D-04 strict authored gate"
      pattern: "AUTHORED_ALIASES_MESSAGE"
    - from: "FrontmatterAliases.fromNoteContent / fromFrontmatter"
      to: "plain aliases only"
      via: "isValidPlainAliasText filter (D-02)"
      pattern: "validAliasesFromRawItems"
    - from: "authoredAliasesValidation.ts"
      to: "FrontmatterAliases authored rules"
      via: "whole-item wiki-link accept lockstep message"
      pattern: "AUTHORED_ALIASES_MESSAGE"
---

## Phase Goal

**As a** note author, **I want to** declare overlap by putting well-formed wiki-link tokens in the aliases frontmatter list, **so that** overlapping notes are declared for later grading without breaking plain-alias wiki-resolve, search, or cloze masking.

<objective>
Ship the production-quality tracer for OVL-02/OVL-03 declaration: FrontmatterAliases classifies plain vs wiki-link alias items, accepts well-formed wiki-links in authored validation (D-01, D-04), keeps fromNoteContent/fromFrontmatter/matchesFromNoteContent plain-only (D-02), exposes overlapWikiLinkTokensFrom* (D-03), proves HTTP save accept via TextContentControllerTests, and mirrors rules in frontend authoredAliasesValidation.

Purpose: Prove the parse/validation seam end-to-end (algorithm → content PATCH → UI preflight) before consumer regression expansions.
Output: Updated FrontmatterAliases + unit/controller tests + frontend validation parity specs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-CONTEXT.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-RESEARCH.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-PATTERNS.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-VALIDATION.md
</context>

<nyquist_validation>
Per 05-VALIDATION.md and RESEARCH § Validation Architecture:
- Wave 0 in this plan: extend FrontmatterAliasesTest, TextContentControllerTests aliases nested cases, authoredAliasesValidation.spec.ts (+ property-rows inherit).
- Sampling: after each task run targeted backend:test_only or frontend Vitest; after wave merge run both.
- Index/search/resolve/cloze/AM regressions remain Wave 2 (05-02 / 05-03).
</nyquist_validation>

## Artifacts this phase produces

- `FrontmatterAliases` plain/wiki classification + `overlapWikiLinkTokensFromNoteContent` / `FromFrontmatter`
- Updated `AUTHORED_ALIASES_MESSAGE` (backend + frontend lockstep) mentioning wiki-link overlap declarations (D-04)
- Capability-named unit + HTTP accept tests for wiki-link alias authoring
- Frontend authored-alias validation parity (required for OVL-02 UX)

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end wiki-link alias declaration — parse → authored save → overlap API</name>
  <precondition>Phase 1 AnswerOutcome.OVERLAP / AnsweredQuestion.overlap exist on the contract but remain unused by grading; FrontmatterAliases currently rejects any item containing [[ or ]].</precondition>
  <reversibility rating="costly">D-02 keeps fromNoteContent / fromFrontmatter / matchesFromNoteContent plain-only; reversing that default would reintroduce alias blast-radius bugs across index, resolve, cloze, and matchAnswer.</reversibility>
  <files>backend/src/main/java/com/odde/doughnut/algorithms/FrontmatterAliases.java, backend/src/test/java/com/odde/doughnut/algorithms/FrontmatterAliasesTest.java, backend/src/test/java/com/odde/doughnut/controllers/TextContentControllerTests.java</files>
  <read_first>
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-CONTEXT.md (D-01–D-04)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-RESEARCH.md (Recommended Approach §§1–3, Consumer Inventory, Pitfalls 1–2)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-PATTERNS.md (FrontmatterAliases classification + TextContentControllerTests aliases nested)
    - backend/src/main/java/com/odde/doughnut/algorithms/FrontmatterAliases.java
    - backend/src/main/java/com/odde/doughnut/algorithms/WikiLinkMarkdown.java (INNER_LINK_PATTERN, splitInner)
    - backend/src/test/java/com/odde/doughnut/algorithms/FrontmatterAliasesTest.java
    - backend/src/test/java/com/odde/doughnut/controllers/TextContentControllerTests.java (rejectInvalidAuthoredAliasesTest / accepts_valid_alias_list)
    - backend/src/main/java/com/odde/doughnut/validators/AuthoredNoteContent.java
  </read_first>
  <behavior>
    - Mixed list (plain color + "[[Other Note]]" + "[[Shared Notebook:Hue|display]]"): fromFrontmatter returns only ["color"]; overlapWikiLinkTokensFromFrontmatter returns both wiki-link tokens in order (D-01 target segment for overlap semantics; tokens preserve authored form including pipe display) (D-02, D-03).
    - Empty aliases list / missing aliases → empty plain list and empty overlap list; wiki-link-only list → empty plain list and non-empty overlap tokens (EDGE).
    - authoredValidationErrorForNoteContent empty for well-formed [[Title]], [[Notebook:Title]], [[Title|display]], [[Notebook:Title|display]] alongside plain items; dead targets allowed (D-01, D-04).
    - authoredValidationErrorForNoteContent returns AUTHORED_ALIASES_MESSAGE for bare "[[", embedded "see [[X]]", "[[a]][[b]]", blank items, nested YAML, and plain items with INVALID_ALIAS_CHARACTERS (soft from* still skips invalids) (D-04 / EDGE).
    - TextContentController updateNoteContent accepts mixed plain+wiki-link aliases content; still BINDING_ERROR on aliases for malformed wiki-link items.
    - Do not modify MemoryTrackerService or NoteAliasIndexService in this task.
  </behavior>
  <action>
    Implement the Structure-phase tracer through FrontmatterAliases and the content-save boundary (OVL-02, OVL-03 parse seam; D-01–D-04). Write failing FrontmatterAliasesTest + TextContentControllerTests cases first (RED), then minimal production change (GREEN).

    1. Split validation predicates inside FrontmatterAliases per RESEARCH: isWikiLinkAliasItem using WikiLinkMarkdown.INNER_LINK_PATTERN.matcher(trimmed).matches() (whole item only — not find), non-empty inner, and non-empty splitInner(target); isValidPlainAliasText keeping today's reject of brackets plus INVALID_ALIAS_CHARACTERS; isAcceptableAuthoredAliasItem = wiki-link OR plain. Prefer least call-site churn: leave method names fromNoteContent / fromFrontmatter / matchesFromNoteContent as the plain-only contract (D-02, D-03 discretion).

    2. Soft validAliasesFromRawItems must filter with isValidPlainAliasText only so wiki-link items never enter plain alias lists (D-02). Soft parse continues to skip invalids.

    3. Authored path uses isAcceptableAuthoredAliasItem. Update AUTHORED_ALIASES_MESSAGE once to mention both plain aliases and wiki-link overlap declarations while remaining a single stable string (D-04). Do not require target note existence at save time.

    4. Add public overlapWikiLinkTokensFromNoteContent(String) and overlapWikiLinkTokensFromFrontmatter(Frontmatter) returning List of raw authored token strings including brackets; lenient skip of non-wiki / malformed items; dedupePreserveOrder via normalizedLookupKey of the full token string (D-03 discretion default). Do not call these from grading services.

    5. Capability-named tests only (no phase numbers). Cover mixed/empty/wiki-only lists, accept forms, reject malformed, and HTTP accept/reject via existing TextContentControllerTests aliases nested patterns. Leave Flyway / NoteAliasIndex entity / OpenAPI untouched.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - FrontmatterAliases.java defines overlapWikiLinkTokensFromNoteContent and overlapWikiLinkTokensFromFrontmatter.
    - FrontmatterAliasesTest proves mixed list plain-only fromFrontmatter + overlap tokens; empty and wiki-only EDGE cases; authored accept for D-01 forms; authored reject for malformed wiki-link items; soft skip of invalids.
    - TextContentControllerTests accepts well-formed wiki-link alias list items and rejects malformed ones with aliases binding error.
    - `rg -n 'overlapWikiLinkTokensFrom' backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java` exits 1 (no matches).
    - `rg -n 'AnswerOutcome\\.OVERLAP|setOverlap\\(' backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java` exits 1 (no matches).
    - No new files under backend/src/main/resources/db/migration/ (or equivalent Flyway path) in this plan's diff.
    - backend:test_only exits 0.
  </acceptance_criteria>
  <done>Authors can save well-formed wiki-link alias items; plain-only accessors and overlap API are segregated; HTTP accept path green; no grading wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Frontend authoredAliasesValidation parity for wiki-link overlap items</name>
  <precondition>Backend FrontmatterAliases accepts well-formed wiki-link alias items and exposes the updated AUTHORED_ALIASES_MESSAGE string.</precondition>
  <reversibility rating="reversible">Client preflight mirror of backend authored rules; easy to adjust with Vitest.</reversibility>
  <files>frontend/src/utils/authoredAliasesValidation.ts, frontend/tests/utils/authoredAliasesValidation.spec.ts, frontend/tests/utils/noteContentPropertyRows.spec.ts</files>
  <read_first>
    - frontend/src/utils/authoredAliasesValidation.ts
    - frontend/tests/utils/authoredAliasesValidation.spec.ts
    - frontend/src/utils/wikiPropertyValueField.ts (whole-item ^[[…]]$ analog)
    - frontend/tests/utils/noteContentPropertyRows.spec.ts (aliases row validation)
    - backend/src/main/java/com/odde/doughnut/algorithms/FrontmatterAliases.java (message + accept rules after Task 1)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-RESEARCH.md (§ Frontend parity)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-PATTERNS.md (authoredAliasesValidation section)
  </read_first>
  <behavior>
    - authoredAliasesValidationErrorForPropertyValue returns undefined for list items [[Title]], [[Notebook:Title]], [[Title|display]], [[Notebook:Title|display]] mixed with plain aliases.
    - Returns AUTHORED_ALIASES_MESSAGE for bare "[[", "see [[X]]", "[[a]][[b]]", "bad|alias", blanks, and scalar aliases values (existing).
    - AUTHORED_ALIASES_MESSAGE string matches backend constant text after D-04 update.
    - noteContentPropertyRows validatePropertyRowsForRichEdit accepts wiki-link alias list rows (inherits util).
  </behavior>
  <action>
    Mirror backend whole-item wiki-link acceptance in authoredAliasesValidation.ts (RESEARCH required parity / D-01, D-04). Prefer anchored whole-string match equivalent to Matcher.matches (see wikiPropertyValueField closed regex analog); require non-empty target after first-pipe split semantics consistent with WikiLinkMarkdown.splitInner. Update AUTHORED_ALIASES_MESSAGE in lockstep with backend. Leave frontmatterAliases.ts and wikidataTitleActions.ts unchanged. Extend authoredAliasesValidation.spec.ts with capability-named accept/reject cases; add a noteContentPropertyRows.spec.ts accept case for wiki-link alias items if not already covered by inheritance. Do not add recall UI or E2E in this plan.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/utils/authoredAliasesValidation.spec.ts tests/utils/noteContentPropertyRows.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - authoredAliasesValidation.ts accepts D-01 well-formed wiki-link list items and rejects malformed/embedded forms.
    - Frontend AUTHORED_ALIASES_MESSAGE text equals backend FrontmatterAliases.AUTHORED_ALIASES_MESSAGE.
    - Specs assert accept for [[Title]] / qualified / pipe forms and reject for bare open brackets and embedded wiki text.
    - Targeted Vitest files exit 0.
  </acceptance_criteria>
  <done>UI property editor preflight allows the same wiki-link overlap alias items the backend accepts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → note content YAML | Untrusted aliases list items cross AuthoredNoteContent / frontend preflight |
| Parse → derived indexes | Soft plain-only parse must not feed wiki-link strings into note_alias_index (later plans prove; this plan establishes choke point) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | FrontmatterAliases authored validation | high | mitigate | Whole-item wiki-link match only; malformed [[ cannot smuggle INVALID_ALIAS_CHARACTERS into plain aliases (D-04) |
| T-05-02 | Tampering | Soft from* / future index | high | mitigate | Keep fromNoteContent plain-only (D-02); never treat wiki-link items as plain alias text |
| T-05-03 | Information Disclosure | Dead overlap targets | low | accept | Dead targets are valid declarations (D-04); no new read of other users' notes in this plan |
| T-05-SC | Tampering | package installs | high | accept | No new npm/pip/cargo packages |

</threat_model>

<verification>
CURSOR_DEV=true nix develop -c pnpm backend:test_only
CURSOR_DEV=true nix develop -c pnpm -C frontend test tests/utils/authoredAliasesValidation.spec.ts tests/utils/noteContentPropertyRows.spec.ts
</verification>

<success_criteria>
- Wiki-link alias items are authorable via HTTP and UI validation (OVL-02).
- Plain-only accessors + overlap API segregate consumers at parse time (D-02, D-03).
- Malformed wiki-link items still fail authored validation (D-04).
- Stop-safe: if later plans stop, declarations are stored and safely ignored by plain-alias consumers still covered by existing behavior until 05-02/05-03 regressions land.
</success_criteria>

<output>
Create `.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-01-SUMMARY.md` when done
</output>
