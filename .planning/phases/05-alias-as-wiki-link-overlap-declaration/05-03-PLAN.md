---
phase: 05-alias-as-wiki-link-overlap-declaration
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/src/test/java/com/odde/doughnut/services/WikiLinkResolverYamlAndBodyIntegrationTest.java
  - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java
autonomous: true
requirements: [OVL-03]
must_haves:
  truths:
    - "OVL-03: wiki-resolve by plain alias still works; a note that only declares overlap via wiki-link aliases is not resolvable as an alias target for the inner title or raw token."
    - "OVL-03: cloze masking still masks plain aliases; overlap wiki-link items do not cloze-mask the overlap target title solely because of the wiki-link alias item."
    - "OVL-03: matchAnswer still accepts plain aliases; answering with overlap target title or raw [[…]] is not correct via alias."
    - "OVL-03: accidental-match alias leg ignores wiki-link alias items (typing an overlap target title does not accidental-match via the overlap declaration row)."
    - "Soft parse skips invalid; authored validation rejects malformed [[ (already in 05-01; this plan must not regress those consumers)."
  prohibitions:
    - "Must NOT wire MemoryTrackerService OVERLAP grading / AnsweredQuestion.overlap in this phase"
    - "Must NOT index wiki-link alias strings into note_alias_index"
    - "Must NOT change Flyway schema unless research proves necessary (prefer no migration)"
  artifacts:
    - path: "backend/src/test/java/com/odde/doughnut/services/WikiLinkResolverYamlAndBodyIntegrationTest.java"
      provides: "Wiki-resolve alias-target regressions for overlap wiki-link items"
      contains: "wiki_link"
    - path: "backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java"
      provides: "Cloze, matchAnswer, and accidental-match alias-leg OVL-03 regressions"
      contains: "overlap"
  key_links:
    - from: "WikiLinkResolver.aliasTargetCandidates"
      to: "NoteAliasIndex lookup keys"
      via: "plain-only index from 05-01/05-02"
      pattern: "findByNotebookNameAndAliasLookupKey"
    - from: "Note.createMaskedContentForRecall / matchAnswer"
      to: "FrontmatterAliases.fromNoteContent / matchesFromNoteContent"
      via: "plain-only accessors"
      pattern: "hideAliases"
    - from: "WikiLinkResolver.aliasAccidentalCandidates"
      to: "NoteAliasIndex"
      via: "plain-only lookup keys"
      pattern: "aliasAccidental"
---

## Phase Goal

**As a** note author, **I want to** declare overlap by putting well-formed wiki-link tokens in the aliases frontmatter list, **so that** overlapping notes are declared for later grading without breaking plain-alias wiki-resolve, search, or cloze masking.

<objective>
Prove remaining OVL-03 consumers: wiki-resolve alias targets, cloze masking, Note.matchAnswer, and accidental-match alias leg all ignore wiki-link overlap alias items while plain aliases keep today's behavior. Do not wire Phase 6 OVERLAP grading.

Purpose: Complete alias blast-radius regression gate after parse (05-01) and index/search (05-02) proofs.
Output: Capability-named extensions to WikiLinkResolverYamlAndBodyIntegrationTest and RecallPromptControllerTests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-CONTEXT.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-RESEARCH.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-PATTERNS.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-01-SUMMARY.md
</context>

<nyquist_validation>
Per 05-VALIDATION.md and RESEARCH consumer inventory:
- Extend WikiLinkResolverYamlAndBodyIntegrationTest + RecallPromptControllerTests (cloze, matchAnswer, accidental-match alias fixtures).
- Prefer no production edits to WikiLinkResolver / Note / MemoryTrackerService.
- Sampling: backend:test_only after each task / wave merge.
</nyquist_validation>

## Artifacts this phase produces

- Wiki-resolve alias-target regression coverage for overlap wiki-link items
- Cloze + matchAnswer + accidental-match alias-leg OVL-03 regressions
- Phase 5 Structure complete: declarations authorable and safely ignored by all plain-alias consumers

<tasks>

<task type="auto" tdd="true">
  <name>Wiki-resolve alias targets ignore overlap wiki-link alias items</name>
  <precondition>05-01 complete (plain-only fromNoteContent). Prefer 05-02 index proofs already green when available; if executing in parallel with 05-02, call noteAliasIndexService.refreshForNote in fixtures so resolve reads current rows.</precondition>
  <reversibility rating="reversible">Integration test expansion; resolver production code preferably untouched.</reversibility>
  <files>backend/src/test/java/com/odde/doughnut/services/WikiLinkResolverYamlAndBodyIntegrationTest.java</files>
  <read_first>
    - backend/src/test/java/com/odde/doughnut/services/WikiLinkResolverYamlAndBodyIntegrationTest.java (wikiLinkResolver_resolvesUnambiguousFrontmatterAliasInFocusNotebook)
    - backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java (aliasTargetCandidates)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-RESEARCH.md (wiki-resolve consumer row)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-PATTERNS.md (WikiLinkResolverYamlAndBodyIntegrationTest section)
  </read_first>
  <behavior>
    - Plain frontmatter alias still resolves (positive control unchanged).
    - Note whose aliases contain only "[[Other Note]]" (refresh index) is not an alias-resolution target for Other Note or the raw [[Other Note]] token from a linker note (OVL-03 / D-02).
    - Mixed list: plain alias still resolves; wiki-link item still does not create an alias target for its inner title.
  </behavior>
  <action>
    Extend WikiLinkResolverYamlAndBodyIntegrationTest with capability-named cases (OVL-03). Reuse MakeMe + noteAliasIndexService.refreshForNote pattern from the existing plain-alias resolve test. Assert resolveWikiLinksForCache / equivalent does not treat overlap wiki-link alias items as alias targets. Prefer no WikiLinkResolver production edits — safety comes from plain-only index rows. Do not implement OVERLAP grading.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - Integration test proves plain alias resolve still works.
    - Integration test proves wiki-link-only / mixed overlap items do not make the note resolvable via alias for the inner title or raw token.
    - WikiLinkResolver.java unchanged unless a proven bug requires a fix outside alias classification.
    - backend:test_only exits 0.
  </acceptance_criteria>
  <done>Wiki-resolve by alias remains correct for plain aliases and ignores overlap wiki-link declarations.</done>
</task>

<task type="auto" tdd="true">
  <name>Cloze, matchAnswer, and accidental-match alias leg ignore wiki-link overlap items</name>
  <precondition>05-01 complete. Accidental-match alias fixtures in RecallPromptControllerTests already exist for plain aliases (Phases 2–3).</precondition>
  <reversibility rating="reversible">Controller/boundary regressions only; no grading outcome changes.</reversibility>
  <files>backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java</files>
  <read_first>
    - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java (spellingQuestionMasksFrontmatterAliasesInStem, answerOneOfTheFrontmatterAliases, accidental-match alias fixtures ~932+)
    - backend/src/main/java/com/odde/doughnut/entities/Note.java (createMaskedContentForRecall, matchAnswer)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-RESEARCH.md (cloze / matchAnswer / AM alias leg rows; D-03 no grading)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-PATTERNS.md (RecallPromptControllerTests section)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-CONTEXT.md (D-02, D-03)
  </read_first>
  <behavior>
    - Cloze: note with plain alias color still masks color in stem; note with only "[[Other Note]]" in aliases does not mask Other Note in body text solely due to that alias item (OVL-03 / EDGE mixed: plain still masks, wiki-link does not add masking).
    - matchAnswer: plain alias still correct; answering Other Note or "[[Other Note]]" is not correct via alias when that string appears only as a wiki-link overlap item (OVL-03).
    - Accidental-match alias leg: a different note that declares aliases: ["[[ReviewedTitle]]"] (or similar overlap wiki-link) must not cause ACCIDENTAL_MATCH when the user types the reviewed title for an unrelated review — alias leg ignores wiki-link items (OVL-03 / EDGE). Plain-alias accidental-match positive path remains green.
    - AnswerOutcome stays free of OVERLAP wiring in MemoryTrackerService (Phase 6).
  </behavior>
  <action>
    Extend RecallPromptControllerTests with capability-named cloze, matchAnswer, and accidental-match alias-leg cases covering mixed and wiki-link-only aliases (OVL-03 / D-02). Follow existing MakeMe + refresh patterns from plain-alias fixtures. Prefer no production edits to Note.java or MemoryTrackerService. Explicitly leave answerSpelling OVERLAP / AnsweredQuestion.overlap unset (D-03 / Phase 6). Do not add frontend recall UI.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - RecallPromptControllerTests includes capability-named cloze proof that wiki-link overlap items do not drive alias cloze masking of the target title.
    - Includes matchAnswer proof that overlap target title / raw token is not correct via alias.
    - Includes accidental-match alias-leg proof that wiki-link alias items are ignored.
    - `rg -n 'overlapWikiLinkTokensFrom' backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java` exits 1 (no matches).
    - backend:test_only exits 0.
  </acceptance_criteria>
  <done>Cloze, matchAnswer, and accidental-match alias leg preserve plain-alias behavior and ignore overlap wiki-link items; Phase 5 Structure OVL-03 gate complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Note content → cloze / matchAnswer | Direct FrontmatterAliases plain-only accessors |
| Alias index → wiki-resolve / accidental-match | Index readers trust plain-only lookup keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Tampering | Cloze hideAliases / matchAnswer | medium | mitigate | Plain-only fromNoteContent / matchesFromNoteContent; regression tests for wiki-link items |
| T-05-07 | Tampering | Accidental-match alias leg | high | mitigate | Alias candidates from index only; prove wiki-link overlap items never create accidental-match via alias |
| T-05-08 | Elevation of Privilege | MemoryTrackerService grading | high | mitigate | Do not call overlap accessors or set OVERLAP outcome in this phase (D-03); grep gate in acceptance |
| T-05-SC | Tampering | package installs | high | accept | No new packages |

</threat_model>

<verification>
CURSOR_DEV=true nix develop -c pnpm backend:test_only
</verification>

<success_criteria>
- Wiki-resolve, cloze, matchAnswer, and accidental-match alias leg ignore wiki-link overlap items (OVL-03 success criteria 2 and 4 + EDGE).
- Plain-alias behavior unchanged.
- No Phase 6 OVERLAP grading wired.
- Phase 5 stop-safe: wiki-link aliases authorable (05-01) and safely ignored by all plain-alias consumers.
</success_criteria>

<output>
Create `.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-03-SUMMARY.md` when done
</output>
