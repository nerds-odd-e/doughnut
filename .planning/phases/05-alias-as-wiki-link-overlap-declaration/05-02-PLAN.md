---
phase: 05-alias-as-wiki-link-overlap-declaration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/src/test/java/com/odde/doughnut/services/NoteAliasIndexServiceTest.java
  - backend/src/test/java/com/odde/doughnut/controllers/SearchControllerAliasTests.java
  - backend/src/test/java/com/odde/doughnut/services/WikiTitleCacheServiceTest.java
autonomous: true
requirements: [OVL-03]
must_haves:
  truths:
    - "Mixed plain+wiki-link aliases list: plain items produce note_alias_index rows; wiki-link items produce zero index rows (D-02 / EDGE)."
    - "Empty aliases → no index rows; wiki-link-only aliases → no index rows while overlapWikiLinkTokensFrom* would still return tokens (EDGE)."
    - "OVL-03 search: wiki-link alias items are not searchable as alias text (neither raw token nor inner title as alias_lookup_key)."
    - "Plain aliases remain searchable/indexable after wiki-link overlap items are present on the same note."
  prohibitions:
    - "Must NOT wire MemoryTrackerService OVERLAP grading / AnsweredQuestion.overlap in this phase"
    - "Must NOT index wiki-link alias strings into note_alias_index"
    - "Must NOT change Flyway schema unless research proves necessary (prefer no migration)"
  artifacts:
    - path: "backend/src/test/java/com/odde/doughnut/services/NoteAliasIndexServiceTest.java"
      provides: "Refresh proofs that wiki-link overlap items never become alias index rows"
      contains: "indexes_only_plain_aliases"
    - path: "backend/src/test/java/com/odde/doughnut/controllers/SearchControllerAliasTests.java"
      provides: "Search regression that overlap wiki-link items are not alias search hits"
      contains: "wiki_link"
  key_links:
    - from: "NoteAliasIndexService.refreshForNote"
      to: "FrontmatterAliases.fromNoteContent"
      via: "unchanged service code; plain-only choke point from 05-01"
      pattern: "fromNoteContent"
    - from: "NoteAliasSearchService"
      to: "note_alias_index"
      via: "inherits plain-only rows"
      pattern: "alias_lookup_key"
---

## Phase Goal

**As a** note author, **I want to** declare overlap by putting well-formed wiki-link tokens in the aliases frontmatter list, **so that** overlapping notes are declared for later grading without breaking plain-alias wiki-resolve, search, or cloze masking.

<objective>
Prove OVL-03 for derived alias index and alias search: after 05-01 segregation, NoteAliasIndexService.refreshForNote indexes only plain aliases when wiki-link overlap items are present, and SearchController alias search does not surface notes via wiki-link tokens or their inner titles.

Purpose: Close the strongest blast-radius vector (index poisoning) before resolve/cloze expansions.
Output: Capability-named NoteAliasIndexServiceTest + SearchControllerAliasTests (+ optional WikiTitleCacheServiceTest awareness) with no production service changes unless a regression forces a fix at the FrontmatterAliases choke point.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-CONTEXT.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-RESEARCH.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-PATTERNS.md
@.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-01-SUMMARY.md
</context>

<nyquist_validation>
Per 05-VALIDATION.md:
- Extend NoteAliasIndexServiceTest and SearchControllerAliasTests in this plan.
- Prefer no production edits; if index still stores wiki-link strings, fix FrontmatterAliases plain filter (not a local filter inside NoteAliasIndexService — RESEARCH anti-pattern).
- Sampling: backend:test_only after each task / wave merge.
</nyquist_validation>

## Artifacts this phase produces

- Regression proofs that `note_alias_index` never stores wiki-link alias items
- Alias search remains plain-only when overlap declarations exist

<tasks>

<task type="auto" tdd="true">
  <name>Alias index refresh indexes plain aliases only when overlap wiki-links present</name>
  <precondition>05-01 complete: fromNoteContent returns plain aliases only; wiki-link items authorable.</precondition>
  <reversibility rating="reversible">Test-only expansion of existing refresh contract; production service preferably untouched.</reversibility>
  <files>backend/src/test/java/com/odde/doughnut/services/NoteAliasIndexServiceTest.java, backend/src/test/java/com/odde/doughnut/services/WikiTitleCacheServiceTest.java</files>
  <read_first>
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-RESEARCH.md (Consumer Inventory — derived index; Pitfall 1, 5, 6)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-PATTERNS.md (NoteAliasIndexServiceTest + refresh pattern)
    - backend/src/main/java/com/odde/doughnut/services/NoteAliasIndexService.java
    - backend/src/test/java/com/odde/doughnut/services/NoteAliasIndexServiceTest.java
    - backend/src/main/java/com/odde/doughnut/services/WikiTitleCacheService.java (refreshForNote orchestration)
    - backend/src/test/java/com/odde/doughnut/services/WikiTitleCacheServiceTest.java
  </read_first>
  <behavior>
    - Note with aliases color + "[[Other Note]]" + "[[Shared Notebook:Hue|display]]": after refreshForNote, rows are only plain aliases (e.g. color); no alias_display containing "[[" (D-02 / EDGE).
    - Wiki-link-only aliases list → zero NoteAliasIndex rows after refresh (EDGE).
    - Empty / absent aliases → zero rows (EDGE).
    - Refresh still replace-all: removing a plain alias deletes its prior row (existing behavior preserved).
    - Optional WikiTitleCacheServiceTest: after refreshForNote, plain alias index still populated; do not treat resolved wiki-title-cache edges from aliases-list [[…]] as OVL-03 failures (RESEARCH side-channel).
  </behavior>
  <action>
    Extend NoteAliasIndexServiceTest with capability-named cases proving plain-only indexing under mixed and wiki-link-only aliases (OVL-03 / D-02). Call existing noteAliasIndexService.refreshForNote (or WikiTitleCacheService.refreshForNote if asserting orchestration). Prefer zero production edits to NoteAliasIndexService — do not add a local [[ filter there. If a test fails because wiki-link strings were indexed, fix FrontmatterAliases soft plain filter from 05-01 rather than forking classification into the index service. Do not add Flyway/type columns. Optionally add a thin WikiTitleCacheServiceTest awareness assertion that plain alias rows still appear after refreshForNote; do not exclude aliases from wiki extraction.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - NoteAliasIndexServiceTest includes a capability-named case for mixed plain+wiki-link content asserting only plain alias_display values and zero rows whose display contains "[[".
    - Wiki-link-only and empty/absent aliases cases assert zero index rows.
    - NoteAliasIndexService.java production logic unchanged unless a proven bug requires a one-line fix — classification stays in FrontmatterAliases.
    - No new Flyway migration files in the plan diff.
    - backend:test_only exits 0.
  </acceptance_criteria>
  <done>Derived alias index never stores wiki-link overlap declarations; plain aliases still index.</done>
</task>

<task type="auto" tdd="true">
  <name>Alias search ignores wiki-link overlap alias items</name>
  <precondition>Index refresh plain-only behavior proven in Task 1.</precondition>
  <reversibility rating="reversible">Controller regression tests only.</reversibility>
  <files>backend/src/test/java/com/odde/doughnut/controllers/SearchControllerAliasTests.java</files>
  <read_first>
    - backend/src/test/java/com/odde/doughnut/controllers/SearchControllerAliasTests.java
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-RESEARCH.md (NoteAliasSearchService consumer row)
    - .planning/phases/05-alias-as-wiki-link-overlap-declaration/05-PATTERNS.md (SearchControllerAliasTests fixture helpers)
    - backend/src/main/java/com/odde/doughnut/services/search/NoteAliasSearchService.java
  </read_first>
  <behavior>
    - Note whose aliases include only or mixed "[[Other Note]]" (and optionally a distinct plain alias) is not returned when searching the raw wiki-link token or the inner title Other Note as an alias query (OVL-03).
    - Plain alias on the same or another note remains searchable as today.
  </behavior>
  <action>
    Extend SearchControllerAliasTests using existing createAliasNote / aliasMarkdown helpers (PATTERNS). Add capability-named regressions for wiki-link-only and mixed lists: after refresh, search must not treat the wiki-link token or its inner title as an alias hit. Keep plain-alias positive control. Prefer no production search-service changes; inherit safety from plain-only index.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - SearchControllerAliasTests asserts wiki-link alias items do not produce alias search hits for token or inner title.
    - Plain alias search positive path still passes.
    - backend:test_only exits 0.
  </acceptance_criteria>
  <done>Alias search behavior unchanged for plain aliases; overlap wiki-link items are invisible to alias search.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored content → note_alias_index | Refresh rebuilds derived rows from soft-parsed plain aliases only |
| note_alias_index → search API | Search trusts index lookup keys; poisoned rows would elevate discoverability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | NoteAliasIndexService.refreshForNote | high | mitigate | Rely on FrontmatterAliases plain-only fromNoteContent; prove zero [[ rows in tests (D-02) |
| T-05-05 | Information Disclosure | NoteAliasSearchService | medium | mitigate | Regression: inner titles of overlap wiki-links are not alias search hits |
| T-05-SC | Tampering | package installs | high | accept | No new packages |

</threat_model>

<verification>
CURSOR_DEV=true nix develop -c pnpm backend:test_only
</verification>

<success_criteria>
- note_alias_index contains only plain aliases when overlap wiki-links are authored (OVL-03).
- Alias search does not surface wiki-link overlap items (OVL-03 success criterion 3).
- No Flyway / schema change.
</success_criteria>

<output>
Create `.planning/phases/05-alias-as-wiki-link-overlap-declaration/05-02-SUMMARY.md` when done
</output>
