---
phase: 04-enforce-safe-blocking-boundaries
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .cursor/rules/frontend-api.mdc
autonomous: true
requirements:
  - COHE-02
must_haves:
  truths:
    - statement: "Every whole-UI blocker and long-running note-refinement AI request has an explicit cancelable / intentionally noncancelable / nonblocking classification in the living frontend-api contract (D-05, D-10)."
      verification: "frontend-api.mdc contains inventory table covering RESEARCH seed sites including create, layout, preview, remove, assimilate, book-layout, delete, relationship, attach-book, and nonblocking exports."
    - statement: "In-scope cancelable adopters remain exactly layout generation and extraction-preview generation using the shared cancelable overload (D-06)."
      verification: "Inventory allowlist names only those two; layout + preview cancel Vitest suites still green."
    - statement: "All other whole-UI blockers are labeled intentionally noncancelable for v1 with no Cancel migration (D-07); thin-bar/export helpers are nonblocking (D-08)."
      verification: "Inventory rows match RESEARCH classifications; ADPT-01 candidates noted without opt-in."
  artifacts:
    - path: .cursor/rules/frontend-api.mdc
      provides: "COHE-02 classification inventory + cancelable allowlist + intentional noncancelable examples for agents."
  key_links:
    - from: frontend-api.mdc inventory
      to: NoteRefinement layout + preview call sites
      via: "Documented allowlist matches shared { blockUi: true, cancelable: true } overload."
    - from: frontend-api.mdc create-note row
      to: REFN-05 noncancelable create
      via: "Intentionally noncancelable classification documents why Cancel stays absent."
---

## Phase Goal

**As a** note author finishing Refine note extraction, **I want to** see the shared create-note blocker without Cancel while the mutation runs, **so that** I am never offered unsafe client-only cancellation of a transactional write.

## Artifacts this phase produces

| File / symbol | Role |
|---|---|
| `.cursor/rules/frontend-api.mdc` inventory | Living COHE-02 classification table + allowlist |

<objective>
Persist the full cancellation classification inventory in `.cursor/rules/frontend-api.mdc` so every whole-UI blocker and note-refinement AI request is explicitly cancelable, intentionally noncancelable, or nonblocking (D-05–D-10), while confirming layout + preview remain the only cancelable adopters (D-06).

Purpose: Wave 2 vertical slice for COHE-02 cohesion — close the milestone map in the living agent contract, not a planning diary.
Output: Complete frontend-api inventory; layout + preview cancel suites still green; no ADPT-01 Cancel migrations.
</objective>

<execution_context>
@/Users/terryyin/.cursor/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-01-PLAN.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-CONTEXT.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-RESEARCH.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-UI-SPEC.md
@.cursor/rules/frontend-api.mdc
@frontend/src/components/recall/NoteRefinement.vue

<interfaces>
Seed the inventory from 04-RESEARCH.md Verified Call-Site Inventory. Keep existing Cancelable blocking calls subsection; extend it with a classification table (exact wording/layout is Claude's Discretion per CONTEXT). Do not migrate any intentionally noncancelable site to Cancel. Do not invent cancelable runWithBlockingApiLoading.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist COHE-02 classification inventory in frontend-api.mdc</name>
  <files>.cursor/rules/frontend-api.mdc</files>
  <read_first>
    - .cursor/rules/frontend-api.mdc
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-CONTEXT.md
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-RESEARCH.md
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-UI-SPEC.md
  </read_first>
  <action>
    Extend `.cursor/rules/frontend-api.mdc` with the full COHE-02 classification inventory per D-05 and D-10 (living contract home — not a planning-only diary). Include: (1) cancelable allowlist — only layout generation (`AI is generating layout...`) and extraction-preview generation (`AI is generating preview...`) using the shared cancelable overload (D-06); (2) intentionally noncancelable whole-UI blockers from RESEARCH seed — create-note (`AI is creating note...`), remove-content, assimilate, load next assimilation note, book-layout mutations, book-layout AI suggest (note ADPT-01 candidate), book-layout apply, note delete/reduce, relationship finalize, attach-book upload (D-07); (3) nonblocking note-refinement helpers — export extract/layout request fetches and thin-bar loads without blockUi (D-08). Keep existing forbids: no cancelable mutations, no cancelable runWithBlockingApiLoading, client-only abort, no parallel AbortError-name helpers. Document create-note Cancel absence as intentional safety tied to REFN-05 / D-01 / D-04. Re-verify with `rg 'blockUi: true|runWithBlockingApiLoading'` under frontend/src that no whole-UI site is missing from the table; add any newly found site as intentionally noncancelable rather than silently omitting it.
  </action>
  <acceptance_criteria>
    - frontend-api.mdc has an inventory/classification section with cancelable, intentionally noncancelable, and nonblocking categories.
    - Cancelable allowlist names only layout + extraction-preview.
    - Create-note is explicitly intentionally noncancelable with message AI is creating note... and safety rationale.
    - Book-layout AI suggest is intentionally noncancelable and marked ADPT-01 candidate (not opted in).
    - Existing cancelable overload guidance and mutation forbids remain present.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c bash -lc 'set -euo pipefail; RULE=.cursor/rules/frontend-api.mdc; rg -n -i "intentionally noncancelable|nonblocking|allowlist|classification|inventory" "$RULE"; rg -n "AI is generating layout|AI is generating preview|AI is creating note" "$RULE"; rg -n -i "assimilat|book layout|Deleting note|relationship|Uploading book|export" "$RULE"; rg -n -i "ADPT-01|mutation|runWithBlockingApiLoading" "$RULE"'</automated>
  </verify>
  <done>Living frontend-api inventory satisfies D-05–D-10 without migrating ADPT-01 sites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-verify layout and preview cancelable adopters stay green</name>
  <files>.cursor/rules/frontend-api.mdc</files>
  <read_first>
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-RESEARCH.md
    - frontend/tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts
    - frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts
    - frontend/src/components/recall/NoteRefinement.vue
  </read_first>
  <behavior>
    - Layout pending still exposes Cancel with AI is generating layout... (D-06).
    - Preview pending still exposes Cancel with AI is generating preview... (D-06).
    - Docs inventory matches the two product cancelable true sites.
  </behavior>
  <action>
    Run the Phase 2 layout cancel suite and Phase 3 extraction-preview cancel core suite to prove D-06 adopters remain on the shared cancelable contract after the inventory docs land. Confirm under frontend/src that cancelable true appears only in NoteRefinement.vue (layout + preview) and clientSetup.ts (type/options). Do not change product cancel UX. Do not add Cancel to create or other inventory noncancelable rows.
  </action>
  <acceptance_criteria>
    - NoteRefinement.layoutGeneration.cancel.spec.ts passes.
    - NoteRefinement.extractionPreview.cancel.spec.ts passes.
    - Under frontend/src, cancelable true only in NoteRefinement.vue and clientSetup.ts.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c bash -lc 'set -euo pipefail; pnpm frontend:test tests/components/recall/NoteRefinement.layoutGeneration.cancel.spec.ts tests/components/recall/NoteRefinement.extractionPreview.cancel.spec.ts; MATCHES=$(rg -n "cancelable:\\s*true" frontend/src); echo "$MATCHES"; echo "$MATCHES" | rg -q "NoteRefinement\\.vue"; echo "$MATCHES" | rg -q "clientSetup\\.ts"; ! echo "$MATCHES" | rg -v "NoteRefinement\\.vue|clientSetup\\.ts" | rg -q .'</automated>
  </verify>
  <done>Inventory docs and cancelable adopters remain aligned and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Agent rules → future callers | Inventory must not encourage Cancel on transactional mutations. |
| Docs allowlist → product call sites | Only layout + preview may stay cancelable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-04 | Spoofing (misleading safety UX) | frontend-api.mdc create-note row | high | mitigate | Classify create as intentionally noncancelable with explicit safety rationale (D-01, D-04, D-10). |
| T-04-05 | Tampering | Premature ADPT of book-layout AI cancel | medium | mitigate | Mark book-layout AI suggest intentionally noncancelable + ADPT-01 candidate; do not opt in (D-07). |
| T-04-06 | Elevation of privilege over shared contract | Parallel cancel helpers in docs | medium | mitigate | Keep forbid of AbortError-name matching and cancelable runWithBlockingApiLoading (D-09). |
| T-04-SC | Tampering | Package installs | low | accept | No new packages. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| REQ | COHE-02 | Classify every blocker; safe ops use shared solution | 04-02 | COVERED |
| CONTEXT | D-05 | Explicit inventory of all blockers | 04-02 Task 1 | COVERED |
| CONTEXT | D-06 | Only layout + preview cancelable | 04-02 Tasks 1–2 | COVERED |
| CONTEXT | D-07 | Others intentionally noncancelable; no ADPT migrate | 04-02 Task 1 | COVERED |
| CONTEXT | D-08 | Thin-bar/export nonblocking | 04-02 Task 1 | COVERED |
| CONTEXT | D-10 | Persist in frontend-api.mdc | 04-02 Task 1 | COVERED |
| RESEARCH | Verified Call-Site Inventory | Seed table | 04-02 Task 1 | COVERED |
| RESEARCH | Wave 0 inventory gap | frontend-api extend | 04-02 | COVERED |
</source_audit>

<verification>
frontend-api.mdc inventory complete. Layout + preview cancel suites green. rg exclusivity for cancelable true holds. No Cancel migrations outside allowlist.
</verification>

<success_criteria>
- COHE-02 inventory lives in frontend-api.mdc with all three classifications.
- Layout + preview remain the only cancelable product adopters and stay green.
</success_criteria>

<output>
Create `.planning/phases/04-enforce-safe-blocking-boundaries/04-02-SUMMARY.md` when done.
</output>
