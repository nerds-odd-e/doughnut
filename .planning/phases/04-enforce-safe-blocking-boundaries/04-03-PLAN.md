---
phase: 04-enforce-safe-blocking-boundaries
plan: 03
type: tdd
wave: 3
depends_on:
  - 04-02
files_modified:
  - frontend/tests/managedApi/cancelableAllowlist.spec.ts
  - .cursor/rules/frontend-api.mdc
autonomous: true
requirements:
  - COHE-02
  - REFN-05
must_haves:
  truths:
    - statement: "Production cancelable true under frontend/src appears only at NoteRefinement layout + preview and the clientSetup type/options literal (D-06, D-11)."
      verification: "Automated Vitest allowlist guard fails if any other production file gains cancelable true."
    - statement: "No in-scope caller reimplements AbortController ownership, AbortError-name matching, loading-state pop-by-id, or cancellation toast suppression outside managedApi (D-09)."
      verification: "Automated audit asserts AbortController construction only under frontend/src/managedApi/; related duplication checks documented/green."
    - statement: "Create-note cannot silently gain Cancel via a new cancelable true site (REFN-05 reinforcement)."
      verification: "Allowlist guard + REFN-05 Cancel-absent edges remain green together."
  artifacts:
    - path: frontend/tests/managedApi/cancelableAllowlist.spec.ts
      provides: "Capability-named automated allowlist + managedApi-only abort ownership guard."
    - path: .cursor/rules/frontend-api.mdc
      provides: "Optional pointer that allowlist is test-gated (if needed for agent discoverability)."
  key_links:
    - from: cancelableAllowlist.spec.ts
      to: NoteRefinement.vue + clientSetup.ts
      via: "Static scan of frontend/src sources for cancelable true exclusivity."
    - from: cancelableAllowlist.spec.ts
      to: frontend/src/managedApi/
      via: "Abort ownership must remain only inside managedApi (D-09)."
---

## Phase Goal

**As a** note author finishing Refine note extraction, **I want to** see the shared create-note blocker without Cancel while the mutation runs, **so that** I am never offered unsafe client-only cancellation of a transactional write.

## Artifacts this phase produces

| File / symbol | Role |
|---|---|
| `cancelableAllowlist.spec.ts` | Automated COHE-02 / D-11 allowlist + D-09 duplication gate |
| frontend-api.mdc (minor) | Point agents at the automated allowlist if useful |

<objective>
Add a lightweight automated guard so production `cancelable: true` stays only on the documented note-refinement allowlist (+ clientSetup type), and re-verify that abort / cancel-cleanup mechanics are not duplicated outside `frontend/src/managedApi/` (D-09, D-11) — reinforcing REFN-05 against accidental create cancelable.

Purpose: Wave 3 vertical slice — turn the cohesion map into a regression gate so the milestone cannot silently offer Cancel on unsafe sites.
Output: Green allowlist Vitest; D-09 audit green; REFN-05 edges still green; no new packages.
</objective>

<execution_context>
@/Users/terryyin/.cursor/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-02-PLAN.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-CONTEXT.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-RESEARCH.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-VALIDATION.md
@.cursor/rules/frontend-api.mdc
@frontend/src/managedApi/clientSetup.ts
@frontend/tests/managedApi/clientSetup.spec.ts

<interfaces>
Prefer a small Vitest that reads `frontend/src` sources (fs) and asserts allowlist exclusivity — zero new deps (RESEARCH). Capability-name the file `cancelableAllowlist.spec.ts` (no phase numbers). Allowed cancelable true hits: `NoteRefinement.vue` (exactly two product sites) and `clientSetup.ts` (type/options). Abort ownership: `new AbortController` only under `frontend/src/managedApi/`. Do not invent product Abort helpers in components.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cancelable allowlist Vitest guard</name>
  <files>frontend/tests/managedApi/cancelableAllowlist.spec.ts</files>
  <read_first>
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-CONTEXT.md
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-RESEARCH.md
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-VALIDATION.md
    - frontend/src/managedApi/clientSetup.ts
    - frontend/src/components/recall/NoteRefinement.vue
    - frontend/tests/managedApi/clientSetup.spec.ts
  </read_first>
  <behavior>
    - Scanning frontend/src for cancelable true yields only NoteRefinement.vue and clientSetup.ts (D-06, D-11).
    - NoteRefinement.vue contains exactly two cancelable true product opt-ins (layout + preview).
    - Guard fails if a third product file or create path gains cancelable true (REFN-05 reinforcement).
  </behavior>
  <action>
    Create capability-named `frontend/tests/managedApi/cancelableAllowlist.spec.ts`. Implement a lightweight source scan (Node fs / Vitest) over `frontend/src` `*.ts` / `*.vue` that finds `cancelable: true` (allow flexible whitespace). Assert: every matching file path is only `NoteRefinement.vue` or `clientSetup.ts`; NoteRefinement has exactly two matches; clientSetup has at least one (type/options). Write the test first and confirm it passes against current codebase (research already verified exclusivity). Do not add npm packages. Do not change product cancelable sites. Keep the file under the 250-line gate.
  </action>
  <acceptance_criteria>
    - cancelableAllowlist.spec.ts exists under frontend/tests/managedApi/.
    - Spec passes and encodes allowlist: NoteRefinement.vue + clientSetup.ts only.
    - NoteRefinement cancelable true count is exactly 2.
    - No phase numbers in the test filename.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/managedApi/cancelableAllowlist.spec.ts</automated>
  </verify>
  <done>Automated allowlist guard locks D-06 / D-11 cancelable exclusivity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate managedApi-only abort ownership and re-verify REFN-05</name>
  <files>frontend/tests/managedApi/cancelableAllowlist.spec.ts, .cursor/rules/frontend-api.mdc</files>
  <read_first>
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-CONTEXT.md
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-RESEARCH.md
    - frontend/tests/managedApi/cancelableAllowlist.spec.ts
    - .cursor/rules/frontend-api.mdc
    - frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.edges.spec.ts
  </read_first>
  <behavior>
    - new AbortController appears only under frontend/src/managedApi/ (D-09).
    - REFN-05 create Cancel-absent edges remain green alongside the allowlist guard.
    - frontend-api.mdc optionally notes that allowlist exclusivity is test-gated.
  </behavior>
  <action>
    Extend the allowlist spec (or a sibling assertion in the same file) to scan `frontend/src` for `new AbortController` and assert every hit lives under `frontend/src/managedApi/` (D-09). If any duplication is found outside managedApi, remove the parallel helper and keep ownership in clientSetup — do not add a second abort owner. Optionally add one short sentence to frontend-api.mdc that production cancelable true exclusivity is enforced by the managedApi allowlist Vitest (discoverability). Re-run REFN-05 create Cancel-absent edges to prove create still cannot show Cancel. Do not migrate ADPT-01 sites. Do not invent cancelable create.
  </action>
  <acceptance_criteria>
    - Allowlist spec asserts AbortController construction only under managedApi.
    - cancelableAllowlist.spec.ts and REFN-05 edges spec both pass.
    - If frontend-api.mdc is touched, inventory from Plan 02 remains intact and mentions allowlist gating or exclusivity.
    - Under frontend/src, no new cancelable true files appear.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c bash -lc 'set -euo pipefail; pnpm frontend:test tests/managedApi/cancelableAllowlist.spec.ts tests/components/recall/NoteRefinement.extractionPreview.cancel.edges.spec.ts; rg -n "new AbortController" frontend/src --glob "*.{ts,vue}"; ! rg -n "new AbortController" frontend/src --glob "*.{ts,vue}" | rg -v "managedApi/" | rg -q .'</automated>
  </verify>
  <done>D-09 duplication audit is automated; REFN-05 Cancel-absent remains green; allowlist prevents unsafe Cancel regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Future PR adding cancelable true | Allowlist guard must fail before misleading Cancel ships on mutations. |
| Caller-local abort helpers | Abort ownership must stay in managedApi to avoid silent cancel races. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Spoofing (misleading safety UX) | Accidental Cancel on create | high | mitigate | Allowlist blocks cancelable true outside layout/preview; REFN-05 edges re-verified (D-04, D-11). |
| T-04-07 | Tampering | Caller-local AbortController | high | mitigate | Automated scan requires AbortController only under managedApi (D-09). |
| T-04-08 | Denial of Service / incoherent state | Duplicated cancel-toast suppression | medium | mitigate | D-09 audit; keep cancellation silence inside managedApi only. |
| T-04-SC | Tampering | Package installs | low | accept | No new packages; Vitest fs scan only. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| REQ | COHE-02 | Safe ops use shared solution; no duplicated abort logic | 04-03 | COVERED |
| REQ | REFN-05 | Reinforce create Cancel-absent against regression | 04-03 Task 2 | COVERED |
| CONTEXT | D-09 | No abort/cleanup duplication outside managedApi | 04-03 Task 2 | COVERED |
| CONTEXT | D-11 | Optional lightweight allowlist guard | 04-03 Task 1 | COVERED |
| RESEARCH | Allowlist automation medium | Prefer Vitest over manual rg alone | 04-03 | COVERED |
| RESEARCH | Duplication audit already clean | Re-verify + automate | 04-03 | COVERED |
| GOAL | — | No unsafe Cancel; consistent classification | 04-01–04-03 | COVERED |
</source_audit>

<verification>
Allowlist Vitest green. AbortController only under managedApi. REFN-05 edges green. Layout/preview exclusivity still holds. No new network surface.
</verification>

<success_criteria>
- Automated allowlist prevents cancelable true outside NoteRefinement + clientSetup.
- D-09 abort ownership stays in managedApi.
- Create-note Cancel-absent remains proven.
</success_criteria>

<output>
Create `.planning/phases/04-enforce-safe-blocking-boundaries/04-03-SUMMARY.md` when done.
</output>
