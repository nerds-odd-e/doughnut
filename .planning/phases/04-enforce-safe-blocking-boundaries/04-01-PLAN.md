---
phase: 04-enforce-safe-blocking-boundaries
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.edges.spec.ts
  - frontend/tests/components/recall/NoteRefinement.extractNote.spec.ts
autonomous: true
requirements:
  - REFN-05
must_haves:
  truths:
    - statement: "While final extracted-note creation is pending, the shared blocking spinner shows AI is creating note... and does not expose Cancel (REFN-05 / D-01)."
      verification: "Vitest create-pending case asserts loadingModalMask, message text, and Cancel absent from document.body."
    - statement: "Successful final creation still completes once and navigates via the existing focusNoteRealm / routerReplace path (D-02)."
      verification: "NoteRefinement.extractNote.spec.ts create happy-path still expects routerReplace with note location."
    - statement: "createExtractedNote is not opted into cancelable (D-04); product path remains noncancelable runWithBlockingApiLoading (or equivalent noncancelable blockUi)."
      verification: "No product cancelable opt-in on create; Cancel-absent case remains green; createExtractedNote body unchanged unless Discretion flatten preserves observables."
  artifacts:
    - path: frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.edges.spec.ts
      provides: "Explicit REFN-05-named create-note Cancel-absent proof (promoted from Phase 3 D-10 edge)."
    - path: frontend/tests/components/recall/NoteRefinement.extractNote.spec.ts
      provides: "Unchanged success-navigation regression for one-shot create."
  key_links:
    - from: Create note button on extraction preview
      to: LoadingModal without Cancel
      via: "runWithBlockingApiLoading(..., AI is creating note...) with no cancelControl on selected blocker."
    - from: Successful createExtractedNote
      to: focusNoteRealm / routerReplace
      via: "Existing post-success navigation inside the noncancelable composite (D-02)."
---

## Phase Goal

**As a** note author finishing Refine note extraction, **I want to** see the shared create-note blocker without Cancel while the mutation runs, **so that** I am never offered unsafe client-only cancellation of a transactional write.

## Artifacts this phase produces

| File / symbol | Role |
|---|---|
| REFN-05 create Cancel-absent case | Promoted proof that pending create shows message without Cancel |
| extractNote create happy-path | Regression that success still navigates once |

<objective>
Promote and strengthen the existing create-note Cancel-absent edge into the explicit REFN-05 proof, and keep the create success navigation path green — without inventing cancelable create or changing product cancel UX (D-01–D-04).

Purpose: Wave 1 vertical slice for REFN-05 enforcement. Create behavior is already correct after Phase 3; this plan makes the safety boundary the named, durable proof and confirms success navigation.
Output: REFN-05-named green Cancel-absent coverage + green extractNote create success path; no cancelable createExtractedNote.
</objective>

<execution_context>
@/Users/terryyin/.cursor/gsd-core/workflows/execute-plan.md
@/Users/terryyin/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-CONTEXT.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-RESEARCH.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-VALIDATION.md
@.planning/phases/04-enforce-safe-blocking-boundaries/04-UI-SPEC.md
@.planning/phases/03-cancel-extraction-preview-generation/03-03-SUMMARY.md
@.cursor/rules/frontend-api.mdc
@frontend/src/components/recall/NoteRefinement.vue
@frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.edges.spec.ts
@frontend/tests/components/recall/NoteRefinement.extractNote.spec.ts

<interfaces>
Reuse `createDeferredGate`, `loadingModalMask`, `mountNoteRefinementReady`, `openExtractionPreview`, `clickCreateNoteFromExtractionPreview`, and `sampleExtractionPreview` already used by the create-note edge. Do not invent a cancelable create path. Leave `createExtractedNote` on noncancelable `runWithBlockingApiLoading` unless post-change-refactor finds a zero-risk Discretion flatten that keeps Cancel absent and success navigation identical (prefer leave as-is per RESEARCH).
</interfaces>

<planner_assumptions>
- A1: Existing create-note Cancel-absent edge is already green; promotion is rename/strengthen, not a product rewrite (D-03).
- A2: No Cypress E2E required for REFN-05 if Vitest browser coverage remains thorough (D-11).
- A3: Capability naming only — no phase numbers in product/test filenames (planning.mdc).
</planner_assumptions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote create-note Cancel-absent to REFN-05 proof</name>
  <files>frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.edges.spec.ts</files>
  <read_first>
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-CONTEXT.md
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-UI-SPEC.md
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-RESEARCH.md
    - frontend/tests/components/recall/NoteRefinement.extractionPreview.cancel.edges.spec.ts
    - frontend/src/components/recall/NoteRefinement.vue
  </read_first>
  <behavior>
    - Pending create after Create note: loadingModalMask present; body contains AI is creating note...; body does not contain Cancel (REFN-05 / D-01 / D-03).
    - After resolve of deferred createExtractedNote, blocker clears (mask null) without introducing Cancel mid-flight.
    - Do not assert retry-after-abort affordances for create (D-02 forbids).
  </behavior>
  <action>
    Promote the existing create-note Cancel-absent edge into the explicit REFN-05 proof per D-03: rename the it-title/comment from Phase-3 D-10 wording to REFN-05 (and D-01) so the requirement id is obvious to future readers. Keep the deferred `createExtractedNote` gate pattern, `openExtractionPreview` + Create note click, and assertions that the shared blocker shows `AI is creating note...` with Cancel absent. Optionally strengthen observability only if needed (e.g. assert mask present before Cancel-absent check) without changing product Vue. Do not opt create into cancelable. Do not migrate other ADPT-01 sites. Do not add a new phase-numbered test filename — stay in the capability-named edges file (or a capability-named create-boundary sibling if the edges file would exceed the 250-line gate; prefer in-place rename).
  </action>
  <acceptance_criteria>
    - Spec case title or description cites REFN-05 (and intentional noncancelable create safety).
    - Pending create: message AI is creating note... present; Cancel string absent from document.body while mask is visible.
    - After gate resolve + flush, loading modal mask is cleared.
    - No frontend/src product change required for this task unless a pre-existing failure forces a noncancelable-only fix.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/recall/NoteRefinement.extractionPreview.cancel.edges.spec.ts</automated>
  </verify>
  <done>REFN-05 Cancel-absent proof is named and green without inventing cancelable create.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep create success navigation green</name>
  <files>frontend/tests/components/recall/NoteRefinement.extractNote.spec.ts, frontend/src/components/recall/NoteRefinement.vue</files>
  <read_first>
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-CONTEXT.md
    - .planning/phases/04-enforce-safe-blocking-boundaries/04-RESEARCH.md
    - frontend/tests/components/recall/NoteRefinement.extractNote.spec.ts
    - frontend/src/components/recall/NoteRefinement.vue
  </read_first>
  <behavior>
    - Successful create still navigates once via existing routerReplace / note location (D-02).
    - createExtractedNote remains noncancelable — no cancelable true on that call site (D-04).
  </behavior>
  <action>
    Re-run `NoteRefinement.extractNote.spec.ts` and confirm the create happy-path (`creates a note from the preview and navigates to the new note`) still expects `routerReplace` with the note show location. Confirm `createExtractedNote` in `NoteRefinement.vue` still uses noncancelable `runWithBlockingApiLoading` with message `AI is creating note...` and does not pass cancelable opt-in (D-04). Prefer leave the composite helper as-is (RESEARCH recommendation); only flatten to a single noncancelable `apiCallWithLoading` + `{ blockUi: true, message }` under Claude's Discretion if that preserves identical Cancel-absent and navigation observables with no behavior change. Do not invent Cancel, retry-after-abort, or server-stop claims.
  </action>
  <acceptance_criteria>
    - extractNote create happy-path still passes with routerReplace called for the new note.
    - createExtractedNote call site has no cancelable true opt-in.
    - REFN-05 edges case from Task 1 still passes after any Discretion touch.
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c bash -lc 'set -euo pipefail; pnpm frontend:test tests/components/recall/NoteRefinement.extractNote.spec.ts tests/components/recall/NoteRefinement.extractionPreview.cancel.edges.spec.ts; rg -n "createExtractedNote|AI is creating note" frontend/src/components/recall/NoteRefinement.vue; ! rg -n "cancelable:\\s*true" frontend/src/components/recall/NoteRefinement.vue | rg -q "createExtractedNote"'</automated>
  </verify>
  <done>REFN-05 proof + unchanged success navigation; create stays intentionally noncancelable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Create-note UI → transactional mutation | Client abort would not roll back server commit; Cancel must stay absent. |
| LoadingModal Cancel projection | Only selected blocker cancelControl may expose Cancel — create must not supply one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Spoofing (misleading safety UX) | createExtractedNote / LoadingModal | high | mitigate | Keep Cancel absent while AI is creating note...; REFN-05 Vitest proves absence (D-01, D-04). |
| T-04-02 | Tampering / Integrity | Accidental cancelable create | high | mitigate | Do not opt create into cancelable; leave runWithBlockingApiLoading noncancelable; success path unchanged (D-02, D-04). |
| T-04-03 | Information Disclosure | Error/toast on aborted create | low | accept | Cancel not offered; ordinary create errors keep existing createError banner — out of cancel scope. |
| T-04-SC | Tampering | Package installs | low | accept | No new packages in this plan. |
</threat_model>

<source_audit>
| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Consistent blocker; no unsafe Cancel on transactional creation | 04-01 | COVERED |
| REQ | REFN-05 | Shared blocker without Cancel while create pending | 04-01 | COVERED |
| CONTEXT | D-01 | Message + Cancel absent | 04-01 Task 1 | COVERED |
| CONTEXT | D-02 | Success focusNoteRealm unchanged | 04-01 Task 2 | COVERED |
| CONTEXT | D-03 | Promote Phase 3 Cancel-absent as REFN-05 proof | 04-01 Task 1 | COVERED |
| CONTEXT | D-04 | Do not opt create into cancelable | 04-01 Tasks 1–2 | COVERED |
| RESEARCH | Pitfall 1 | Do not rewrite create product path | 04-01 | COVERED |
| UI-SPEC | Create-note ENFORCE | Cancel absent chrome | 04-01 | COVERED |
</source_audit>

<verification>
REFN-05 Cancel-absent edges green. extractNote create success green. createExtractedNote remains noncancelable. No ADPT-01 / SERV scope.
</verification>

<success_criteria>
- Pending create shows AI is creating note... without Cancel.
- Successful create still navigates once.
- No cancelable createExtractedNote.
</success_criteria>

<output>
Create `.planning/phases/04-enforce-safe-blocking-boundaries/04-01-SUMMARY.md` when done.
</output>
