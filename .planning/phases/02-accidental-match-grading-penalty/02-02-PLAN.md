---
phase: 02-accidental-match-grading-penalty
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - backend/src/main/java/com/odde/doughnut/entities/repositories/NoteAliasIndexRepository.java
  - backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java
  - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java
autonomous: true
requirements: [AM-01, AM-02]
must_haves:
  truths:
    - "A spelling answer that is wrong for the reviewed note but exactly matches the ALIAS (frontmatter alias, normalized via FrontmatterAliases.normalizedLookupKey) of a different readable note — when no readable note's TITLE matches — is graded as an accidental match via the alias leg: Answer.correct=false, Answer.outcome=ACCIDENTAL_MATCH, Answer.matchedNoteId=<the alias-bearing note's id>."
    - "The accidental-match lookup prefers a title match over an alias match (title-then-alias fallback, mirroring WikiLinkResolver.noteCandidates 123-130): if a readable title match exists, the alias leg is not consulted."
    - "Near the forgetting-curve floor, an accidental match does NOT push the index below DEFAULT_FORGETTING_CURVE_INDEX (100.0f): a tracker whose index is already at 100.0f stays at 100.0f after an accidental match (the partialFail() add() clamp preserves the 'lighter than wrong' invariant at the floor — Pitfall 1)."
    - "An accidental match still counts toward the wrong-answer re-assimilation threshold: Answer.correct=false is counted by RecallPromptRepository.countWrongAnswersSinceForMemoryTracker, and the markAsAccidentalMatch service path runs hasExceededWrongAnswerThreshold (D-04) — no new counter is needed."
    - "The alias leg reuses the same Java-side AuthorizationService.userMayReadNotebook readability filter and reviewed-note exclusion as the title leg, so an alias match in an UNREADABLE notebook is never leaked as matchedNoteId (IDOR guard extends to the alias leg)."
  artifacts:
    - path: "backend/src/main/java/com/odde/doughnut/entities/repositories/NoteAliasIndexRepository.java"
      provides: "findByAliasLookupKeyOrderByNoteIdAsc(@Param aliasLookupKey) — non-notebook-scoped alias lookup, JOIN FETCH i.note n + JOIN FETCH n.notebook nb, deletedAt guards, ORDER BY n.id ASC"
      contains: "findByAliasLookupKeyOrderByNoteIdAsc"
    - path: "backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java"
      provides: "findAccidentalMatch alias leg — title-then-alias fallback (title leg from Plan 01; alias leg added here) using findByAliasLookupKeyOrderByNoteIdAsc + normalizedLookupKey, with the ArrayList/HashSet dedupe from aliasTargetCandidates"
      contains: "findByAliasLookupKeyOrderByNoteIdAsc"
    - path: "backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java"
      provides: "@Nested AccidentalMatch gains alias-match, floor-clamp, and threshold-counts tests"
      contains: "shouldGradeAsAccidentalMatchWhenWrongAnswerMatchesAnotherReadableNoteAlias"
  key_links:
    - from: "backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java"
      to: "backend/src/main/java/com/odde/doughnut/entities/repositories/NoteAliasIndexRepository.java"
      via: "findAccidentalMatch alias leg calls findByAliasLookupKeyOrderByNoteIdAsc(FrontmatterAliases.normalizedLookupKey(answer)) when the title leg returns no readable match"
      pattern: "findByAliasLookupKeyOrderByNoteIdAsc"
    - from: "backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java"
      to: "backend/src/main/java/com/odde/doughnut/algorithms/FrontmatterAliases.java"
      via: "FrontmatterAliases.normalizedLookupKey(answer) produces the NFKC+lower lookup key that matches the stored alias_lookup_key column"
      pattern: "normalizedLookupKey"
  prohibitions:
    - statement: "Must NOT route the accidental match through MemoryTracker.recallFailed — the alias leg reuses the same markAsAccidentalMatch + partialFail path from Plan 01 (no new penalty seam)."
      status: unverified
      flagged: true
      verification: backstop
    - statement: "Must NOT populate AnsweredQuestion.matchedNotes or AnsweredQuestion.overlap — the alias leg still writes only the singular Answer.matchedNoteId + outcome=ACCIDENTAL_MATCH (per D-05; Phase 3 / Phase 6 own the list/flag)."
      status: unverified
      flagged: true
      verification: backstop
    - statement: "Must NOT add a new wrong-answer counter for the accidental match — D-04 is satisfied automatically by Answer.correct=false being counted by the existing countWrongAnswersSinceForMemoryTracker (the threshold check already runs in markAsAccidentalMatch from Plan 01)."
      status: unverified
      flagged: true
      verification: backstop
    - statement: "Must NOT use a DB-level readability predicate (searchExactForUserInAllMyNotebooks/Subscriptions/Circle) for the alias leg — those miss bazaar-readable notebooks; the Java-side userMayReadNotebook filter (covers owns/subscription/bazaar) is the single source of truth."
      status: unverified
      flagged: true
      verification: backstop
---

<objective>
Complete the accidental-match lookup's alias leg and prove the two remaining edge cases (floor-clamp near the forgetting-curve floor, and threshold-counting), so the accidental-match behavior is fully covered: title AND alias detection, lighter-than-wrong penalty preserved at the floor, and the wrong-answer threshold still counts.

Purpose: Plan 01 proved the title leg end-to-end. This plan adds the alias leg (the second detection surface required by AM-01) and the two edge-case tests that prove the "lighter than wrong" invariant holds near the floor (Pitfall 1) and that the accidental match still counts toward the re-assimilation threshold (D-04). After this plan, Phase 2's one observable behavior — accidental-match grading + lighter SRS penalty — is fully delivered and verified.
Output: A passing alias-match controller test, a passing floor-clamp test, and a passing threshold-counts test; the full backend suite stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-accidental-match-grading-penalty/02-CONTEXT.md
@.planning/phases/02-accidental-match-grading-penalty/02-RESEARCH.md
@.planning/phases/02-accidental-match-grading-penalty/02-PATTERNS.md
@.planning/phases/02-accidental-match-grading-penalty/02-VALIDATION.md
@.planning/phases/02-accidental-match-grading-penalty/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alias leg of the accidental-match lookup</name>
  <precondition>Plan 01 is committed and green: WikiLinkResolver.findAccidentalMatch exists with the title leg (findByNoteTitleOrderByIdAsc + userMayReadNotebook filter + reviewed-note exclusion), and MemoryTrackerService.answerSpelling wires the accidental-match branch. The alias leg is the only missing detection surface.</precondition>
  <files>backend/src/main/java/com/odde/doughnut/entities/repositories/NoteAliasIndexRepository.java, backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java, backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java</files>
  <read_first>
    - backend/src/main/java/com/odde/doughnut/entities/repositories/NoteAliasIndexRepository.java (the file being modified — see findByNotebookNameAndAliasLookupKeyOrderByNoteIdAsc 22-30, SELECT_ALIAS_WITH_NOTEBOOK 12-13, ACTIVE_NOTE_AND_NOTEBOOK 14)
    - backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java (the file being modified — see the findAccidentalMatch title leg added in Plan 01, aliasTargetCandidates 132-149 for the dedupe pattern, noteCandidates 123-130 for the title-then-alias fallback)
    - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java (the file being modified — the @Nested AccidentalMatch from Plan 01)
    - backend/src/main/java/com/odde/doughnut/algorithms/FrontmatterAliases.java (reuse — normalizedLookupKey 108-110, NFKC + lower; matchesFromNoteContent 43-45 for how aliases are declared on a note)
    - backend/src/main/java/com/odde/doughnut/services/AuthorizationService.java (reuse — userMayReadNotebook 128-136, applied to alias-leg candidates too)
    - .planning/phases/02-accidental-match-grading-penalty/02-PATTERNS.md (aliasTargetCandidates dedupe pattern 132-149, the new findByAliasLookupKeyOrderByNoteIdAsc sketch)
  </read_first>
  <behavior>
    - Alias-match: a wrong spelling answer that exactly equals an ALIAS of a different readable note (and matches NO readable note's title) is graded as an accidental match via the alias leg — correct=false, outcome=ACCIDENTAL_MATCH, matchedNoteId=alias-bearing note's id, matchedNotes/overlap null.
    - Title preference: when a readable title match exists, the alias leg is not consulted (title-then-alias fallback mirrors noteCandidates 123-130).
    - IDOR on alias leg: an alias match in an UNREADABLE notebook is never returned as matchedNoteId (the userMayReadNotebook filter applies to alias-leg candidates too).
  </behavior>
  <action>
    Write the failing alias-match controller test FIRST (RED), then add the alias leg to findAccidentalMatch (GREEN). The alias leg completes the D-01 wider-lookup (title-then-alias across all readable notebooks, excluding the reviewed note) — the title leg was wired in Plan 01.

    1. Test first (RED): add `shouldGradeAsAccidentalMatchWhenWrongAnswerMatchesAnotherReadableNoteAlias` to the `@Nested AccidentalMatch` class (from Plan 01). Build the reviewed note + memory tracker + recall prompt as in AnswerSpelling (459-477). Create a SECOND readable note (owned by currentUser, different notebook) whose TITLE does NOT match the wrong answer, but which has a frontmatter ALIAS equal to the wrong answer. Set the alias via the existing makeMe/note-content frontmatter pattern (the note's content frontmatter declares an alias; makeMe refreshes the NoteAliasIndex on save — research assumption A1). Set answerDTO.setSpellingAnswer to that alias string (wrong for the reviewed note, matches the second note's alias, matches no title). Call controller.answerSpelling(recallPrompt, answerDTO). Assert: assertFalse(answerResult.getAnswer().getCorrect()); assertThat(answerResult.getAnswer().getOutcome(), is(AnswerOutcome.ACCIDENTAL_MATCH)); assertThat(answerResult.getAnswer().getMatchedNoteId(), equalTo(secondNote.getId())); assertNull(answerResult.getMatchedNotes()); assertNull(answerResult.getOverlap()). Use plain ASCII for the alias to avoid NFKC edge cases (Pitfall 4). Run and confirm it FAILS for the right reason (the title leg returns empty and there is no alias leg yet, so outcome is null / plain wrong). Commit: test(02-02): add failing alias-match test.

    2. Implement (GREEN) — two small wiring changes:
      a. NoteAliasIndexRepository.java: add `findByAliasLookupKeyOrderByNoteIdAsc(@Param("aliasLookupKey") String aliasLookupKey)` mirroring `findByNotebookNameAndAliasLookupKeyOrderByNoteIdAsc` (22-30) but DROPPING the `AND LOWER(nb.name) = LOWER(:notebookName)` clause — reuse the shared `SELECT_ALIAS_WITH_NOTEBOOK` (12-13) and `ACTIVE_NOTE_AND_NOTEBOOK` (14) constants (both already `JOIN FETCH i.note n` + `JOIN FETCH n.notebook nb`, so the readability filter has the notebook ready), keep `WHERE i.aliasLookupKey = :aliasLookupKey` and `ORDER BY n.id ASC`. Parameterized `:aliasLookupKey` only (V5 — no injection).
      b. WikiLinkResolver.java: extend `findAccidentalMatch` (from Plan 01) to add the alias leg as a fallback when the title leg returns no readable match — mirror `noteCandidates` (123-130) title-then-alias ordering. Concretely: collect title candidates via `noteRepository.findByNoteTitleOrderByIdAsc(answer)`; if no readable, non-reviewed match is found among them, collect alias candidates via `noteAliasIndexRepository.findByAliasLookupKeyOrderByNoteIdAsc(FrontmatterAliases.normalizedLookupKey(answer))` and dedupe to distinct notes via the `ArrayList`/`HashSet seenNoteIds` pattern from `aliasTargetCandidates` (132-149). Iterate the combined candidate list in id-ASC order, applying the SAME filter as the title leg: `authorizationService.userMayReadNotebook(viewer, candidate.getNotebook()) && !candidate.getId().equals(reviewedNote.getId())`, returning the first readable match (or Optional.empty()). Keep the method a single cohesive lookup; do not duplicate the filter logic — extract a small private helper if it clarifies, but do not over-engineer. FrontmatterAliases is already imported in WikiLinkResolver (line 3). Commit: feat(02-02): add alias leg to accidental-match lookup.

    Do NOT change the penalty seam (markAsAccidentalMatch / partialFail from Plan 01) — the alias leg reuses it. Do NOT mutate the Plan 01 title-match/IDOR/skip-when-correct tests.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - NoteAliasIndexRepository.java contains `findByAliasLookupKeyOrderByNoteIdAsc` with `@Param("aliasLookupKey")` and `ORDER BY n.id ASC` and reuses `SELECT_ALIAS_WITH_NOTEBOOK` + `ACTIVE_NOTE_AND_NOTEBOOK` (grep -c 'findByAliasLookupKeyOrderByNoteIdAsc' == 1).
    - WikiLinkResolver.findAccidentalMatch references `findByAliasLookupKeyOrderByNoteIdAsc` and `normalizedLookupKey` (grep -c 'findByAliasLookupKeyOrderByNoteIdAsc' in WikiLinkResolver.java == 1).
    - RecallPromptControllerTests.java contains `shouldGradeAsAccidentalMatchWhenWrongAnswerMatchesAnotherReadableNoteAlias` (grep -c 'shouldGradeAsAccidentalMatchWhenWrongAnswerMatchesAnotherReadableNoteAlias' == 1).
    - The alias-match test asserts outcome=ACCIDENTAL_MATCH and matchedNoteId=secondNote.getId() for a wrong answer matching an alias (not a title).
    - `CURSOR_DEV=true nix develop -c pnpm backend:test_only` exits 0 (full backend suite green, including all Plan 01 tests).
  </acceptance_criteria>
  <done>The alias leg of findAccidentalMatch is wired: a wrong spelling answer matching another readable note's alias (when no title matches) is graded as an accidental match via the alias leg, with the same IDOR guard and reviewed-note exclusion as the title leg. The full backend suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Floor-clamp and threshold-counts edge-case tests</name>
  <precondition>Plan 01 is committed and green: ForgettingCurve.partialFail() reuses the add() floor clamp at DEFAULT_FORGETTING_CURVE_INDEX (100.0f), and MemoryTrackerService.markAsAccidentalMatch runs hasExceededWrongAnswerThreshold. Task 1 (alias leg) is committed. Both invariants to be tested are already implemented by Plan 01 production code; this task proves them at the edges.</precondition>
  <files>backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java</files>
  <read_first>
    - backend/src/test/java/com/odde/doughnut/controllers/RecallPromptControllerTests.java (the file being modified — the @Nested AccidentalMatch from Plan 01, plus WrongAnswer 670-716 for the timeTravelTo + forgettingCurveIndex assertion pattern)
    - backend/src/main/java/com/odde/doughnut/entities/ForgettingCurve.java (source of truth — add() clamp 19-25, partialFail from Plan 01, DEFAULT_FORGETTING_CURVE_INDEX line 6)
    - backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java (source of truth — markAsAccidentalMatch from Plan 01, calculateNextRecallAt 115-118)
    - backend/src/main/java/com/odde/doughnut/entities/repositories/RecallPromptRepository.java (source of truth — countWrongAnswersSinceForMemoryTracker 64-73 counts quiz_answer.correct=false rows)
    - backend/src/main/java/com/odde/doughnut/services/MemoryTrackerService.java (source of truth — markAsAccidentalMatch service method from Plan 01 runs hasExceededWrongAnswerThreshold; WRONG_ANSWER_PERIOD_DAYS / WRONG_ANSWER_THRESHOLD constants)
    - .planning/phases/02-accidental-match-grading-penalty/02-RESEARCH.md (Pitfall 1 floor-clamp, Pitfall 2 nextRecallAt, D-04 threshold-counts)
  </read_first>
  <behavior>
    - Floor clamp: a tracker whose forgettingCurveIndex is already at the floor (100.0f) stays at 100.0f after an accidental match — partialFail()'s add() clamp prevents the index from dropping below 100, so the accidental match is never harsher than the plain-wrong floor (Pitfall 1 — "lighter than wrong" holds at the floor).
    - Threshold counts: an accidental match (correct=false) is counted by the wrong-answer re-assimilation threshold; after enough accidental matches within the threshold period, hasExceededWrongAnswerThreshold returns true (D-04). The threshold logic itself is unchanged.
  </behavior>
  <action>
    Add two test methods to the `@Nested AccidentalMatch` class. Both are pure test additions proving edge cases of the Plan 01 production code.

    1. Floor-clamp test `shouldNotDropForgettingCurveIndexBelowFloorOnAccidentalMatch`: build the reviewed note + memory tracker + recall prompt, but set the memory tracker's forgettingCurveIndex to the floor 100.0f (use the existing makeMe builder `.forgettingCurveAndNextRecallAt(100.0f)` — the index starts at the floor). Create a second readable note (owned by currentUser, different notebook) whose title is the wrong answer. Call testabilitySettings.timeTravelTo(memoryTracker.getNextRecallAt()) before answering (mirroring WrongAnswer 679/688). Set answerDTO.setSpellingAnswer to the second note's title. Call controller.answerSpelling(recallPrompt, answerDTO). Assert: assertThat(memoryTracker.getForgettingCurveIndex(), equalTo(100.0f)) (the clamp held — the index did NOT drop below 100) AND assertThat(memoryTracker.getAnswer().getOutcome()... or via answerResult, outcome=ACCIDENTAL_MATCH) to confirm it was graded as an accidental match (not a plain wrong, which would also clamp at 100 via failed() — so the outcome assertion distinguishes the path). Also assert nextRecallAt > currentUTCTimestamp (Pitfall 2 — future-facing even at the floor). Commit: test(02-02): add floor-clamp edge test.

    2. Threshold-counts test `shouldStillCountAccidentalMatchTowardWrongAnswerThreshold`: this proves D-04 — an accidental match counts toward the 5-wrong-in-14-days threshold because correct=false is counted by countWrongAnswersSinceForMemoryTracker. Build the reviewed note + memory tracker + recall prompt and a second readable note whose title is the wrong answer. First, drive the tracker to near the threshold by answering wrongly enough times within the threshold window — reuse the existing pattern: answer the same recall prompt repeatedly is not possible (a recall prompt is single-answer), so create multiple recall prompts for the same memory tracker and answer each with the accidental-match answer (wrong for the reviewed note, matching the second note's title), using testabilitySettings.timeTravelTo to keep timestamps within WRONG_ANSWER_PERIOD_DAYS. After WRONG_ANSWER_THRESHOLD accidental matches, assert that the service reports the threshold is exceeded — assert via the controller-observable consequence (e.g. the AnsweredQuestion / memory tracker state reflects assimilation, or call memoryTrackerService.isThresholdExceeded(memoryTracker, currentTime) and assertThat(..., is(true))). The exact threshold constants (WRONG_ANSWER_PERIOD_DAYS, WRONG_ANSWER_THRESHOLD) are read from MemoryTrackerService — do not hardcode magic numbers; reference the existing constants or assert the boolean outcome. Confirm the count comes from correct=false rows (the accidental answers), proving no new counter was needed. Commit: test(02-02): add threshold-counts edge test.

    Use `assertThat` + matchers, one behavior per test (backend-testing.mdc). Do NOT mutate Plan 01/Task 1 tests. If driving the tracker to the threshold via the controller is impractical within a ~5-min slice, fall back to a focused service-level assertion (memoryTrackerService.isThresholdExceeded after the accidental answers) — that is still a stable-boundary behavior test, not an implementation test.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - RecallPromptControllerTests.java contains `shouldNotDropForgettingCurveIndexBelowFloorOnAccidentalMatch` and `shouldStillCountAccidentalMatchTowardWrongAnswerThreshold` (grep -c 'shouldNotDropForgettingCurveIndexBelowFloorOnAccidentalMatch' == 1; grep -c 'shouldStillCountAccidentalMatchTowardWrongAnswerThreshold' == 1).
    - The floor-clamp test asserts the forgettingCurveIndex stays at 100.0f (equalTo) after an accidental match starting from the floor, AND that the outcome is ACCIDENTAL_MATCH (distinguishing the accidental path from the plain-wrong path which also clamps at 100).
    - The threshold-counts test asserts the wrong-answer threshold is exceeded after enough accidental matches (correct=false) within the threshold period — proving D-04 without a new counter.
    - `CURSOR_DEV=true nix develop -c pnpm backend:test_only` exits 0 (full backend suite green).
  </acceptance_criteria>
  <done>The "lighter than wrong" invariant holds at the forgetting-curve floor (index never drops below 100 on an accidental match) and the accidental match counts toward the wrong-answer re-assimilation threshold (correct=false counted, no new counter) — both proven by passing tests, full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → POST /api/recall-prompts/{id}/answer-spelling | The spelling answer (user input) is used as an alias lookup key (normalized via FrontmatterAliases.normalizedLookupKey). Existing gate unchanged from Plan 01. |
| Wider alias lookup → matchedNoteId in response | The alias leg of findAccidentalMatch returns a note the viewer did not navigate to. The same IDOR trust boundary as Plan 01's title leg applies to alias-leg candidates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-05 | Information Disclosure (IDOR via the alias leg) | WikiLinkResolver.findAccidentalMatch alias leg → Answer.matchedNoteId | medium | mitigate | The alias leg reuses the SAME Java-side AuthorizationService.userMayReadNotebook filter and reviewed-note exclusion as the Plan 01 title leg — applied to every alias candidate (not a DB predicate). An alias match in an UNREADABLE notebook is never returned as matchedNoteId. The alias-match test (Task 1) uses a readable alias-bearing note; the IDOR guarantee for the alias leg is the same filter already proven for the title leg in Plan 01's shouldNotLeakMatchedNoteIdFromUnreadableNotebook. |
| T-2-06 | Tampering (SQL injection via the alias lookup key) | NoteAliasIndexRepository.findByAliasLookupKeyOrderByNoteIdAsc | low | accept | Parameterized Spring Data JPQL (`:aliasLookupKey` bind parameter); the answer is normalized via FrontmatterAliases.normalizedLookupKey then bound, never concatenated. Mirrors the existing findByNotebookNameAndAliasLookupKeyOrderByNoteIdAsc pattern. |
| T-2-07 | Information Disclosure (stale alias index missing a match) | NoteAliasIndexRepository alias leg | low | accept | Research assumption A1: the note_alias_index is populated by the existing refresh/backfill path; makeMe triggers the refresh on save, so tests create fresh alias rows. A stale index in prod would miss an alias accidental match (title-only matches still work) — a pre-existing concern, not introduced by Phase 2. No mitigation added; documented as assumption A1. |
| T-2-SC | Tampering | npm/pip/cargo installs | low | accept | Plan 02 installs NO external packages. No package-legitimacy checkpoint needed. |

ASVS L1, block-on high. No high-severity findings — the alias-leg IDOR risk (T-2-05) is medium and is mitigated by reusing the Plan 01 readability filter.
</threat_model>

<verification>
- Full backend suite green: `CURSOR_DEV=true nix develop -c pnpm backend:test_only` exits 0 (run ALL backend unit tests — backend-testing.mdc).
- The `@Nested AccidentalMatch` class now covers all 7 rows of 02-VALIDATION.md's Per-Task Verification Map: title-match (Plan 01), alias-match (Plan 02 Task 1), plain-wrong (pre-existing 707-715), IDOR-unreadable (Plan 01 Task 2), lighter-penalty (Plan 01 Task 1), threshold-counts (Plan 02 Task 2), skip-when-correct-shared-title (Plan 01 Task 2).
- The floor-clamp test proves the "lighter than wrong" invariant holds at the floor (Pitfall 1); the threshold-counts test proves D-04 (correct=false counted, no new counter).
- All Plan 01 tests still pass unchanged (no fixture regressions — no note titled/aliased "wrong").
- No Flyway migration; no @Column; no DTO/OpenAPI/frontend/types.gen.ts change; no new endpoint.
- Phase gate before /gsd-verify-work: `CURSOR_DEV=true nix develop -c pnpm backend:verify` (includes migration test DB).
</verification>

<success_criteria>
- A wrong spelling answer matching another readable note's ALIAS (when no title matches) is graded as an accidental match via the alias leg — AM-01 (alias leg) complete.
- The accidental-match lookup prefers title over alias (title-then-alias fallback).
- The "lighter than wrong" penalty holds at the forgetting-curve floor (index never drops below 100) — AM-02 / Pitfall 1.
- The accidental match counts toward the wrong-answer re-assimilation threshold (correct=false counted, no new counter) — D-04.
- The full backend suite is green; all Plan 01 tests pass unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-accidental-match-grading-penalty/02-02-SUMMARY.md` when done.

## Artifacts this phase produces

Plan 02 creates the following NEW symbols (the alias leg + edge-case tests; it reuses the Plan 01 penalty seam unchanged):
- `NoteAliasIndexRepository.findByAliasLookupKeyOrderByNoteIdAsc(String aliasLookupKey)` — non-notebook-scoped alias lookup query method (reuses SELECT_ALIAS_WITH_NOTEBOOK + ACTIVE_NOTE_AND_NOTEBOOK constants).
- `WikiLinkResolver.findAccidentalMatch` gains the alias leg: a title-then-alias fallback calling `findByAliasLookupKeyOrderByNoteIdAsc(FrontmatterAliases.normalizedLookupKey(answer))` with the ArrayList/HashSet dedupe from aliasTargetCandidates, applying the same userMayReadNotebook filter + reviewed-note exclusion as the title leg.
- `RecallPromptControllerTests.@Nested AccidentalMatch` gains: `shouldGradeAsAccidentalMatchWhenWrongAnswerMatchesAnotherReadableNoteAlias`, `shouldNotDropForgettingCurveIndexBelowFloorOnAccidentalMatch`, `shouldStillCountAccidentalMatchTowardWrongAnswerThreshold`.

No new files (all additions are new methods/members on existing files). No new packages, no DTO/OpenAPI/frontend/types.gen.ts changes, no Flyway migration. Plan 02 reuses ForgettingCurve.partialFail, MemoryTracker.markAsAccidentalMatch, and MemoryTrackerService.markAsAccidentalMatch from Plan 01 unchanged — the alias leg routes through the same lighter-penalty seam.
</output>
