---
phase: 01-commissioned-tracker-model
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java
  - backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java
  - backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
  - backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java
  - backend/src/test/java/com/odde/doughnut/services/QuestionGenerationBatchLocalPlanningTest.java
  - docs/database-erd.md
  - packages/generated/doughnut-backend-api
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC1 (Structure / backend unit gate): Existing assimilation and recall backend unit suites remain green via pnpm backend:verify. E2E N/A unless a product path was accidentally touched."
    - "A note with only a commissioned memory tracker still appears in the ordinary assimilation queue (Phase 2-ready join)."
    - "Batch question-generation candidate selection does not treat commissioned trackers as ordinary due work — proven by a focused unit assertion (not SQL-presence alone)."
    - "docs/database-erd.md reflects memory_tracker.commissioned after migration."
  artifacts:
    - path: "backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java"
      provides: "joinMemoryTracker ordinary-only (non-commissioned)"
      contains: "commissioned"
    - path: "backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java"
      provides: "batch candidates exclude commissioned"
      contains: "mt.commissioned IS FALSE"
    - path: "backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java"
      provides: "commissioned-only note still in assimilation queue"
      contains: "commissioned"
    - path: "backend/src/test/java/com/odde/doughnut/services/QuestionGenerationBatchLocalPlanningTest.java"
      provides: "focused assertion: due commissioned tracker excluded from planLocalBatchForUser candidates"
      contains: "commissioned"
    - path: "docs/database-erd.md"
      provides: "ERD regenerated from live schema"
      contains: "commissioned"
  key_links:
    - from: "NoteRepository.joinMemoryTracker"
      to: "MemoryTracker JPQL ordinary filter"
      via: "LEFT JOIN rp … AND non-commissioned note-level"
      pattern: "commissioned"
    - from: "findBatchQuestionGenerationCandidatesByUser"
      to: "memory_tracker.commissioned"
      via: "AND mt.commissioned IS FALSE next to spelling filter"
      pattern: "mt.commissioned IS FALSE"
---

<objective>
Expand the tracer so ordinary assimilation detection and batch question-gen ignore commissioned trackers, regenerate ERD (and OpenAPI client if Springdoc requires it), and confirm SC1 regression green.

Purpose: Phase 2 stop-safe — a commissioned-only note must remain ordinarily assimilable; AI prep must not pull commissioned IDs as ordinary due work (RESEARCH A1/A2).

Output: NoteRepository join filter, batch SQL filter, AssimilationControllerTests proof, regenerated `docs/database-erd.md`, optional generated client update.

**Specless probe skip (recorded):** Same as Plan 01 — no REQ IDs; SC1–SC3 via must_haves / VALIDATION.md.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
@/Users/terryyin/git/doughnut/.agents/skills/execute-plan/SKILL.md
@/Users/terryyin/git/doughnut/.agents/skills/post-change-refactor/SKILL.md
@/Users/terryyin/git/doughnut/.agents/skills/database-erd/SKILL.md
@/Users/terryyin/git/doughnut/.agents/skills/generate-api-client/SKILL.md
</execution_context>

<context>
@.planning/phases/01-commissioned-tracker-model/01-01-PLAN.md
@.planning/phases/01-commissioned-tracker-model/01-RESEARCH.md
@.planning/phases/01-commissioned-tracker-model/01-PATTERNS.md
@.planning/phases/01-commissioned-tracker-model/01-VALIDATION.md
@.cursor/rules/planning.mdc
@.cursor/rules/unit-testing.mdc

# Depends on Plan 01 tracer: column, entity, byUserIdFrom, makeMe.commissioned(), RecallsController proof
# Still out of scope: assimilate caret/UI, AssimilationRequestDTO commissioned flag, MemoryTrackerService.assimilate coexistence short-circuit (Phase 2), Learning Session entities, E2E graduation
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

(See Plan 01 for column/entity/builder/due filter. This plan adds:)

| Symbol / artifact | Kind |
|-------------------|------|
| Ordinary-only `joinMemoryTracker` / JPQL constant | Assimilation queue ignores commissioned-only as “already assimilated” |
| `findBatchQuestionGenerationCandidatesByUser` + `commissioned IS FALSE` | Batch AI prep exclusion |
| AssimilationControllerTests commissioned-only case | Structure proof for Phase 2 |
| `docs/database-erd.md` | `commissioned` on `memory_tracker` |
| Generated `MemoryTracker.commissioned?` (if Springdoc emits) | TS client via `pnpm generateTypeScript` — never hand-edit |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Exclude commissioned from assimilation join and batch candidates</name>
  <reversibility rating="costly">JPQL/SQL selection semantics become the ordinary-vs-commissioned contract Phase 2–3 rely on; changing later needs coordinated test and product review, but no irreversible data migration beyond Plan 01.</reversibility>
  <files>
    backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java,
    backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java,
    backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java,
    backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java,
    backend/src/test/java/com/odde/doughnut/services/QuestionGenerationBatchLocalPlanningTest.java
  </files>
  <read_first>
    - `.planning/phases/01-commissioned-tracker-model/01-PATTERNS.md` (NoteRepository joinMemoryTracker, batch candidates, AssimilationControllerTests recommended scenario)
    - `backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java` (`JPA_WHERE_NOTE_LEVEL_TRACKER`, `JPA_WHERE_NOTE_LEVEL_TARGET_TRACKER`)
    - `backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java` (`joinMemoryTracker` ~154–158 and consumers)
    - `backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java` (`findBatchQuestionGenerationCandidatesByUser` ~98–126)
    - `backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java` (`Next.countsAreCorrect` / next-unit patterns)
    - `backend/src/test/java/com/odde/doughnut/services/QuestionGenerationBatchLocalPlanningTest.java` (`createsPlannedBatchWithOneRequestPerCandidateTracker`)
  </read_first>
  <behavior>
    - Commissioned-only note: `makeMe.aMemoryTrackerFor(note).commissioned().please()` then `AssimilationController.next("Asia/Shanghai")` still surfaces that note as next / in unassimilated path (assert the note id or due count delta that proves the join ignores commissioned).
    - Batch candidates: with an ordinary due tracker and a commissioned due tracker for the same user, `QuestionGenerationBatchPlanningService.planLocalBatchForUser` (or equivalent boundary) includes only the ordinary tracker id — commissioned id must be absent. Assert the delta (exclusion), not the full batch shape already covered by existing planning tests.
  </behavior>
  <action>
    1. Restrict `NoteRepository.joinMemoryTracker` so the LEFT JOIN matches only ordinary (non-commissioned) note-level trackers. Prefer extending `MemoryTracker` JPQL constants (cohesion with `JPA_WHERE_NOTE_LEVEL_TRACKER`) and referencing them from the join — e.g. AND `(rp.commissioned IS NULL OR rp.commissioned = FALSE)` aligned with entity Boolean mapping. Apply consistently for alias patterns used (`rp` / `tmt` if both need it). One join fix must cover `findByOwnershipWhereThereIsNoMemoryTracker` and related count/notebook variants that reuse `joinMemoryTracker`.

    2. In `findBatchQuestionGenerationCandidatesByUser`, add `AND mt.commissioned IS FALSE` beside the existing `mt.spelling IS FALSE` filter. Keep `@Param` binding only.

    3. Add AssimilationControllerTests case for commissioned-only note still in assimilation queue (PATTERNS recommended scenario). Small-test style: assert unique delta only.

    4. Required batch-exclusion unit assertion (backs must_have): extend `QuestionGenerationBatchLocalPlanningTest` with a case that creates one ordinary due tracker and one commissioned due tracker (makeMe `.commissioned()`), calls `planLocalBatchForUser`, and asserts the commissioned tracker id is not among planned request memory-tracker ids (ordinary remains). Do not rely on SQL-string presence alone.

    5. Do not change `MemoryTrackerService.assimilate` short-circuit (Phase 2). Do not filter `findByUserAndNote`. Do not filter `byUserIdWhere` recent lists (RESEARCH Q1 RESOLVED). Do not add E2E.

    TDD: write assimilation-queue failing test first, then join/SQL until green; write batch-exclusion failing test next to LocalPlanningTest, then SQL until green.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - `joinMemoryTracker` (via MemoryTracker JPQL constant or equivalent) excludes commissioned trackers from counting as ordinary assimilation
    - AssimilationControllerTests proves commissioned-only note remains in assimilation queue
    - Batch candidate SQL includes `mt.commissioned IS FALSE`
    - `QuestionGenerationBatchLocalPlanningTest` (or sibling) asserts commissioned due tracker is excluded from `planLocalBatchForUser` candidates
    - `pnpm backend:test_only` green
    - No Phase 2 assimilate API/UI files changed
  </acceptance_criteria>
  <done>Ordinary assimilation queue and batch gen treat commissioned as non-ordinary; Phase 2-ready Structure complete for selection seams</done>
</task>

<task type="auto">
  <name>Regenerate ERD and OpenAPI client if schema/wire changed</name>
  <files>
    docs/database-erd.md,
    packages/generated/doughnut-backend-api
  </files>
  <read_first>
    - `.agents/skills/database-erd/SKILL.md`
    - `.agents/skills/generate-api-client/SKILL.md` (only if Springdoc exposes new field)
    - `docs/database-erd.md` (current memory_tracker stub)
  </read_first>
  <action>
    1. Ensure local MySQL has applied the Plan 01 migration (backend tests / SUT). Run `CURSOR_DEV=true nix develop -c pnpm export:database-erd` per database-erd skill. Do not hand-edit the Mermaid block. Confirm `commissioned` appears on `memory_tracker`.

    2. If Springdoc/OpenAPI now includes `MemoryTracker.commissioned`, run `CURSOR_DEV=true nix develop -c pnpm generateTypeScript` and commit generated diffs only — never hand-edit `packages/generated/`. If the field is absent from the spec, skip regen and note why in SUMMARY.

    3. Leave frontend UI untouched; fixture type updates from generateTypeScript are OK.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm export:database-erd && grep -n commissioned docs/database-erd.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/database-erd.md` contains `commissioned` under `memory_tracker` after export
    - Generated client updated only via generateTypeScript when OpenAPI includes the field
    - No hand-edited Mermaid or generated TS
  </acceptance_criteria>
  <done>ERD (and client if needed) match migrated schema / wire</done>
</task>

<task type="auto">
  <name>SC1 regression verify and phase wrap-up hygiene</name>
  <files>
    .planning/phases/01-commissioned-tracker-model/01-VALIDATION.md,
    .planning/phases/01-commissioned-tracker-model/01-02-SUMMARY.md
  </files>
  <read_first>
    - `.planning/phases/01-commissioned-tracker-model/01-VALIDATION.md`
    - `.agents/skills/execute-plan/SKILL.md` (Jidoka, post-change-refactor, commit+push)
    - `.agents/skills/post-change-refactor/SKILL.md`
  </read_first>
  <action>
    1. Run full backend verify: `CURSOR_DEV=true nix develop -c pnpm backend:verify` — this is ROADMAP SC1 for Phase 1 (Structure backend unit gate). E2E is N/A unless a product path was accidentally touched; if so, run targeted E2E only. Do not add Phase 1 E2E scenarios.

    2. Jidoka: confirm no Learning Session / assimilate caret / Phase 3 UI leaked in; unique-key decision still matches `01-UNIQUE-KEY-DECISION.md` / SUMMARY.

    3. Run post-change-refactor on the uncommitted Phase 1 concept (MemoryTracker commissioned discriminator + selection filters) — cohesion only; stop if scope expands beyond commissioned selection.

    4. Update VALIDATION.md task status rows if present; write `01-02-SUMMARY.md`; mark PLAN checkboxes in ROADMAP when orchestrator/execute-plan closes the phase.

    5. Phase close (execute-plan): commit + push after green. Capability-named commits — no phase numbers in product paths.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:verify</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm backend:verify` green (SC1 Structure unit gate)
    - No new `@wip` E2E; no Learning Session / assimilate UI files in the diff
    - SUMMARY written; wrap-up steps (Jidoka, refactor, commit+push) performed by execute-plan
  </acceptance_criteria>
  <done>Phase 1 Structure verified green and ready for Phase 2 Behavior planning/execution</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assimilation queue queries | Same authz as existing AssimilationController |
| Batch question-gen SQL | Native query; parameterized only |
| Generated OpenAPI/TS | Build artifact — no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of privilege (IDOR) | AssimilationController / recalls | medium | accept | Existing authz unchanged; no new create path |
| T-01-02 | Tampering (SQLi) | Batch candidate native SQL | high | mitigate | Add only literal `AND mt.commissioned IS FALSE`; keep `@Param` for userId/dueBy |
| T-01-03 | Tampering (unique-key race) | memory_tracker unique | medium | accept | Constraint owned by Plan 01; this plan does not weaken it |
| T-01-06 | Information disclosure | Generated TS `commissioned` | low | accept | Default false; no UI copy in Phase 1 |
| T-01-SC | Tampering | package installs | high | accept | N/A — no new packages; generateTypeScript only |
</threat_model>

<verification>
- `pnpm backend:test_only` after selection expansion
- `pnpm export:database-erd` + commissioned present in ERD
- `pnpm backend:verify` for SC1 gate
</verification>

<success_criteria>
- SC1 green
- Commissioned-only notes remain ordinarily assimilable
- Batch gen excludes commissioned
- ERD updated
- Phase wrap-up ready for execute-plan commit+push
</success_criteria>

<output>
Create `.planning/phases/01-commissioned-tracker-model/01-02-SUMMARY.md` when done.
Execute-plan wrap-up after phase: Jidoka → post-change-refactor → update plan → commit → push.
</output>
