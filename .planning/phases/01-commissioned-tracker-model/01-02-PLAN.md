---
phase: 01-commissioned-tracker-model
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
  - backend/src/main/java/com/odde/doughnut/entities/repositories/NotePropertyIndexRepository.java
  - backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java
  - backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java
  - backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java
  - backend/src/test/java/com/odde/doughnut/services/QuestionGenerationBatchCandidateMemoryTrackersTest.java
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC1: Existing assimilation and recall backend unit suites still pass (`pnpm backend:verify`)."
    - "A note with only a commissioned tracker still appears in the ordinary assimilation queue (Phase 2-ready join)."
    - "Batch question-generation candidates never include due COMMISSIONED trackers."
  artifacts:
    - path: "backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java"
      provides: "joinMemoryTracker ignores COMMISSIONED for ordinary assimilation detection"
      contains: "COMMISSIONED"
    - path: "backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java"
      provides: "batch candidates exclude COMMISSIONED beside SPELLING"
      contains: "COMMISSIONED"
    - path: "backend/src/test/java/com/odde/doughnut/services/QuestionGenerationBatchCandidateMemoryTrackersTest.java"
      provides: "excludesCommissionedTracker unit proof"
      contains: "commissioned"
  key_links:
    - from: "AssimilationController.next / getUnassimilatedNotes"
      to: "NoteRepository.joinMemoryTracker"
      via: "WHERE rp IS NULL after ordinary-only join"
      pattern: "rp.type <> MemoryTrackerType.COMMISSIONED"
    - from: "QuestionGenerationBatchPlanningService"
      to: "findBatchQuestionGenerationCandidatesByUser"
      via: "native SQL type filters"
      pattern: "AND mt.type <> 'COMMISSIONED'"
---

<objective>
Expand from the due-recall tracer: exclude COMMISSIONED from ordinary assimilation detection (note join + property target gate) and from batch question-gen candidates, with unit proofs — Structure only so Phase 2 assimilate-as-commissioned can see commissioned-only notes in the ordinary queue (per D-01 coexistence; D-02 type filters, not a boolean column).

Purpose: Stop-safe Phase-2 enablement without assimilate create UI/DTO or Learning Session work.
Output: JPQL/SQL ordinary-path filters + queue/batch proofs + SC1 `backend:verify` green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-commissioned-tracker-model/CONTEXT.md
@.planning/phases/01-commissioned-tracker-model/01-RESEARCH.md
@.planning/phases/01-commissioned-tracker-model/01-PATTERNS.md
@.planning/phases/01-commissioned-tracker-model/01-01-PLAN.md

@backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
@backend/src/main/java/com/odde/doughnut/entities/repositories/NotePropertyIndexRepository.java
@backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java
@backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java
@backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java
@backend/src/test/java/com/odde/doughnut/services/QuestionGenerationBatchCandidateMemoryTrackersTest.java
</context>

## Artifacts this phase produces

| Artifact | Role |
|----------|------|
| `NoteRepository.joinMemoryTracker` COMMISSIONED exclusion | Commissioned-only notes stay ordinarily assimilable in the queue |
| `NotePropertyIndexRepository` target-gate COMMISSIONED exclusion | Commissioned-only targets do not falsely satisfy “target assimilated” |
| Batch candidate SQL COMMISSIONED exclusion | AI question prep ignores commissioned due work |
| Queue + batch unit proofs | Observable Structure proofs without product UI |
| SC1 `backend:verify` | Existing assimilation/recall suites unchanged |

**Specless REQ probe:** `requirements: []` — SC1 is this plan’s ROADMAP success criterion; SC2/SC3 covered in 01-01.

<tasks>

<task type="auto" tdd="true">
  <name>Ordinary assimilation queue ignores commissioned-only trackers</name>
  <reversibility rating="reversible">JPQL join predicate on existing enum; undo by removing the enum inequality.</reversibility>
  <files>backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java, backend/src/main/java/com/odde/doughnut/entities/repositories/NotePropertyIndexRepository.java, backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java, backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java</files>
  <read_first>
    - `.planning/phases/01-commissioned-tracker-model/01-RESEARCH.md` (Pitfall 3, Pitfall 5, Open Question A2, Pattern 2)
    - `.planning/phases/01-commissioned-tracker-model/01-PATTERNS.md` (`joinMemoryTracker`, optional target gate, AssimilationControllerTests queue shape)
    - `backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java` (`joinMemoryTracker`, `recallWhereClause`)
    - `backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java` (`JPA_WHERE_NOTE_LEVEL_TRACKER` / target sibling)
    - `backend/src/main/java/com/odde/doughnut/entities/repositories/NotePropertyIndexRepository.java` (`unassimilatedJoinPropertyTracker` SPELLING filter; `targetNoteKeyGateWhere`)
    - `backend/src/test/java/com/odde/doughnut/controllers/AssimilationControllerTests.java` (`Next` nested class)
  </read_first>
  <behavior>
    - Note with only a commissioned tracker still appears in ordinary assimilation `controller.next(...)` (next unit / unassimilated counts include that note) — Phase-2-ready queue.
    - Do not assert `assimilate()` create behavior when COMMISSIONED already exists (Phase 2 / Pitfall 5).
  </behavior>
  <action>
    Add a failing assimilation-queue proof first (capability-named): note owned by current user, only `.commissioned()` tracker, assert `AssimilationController.next` still surfaces the note as unassimilated ordinary work (mirror `Next.countsAreCorrect` / PATTERNS queue shape; or service-level `getUnassimilatedNotes` if controller wiring is clearer — prefer controller boundary per unit-testing.mdc).

    Then exclude COMMISSIONED on `NoteRepository.joinMemoryTracker` using fully-qualified JPQL enum constant (mirror NotePropertyIndex SPELLING pattern). Prefer one representation: extend or sibling `MemoryTracker.JPA_WHERE_*` if the same ordinary-note-level predicate is reused; otherwise append on the join only. Also apply the same COMMISSIONED exclusion on `NotePropertyIndexRepository` target-note gate (`tmtBlock`) per RESEARCH A2 recommendation — cheap same-pattern fix so commissioned-only targets do not count as ordinarily assimilated.

    Do not edit `MemoryTrackerAssimilation` create/ignore logic, AssimilationRequestDTO, or any assimilate UI (Phase 2). Mitigate T-01-02 for JPQL: enum constant in query string, userId stays `@Param`.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only -- --tests com.odde.doughnut.controllers.AssimilationControllerTests</automated>
  </verify>
  <acceptance_criteria>
    - Capability-named test proves a commissioned-only note still appears in ordinary assimilation next/queue.
    - `joinMemoryTracker` excludes `MemoryTrackerType.COMMISSIONED`.
    - Property target-note gate also excludes COMMISSIONED on the target tracker join.
    - No changes to `MemoryTrackerAssimilation` assimilate create path in this plan.
    - AssimilationControllerTests pass under `pnpm backend:test_only`.
  </acceptance_criteria>
  <done>Commissioned-only notes remain in the ordinary assimilation queue; property target gate ignores COMMISSIONED.</done>
</task>

<task type="auto" tdd="true">
  <name>Batch candidates exclude COMMISSIONED and SC1 verify</name>
  <reversibility rating="reversible">Native SQL literal beside existing SPELLING filter; undo by removing one AND clause.</reversibility>
  <files>backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java, backend/src/test/java/com/odde/doughnut/services/QuestionGenerationBatchCandidateMemoryTrackersTest.java</files>
  <read_first>
    - `.planning/phases/01-commissioned-tracker-model/01-RESEARCH.md` (Pitfall 4; batch filter)
    - `.planning/phases/01-commissioned-tracker-model/01-PATTERNS.md` (`excludesSpellingTracker` analog)
    - `backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java` (`findBatchQuestionGenerationCandidatesByUser`)
    - `backend/src/test/java/com/odde/doughnut/services/QuestionGenerationBatchCandidateMemoryTrackersTest.java` (`excludesSpellingTracker`, `candidateIds`)
  </read_first>
  <behavior>
    - Due commissioned tracker id is absent from `findCandidateMemoryTrackersForBatchGeneration` / `candidateIds()` (mirror `excludesSpellingTracker`).
  </behavior>
  <action>
    Add `excludesCommissionedTracker` (capability-named) mirroring `excludesSpellingTracker` with `.commissioned().nextRecallAt(...)`. Confirm red, then add literal `AND mt.type <> 'COMMISSIONED'` beside the existing SPELLING filter in `findBatchQuestionGenerationCandidatesByUser`. Keep `@Param` for userId/dueBy (T-01-02). Do not enlarge `QuestionGenerationBatchLocalPlanningTest` unless the candidate test is insufficient.

    After green, run full `pnpm backend:verify` for SC1 (existing assimilation/recall suites unchanged). No OpenAPI client regen, no ERD regen, no new Flyway (schema unchanged). Per D-02 foundation: filters only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only -- --tests com.odde.doughnut.services.QuestionGenerationBatchCandidateMemoryTrackersTest</automated>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:verify</automated>
  </verify>
  <acceptance_criteria>
    - `QuestionGenerationBatchCandidateMemoryTrackersTest` asserts commissioned due trackers are not candidate ids.
    - Batch native query excludes COMMISSIONED with a literal type comparison next to SPELLING.
    - `pnpm backend:verify` exits 0 (SC1).
    - No new migration files; no generate-api-client / database-erd run required.
  </acceptance_criteria>
  <done>Batch planning ignores commissioned trackers; SC1 backend verify is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Learner session → Assimilation / batch planning | Existing authz; no new create endpoints |
| JPQL / native repository queries | User id and due timestamps bound via `@Param` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of privilege | Assimilation / batch APIs | medium | accept | No new endpoints; no assimilate-as-commissioned create in Phase 1 |
| T-01-02 | Tampering (SQLi) | NoteRepository / NotePropertyIndexRepository / batch native SQL | high | mitigate | JPQL fully-qualified enum constants + native literal COMMISSIONED; `@Param` for user-bound values only |
| T-01-03 | Tampering | Unique key on `type` | medium | accept | Unchanged from quick 006 |
| T-01-SC | Tampering | Package installs | high | accept | No package installs |
</threat_model>

<verification>
- Assimilation queue + batch candidate proofs green.
- `pnpm backend:verify` green (SC1).
- Structure stop-safe: no assimilate UI/DTO/Learning Session entities.
</verification>

<success_criteria>
Ordinary assimilation detection and batch candidates ignore COMMISSIONED; existing backend suites pass; Phase 2 can build assimilate-as-commissioned on this Structure.
</success_criteria>

<output>
Create `.planning/phases/01-commissioned-tracker-model/01-02-SUMMARY.md` when done
</output>
