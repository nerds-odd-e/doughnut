---
phase: 01-commissioned-tracker-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V300000238__add_memory_tracker_commissioned.sql
  - backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java
  - backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java
  - backend/src/test/java/com/odde/doughnut/testability/builders/MemoryTrackerBuilder.java
  - backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java
autonomous: false
requirements: []
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC2: Domain can persist an ordinary memory tracker and a commissioned memory tracker on the same note for the same user (coexistence)."
    - "SC3: Due-recall selection via RecallsController.recalling returns only the ordinary due tracker when both ordinary and commissioned are due."
    - "Existing rows default to commissioned=false; ordinary recall path unchanged when no commissioned fixtures exist."
  artifacts:
    - path: "backend/src/main/resources/db/migration/V300000238__add_memory_tracker_commissioned.sql"
      provides: "commissioned column + unique key including commissioned"
      contains: "commissioned"
    - path: "backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java"
      provides: "Boolean commissioned field default false"
      contains: "commissioned"
    - path: "backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java"
      provides: "byUserIdFrom excludes commissioned"
      contains: "commissioned IS FALSE"
    - path: "backend/src/test/java/com/odde/doughnut/testability/builders/MemoryTrackerBuilder.java"
      provides: "fluent .commissioned() fixture helper"
      contains: "commissioned()"
    - path: "backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java"
      provides: "controller-boundary coexistence + due exclusion proof"
      contains: "Commissioned"
  key_links:
    - from: "MemoryTrackerBuilder.commissioned()"
      to: "memory_tracker.commissioned"
      via: "persist via makeMe → JPA MemoryTracker.commissioned"
      pattern: "setCommissioned\\(true\\)"
    - from: "RecallsController.recalling"
      to: "MemoryTrackerRepository.findAllByUserAndNextRecallAtLessThanEqualOrderByNextRecallAt"
      via: "RecallService → UserService → byUserIdFrom SQL"
      pattern: "commissioned IS FALSE"
    - from: "V300000238 unique key"
      to: "ordinary + commissioned rows same note"
      via: "user_note_spelling_active includes commissioned"
      pattern: "user_note_spelling_active"
---

<objective>
Persist a commissioned memory tracker variant and prove ordinary due-recall excludes it, without any user-visible create path (Structure phase).

Purpose: Unlock Phase 2 assimilate-as-commissioned by making coexistence and due exclusion real in the database and selection seam (CONTEXT coexistence / D-02; ROADMAP SC2–SC3).

Output: Flyway migration + entity field + makeMe helper + due-SQL filter + RecallsController unit proof.

**Specless probe skip (recorded):** Phase has no requirement IDs to probe — probe fallback skipped; success criteria SC1–SC3 in must_haves are the verification contract.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
@/Users/terryyin/git/doughnut/.agents/skills/execute-plan/SKILL.md
@/Users/terryyin/git/doughnut/.agents/skills/post-change-refactor/SKILL.md
@/Users/terryyin/git/doughnut/.agents/skills/adr-awareness/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-commissioned-tracker-model/CONTEXT.md
@.planning/phases/01-commissioned-tracker-model/01-RESEARCH.md
@.planning/phases/01-commissioned-tracker-model/01-PATTERNS.md
@.planning/phases/01-commissioned-tracker-model/01-VALIDATION.md
@.cursor/rules/planning.mdc
@.cursor/rules/db-migration.mdc
@.cursor/rules/unit-testing.mdc
@.cursor/rules/error-handling.mdc

# Decision map (CONTEXT has no D-XX headings — planner numbering for coverage)
# D-02 Coexistence: commissioned tracker coexists with ordinary trackers on the same note — THIS PLAN
# D-01 Opt-in assimilate caret, D-03 score→schedule, D-04–D-08 UI/session/lifecycle — later phases / deferred from Phase 1

# Assumptions locked for execution (RESEARCH A1–A5, planner auto-mode):
# - Boolean `commissioned` tinyint(1) NOT NULL DEFAULT 0 (mirror spelling)
# - Rebuild UNIQUE `user_note_spelling_active` to include `commissioned`; keep index name (Q2 RESOLVED)
# - Put `AND rp.commissioned IS FALSE` in `byUserIdFrom` (due + countByUserNotRemoved)
# - Leave `byUserIdWhere` recent/list queries unfiltered (Q1 RESOLVED)
# - Do not change assimilate HTTP/UI or MemoryTrackerService.assimilate short-circuit (Phase 2)
# - Serialize `commissioned` on MemoryTracker JSON (default false) — Structure-acceptable wire
# - Proposed ADRs 0001/0003/0005 guide naming only — do not approve
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

| Symbol / artifact | Kind | Introduced in |
|-------------------|------|---------------|
| `memory_tracker.commissioned` | DB column `tinyint(1) NOT NULL DEFAULT 0` | Plan 01 |
| `user_note_spelling_active` (rebuild) | UNIQUE KEY including `commissioned` + soft-delete expr | Plan 01 |
| `MemoryTracker.commissioned` | Java `Boolean` field default `false` | Plan 01 |
| `MemoryTrackerBuilder.commissioned()` | Test fixture fluent helper | Plan 01 |
| `MemoryTrackerRepository.byUserIdFrom` filter | SQL `AND rp.commissioned IS FALSE` | Plan 01 |
| `RecallsControllerTests` commissioned exclusion case | Unit proof SC2+SC3 | Plan 01 |
| `MemoryTracker.JPA_WHERE_*` ordinary-only join fragment (or equivalent AND) | JPQL ordinary assimilation join | Plan 02 |
| `MemoryTrackerRepository.findBatchQuestionGenerationCandidatesByUser` filter | SQL `AND mt.commissioned IS FALSE` | Plan 02 |
| Assimilation queue commissioned-only case | Unit proof Phase-2-ready join | Plan 02 |
| `docs/database-erd.md` | Regenerated ERD including `commissioned` | Plan 02 |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm one-way unique-key rebuild for commissioned coexistence</name>
  <files></files>
  <read_first>
    - `.planning/phases/01-commissioned-tracker-model/01-RESEARCH.md` (Pitfall 1, Flyway skeleton, Alternatives Considered)
    - `backend/src/main/resources/db/migration/V100000000__baseline.sql` (UNIQUE KEY `user_note_spelling_active`)
    - `.cursor/rules/db-migration.mdc`
  </read_first>
  <decision>
    Rebuild `user_note_spelling_active` on `memory_tracker` to include boolean `commissioned` so an ordinary and a commissioned active tracker can coexist for the same `(user_id, note_id, spelling, property_key)` (CONTEXT coexistence / D-02).
  </decision>
  <context>
    This is a one-way schema door: after production applies the DROP/ADD UNIQUE, undoing requires another migration and may conflict with rows that already rely on the wider key. RESEARCH rejected separate tables and `property_key` encoding. Phase 1 still has no public create path — fixtures/tests prove the constraint.
  </context>
  <options>
    <option id="boolean-in-unique-key">
      <name>Boolean `commissioned` column + UNIQUE includes `commissioned` (RESEARCH recommendation)</name>
      <pros>Matches `spelling` pattern; race-safe DB coexistence; minimal surface; Phase 2 assimilate can insert without unique collisions</pros>
      <cons>Unique-key rebuild is a one-way/costly migration; index name stays historically spelling-centric</cons>
    </option>
    <option id="separate-table">
      <name>Separate `commissioned_memory_tracker` table</name>
      <pros>Clear type split</pros>
      <cons>Duplicates scheduling columns; fights ADR vocabulary (MemoryTracker variant); heavier Phase 1 Structure</cons>
    </option>
    <option id="property-key-encode">
      <name>Encode commissioned via reserved `property_key`</name>
      <pros>No unique-key rebuild</pros>
      <cons>Collides with property trackers; opaque vs glossary; uniqueness semantics muddy</cons>
    </option>
  </options>
  <action>
    Present the three options. Default recommendation: `boolean-in-unique-key`. Wait for explicit selection before the tracer migration task. Per REVERSIBILITY_GATES, do not apply Flyway until approved. After the human selects, write `.planning/phases/01-commissioned-tracker-model/01-UNIQUE-KEY-DECISION.md` with a single line matching `selected: boolean-in-unique-key` (or the chosen option id) plus a one-line rationale. Do not start the tracer until that file exists.
  </action>
  <verify>
    <automated>grep -E '^selected:[[:space:]]*(boolean-in-unique-key|separate-table|property-key-encode)$' .planning/phases/01-commissioned-tracker-model/01-UNIQUE-KEY-DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - Human selected an option id (expected: boolean-in-unique-key)
    - `.planning/phases/01-commissioned-tracker-model/01-UNIQUE-KEY-DECISION.md` exists with a `selected:` line before migration lands
  </acceptance_criteria>
  <done>Unique-key approach confirmed and recorded in 01-UNIQUE-KEY-DECISION.md; tracer may proceed</done>
  <resume-signal>Select: boolean-in-unique-key, separate-table, or property-key-encode</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>End-to-end commissioned persist + due-recall exclusion — one path</name>
  <reversibility rating="one-way">Flyway DROP/ADD UNIQUE on `user_note_spelling_active` including `commissioned` is a one-way schema door after environments apply it; undo needs a follow-up migration and careful row handling.</reversibility>
  <files>
    backend/src/main/resources/db/migration/V300000238__add_memory_tracker_commissioned.sql,
    backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java,
    backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java,
    backend/src/test/java/com/odde/doughnut/testability/builders/MemoryTrackerBuilder.java,
    backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java
  </files>
  <read_first>
    - `.planning/phases/01-commissioned-tracker-model/01-PATTERNS.md` (migration compose, spelling field analog, byUserIdFrom, RecallsControllerTests pattern)
    - `backend/src/main/java/com/odde/doughnut/entities/MemoryTracker.java` (`spelling` field ~83–86)
    - `backend/src/main/java/com/odde/doughnut/entities/repositories/MemoryTrackerRepository.java` (`byUserIdFrom` ~63–67; due query using it)
    - `backend/src/test/java/com/odde/doughnut/testability/builders/MemoryTrackerBuilder.java` (`spelling()` / `removedFromTracking()`)
    - `backend/src/test/java/com/odde/doughnut/controllers/RecallsControllerTests.java` (`Repeat.shouldExcludeMemoryTrackersForDeletedNotesFromRecallLists`)
    - `backend/src/main/resources/db/migration/V300000232__add_health_remove_empty_folders_default.sql` (boolean ADD precedent)
    - Highest tip: `V300000237__add_memory_tracker_next_recall_at_index.sql` — new version must be greater
  </read_first>
  <behavior>
    - RED then GREEN: With time travel to `now`, one note owns an ordinary due MemoryTracker and a commissioned due MemoryTracker (makeMe). `RecallsController.recalling("Asia/Shanghai", 0)` returns `toRepeat` of size 1 (ordinary only). Persist of both must succeed (unique key allows coexistence / D-02).
    - Default: new MemoryTracker / existing rows behave as `commissioned=false` so suites without commissioned fixtures stay equivalent.
    - Assert only the delta that is unique to this scenario (`toRepeat` size / exclusion); do not re-assert unrelated DueMemoryTrackers shape already covered elsewhere (`unit-testing.mdc`).
  </behavior>
  <action>
    After checkpoint approval of `boolean-in-unique-key` (or halt and replan if another option won):

    1. Add Flyway `V300000238__add_memory_tracker_commissioned.sql` (or next free version greater than `300000237`): `ADD COLUMN commissioned tinyint(1) NOT NULL DEFAULT 0`; then `DROP INDEX user_note_spelling_active` and `ADD UNIQUE KEY user_note_spelling_active (user_id, note_id, spelling, property_key, commissioned, (if((deleted_at is null),1,NULL)))`. Never edit baseline. Capability name only — no phase numbers in filename description beyond version.

    2. On `MemoryTracker`, add `Boolean commissioned = false` with `@Column(name = "commissioned")` beside `spelling`. Leave factory builders defaulting false. Prefer serialize on API (RESEARCH A4) — do not `@JsonIgnore` unless OpenAPI regen forces a freeze discussion (Jidoka).

    3. Extend `MemoryTrackerBuilder` with `commissioned()` setting `entity.setCommissioned(true)` mirroring `spelling()`.

    4. Append `AND rp.commissioned IS FALSE` to `byUserIdFrom` only (parameterized `@Param` queries stay parameterized — never concatenate user input). This excludes commissioned from due list and `countByUserNotRemoved` / `totalAssimilatedCount`. Leave `findByUserAndNote` and `byUserIdWhere` (recent lists) without commissioned filter in this tracer. Do not edit `MemoryTrackerService.assimilate` (Phase 2).

    5. In `RecallsControllerTests.Repeat`, add a small-test case: ordinary + commissioned both due on same note → `toRepeat` size 1. Drive `controller.recalling`; real DB; no repository mocks.

    6. TDD order: write failing test first (may fail on missing builder/column/unique), then migration+entity+SQL+builder until green.

    Out of scope this task: NoteRepository unassimilated join, batch candidate SQL, AssimilationController tests, ERD, Learning Session entities, assimilate caret/UI, E2E `@wip`.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:verify</automated>
  </verify>
  <acceptance_criteria>
    - Migration file version greater than 300000237 exists and adds `commissioned` plus rebuilt unique key including `commissioned`
    - `MemoryTracker` has `commissioned` Boolean default false
    - `MemoryTrackerBuilder.commissioned()` exists
    - `byUserIdFrom` contains `commissioned IS FALSE`
    - RecallsController test proves ordinary+commissioned same note → due list size 1
    - `pnpm backend:verify` green (Flyway applied in tests)
    - No assimilate UI/DTO, Learning Session tables, or new E2E scenarios added
  </acceptance_criteria>
  <done>Tracer path works: coexistence persist + due exclusion proven at RecallsController; SC2 and SC3 satisfied for the due-recall seam</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → RecallsController | Existing authz (`assertLoggedIn`); no new public create path in Phase 1 |
| App → MySQL native queries | SQL strings in repository; only bind via `@Param` |
| DB unique constraint | Concurrent inserts of active trackers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of privilege (IDOR) | Recalls / MemoryTracker show-update | medium | accept | Unchanged AuthorizationService paths; Phase 1 adds no new create endpoint |
| T-01-02 | Tampering (SQLi) | `MemoryTrackerRepository` native due SQL | high | mitigate | Keep parameterized `@Param` only when editing `byUserIdFrom`; no string-built user input |
| T-01-03 | Tampering (unique-key race) | `user_note_spelling_active` | medium | mitigate | DB UNIQUE includes `commissioned`; do not replace with app-only checks; let constraint failures surface (`error-handling.mdc`) |
| T-01-04 | Information disclosure | `MemoryTracker` JSON `commissioned` | low | accept | Default false; no UI copy; Structure-acceptable wire field for Phase 2 |
| T-01-05 | Denial of service | Mass commissioned rows | low | accept | No public create in Phase 1; Phase 2 reuses assimilate authz |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | N/A — no package installs this phase |
</threat_model>

<verification>
- Targeted: `CURSOR_DEV=true nix develop -c pnpm backend:verify` after tracer
- Goal-backward: SC2 coexistence + SC3 due exclusion true via RecallsController test
- Structure: no user-facing assimilate caret / Learning Session / E2E graduation
</verification>

<success_criteria>
- Checkpoint resolved (`boolean-in-unique-key` expected)
- Commissioned column + unique key live under Flyway
- Due-recall SQL excludes commissioned
- Controller-boundary proof green
</success_criteria>

<output>
Create `.planning/phases/01-commissioned-tracker-model/01-01-SUMMARY.md` when done.
After plan/phase close per execute-plan: Jidoka → post-change-refactor → update plan → commit → push.
</output>
