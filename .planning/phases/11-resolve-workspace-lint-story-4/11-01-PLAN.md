---
phase: 11-resolve-workspace-lint-story-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/src/lint/lintWorkspace.ts
  - cli/src/lint/portableContract.ts
  - cli/tests/lintWorkspace.test.ts
  - e2e_test/features/cli/cli_lint_workspace.feature
  - cli/src/commands/lintSlashCommand.ts
autonomous: false
requirements:
  - LINT-01
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "OKF malformed-frontmatter / unknown-key / unrecognised-type behavior and actionable path[:line] severity message findings remain; success string stays Workspace follows the OKF format. (D-02, D-04)"
    - "Duplicate non-empty doughnut_id values on concept .md files (excluding index.md / log.md) each produce error findings; missing doughnut_id is not an error (D-05)"
    - "Broken local Markdown and [[wiki]] targets that do not resolve to an existing workspace file produce errors; http(s) and remote /attachments/… are not flagged (D-06)"
    - "Concept-bearing directories without index.md (including workspace root when it has concepts) produce missing-index errors; empty dirs out of scope (D-07)"
    - "Unsafe / non-portable path shapes among workspace paths or local link targets are reported using Phase 9 unsafePathReason vocabulary (D-08)"
    - "Unit proofs for broken local link and missing index.md are inverted to error findings; E2E conformant fixture is portable-valid; four gap scenarios proven (D-03, D-10)"
    - "/lint remains read-only; Terry-authored previewPullActions.ts is import-only (D-09, HYG-02)"
  artifacts:
    - path: cli/src/lint/portableContract.ts
      provides: "Portable duplicate-id / link / index / path findings as Finding[]"
      contains: "portableContractFindings"
    - path: cli/src/lint/lintWorkspace.ts
      provides: "OKF + portable orchestration into lintReport"
      contains: "portableContractFindings"
    - path: cli/tests/lintWorkspace.test.ts
      provides: "Inverted D-03 proofs + four portable gap units + CONFORMS fixture cascade"
      contains: "doughnut_id"
    - path: e2e_test/features/cli/cli_lint_workspace.feature
      provides: "Capability E2E for four gaps + fixed conformant success"
      contains: "Workspace follows the OKF format."
  key_links:
    - from: lintWorkspace
      to: portableContractFindings
      via: "append portable findings after OKF flatMap"
      pattern: "portableContractFindings"
    - from: portableContract
      to: extractDoughnutId / unsafePathReason
      via: "import-only from previewPullActions.js"
      pattern: "from '../sync/previewPullActions.js'"
    - from: lintWorkspace
      to: lintReport
      via: "combined Finding[] → CONFORMS string"
      pattern: "lintReport\\("
  assumptions:
    - "Wiki resolution is path-oriented (target / target.md relative to source dir or workspace root); no title→note matching in this phase (RESEARCH A1)"
    - "Markdown hrefs starting with / that are not http(s) and not /attachments/… are workspace-root-relative (RESEARCH A2)"
    - "Index rule = directories that directly contain ≥1 concept .md require sibling index.md (RESEARCH A3)"
    - "E2E unsupported-path proof uses unsafe local link target (e.g. ../…) plus unit synthetic unsafePathReason coverage (RESEARCH A4)"
  prohibitions:
    - statement: "Do not edit Terry-authored previewPullActions.ts — import extractDoughnutId and unsafePathReason only (HYG-02)"
      status: active
      verification: "git diff --name-only excludes cli/src/sync/previewPullActions.ts"
    - statement: "Do not install new npm packages"
      status: active
      verification: "cli/package.json dependencies unchanged for new packages"
    - statement: "Do not change Stories 5–6 push modules"
      status: active
      verification: "git diff --name-only excludes cli/src/push/ and push-related command modules"
    - statement: "Do not change the CONFORMS success string (D-04)"
      status: active
      verification: "lintReport.ts still exports Workspace follows the OKF format."
    - statement: "Do not encode phase numbers in product or test names"
      status: active
      verification: "new scenarios/tests use capability names only"
---

<objective>
Strengthen `/lint` so Story 4 / LINT-01 closes all four TRIAGE portable gaps on top of OKF: duplicate `doughnut_id`, broken local links/wikis, missing `index.md` in concept-bearing dirs, and unsupported/unsafe path shapes — proven by `lintWorkspace.test.ts` and `cli_lint_workspace.feature` (D-01..D-11).

Purpose: One coarse Behavior plan (D-11) that lands portable helpers + unit invert + E2E in the same plan without per-rule micro-plans.
Output: Production portable findings wired through `lintWorkspace` + green units + green targeted lint E2E; import-only reuse of Terry path/id helpers.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-resolve-workspace-lint-story-4/11-CONTEXT.md
@.planning/phases/11-resolve-workspace-lint-story-4/11-RESEARCH.md
@.planning/phases/11-resolve-workspace-lint-story-4/11-PATTERNS.md
@.planning/phases/11-resolve-workspace-lint-story-4/11-COVERAGE.md
@.planning/phases/11-resolve-workspace-lint-story-4/11-VALIDATION.md
@.planning/phases/07-publish-triage-decisions/TRIAGE.md
@.planning/phases/09-resolve-preview-before-pull-story-2/09-CONTEXT.md
@.planning/notes/2026-07-24-portable-notebook-workspace.md
@.cursor/rules/planning.mdc
@.cursor/rules/cli.mdc
@cli/src/lint/lintWorkspace.ts
@cli/src/lint/lintReport.ts
@cli/src/lint/okfConcept.ts
@cli/src/lint/okfProblem.ts
@cli/src/lint/bundleFiles.ts
@cli/src/sync/previewPullActions.ts
@cli/src/commands/lintSlashCommand.ts
@cli/tests/lintWorkspace.test.ts
@e2e_test/features/cli/cli_lint_workspace.feature
</context>

## Phase Goal

Story 4 `/lint` participant work is healthy against acceptance: malformed frontmatter, duplicate identities, broken local links, missing indexes, unsupported path mappings, actionable findings; a valid portable workspace succeeds (strengthen path for LINT-01).

This plan delivers the full strengthen in one coarse wave (D-11).

## Artifacts this phase produces

| Symbol / artifact | Path | Role |
|-------------------|------|------|
| `portableContractFindings` | `cli/src/lint/portableContract.ts` | Four portable rule scanners → `Finding[]` |
| `lintWorkspace` (strengthened) | `cli/src/lint/lintWorkspace.ts` | OKF + portable orchestration |
| Unit proofs | `cli/tests/lintWorkspace.test.ts` | D-03 invert + four gaps + fixture cascade |
| Capability E2E | `e2e_test/features/cli/cli_lint_workspace.feature` | Four gaps + fixed conformant success |
| Optional help polish | `cli/src/commands/lintSlashCommand.ts` | Doc/help only if needed (D-09) |
| API coverage decision | `11-COVERAGE.md` | Declares no external API integration |

## Source coverage audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|-----|---------------------|------|--------|-------|
| GOAL | — | Story 4 `/lint` healthy or gone | 01 | COVERED | Strengthen via portable+units+E2E |
| REQ | LINT-01 | Lint matches story 4 acceptance | 01 | COVERED | All four TRIAGE gaps |
| RESEARCH | — | Additive portable*.ts + lintWorkspace | 01 | COVERED | Task 2 |
| RESEARCH | — | Invert broken-link + missing-index proofs | 01 | COVERED | Checkpoint + tasks 2–3 |
| RESEARCH | — | Import extractDoughnutId / unsafePathReason | 01 | COVERED | Task 2 HYG-02 |
| RESEARCH | — | Fixture cascade + conformant E2E fix | 01 | COVERED | Tasks 2–3 |
| CONTEXT | D-01 | Close all four portable gaps | 01 | COVERED | Tasks 2–3 |
| CONTEXT | D-02 | Keep OKF modules; additive checks | 01 | COVERED | Task 2 |
| CONTEXT | D-03 | Invert broken-link + missing-index contracts | 01 | COVERED | Checkpoint + tasks 2–3 |
| CONTEXT | D-04 | Keep CONFORMS success string | 01 | COVERED | Tasks 2–3 |
| CONTEXT | D-05 | doughnut_id duplicates; missing id OK | 01 | COVERED | Task 2 |
| CONTEXT | D-06 | Local MD + wiki; skip remote URLs | 01 | COVERED | Task 2 |
| CONTEXT | D-07 | Missing index on concept-bearing dirs | 01 | COVERED | Task 2 |
| CONTEXT | D-08 | Phase 9 path vocabulary via unsafePathReason | 01 | COVERED | Task 2 |
| CONTEXT | D-09 | Read-only; lint surface only | 01 | COVERED | Tasks 2–3 |
| CONTEXT | D-10 | Prove via feature + lintWorkspace tests | 01 | COVERED | Tasks 2–3 |
| CONTEXT | D-11 | One coarse plan / 1–2 larger tasks | 01 | COVERED | This plan shape |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm one-way invert of broken-link and missing-index lint contracts (D-03)</name>
  <decision>Replace the published OKF unit and E2E contracts that currently accept a missing local link target and a concept-bearing workspace without index.md with error findings so `/lint` matches the portable Story 4 oracle.</decision>
  <context>
    D-03 is one-way: `lintWorkspace.test.ts` under the must-not-reject describe and the E2E conformant scenario currently treat broken `./banana.md` / `/pear` links and missing `index.md` as success. Inverting that is the Story 4 proof surface for LINT-01. Locked CONTEXT already chose invert; under `--auto` select invert-portable without waiting on a human if this gate is still open. Undoing later means rewriting the published capability E2E again and drifting from the oracle (REVERSIBILITY_GATES).
  </context>
  <options>
    <option id="invert-portable">
      <name>Invert broken-link + missing-index → error proofs</name>
      <pros>Matches TRIAGE strengthen, D-01/D-03, and Story 4 oracle; unlocks LINT-01 in one Behavior phase</pros>
      <cons>Published unit/E2E contract for those two cases changes permanently</cons>
    </option>
    <option id="keep-okf-accept">
      <name>Keep OKF accept (abort portable strengthen)</name>
      <pros>No public contract flip</pros>
      <cons>Leaves broken-link and missing-index gaps open; contradicts locked D-01/D-03 — not a valid LINT-01 completion</cons>
    </option>
  </options>
  <resume-signal>Select: invert-portable (expected per locked D-03; auto-select under --auto) or keep-okf-accept</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>End-to-end portable contract findings through lintWorkspace (units)</name>
  <reversibility rating="one-way">D-03: inverting broken-link and missing-index unit expectations changes the published Story 4 contract; paired with the checkpoint above.</reversibility>
  <precondition>Checkpoint resolved with invert-portable (or --auto selected invert-portable); Phase 9 extractDoughnutId and unsafePathReason available to import</precondition>
  <files>cli/src/lint/portableContract.ts, cli/src/lint/lintWorkspace.ts, cli/tests/lintWorkspace.test.ts</files>
  <read_first>
    - .planning/phases/11-resolve-workspace-lint-story-4/11-CONTEXT.md (D-01..D-11)
    - .planning/phases/11-resolve-workspace-lint-story-4/11-RESEARCH.md (Patterns 1–3; Pitfalls 1–8; A1–A4; Wave 0)
    - .planning/phases/11-resolve-workspace-lint-story-4/11-PATTERNS.md (additive orchestration; Finding shape; HYG-02 import)
    - cli/src/lint/lintWorkspace.ts
    - cli/src/lint/lintReport.ts
    - cli/src/lint/okfProblem.ts
    - cli/src/lint/bundleFiles.ts
    - cli/src/sync/previewPullActions.ts (extractDoughnutId, unsafePathReason — import only)
    - cli/tests/lintWorkspace.test.ts
  </read_first>
  <behavior>
    - Two concept files sharing the same non-empty doughnut_id each produce error findings naming the paths; index.md / log.md are excluded from the identity map; missing doughnut_id alone still CONFORMS when otherwise valid (D-05)
    - Local Markdown link to a missing workspace path (including workspace-root-relative `/pear`) and broken `[[wiki]]` path target produce errors; `https://…` and `/attachments/…` do not (D-06, A1, A2)
    - Concept-bearing directory without sibling index.md produces an index.md error; after cascade, previously CONFORMS fixtures that add minimal index.md still CONFORMS (D-07, A3)
    - Synthetic unsafe relative paths / link targets that match unsafePathReason surface that vocabulary (D-08, A4)
    - Former broken-link and missing-index must-not-reject cases now match /error/i (and path/link or index.md substrings); unrecognised type and unknown keys still CONFORMS (D-03)
    - Success string remains exactly Workspace follows the OKF format. when there are no errors (D-04)
  </behavior>
  <action>
    After invert-portable confirmation (D-03): RED first — rewrite the two conflicting unit cases under the must-not-reject describe so broken `/pear` (with a minimal root index.md once D-07 is in play) and concept-without-index expect error findings; add units for duplicate doughnut_id on two concepts, broken wiki target, and unsafe link/path messaging via unsafePathReason; cascade minimal `index.md` (and nested `fruit/index.md` where needed) into every remaining CONFORMS fixture in the same change (Pitfall 1). Keep unrecognised `type` and unknown-key cases green. GREEN — add `cli/src/lint/portableContract.ts` exporting one public `portableContractFindings` (internal scanners unexported per cli.mdc) that returns `Finding[]` for: (1) doughnut_id collisions via import-only `extractDoughnutId`, excluding reserved basenames index.md/log.md (D-05); (2) local MD `[…](href)` and `[[wiki]]` targets with path-oriented resolve, skipping http(s) and `/attachments/…`, treating leading-`/` non-remote hrefs as workspace-root-relative (D-06, A1, A2); (3) directories that directly contain ≥1 concept `.md` without sibling `index.md` (D-07, A3); (4) workspace `.md` paths and local link targets checked with import-only `unsafePathReason`, mirroring preview wording where practical (D-08). Wire `lintWorkspace` to keep `problemsIn` OKF dispatch, then append portable findings before non-md warnings (D-02). Prefer severity `error` for oracle gaps. Do not edit `previewPullActions.ts` (HYG-02). Do not change `lintReport` CONFORMS string (D-04). Do not write the workspace from lint (D-09). No new npm packages. Prefer one coherent commit for this unit-green slice (D-11).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm -C cli exec vitest run tests/lintWorkspace.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Unit: duplicate doughnut_id on two concept paths reports error on each colliding path (D-05)
    - Unit: broken local MD and wiki targets report errors; http(s) and /attachments/… do not (D-06)
    - Unit: concept-bearing dir without index.md reports error; empty dirs ignored (D-07)
    - Unit: unsafe path / link target surfaces unsafePathReason vocabulary (D-08)
    - Unit: former broken-link and missing-index accept cases now fail closed with errors (D-03)
    - Unit: unrecognised type and unknown keys still CONFORMS; CONFORMS string unchanged (D-03, D-04)
    - Unit: all prior CONFORMS fixtures that write concepts include required index.md and stay green (fixture cascade)
    - `git diff --name-only` excludes `cli/src/sync/previewPullActions.ts` (HYG-02)
  </acceptance_criteria>
  <done>Production OKF+portable lint path is unit-green for all four gaps; D-03 unit contracts inverted; CONFORMS fixtures cascaded.</done>
</task>

<task type="auto" tdd="true">
  <name>Capability E2E — four portable gaps + conformant success</name>
  <reversibility rating="one-way">D-03: published cli_lint_workspace.feature conformant scenario and new gap scenarios lock the portable error contract.</reversibility>
  <precondition>Task 2 lintWorkspace units green; invert-portable still in force</precondition>
  <files>e2e_test/features/cli/cli_lint_workspace.feature, cli/src/commands/lintSlashCommand.ts</files>
  <read_first>
    - .planning/phases/11-resolve-workspace-lint-story-4/11-CONTEXT.md (D-03, D-04, D-10, D-11)
    - .planning/phases/11-resolve-workspace-lint-story-4/11-RESEARCH.md (Pitfalls 2, 4; Wave 0 E2E; A4)
    - .planning/phases/11-resolve-workspace-lint-story-4/11-PATTERNS.md (feature scenario shape)
    - e2e_test/features/cli/cli_lint_workspace.feature
    - cli/src/commands/lintSlashCommand.ts
  </read_first>
  <behavior>
    - Conformant workspace includes resolvable banana.md plus required index.md (and any nested indexes) and reports Workspace follows the OKF format. (D-03, D-04)
    - Duplicate doughnut_id scenario shows error findings naming colliding paths (D-05, D-10)
    - Broken local link scenario shows an error for the missing target (D-06, D-10)
    - Missing index.md scenario shows an error mentioning index.md (D-07, D-10)
    - Unsupported/unsafe path scenario uses an unsafe local link target (e.g. ../…) and shows unsafe/portable path messaging (D-08, A4, D-10)
    - Existing malformed-frontmatter, non-md warning, and multi-finding scenarios stay green; warning-only success paths that expect CONFORMS cascade index.md if they write concepts (D-04, D-10)
  </behavior>
  <action>
    Fix `A conformant bundle reports nothing` so the sample is portable-valid: add `banana.md` (valid concept frontmatter + body) and root `index.md`; keep `.git/config.md` unscanned proof (D-03, Pitfall 2). Add capability-named scenarios (no phase numbers) for duplicate doughnut_id, broken local link, missing index.md, and unsupported/unsafe path via unsafe link target (A4 / Pitfall 4). Cascade `index.md` into warning-only E2E paths that still expect CONFORMS when they write concepts. Assert stable substrings (`doughnut_id`, `index.md`, link/missing/broken, unsafe) plus the existing CONFORMS string (D-04). Optionally polish `/lint` help/doc only — do not change run wiring or Stories 5–6 modules (D-09). Tag new scenarios `@wip` only while red; remove `@wip` before phase close. Prefer grouping E2E+optional help in one commit once green (D-11). Run post-change-refactor on the uncommitted change before closing (execute-plan wrap-up).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_lint_workspace.feature</automated>
  </verify>
  <acceptance_criteria>
    - Conformant E2E has banana.md + index.md and reports Workspace follows the OKF format. (D-03, D-04)
    - E2E scenarios cover duplicate id, broken local link, missing index, unsupported/unsafe path (D-01, D-10)
    - Existing malformed / warning / multi-finding scenarios pass without leftover `@wip` (D-10)
    - Capability names only — no phase numbers in scenario titles (D-10 / planning.mdc)
    - Diff excludes previewPullActions.ts and push modules (HYG-02 / D-09)
  </acceptance_criteria>
  <done>Targeted cli_lint_workspace.feature proves all four portable gaps + portable-valid success for LINT-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied workspace directory → lint scan | Untrusted path argument scoped by existing `parseDirectoryArgument` |
| Markdown / wiki link hrefs → path resolution | Untrusted link text may contain `..`, absolute, or escape-shaped targets |
| Lint → filesystem | Read-only reads under workspace; no writes to workspace, `.doughnut-sync/`, or Doughnut |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering / Information disclosure | Link/`..` path resolution | high | mitigate | Resolve for existence checks only; flag via `unsafePathReason`; never write or follow links outside the bundle (task 2) |
| T-11-02 | Tampering | Lint mutates workspace / sync metadata | high | mitigate | D-09 read-only — no `writeFile` / baseline writes in lint path (task 2) |
| T-11-03 | Tampering | Absolute / empty-segment / backslash paths | medium | mitigate | Reuse import-only `unsafePathReason` vocabulary aligned with Phase 9 preview (D-08, task 2) |
| T-11-04 | Information disclosure | Finding messages | low | accept | Report only workspace-relative paths already under the scanned root; no secrets/tokens |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — legitimacy audit N/A for installs |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c pnpm -C cli exec vitest run tests/lintWorkspace.test.ts` green
- `CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_lint_workspace.feature` green (assume `pnpm sut` running)
- Diff excludes Terry-authored `previewPullActions.ts` (HYG-02)
- All four TRIAGE Story 4 gaps closed; CONFORMS string unchanged; no `@wip` left on new lint scenarios
- post-change-refactor + commit+push per execute-plan / local wrap-up
</verification>

<success_criteria>
LINT-01 satisfied via strengthen: `/lint` reports duplicate doughnut_id, broken local links/wikis, missing indexes, and unsupported/unsafe paths on top of OKF, with actionable findings and Workspace follows the OKF format. on valid workspaces, proven by units + targeted `cli_lint_workspace.feature`, with D-03 contract invert confirmed.
</success_criteria>

<output>
Create `.planning/phases/11-resolve-workspace-lint-story-4/11-01-SUMMARY.md` when done
</output>
