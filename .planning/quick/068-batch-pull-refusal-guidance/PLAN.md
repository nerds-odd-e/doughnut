# Give accurate guidance when a divergent batch cannot be pulled

## Source and review evidence

[SEED-009 story 18a](../../seeds/SEED-009-git-backed-local-notebook-workflow.md#story-18a),
a bounded correction to delivered story 18 / quick 066.
Original contract: `fe081db7cb`. Reviewed execution patches: `565e0acde7`
(eligibility and guidance), `f815eb188a` (installed CLI composition), and
`fc9bc7a477` (learning retention and completion); `28bffb2a4e` is seed cleanup.
Unrelated interleaved worktree-environment changes are excluded.

`LOCAL_TWO_NOTE_UNSUPPORTED_ACCEPTED` tells an owner to publish divergent local
work. `assertLocalMainFollowsAcceptedHistory` requires a direct child of the
current accepted head, so that proposed next step cannot succeed as stated.
The finding remains present after cleanup commit `9e7b8e5f99`.

## Goal and scope

An owner receives truthful guidance after a refused batch pull: local work is
preserved; reconciliation or recreation onto accepted history is required
before publication. Change only the batch-specific error and its observable
assertions. Preserve all eligibility, Git mutation, and publication behavior.
Exclude automatic recovery, recovery tutorials, repeated-pull support, broader
batch rebase, new commands, and backend changes.

## Ordered slices

### 1. Explain the publication prerequisite after a refused batch pull

Type: Behavior
Status: planned
Proof: Drive `run(['notebook', 'pull', directory])` with the existing real-Git
two-note rejection fixtures and mocked Donut HTTP. Observe accurate guidance
and unchanged checkout; retain the supported-rebase success observation.

Behavior: One A/B commit diverges from two accepted C saves → pull → refusal
explains preserved work and reconciliation onto accepted history before publish.

Replace “Reduce or publish the local work” in `notebookLocalCandidate.ts` with
that prerequisite. Update the shared expected message in the batch fixture;
reuse the existing rejection assertions rather than adding a duplicate suite.
Do not change `notebookPublishAncestry.ts` or loosen rejection predicates.

Focused verification:
`CURSOR_DEV=true nix develop -c pnpm -C cli exec vitest run tests/notebookPull.test.ts`.

Sizing hypothesis: about five minutes including the focused loop, high
confidence; no Structure leaf or additional refinement needed.

## Promise ownership and current status

Slice 1 owns accurate failure guidance, checkout preservation, and unchanged
supported success guidance, observed by the existing rejection and success
cases through CLI `run`. Publication's ancestry rule remains unchanged.

Ready for direct execution. Generated by execution retrospective; not executed.
