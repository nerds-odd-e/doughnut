# Exclusive OpenAI request recording proof

Source: [SEED-015 story 3](../../seeds/SEED-015-concurrent-worktree-environments.md#story-3).
Status: planned; generated by execution retrospective, not executed.

## Goal and scope

Complete the existing evidence that each worktree retains only its own OpenAI
requests across a peer mock reset. Preserve the delivered content suggestion,
unavailable-service case, private routing, and supported single-spec boundary.
No broader mock support, new product behavior, or generic proof framework.

## Evidence and current decisions

Original contract is recoverable at `6e17a7e58c^` under quick/070. Reviewed
execution set: `6e17a7e58c`, `005898f8d4`, `724d0d71e0`, `bef7001402`,
`c60532bf33`, `39537a92b6`, `47c4b24eba`; `cfa7bac567` is backlog provenance.
Review selected patches, excluding unrelated intervening changes. The relevant
assertions remain unchanged in the current tree.

The feature checks recordings before/after the reset barrier, but
`worktree_reset_isolation.ts` delegates “only my request marker” to
`openAiService.ts::expectLastResponsesPostBodyContains`, which only asserts
presence. A recording containing both worktrees' markers passes. This is a
proof gap, not evidence that private routing currently leaks requests.

`fetchOpenAiImposterRequests` also has only a test caller while the browser uses
`cyFetchOpenAiImposterRequests`; consolidate the implicated recording-read
contract as part of the same proof correction, preserving high-level coverage.

## Outside-in proof and ordered slice

### 1. Reject peer requests while preserving the owner's recorded requests
Type: Behavior
Status: planned
Proof: Paired completion scenario, distinct own/peer markers, assertions before
and after peer reset. Both directions retain the owner's marker and reject the
other marker. A focused mixed-recording counterexample fails the same assertion
used by the browser. Ordinary unpaired completion remains green.

Behavior: Two worktrees configure distinct responses and record their requests
→ peer resets its mock → each recording retains its own request and contains
no request carrying the other worktree's marker.

Pass both marker expectations through the existing harness proof context;
do not hardcode the default peer marker in the browser. Preserve the barrier
and test only the added exclusivity condition. Remove the test-only fetch path
or make a single real recording contract serve both browser and focused proof.

Focused verification: `CURSOR_DEV=true nix develop -c pnpm -C cli exec tsx
--test ../e2e_test/start/mock_services/openAiMockEndpointContext.test.ts`, then
ordinary completion and `CURSOR_DEV=true nix develop -c node
scripts/worktree-reset-isolation-harness.mjs --mode openai-mock --peer <A>
--resetter <B>` with two owning SUTs; reverse A/B. Record actual paths/results
during execution. Do not treat harness spawn-argument unit tests as paired proof.

Target ~5 minutes for the assertion/context change; paired SUT/Cypress runtime
is an explicit external-wait exception. One proof loop, no standalone Structure
leaf. All promises above map to this leaf. Ready for direct execution.
No implementation, commit, or push performed.
