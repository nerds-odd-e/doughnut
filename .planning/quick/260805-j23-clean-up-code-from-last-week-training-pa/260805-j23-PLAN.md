---
phase: 260805-j23-clean-up-code-from-last-week-training-pa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/src/sync/
  - cli/src/lint/
  - cli/src/commands/notebook/exportSlashCommand.tsx
  - cli/src/commands/notebook/syncSlashCommand.tsx
  - cli/src/commands/notebook/pushSlashCommand.tsx
  - cli/src/commands/notebook/notebookStageSlashCommands.ts
  - cli/src/commands/lintSlashCommand.ts
  - cli/src/commands/interactiveSlashCommands.ts
  - cli/src/backendApi/doughnutBackendClient.ts
  - cli/tests/
  - e2e_test/features/cli/
  - e2e_test/step_definitions/
  - e2e_test/start/pageObjects/cli/
  - backend/src/main/java/com/odde/doughnut/services/notebookExport/
  - backend/src/main/java/com/odde/doughnut/services/NotebookExportService.java
  - backend/src/main/java/com/odde/doughnut/controllers/NotebookController.java
  - backend/src/test/java/com/odde/doughnut/services/notebookExport/
  - backend/src/test/java/com/odde/doughnut/controllers/NotebookExportControllerTest.java
autonomous: true
requirements:
  - QUICK-260805-j23
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - CLI no longer offers notebook /export, /sync, /push, or top-level /lint slash commands
    - Entire cli/src/sync and cli/src/lint trees are gone; related CLI unit tests and portable-workspace E2E features are gone
    - Catalog notebook ZIP export still downloads a zip (notebook_export.feature passes)
    - ZIP markdown assembly no longer performs sync-era wiki rewrite, attachment absolute-URL rewrite, or publicOrigin plumbing
    - Product code and tests under cli/, backend/, frontend/, e2e_test/ contain no sync-baseline metadata path strings
  artifacts:
    - e2e_test/features/notebooks/notebook_export.feature
    - frontend/src/utils/notebookExport.ts
    - backend/.../NotebookExportService.java
    - backend/.../NotebookZipBuilder.java
    - backend/.../ExportNoteMarkdown.java
  key_links:
    - NotebookController GET .../export → NotebookExportService → NotebookZipBuilder → ExportNoteMarkdown
    - frontend notebookExport.ts fetch → catalog export button → downloaded zip
    - notebookStageSlashCommands / interactiveSlashCommands registries no longer register removed commands
---

<objective>
Aggressively remove LIA training portable-workspace CLI sync/lint stacks and sync-era ZIP strengthen, while keeping the minimal web catalog ZIP download that `notebook_export.feature` needs (ADR 0002; CONTEXT 1A/2A/3A as D-01/D-02/D-03).

Purpose: After Approved ADR 0002, `.doughnut-sync` two-way sync is retired; training strengthen work is obsolete. Stop-safe Structure deletes first (green CLI), then simplify backend ZIP, then prove catalog export E2E.

Output: One commit-ready cleanup: CLI sync/lint gone, minimal ZIP export green, no sync-baseline residue in product/tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
@/Users/terryyin/git/doughnut/.agents/skills/post-change-refactor/SKILL.md
</execution_context>

<context>
@.planning/quick/260805-j23-clean-up-code-from-last-week-training-pa/260805-j23-CONTEXT.md
@docs/adrs/0002-git-native-notebooks-backed-by-mysql.md
@e2e_test/features/notebooks/notebook_export.feature
@.cursor/rules/planning.mdc
@.cursor/rules/gsd-coexistence.mdc

# Locked decisions (map CONTEXT → D-IDs for coverage)
# D-01 = CONTEXT 1A — remove ALL CLI portable workspace stack
# D-02 = CONTEXT 2A — remove /lint entirely
# D-03 = CONTEXT 3A — keep minimal ZIP for notebook_export.feature; strip sync-oriented strengthen
# D-04 = aggressive dead code — delete unused helpers/tests; no speculative keep
# D-05 = do not remove unrelated Terry this-week work; prefer delete current tree over git revert archaeology

# Do NOT update ROADMAP.md for this quick task.
# Do NOT touch: SRS late-success penalty, notebook description UI, rename-from-summary, ADR docs.
# Prefer `CURSOR_DEV=true nix develop -c … trash <paths>` over `rm -rf`.
</context>

## Source audit

| Source | Item | Plan coverage |
|--------|------|---------------|
| GOAL | Remove training portable-workspace code; keep catalog ZIP E2E | Tasks 1–3 |
| CONTEXT D-01 (1A) | Remove CLI export/sync/push + sync/** + related E2E/tests | Task 1 |
| CONTEXT D-02 (2A) | Remove /lint + lint/** + related E2E/tests | Task 1 |
| CONTEXT D-03 (3A) | Keep web ZIP + notebook_export.feature; strip sync strengthen | Tasks 2–3 |
| CONTEXT D-04 | Aggressive dead code / no residual sync-baseline paths | Tasks 1–3 |
| CONTEXT D-05 | Preserve unrelated Terry work; delete tree not revert | All tasks (scope guard) |
| ADR 0002 | Sync baselines retired; ZIP may remain one-way | Tasks 2–3 |

<tasks>

<task type="tracer">
  <name>Task 1: Trash CLI portable workspace + lint stack end-to-end</name>
  <files>cli/src/sync/, cli/src/lint/, cli/src/commands/notebook/exportSlashCommand.tsx, cli/src/commands/notebook/syncSlashCommand.tsx, cli/src/commands/notebook/pushSlashCommand.tsx, cli/src/commands/notebook/notebookStageSlashCommands.ts, cli/src/commands/lintSlashCommand.ts, cli/src/commands/interactiveSlashCommands.ts, cli/src/backendApi/doughnutBackendClient.ts, cli/tests/, e2e_test/features/cli/cli_export.feature, e2e_test/features/cli/cli_sync_dry_run.feature, e2e_test/features/cli/cli_sync_pull.feature, e2e_test/features/cli/cli_push_dry_run.feature, e2e_test/features/cli/cli_lint_workspace.feature, e2e_test/step_definitions/cli_export.ts, e2e_test/step_definitions/cli_sync_pull.ts, e2e_test/step_definitions/cli_workspace.ts, e2e_test/step_definitions/cli.ts, e2e_test/start/pageObjects/cli/</files>
  <action>
  Per D-01 and D-02: remove the entire CLI portable-workspace and lint surfaces in one vertical cut so remaining CLI commands still compile and unit-test green.

  Trash (Nix `trash`, not `rm -rf`) production trees and command modules:
  - Entire `cli/src/sync/**` and `cli/src/lint/**`
  - `exportSlashCommand.tsx`, `syncSlashCommand.tsx`, `pushSlashCommand.tsx`, `lintSlashCommand.ts`
  - Unregister from `notebookStageSlashCommands.ts` (keep attach + leave/exit only) and `interactiveSlashCommands.ts` (drop lint import/entry)

  Trash CLI unit/harness tests that only exercise the removed stack (non-exhaustive verify during exec; expand if `cli:test` points at more):
  - applyPull, previewPull*, previewPush*, pushBaseline, pushArgument, syncArgument, writeNotebookExport, exportDestination, exportSlashCommand, diffReport, readWorkspace, unzip, unifiedDiff, contentDispositionFileName, lintWorkspace*, lintWorkspaceFixture
  - Update `notebookStageSlashCommands.test.ts` and `slashCommandCompletion.test.ts` so they no longer expect /export, /sync, /push, or /lint (rewrite /ex completion to only /exit once /export is gone; drop /lint fixtures used only as negative examples)

  Remove CLI HTTP zip download path that existed only for sync/export commands (D-04):
  - Delete `downloadNotebookExportZip` (and its sync-module imports) from `doughnutBackendClient.ts`
  - Remove `downloadNotebookExportZip` describe block from `doughnutBackendClient.errors.test.ts`; keep other client error coverage

  Trash E2E portable-workspace features + glue (D-01/D-02):
  - Features: `cli_export.feature`, `cli_sync_dry_run.feature`, `cli_sync_pull.feature`, `cli_push_dry_run.feature`, `cli_lint_workspace.feature`
  - Step defs: `cli_export.ts`, `cli_sync_pull.ts`, `cli_workspace.ts`
  - Page objects unused after: `exportDestination.ts`, `syncPull.ts`, `workspace.ts` — then slim `e2e_test/start/pageObjects/cli/index.ts`
  - In `e2e_test/step_definitions/cli.ts`, stop calling workspace-name path substitution on slash commands (pass the command string through unchanged)

  Scope guard (D-05): do not edit SRS late-success penalty, notebook description UI, rename-from-summary, or ADR docs. Do not specially preserve student sync code. Prefer deleting the current tree over archaeology/revert.

  After deletes: run post-change-refactor on the CLI command-registry / client concepts implicated by this change (dead imports, registry tests, oversized files). Then atomic commit for this task only (code/tests; not planning docs).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cli:test</automated>
  </verify>
  <done>
  D-01/D-02: `cli/src/sync` and `cli/src/lint` directories absent; export/sync/push/lint command modules and their E2E features gone; registries no longer list them; `pnpm cli:test` green; `downloadNotebookExportZip` removed from the CLI client.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip sync-era ZIP strengthen; keep minimal download ZIP</name>
  <files>backend/src/main/java/com/odde/doughnut/services/notebookExport/ExportNoteMarkdown.java, backend/src/main/java/com/odde/doughnut/services/notebookExport/NotebookZipBuilder.java, backend/src/main/java/com/odde/doughnut/services/NotebookExportService.java, backend/src/main/java/com/odde/doughnut/controllers/NotebookController.java, backend/src/test/java/com/odde/doughnut/services/notebookExport/ExportNoteMarkdownTest.java, backend/src/test/java/com/odde/doughnut/services/notebookExport/NotebookZipBuilderTest.java, backend/src/test/java/com/odde/doughnut/controllers/NotebookExportControllerTest.java</files>
  <behavior>
    - ExportNoteMarkdown.assemble emits title heading + body (and preserves author frontmatter when present) without wiki→MD rewrite and without attachment absolute-URL rewrite
    - NotebookZipBuilder.build produces a downloadable zip with index.md / note .md files / nested folders using NotebookExportFilenames; no publicOrigin parameter
    - NotebookController.exportNotebook still returns application/zip attachment with a filename; auth denial still throws
    - Tests that pinned wiki rewrite / attachment origin / publicOriginFrom are removed or rewritten to the minimal contract above
  </behavior>
  <action>
  Per D-03: keep `GET /api/notebooks/{notebook}/export`, `NotebookExportService`, `NotebookZipBuilder`, `ExportNoteMarkdown`, and frontend catalog export (`frontend/src/utils/notebookExport.ts` and catalog export UI) — do not delete the web ZIP path.

  Simplify markdown assembly to the download E2E contract only:
  - `ExportNoteMarkdown.assemble` retains frontmatter preservation + `# title` + body; remove wiki rewrite helpers, attachment rewrite helpers, and path-relativize helpers used only for those rewrites
  - Drop `publicOrigin` / notebookName / noteIdToZipPath / titleToNoteId parameters once unused by assemble
  - Thread the simplification through `NotebookZipBuilder.build` and `NotebookExportService.exportNotebookAsZip` (signature without publicOrigin)
  - In `NotebookController.exportNotebook`, stop computing/passing publicOrigin; delete `publicOriginFrom` if it has no remaining callers
  - Rewrite unit tests to the minimal ZIP behaviors (structure, frontmatter, controller zip headers). Delete or rewrite tests whose only purpose was sync-era strengthen (wiki→MD, absolute attachment URLs, publicOrigin port assertions)

  Do not change frontend export wiring unless a compile break forces a touch — D-03 says keep it. Do not touch unrelated Terry work (D-05).

  After green backend tests: post-change-refactor on the notebook-export concept (dead params, dead helpers, file size). Atomic commit for this task.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c backend/gradlew -p backend test -Dspring.profiles.active=test --tests "com.odde.doughnut.services.notebookExport.*" --tests "com.odde.doughnut.controllers.NotebookExportControllerTest"</automated>
  </verify>
  <done>
  D-03: ZIP export still builds and controller returns a zip attachment; sync-era wiki/attachment/publicOrigin strengthen removed from export assembly and matching tests; focused backend export tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove catalog ZIP E2E + residual cleanup gate</name>
  <files>e2e_test/features/notebooks/notebook_export.feature, frontend/src/utils/notebookExport.ts</files>
  <action>
  Per D-03 and D-04: confirm the kept web path still downloads a zip end-to-end, and scrub any leftover portable-workspace residue in product/test trees.

  Run targeted E2E only (not full suite):
  `CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/notebooks/notebook_export.feature`

  Grep gate (product + tests; exclude docs/, .planning/, ADR prose): under `cli/`, `backend/`, `frontend/`, `e2e_test/` there must be zero matches for the sync-baseline directory token. If any remain, trash or rewrite those leftovers (D-04).

  Confirm `notebook_export.feature` and `frontend/src/utils/notebookExport.ts` / catalog export buttons remain. Confirm ADR docs and unrelated Terry work were not reverted (D-05).

  Final post-change-refactor over the full uncommitted delta if any residual edits; then atomic commit if there are leftover code fixes from this task (skip empty commit).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/notebooks/notebook_export.feature &amp;&amp; ( ! rg -n 'doughnut-sync' cli backend frontend e2e_test --glob '!**/node_modules/**' --glob '!**/build/**' --glob '!**/dist/**' )</automated>
  </verify>
  <done>
  D-03/D-04: `notebook_export.feature` passes; catalog ZIP download path kept; no sync-baseline directory token remains under cli/backend/frontend/e2e_test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/notebooks/{id}/export | Authenticated read; zip bytes leave the server |
| CLI interactive slash registry | Commands available to local token holder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260805-j23-01 | Elevation of Privilege | NotebookController.exportNotebook | medium | mitigate | Keep existing read-authorization check on export; Task 2 must not weaken auth when simplifying signatures |
| T-260805-j23-02 | Information Disclosure | ZIP contents | low | accept | One-way export of notebook content the user can already read; ADR 0002 allows ZIP for portability |
| T-260805-j23-03 | Tampering | Removed CLI sync apply path | low | accept | Deleting apply/pull/push reduces local workspace mutation surface; no new write path added |
| T-260805-j23-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs in this plan |
</threat_model>

<verification>
1. `CURSOR_DEV=true nix develop -c pnpm cli:test` green after Task 1
2. Focused backend notebookExport + NotebookExportControllerTest green after Task 2
3. Targeted `notebook_export.feature` E2E green after Task 3
4. No sync-baseline directory token under cli/backend/frontend/e2e_test
5. Unrelated Terry this-week work and ADR docs untouched
</verification>

<success_criteria>
- CLI portable workspace + lint stacks fully removed (D-01, D-02)
- Minimal catalog ZIP export remains and E2E downloads a zip (D-03)
- Aggressive dead code / sync-baseline residue cleared from product tests (D-04)
- No ROADMAP edits; planning artifacts stay under this quick dir only
</success_criteria>

<output>
Create `.planning/quick/260805-j23-clean-up-code-from-last-week-training-pa/260805-j23-SUMMARY.md` when execution finishes (orchestrator/docs commit owns PLAN/SUMMARY/STATE).
</output>
