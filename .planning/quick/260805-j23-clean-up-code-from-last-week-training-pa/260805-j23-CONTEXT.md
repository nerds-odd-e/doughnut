# Quick Task 260805-j23: Clean up code from last week training participants - Context

**Gathered:** 2026-08-05
**Status:** Ready for planning

<domain>
## Task Boundary

Aggressively remove LeSS-in-Action training participant portable-workspace code (stories 1–6) and the subsequent v1.2 cleanup strengthen work, because Approved ADR 0002 moves to git-native notebooks and retires `.doughnut-sync` sync.

**Keep:** `e2e_test/features/notebooks/notebook_export.feature` and the minimal backend/web ZIP export needed for that E2E to pass.

**Do not remove:** Terry / Yeong Sheng last-week work that is unrelated to the portable sync stack (e.g. dependency bumps). This week's Terry cleanup-of-portable-workspace commits' *effects on the sync stack* should be undone by deletion; unrelated this-week Terry work (SRS late-success penalty, notebook description UI, rename-from-summary, ADR docs) must stay.

</domain>

<decisions>
## Implementation Decisions

### 1. CLI portable workspace stack
- **1A:** Remove all of it — `/export` Markdown-tree write, `/sync` pull + dry-run, `/push --dry-run`, `.doughnut-sync` baselines, `cli/src/sync/**`, related E2E (`cli_export`, `cli_sync_*`, `cli_push_dry_run`) and step defs. Keep only web notebook ZIP export + `notebook_export.feature`.

### 2. CLI `/lint`
- **2A:** Remove `/lint` entirely — `lintSlashCommand`, `cli/src/lint/**`, `cli_lint_workspace.feature`, related tests/step defs.

### 3. Backend/web ZIP export
- **3A:** Keep ZIP export that makes `notebook_export.feature` pass; strip sync-oriented strengthen (e.g. doughnut_id inject, wiki→MD rewrite, attachment absolute-URL rewrite, publicOrigin plumbing) if unused by that E2E. Prefer simplest ZIP that still downloads successfully from the catalog.

### Aggressive dead code
- Remove unused helpers, tests, fixtures, docs/plans scraps, and references even if they might be useful later. No speculative keep.

### Claude's Discretion
- Inventory exact file list during planning; prefer trash over leave-behind stubs.
- After deletes, run targeted CLI unit tests + `notebook_export.feature` E2E; do not run full E2E suite.
- Follow local wrap-up: post-change-refactor, commit atomically per plan task.
- Do not revert git history commit-by-commit; delete/simplify current tree to match decisions.

</decisions>

<specifics>
## Specific Ideas

- Prior cleanup milestone (v1.2) *strengthened* sync; that work is now obsolete — remove, do not preserve strengthen outcomes.
- ADR 0002 Consequence: `.doughnut-sync` baselines give way to Git commit graph — delete all `.doughnut-sync` writers/readers.
- Exception explicitly named by user: `e2e_test/features/notebooks/notebook_export.feature`.

</specifics>

<canonical_refs>
## Canonical References

- `docs/adrs/0002-git-native-notebooks-backed-by-mysql.md` (Approved) — git-native; ZIP may remain one-way; `.doughnut-sync` retired
- `.planning/notes/2026-07-24-portable-notebook-workspace.md` — stories 1–6 (training scope to remove)
- Prior cleanup session: [v1.2 training cleanup](520c5275-fb8a-4c48-bd3a-0be991412591) — completed strengthen; now superseded by ADR 0002

</canonical_refs>
