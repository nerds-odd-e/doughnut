---
phase: 08-resolve-pull-export-story-1
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - e2e_test/features/cli/cli_export.feature
  - e2e_test/step_definitions/cli_export.ts
  - e2e_test/start/pageObjects/cli/exportDestination.ts
autonomous: true
requirements:
  - EXP-01
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "CLI /export writes note files that contain doughnut_id frontmatter on disk (D-01, EXP-01)"
    - "CLI /export writes at least one ordinary relative Markdown link for a resolvable internal wiki ref (D-04)"
    - "CLI /export writes at least one absolute attachment URL containing the API origin and /attachments/images/ (D-05)"
    - "Existing cli_export.feature scenarios (hierarchy, index.md, missing destination, sync-state inventory) remain green without @wip"
    - "No identity/link rewrite duplicated in CLI unzip; proofs consume backend zip bytes only (D-06)"
  artifacts:
    - path: e2e_test/features/cli/cli_export.feature
      provides: "Capability E2E scenarios for three Story 1 gaps"
      contains: "doughnut_id"
    - path: e2e_test/step_definitions/cli_export.ts
      provides: "Thin glue only if new steps required"
  key_links:
    - from: cli_export.feature
      to: "/export → exportNotebook HTTP → zip → writeNotebookExport"
      via: "existing When I export the notebook into … steps"
      pattern: "export the notebook into"
    - from: "destinationFileShouldHold"
      to: "exported .md on disk"
      via: "substring includes assertions for doughnut_id, ](, /attachments/images/"
      pattern: "destinationFileShouldHold|should hold"
  assumptions:
    - "Plan 08-01 backend zip strengthen is merged/available so E2E can go green without CLI rewrite"
    - "Note Content column can seed wiki links; image upload step seeds /attachments/images/ frontmatter or body refs"
    - "Public origin seen by export matches DOUGHNUT_API_BASE_URL / local LB Host used by CLI E2E (RESEARCH A2)"
  prohibitions:
    - statement: "must NOT implement rewrite in writeNotebookExport or unzip (D-06)"
      status: resolved
      verification: "git diff --name-only for this plan excludes cli/src/sync/writeNotebookExport.ts rewrite changes (or only touch if baseline seed break — prefer zero CLI product edits)"
    - statement: "must NOT change applyPull / Story 2 preview surfaces (D-07)"
      status: resolved
      verification: "git diff --name-only excludes cli/src/sync/applyPull.ts"
    - statement: "must NOT leave new scenarios @wip at plan close"
      status: resolved
      verification: "cli_export.feature new scenarios have no @wip tag when green"
    - statement: "must NOT commit @focus or @only"
      status: resolved
      verification: "feature file has neither @focus nor @only"
---

<objective>
Prove Story 1 strengthen end-to-end: CLI `/export` leaves `doughnut_id`, ordinary Markdown links, and absolute remote attachment URLs on disk via `cli_export.feature` (D-01..D-05, EXP-01), without duplicating rewrite in the CLI (D-06).

Purpose: Observable filesystem acceptance for the three TRIAGE gaps after backend zip strengthen.
Output: Green capability E2E scenarios (no `@wip`) plus phase wrap-up checklist.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-resolve-pull-export-story-1/08-CONTEXT.md
@.planning/phases/08-resolve-pull-export-story-1/08-RESEARCH.md
@.planning/phases/08-resolve-pull-export-story-1/08-PATTERNS.md
@.planning/phases/08-resolve-pull-export-story-1/08-01-PLAN.md
@.planning/phases/07-publish-triage-decisions/TRIAGE.md
@.planning/notes/2026-07-24-portable-notebook-workspace.md
@e2e_test/features/cli/cli_export.feature
@e2e_test/step_definitions/cli_export.ts
@e2e_test/start/pageObjects/cli/exportDestination.ts
</context>

## Phase Goal

Story 1 pull/export participant work is healthy against acceptance (strengthen).

This plan proves the three remaining gaps through `/export` on a real notebook filesystem.

## Artifacts this plan produces

| Symbol / artifact | Path | Role |
|-------------------|------|------|
| Identity / link / attachment E2E scenarios | `e2e_test/features/cli/cli_export.feature` | EXP-01 proofs |
| Thin step glue (only if needed) | `cli_export.ts` / `exportDestination.ts` | Reuse `should hold` |

<tasks>

<task type="auto" tdd="true">
  <name>Prove Story 1 gaps on disk via cli_export E2E</name>
  <reversibility rating="costly">D-03: shipping identity without link/attachment E2E proofs leaves EXP-01 incomplete.</reversibility>
  <files>e2e_test/features/cli/cli_export.feature, e2e_test/step_definitions/cli_export.ts, e2e_test/start/pageObjects/cli/exportDestination.ts</files>
  <read_first>
    - e2e_test/features/cli/cli_export.feature
    - e2e_test/step_definitions/cli_export.ts
    - e2e_test/start/pageObjects/cli/exportDestination.ts
    - e2e_test/features/note_view/note_frontmatter_image.feature (upload image pattern)
    - e2e_test/step_definitions/note.ts (upload image; inject notes Content)
    - .planning/phases/08-resolve-pull-export-story-1/08-CONTEXT.md (D-01, D-04, D-05)
    - .planning/phases/08-resolve-pull-export-story-1/08-PATTERNS.md (E2E section)
    - .planning/phases/08-resolve-pull-export-story-1/08-01-SUMMARY.md (if present — zip contract shipped)
    - backend/src/main/java/com/odde/doughnut/services/notebookExport/NotebookZipBuilder.java
    - .planning/phases/08-resolve-pull-export-story-1/08-RESEARCH.md (Open Question 2 — origin behind LB)
  </read_first>
  <behavior>
    - Scenario: exported note file should hold `doughnut_id:` (D-01)
    - Scenario: note with body wiki to another note in the notebook exports an ordinary Markdown link (substring like `](` pointing at the target `.md`) (D-04)
    - Scenario: note with attachment image ref exports absolute URL containing `/attachments/images/` and not only the root-relative form (D-05)
    - Hierarchy / index.md / missing-destination / inventory scenarios still pass
    - Failed export still reports error (existing scenario)
  </behavior>
  <action>
    After Plan 08-01 zip strengthen is available: extend `cli_export.feature` with three focused scenarios under the existing Feature tags/Background and drive them green in one pass (standard granularity — do not require a separate RED-only authoring commit). Prefer seeding wiki via the notes table `Content` column (source note body wiki-links a sibling title). Prefer seeding attachment via existing `I upload an image from fixture "moon.jpg" to the note "…"` after notebook creation. Assert with existing `the file "…" in the export destination "…" should hold "…"` substring checks — add thin glue only if required. Optional `@wip` while iterating locally is fine; remove `@wip` before plan close. If absolute attachment URLs flake on Host/origin mismatch, fix origin derivation on the backend export path (still D-06) rather than rewriting in CLI unzip. Do not put rewrite logic under `cli/src/sync/` (D-06). Do not edit Story 2/3 mutate modules (D-07). Do not weaken existing inventory or failure scenarios.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_export.feature</automated>
  </verify>
  <acceptance_criteria>
    - Targeted `cli_export.feature` Cypress run exits 0
    - Three new scenarios assert `doughnut_id:`, an ordinary MD link (`](`), and `/attachments/images/` with an `http` absolute prefix (D-01, D-04, D-05)
    - No `@wip`, `@focus`, or `@only` on the new scenarios at plan close
    - Existing hierarchy / index / failure scenarios still present and green
    - No rewrite code added under `cli/src/sync/`
  </acceptance_criteria>
  <done>All three Story 1 gap proofs green on disk via /export; no leftover @wip on new scenarios.</done>
</task>

<task type="auto">
  <name>Phase wrap-up — Jidoka, refactor, validation, STATE</name>
  <files>.planning/phases/08-resolve-pull-export-story-1/08-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .agents/skills/post-change-refactor/SKILL.md
    - .cursor/rules/planning.mdc (phase discipline checklist)
    - .cursor/rules/gsd-coexistence.mdc (after-phase wrap-up)
    - .planning/phases/08-resolve-pull-export-story-1/08-VALIDATION.md
    - .planning/phases/07-publish-triage-decisions/TRIAGE.md (Story 1 finish sketch)
    - .planning/REQUIREMENTS.md (EXP-01)
  </read_first>
  <action>
    Run local phase wrap-up: (1) Jidoka — confirm EXP-01 strengthen matches oracle bullets that were gaps; confirm HYG-02 (no Terry Yin / Tan Yeong Sheng hunk rewrites); confirm D-07 surfaces untouched. (2) Run post-change-refactor skill on the uncommitted/phase diff for cohesion (prefer helpers stay under notebookExport/; no speculative layers). (3) Mark Wave 0 items done in `08-VALIDATION.md`, set `nyquist_compliant: true` / `wave_0_complete: true` when all mapped verifies are green, expand task ID rows to 08-01-* / 08-02-*. (4) Update ROADMAP Phase 8 Plans checkboxes and STATE current position to Phase 8 complete when both plans’ SUMMARYs exist. Commit+push per execute-plan / coexistence deploy gate after human/orchestrator approval of the run — do not skip hooks.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only; CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_export.feature</automated>
  </verify>
  <acceptance_criteria>
    - Backend tests + `cli_export.feature` green
    - `08-VALIDATION.md` Wave 0 checkboxes checked; nyquist_compliant true when criteria met
    - Diff review: no Terry/YS-owned files rewritten; no Story 2/3 mutate modules changed unless required for /export green (should be none)
    - STATE/ROADMAP reflect Phase 8 plan completion
  </acceptance_criteria>
  <done>Phase 8 strengthen verified, refactored, documented, and ready for commit+push gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI with user token → export API | Authenticated download of zip bytes |
| Unzip → local workspace | Filesystem write under chosen destination; sync state under `.doughnut-sync/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Information Disclosure | Exported workspace inventory | high | mitigate | Keep E2E inventory asserting baseline + notes only — no credential paths; do not add secret-bearing files in scenarios |
| T-08-06 | Denial of Service | Cypress / CLI E2E | low | accept | Targeted spec only; no full-suite requirement locally |
| T-08-07 | Tampering | E2E seed data | low | accept | Test fixtures only; no production credentials in feature files |
| T-08-04 | Elevation of Privilege | Export auth | high | mitigate | Reuse existing read-auth on export; E2E uses logged-in token path already present |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase |
</threat_model>

<verification>
- Targeted Cypress: `CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_export.feature`
- Backend regression: `CURSOR_DEV=true nix develop -c pnpm backend:test_only`
- HYG-02 / D-07 allowlist on final diff
</verification>

<success_criteria>
- EXP-01: Story 1 acceptance gaps closed and proven on disk via /export
- Hierarchy, index.md, sync-state separation, and failed-export reporting remain green
- D-06: no CLI-side rewrite dual source
- Phase wrap-up complete for execute-plan local overlays
</success_criteria>

<output>
Create `.planning/phases/08-resolve-pull-export-story-1/08-02-SUMMARY.md` when done
</output>
