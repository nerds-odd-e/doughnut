---
phase: 13-resolve-safe-push-story-6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e_test/features/cli/cli_push.feature
  - cli/src/commands/notebook/pushSlashCommand.tsx
  - cli/src/sync/pushArgument.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PUSH-02
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "e2e_test/features/cli/cli_push.feature is absent from the tree (D-02, D-05)"
    - "No Story-6 @ignore mutate-push E2E remains under e2e_test/features/cli/ (D-02, D-05)"
    - "No applyPush (or equivalent mutate-push) module exists under cli/src/sync/ (D-01, D-05)"
    - "parsePushArgument still rejects a workspace path without --dry-run with Usage: /push --dry-run <workspace path> (D-03, D-05)"
    - "Phase 12 dry-run keep set is behavior-intact: previewPush, pushArgument, pushBaseline, cli_push_dry_run.feature and their units still pass non-regression (D-03, D-05)"
    - "pushDoc (and optional pushArgument JSDoc) uses durable dry-run-only product copy with no WIP foreshadowing of a future mutate mode (D-04)"
    - "PUSH-02 is marked complete in REQUIREMENTS/ROADMAP/STATE as removed cleanly — not as a green mutate-push capability (D-01, D-05)"
    - "Terry Yin and Tan Yeong Sheng modules remain untouched (HYG-02)"
  artifacts:
    - path: e2e_test/features/cli/cli_push.feature
      provides: "Story-6-only @ignore WIP E2E — deleted"
      contains: "ABSENT after trash"
    - path: cli/src/commands/notebook/pushSlashCommand.tsx
      provides: "Dry-run /push entry; optional durable pushDoc copy"
      contains: "Requires --dry-run"
    - path: cli/src/sync/pushArgument.ts
      provides: "Mandatory --dry-run parser (behavior unchanged)"
      contains: "Usage: /push --dry-run <workspace path>"
    - path: .planning/REQUIREMENTS.md
      provides: "PUSH-02 checkbox flipped; Traceability Complete"
      contains: "PUSH-02"
  key_links:
    - from: trash cli_push.feature
      to: absence proofs
      via: "file gone + no ^@ignore under features/cli/"
      pattern: "cli_push\\.feature"
    - from: parsePushArgument
      to: pushArgument.test.ts
      via: "rejects workspace path with no dry-run flag"
      pattern: "rejects a workspace path with no dry run flag"
    - from: Phase 12 keep set
      to: cli_push_dry_run.feature
      via: "targeted E2E non-regression of dry-run surface"
      pattern: "cli_push_dry_run\\.feature"
  assumptions:
    - "Durable help: Requires --dry-run. (or equivalent) without WIP foreshadowing is enough for D-04 (RESEARCH A1)"
    - "No Story-6-only training plan under docs/plans/ needs deletion here — leave dry-run known-issues and .planning history for Phase 14 HYG-01 (RESEARCH A2)"
    - "Prefer one commit bundling delete + optional polish + REQUIREMENTS/ROADMAP/STATE/SUMMARY; second docs-only only if hooks force (D-06 / A3)"
    - "Unit non-regression of pushArgument + previewPush* is the fast gate; run targeted cli_push_dry_run at phase gate when pnpm sut is healthy (A4)"
  prohibitions:
    - statement: "Do not implement mutating /push, version-safe update, conflict-refuse-on-mutate, or post-push baseline refresh (D-01)"
      status: active
      verification: "no new mutate-push module under cli/src/sync/; parsePushArgument still requires --dry-run"
    - statement: "Do not delete cli_push_dry_run.feature, dry-run modules, or docs/plans/2026-07-30-cli-push-dry-run-known-issues.md (D-02, D-03)"
      status: active
      verification: "git diff --name-only excludes those deletions"
    - statement: "Do not relax parsePushArgument acceptance rules or USAGE string (D-03, D-04)"
      status: active
      verification: "vitest tests/pushArgument.test.ts green"
    - statement: "Do not edit Terry Yin / Tan Yeong Sheng–owned modules (HYG-02)"
      status: active
      verification: "diff limited to Eric Yeh cli_push.feature delete + Ben Huang pushSlashCommand/pushArgument polish + planning docs"
    - statement: "Do not invent mutate-push E2E or encode phase numbers in product/test names (D-05)"
      status: active
      verification: "no new mutate feature file; capability names only"
    - statement: "Prefer one implementation commit bundling delete + polish + planning close; avoid micro-splits (D-06)"
      status: active
      verification: "plan closes with ≤2 commits (preferably 1)"
    - statement: "Do not install new npm packages"
      status: active
      verification: "cli/package.json dependencies unchanged for new packages"
---

<objective>
Apply TRIAGE Story 6 **remove** so Story-6-only mutating `/push` WIP is cleanly gone: trash `@ignore` `cli_push.feature`, optionally polish dry-run-only help copy, leave the Phase 12 shared dry-run surface intact, and close **PUSH-02** as **removed cleanly** (D-01..D-06).

Purpose: One coarse Behavior plan (D-06) — success is WIP gone, not green mutate-push scenarios.
Output: `cli_push.feature` absent; durable dry-run-only help (optional); dry-run units ± targeted `cli_push_dry_run` green; REQUIREMENTS/ROADMAP/STATE mark PUSH-02 removed cleanly.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-resolve-safe-push-story-6/13-CONTEXT.md
@.planning/phases/13-resolve-safe-push-story-6/13-RESEARCH.md
@.planning/phases/13-resolve-safe-push-story-6/13-PATTERNS.md
@.planning/phases/13-resolve-safe-push-story-6/13-VALIDATION.md
@.planning/phases/07-publish-triage-decisions/TRIAGE.md
@.planning/phases/12-resolve-push-dry-run-story-5/12-01-SUMMARY.md
@.planning/notes/2026-07-24-portable-notebook-workspace.md
@.cursor/rules/planning.mdc
@.cursor/rules/cli.mdc
@e2e_test/features/cli/cli_push.feature
@e2e_test/features/cli/cli_push_dry_run.feature
@cli/src/commands/notebook/pushSlashCommand.tsx
@cli/src/sync/pushArgument.ts
@cli/tests/pushArgument.test.ts
</context>

## Phase Goal

Story 6 safe push participant work is **cleanly gone**: the `@ignore` mutate-push E2E scaffold is deleted, shared dry-run `/push --dry-run` from Phase 12 remains healthy, and PUSH-02 closes as **removed cleanly** (remove path — not keep/strengthen mutate push).

This plan delivers the full remove in one coarse wave (D-06).

## Artifacts this phase produces

| Symbol / artifact | Path | Role |
|-------------------|------|------|
| Story-6 WIP E2E | `e2e_test/features/cli/cli_push.feature` | **Deleted** via `trash` |
| Optional help polish | `cli/src/commands/notebook/pushSlashCommand.tsx` | Durable dry-run-only `pushDoc` (D-04) |
| Optional JSDoc polish | `cli/src/sync/pushArgument.ts` | Durable dry-run-only comment (discretion) |
| Planning close | `.planning/REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md` | PUSH-02 **removed cleanly** |

## Source coverage audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|-----|---------------------|------|--------|-------|
| GOAL | — | Story 6 work healthy or cleanly gone | 01 | COVERED | Remove path |
| REQ | PUSH-02 | Push existing notes — or removed cleanly | 01 | COVERED | Removed cleanly |
| RESEARCH | — | trash cli_push.feature | 01 | COVERED | Task 1 |
| RESEARCH | — | Orphan scan; keep dry-run set | 01 | COVERED | Task 1 |
| RESEARCH | — | Optional durable pushDoc / JSDoc | 01 | COVERED | Task 1 D-04 |
| RESEARCH | — | Absence + dry-run non-regression proofs | 01 | COVERED | Task 1 verify |
| RESEARCH | — | REQUIREMENTS/ROADMAP/STATE close | 01 | COVERED | Task 1 |
| CONTEXT | D-01 | Remove — no mutate push / applyPush | 01 | COVERED | Task 1 |
| CONTEXT | D-02 | Delete cli_push.feature; scan orphans | 01 | COVERED | Task 1 |
| CONTEXT | D-03 | Keep Phase 12 dry-run surface | 01 | COVERED | Task 1 |
| CONTEXT | D-04 | Optional help polish only | 01 | COVERED | Task 1 |
| CONTEXT | D-05 | Prove via absence + non-regression + mark removed | 01 | COVERED | Task 1 |
| CONTEXT | D-06 | 1 plan / 1 task / prefer one commit | 01 | COVERED | This plan shape |

<tasks>

<task type="tracer">
  <name>End-to-end remove Story 6 WIP — trash cli_push.feature + close PUSH-02</name>
  <reversibility rating="one-way">D-01/D-02: deleting the @ignore feature removes the unfinished Story 6 E2E scaffold; rebuild later only if a future milestone wants mutate push. Decision already locked in CONTEXT — no re-ask checkpoint.</reversibility>
  <files>e2e_test/features/cli/cli_push.feature, cli/src/commands/notebook/pushSlashCommand.tsx, cli/src/sync/pushArgument.ts, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/13-resolve-safe-push-story-6/13-CONTEXT.md (D-01..D-06)
    - .planning/phases/13-resolve-safe-push-story-6/13-RESEARCH.md (delete/keep sets; orphan scan; help-copy options; absence proofs; Pitfalls 1–6)
    - .planning/phases/13-resolve-safe-push-story-6/13-PATTERNS.md (trash pattern; CommandDoc tone; REQUIREMENTS close ea566e90df)
    - .planning/phases/07-publish-triage-decisions/TRIAGE.md (Story 6 verdict remove; delete/keep table)
    - e2e_test/features/cli/cli_push.feature
    - cli/src/commands/notebook/pushSlashCommand.tsx (pushDoc)
    - cli/src/sync/pushArgument.ts (JSDoc + parsePushArgument)
    - cli/tests/pushArgument.test.ts
    - .planning/REQUIREMENTS.md (PUSH-02 line + Traceability)
  </read_first>
  <action>
    Per D-06, land the entire Story 6 remove in this single tracer; prefer **one** implementation commit bundling product delete + optional polish + planning close (second docs-only commit only if hooks force).

    Per D-01: apply TRIAGE **remove** only — do not strengthen or build mutating `/push`, version-safe update, conflict-refuse-on-mutate, post-push baseline refresh, or any new mutate module under `cli/src/sync/`. Oracle Story 6 acceptance bullets are gap citations justifying remove, not implement targets.

    Per D-02: delete the sole Story-6-only target with `trash` (prefer over `rm -f`): `CURSOR_DEV=true nix develop -c trash e2e_test/features/cli/cli_push.feature` (or host `trash` if already on PATH). Quick orphan re-scan: confirm no Story-6-only step glue, no mutate-only plan under `docs/plans/` that must go now; leave shared interactive CLI steps and any `"1 note updated."` helpers used by `cli_sync_pull` alone. Do **not** delete `cli_push_dry_run.feature`, dry-run modules listed in TRIAGE keep set, or `docs/plans/2026-07-30-cli-push-dry-run-known-issues.md` (Phase 14 hygiene if spent).

    Per D-03: leave Phase 12 shared dry-run behavior untouched — `pushSlashCommand` wiring, `previewPush`, `parsePushArgument` rules, `pushBaseline`, export/read/unzip helpers, units, and `cli_push_dry_run.feature`.

    Per D-04 (optional, recommended): rephrase `pushDoc.description` to durable dry-run-only product copy using RESEARCH preferred wording (`Preview what pushing the workspace would change in Doughnut. Requires --dry-run.`) or the documented alt; drop WIP foreshadowing tone. Discretion: rephrase `pushArgument` JSDoc to state `/push` is dry-run-only (usage error without the flag), unlike `/sync`. Do **not** change `usage`, `USAGE`, or `parsePushArgument` acceptance rules.

    Per D-05: prove remove via absence + non-regression + planning close — (1) feature file absent; (2) no `^@ignore` under `e2e_test/features/cli/`; (3) `pushArgument.test.ts` still rejects non–dry-run; (4) no mutate module filename under `cli/src/sync/`; (5) run `pushArgument` + `previewPush*` units; prefer targeted `cli_push_dry_run` E2E at phase gate (assume `pnpm sut`); (6) flip REQUIREMENTS PUSH-02 to `[x]` without rewriting the sentence (wording already includes the removed-cleanly clause), set Traceability Status to `Complete`, mark ROADMAP Phase 13 complete / plan checkbox / Progress table, and record in STATE decisions that PUSH-02 closed as **removed cleanly** (deleted `cli_push.feature`; dry-run kept). Capability names only — no phase numbers in product/test names. Do not invent mutate-push E2E.

    HYG-02: touch only Eric Yeh delete target and Ben Huang polish surfaces (+ planning docs). After proofs: post-change-refactor on the uncommitted change → Jidoka → plan/SUMMARY update → prefer one commit → push (local wrap-up).
  </action>
  <verify>
    <automated>test ! -e e2e_test/features/cli/cli_push.feature && ! rg -n '^@ignore' e2e_test/features/cli/ && test ! -e cli/src/sync/applyPush.ts && CURSOR_DEV=true nix develop -c bash -c 'cd cli && pnpm exec vitest run tests/pushArgument.test.ts tests/previewPush.test.ts tests/previewPush.create.test.ts tests/previewPush.conflict.test.ts tests/previewPush.directional.test.ts' && CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_push_dry_run.feature</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e e2e_test/features/cli/cli_push.feature` (D-02, D-05)
    - `rg -n '^@ignore' e2e_test/features/cli/` yields no matches (D-02, D-05)
    - `test ! -e cli/src/sync/applyPush.ts` and no `cli/src/sync/applyPush*` filenames (D-01, D-05)
    - Unit: `parsePushArgument('./BenNotebook')` still returns Usage error requiring `--dry-run` (D-03, D-05)
    - Units: previewPush* suite green; targeted `cli_push_dry_run.feature` green at phase gate (D-03, D-05)
    - `pushDoc.description` has durable dry-run-only copy (RESEARCH preferred or alt); no WIP foreshadowing (D-04)
    - REQUIREMENTS PUSH-02 `[x]` + Traceability Complete; ROADMAP Phase 13 complete; STATE notes **removed cleanly** (D-05)
    - `git diff --name-only` does not delete `cli_push_dry_run.feature` or dry-run known-issues plan; does not touch Terry/YS modules (D-02, D-03, HYG-02)
    - Prefer ≤2 commits for the remove (preferably 1) (D-06)
  </acceptance_criteria>
  <done>PUSH-02 closed as removed cleanly: Story-6 @ignore E2E gone, dry-run surface non-regressed, planning checkboxes flipped without claiming mutate push landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied workspace path → `/push` | Untrusted path still scoped by existing `parsePushArgument` (must keep `--dry-run` mandatory) |
| Cleanup change → product surface | Remove must not introduce a mutate path or delete dry-run defenses |
| Planning checkbox → capability claim | Marking PUSH-02 complete must mean **removed cleanly**, not mutate-push shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | Accidental mutate push during cleanup | high | mitigate | D-01 prohibition; keep `--dry-run` mandatory; prove mutate module absent (task 1) |
| T-13-02 | Tampering | Deleting dry-run defenses with WIP | high | mitigate | D-03 keep set; unit + targeted `cli_push_dry_run` non-regression (task 1) |
| T-13-03 | Spoofing | Leaving @ignore aspirational mutate E2E on mainline | medium | mitigate | trash `cli_push.feature`; prove no `^@ignore` under `features/cli/` (D-02, D-05) |
| T-13-04 | Elevation of privilege | Relaxing parsePushArgument to allow bare `/push` | high | mitigate | Do not change USAGE/acceptance rules; vitest `pushArgument.test.ts` (D-03, D-04) |
| T-13-05 | Information disclosure | Help/report copy | low | accept | Help rephrase stays product-tone only; no new data paths |
| T-13-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — legitimacy audit N/A for installs |
</threat_model>

<verification>
- Absence: `cli_push.feature` gone; no `^@ignore` under `e2e_test/features/cli/`; no mutate module under `cli/src/sync/`
- `CURSOR_DEV=true nix develop -c bash -c 'cd cli && pnpm exec vitest run tests/pushArgument.test.ts tests/previewPush.test.ts tests/previewPush.create.test.ts tests/previewPush.conflict.test.ts tests/previewPush.directional.test.ts'` green
- `CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_push_dry_run.feature` green (assume `pnpm sut` running)
- REQUIREMENTS/ROADMAP/STATE show PUSH-02 **removed cleanly**
- Prefer one implementation commit (D-06); post-change-refactor + commit+push per execute-plan / local wrap-up
</verification>

<success_criteria>
PUSH-02 satisfied via **remove**: Story-6-only `@ignore` mutate-push E2E is gone, Phase 12 dry-run `/push --dry-run` remains green, `/push` without `--dry-run` is still a usage error, and planning artifacts record **removed cleanly** — not a shipped mutate-push capability.
</success_criteria>

<output>
Create `.planning/phases/13-resolve-safe-push-story-6/13-01-SUMMARY.md` when done
</output>
