---
phase: 12-resolve-push-dry-run-story-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/src/sync/previewPush.ts
  - cli/tests/previewPush.test.ts
  - e2e_test/features/cli/cli_push_dry_run.feature
  - cli/src/commands/notebook/pushSlashCommand.tsx
autonomous: true
requirements:
  - PUSH-01
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "previewPush never writes .doughnut-sync/baseline.json (or any sync metadata); it may only loadPushBaseline for directional classify (D-01, D-02)"
    - "Directional E2E/units are primed via export or explicit savePushBaseline — never via a prior dry-run (D-03)"
    - "Intersecting notes keep less.md (push) / less.md (pull) / less.md (CONFLICT); those directional labels are the update signal for existing notes (D-04, A1)"
    - "Local-only Markdown is reported as a create (heading includes (create), push-oriented empty remote); remote-only Markdown is reported as a create (heading includes (create), pull-oriented empty local) (D-04, D-05)"
    - "Conflict rows never carry an update action label; reserved index.md / log.md and .doughnut-sync paths stay out of ordinary create/update rows (D-04, D-05)"
    - "Unchanged intersecting notes stay omitted; empty report remains No changes to push. (D-05)"
    - "cli_push_dry_run.feature + previewPush.test.ts prove create/update visibility, conflict preserved, and zero sync-metadata / .md / Doughnut mutation (D-07)"
    - "No mutating /push; parsePushArgument still requires --dry-run; cli_push.feature untouched; previewPullActions.ts import-only (D-06, HYG-02)"
  artifacts:
    - path: cli/src/sync/previewPush.ts
      provides: "Load-only baseline preview with path-union create/update reporting"
      contains: "loadPushBaseline"
    - path: cli/tests/previewPush.test.ts
      provides: "Flipped baseline-write units + create/update + no-metadata-write proofs"
      contains: "does not write sync metadata"
    - path: e2e_test/features/cli/cli_push_dry_run.feature
      provides: "Export-primed directional + create + adds-no-files non-mutation proofs"
      contains: "The preview adds no files of its own"
  key_links:
    - from: previewPush
      to: loadPushBaseline
      via: "read merge-base only; no savePushBaseline call site"
      pattern: "loadPushBaseline\\("
    - from: previewPush
      to: classifyCreateOrUpdate
      via: "import-only from previewPullActions.js for create/update taxonomy"
      pattern: "from './previewPullActions.js'"
    - from: previewPush
      to: renderNoteDiff / renderDiffReport
      via: "compose headings in previewPush; prefer no diffReport XOR change"
      pattern: "renderNoteDiff\\("
    - from: cli_push_dry_run.feature
      to: /export priming
      via: "Rule Background uses export workspace, not priming dry-run"
      pattern: "export"
  assumptions:
    - "Intersecting (push)/(pull)/(CONFLICT) are the update signal; literal (update) on intersecting rows is optional (RESEARCH A1 / discretion)"
    - "Create heading uses path (create); direction is carried by renderNoteDiff orientation (push create = empty remote; pull create = empty local) (A1)"
    - "Primary non-mutation E2E flip = zip-seeded inventory without .doughnut-sync; optional export-primed baseline-bytes-unchanged if cheap (Open Q2)"
    - "Local reserved/sync filter constants in previewPush are enough — do not edit Terry previewPullActions.ts (A2)"
  prohibitions:
    - statement: "Do not edit Terry-authored previewPullActions.ts — import classifyCreateOrUpdate only (HYG-02)"
      status: active
      verification: "git diff --name-only excludes cli/src/sync/previewPullActions.ts"
    - statement: "Do not delete e2e_test/features/cli/cli_push.feature or implement mutating /push (D-06)"
      status: active
      verification: "git diff --name-only excludes cli_push.feature deletion; parsePushArgument still requires --dry-run"
    - statement: "Do not change loadPushBaseline / savePushBaseline signatures (D-03)"
      status: active
      verification: "pushBaseline.ts public signatures unchanged"
    - statement: "Do not install new npm packages"
      status: active
      verification: "cli/package.json dependencies unchanged for new packages"
    - statement: "Do not encode phase numbers in product or test names (D-07)"
      status: active
      verification: "new scenarios/tests use capability names only"
    - statement: "Prefer one implementation commit covering code+units+E2E; avoid Phase 11-style micro-commits (D-08)"
      status: active
      verification: "plan closes with ≤2 commits for the strengthen (preferably 1)"
---

<objective>
Strengthen `/push --dry-run` / `previewPush` so Story 5 / PUSH-01 closes **both** TRIAGE gaps: (1) exact create and update action reporting including local-only and remote-only paths, and (2) dry-run never writes sync metadata — proven by `previewPush.test.ts` and `cli_push_dry_run.feature` (D-01..D-08).

Purpose: One coarse Behavior plan (D-08) that lands load-only baseline + path-union create/update + unit flips + E2E flips in a single larger task / preferably one implementation commit.
Output: Production `previewPush` with no `savePushBaseline` call, green units, green targeted push dry-run E2E; Story 6 surfaces and Terry files untouched.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-resolve-push-dry-run-story-5/12-CONTEXT.md
@.planning/phases/12-resolve-push-dry-run-story-5/12-RESEARCH.md
@.planning/phases/12-resolve-push-dry-run-story-5/12-PATTERNS.md
@.planning/phases/12-resolve-push-dry-run-story-5/12-VALIDATION.md
@.planning/phases/07-publish-triage-decisions/TRIAGE.md
@.planning/notes/2026-07-24-portable-notebook-workspace.md
@.cursor/rules/planning.mdc
@.cursor/rules/cli.mdc
@cli/src/sync/previewPush.ts
@cli/src/sync/diffReport.ts
@cli/src/sync/pushBaseline.ts
@cli/src/sync/previewPullActions.ts
@cli/src/sync/previewPull.ts
@cli/src/sync/writeNotebookExport.ts
@cli/src/commands/notebook/pushSlashCommand.tsx
@cli/tests/previewPush.test.ts
@cli/tests/previewPull.test.ts
@e2e_test/features/cli/cli_push_dry_run.feature
@e2e_test/features/cli/cli_sync_dry_run.feature
</context>

## Phase Goal

Story 5 push dry-run / conflict preview participant work is healthy against acceptance: distinguishes unchanged / local / remote / divergent; reports create and update actions; conflicts are not LWW updates; mutates neither Doughnut, local `.md`, nor sync metadata (strengthen path for PUSH-01).

This plan delivers the full strengthen in one coarse wave (D-08).

## Artifacts this phase produces

| Symbol / artifact | Path | Role |
|-------------------|------|------|
| `previewPush` (strengthened) | `cli/src/sync/previewPush.ts` | Load-only baseline; path-union create/update report |
| Unit proofs | `cli/tests/previewPush.test.ts` | Flip write/seed units; create/update; no metadata write |
| Capability E2E | `e2e_test/features/cli/cli_push_dry_run.feature` | Export prime; adds-no-files; create scenarios |
| Optional help polish | `cli/src/commands/notebook/pushSlashCommand.tsx` | Doc/help only if needed (D-06) |

## Source coverage audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|-----|---------------------|------|--------|-------|
| GOAL | — | Story 5 dry-run healthy or gone | 01 | COVERED | Strengthen both gaps |
| REQ | PUSH-01 | Push dry-run matches story 5 | 01 | COVERED | Create/update + no metadata mutation |
| RESEARCH | — | Remove savePushBaseline from previewPush | 01 | COVERED | Task 1 |
| RESEARCH | — | Path-union local-only/remote-only | 01 | COVERED | Task 1 |
| RESEARCH | — | Import classifyCreateOrUpdate | 01 | COVERED | Task 1 HYG-02 |
| RESEARCH | — | Re-prime via export / savePushBaseline | 01 | COVERED | Task 1 |
| RESEARCH | — | Invert E2E baseline-write scenario + blurb | 01 | COVERED | Task 1 |
| CONTEXT | D-01 | Close both gaps | 01 | COVERED | Task 1 |
| CONTEXT | D-02 | Load-only baseline on dry-run | 01 | COVERED | Task 1 |
| CONTEXT | D-03 | Seed via export/pull; flip write units | 01 | COVERED | Task 1 |
| CONTEXT | D-04 | Keep directional; add create vs update | 01 | COVERED | Task 1 |
| CONTEXT | D-05 | Include local-only/remote-only; exclude reserved | 01 | COVERED | Task 1 |
| CONTEXT | D-06 | No mutate push; keep cli_push.feature | 01 | COVERED | Task 1 prohibitions |
| CONTEXT | D-07 | Prove via feature + previewPush tests | 01 | COVERED | Task 1 verify |
| CONTEXT | D-08 | One coarse plan / 1–2 larger tasks | 01 | COVERED | This plan shape (1 task) |

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end strengthen previewPush — load-only baseline + create/update (units + E2E)</name>
  <reversibility rating="one-way">D-02/D-03: published dry-run baseline-write E2E/units flip to load-only; directional priming moves to export.</reversibility>
  <files>cli/src/sync/previewPush.ts, cli/tests/previewPush.test.ts, e2e_test/features/cli/cli_push_dry_run.feature, cli/src/commands/notebook/pushSlashCommand.tsx</files>
  <read_first>
    - .planning/phases/12-resolve-push-dry-run-story-5/12-CONTEXT.md (D-01..D-08)
    - .planning/phases/12-resolve-push-dry-run-story-5/12-RESEARCH.md (Patterns 1–4; Pitfalls 1–8; A1–A4; Wave 0; heading discretion)
    - .planning/phases/12-resolve-push-dry-run-story-5/12-PATTERNS.md (pull non-mutation analog; path-union; E2E invert)
    - cli/src/sync/previewPush.ts
    - cli/src/sync/diffReport.ts
    - cli/src/sync/pushBaseline.ts
    - cli/src/sync/previewPullActions.ts (classifyCreateOrUpdate — import only)
    - cli/src/sync/previewPull.ts (non-mutating dry-run contract)
    - cli/tests/previewPush.test.ts
    - cli/tests/previewPull.test.ts (create shape)
    - e2e_test/features/cli/cli_push_dry_run.feature
    - e2e_test/features/cli/cli_sync_dry_run.feature (adds-no-files inventory)
  </read_first>
  <behavior>
    - After previewPush, .doughnut-sync is absent on a zip/temp workspace that had none; existing baseline bytes stay unchanged when pre-seeded via savePushBaseline or export (D-02)
    - Units formerly named seeds/advances/keeps the baseline no longer assert dry-run writes; directional units reseed via savePushBaseline or writeNotebookExport (D-03)
    - Remote-only exported .md reports as create with (create) heading and pull-oriented empty-local diff; local-only workspace .md reports as create with (create) heading and push-oriented empty-remote diff (D-04, D-05, A1)
    - Intersecting directional rows keep path (push) / path (pull) / path (CONFLICT); conflict never labeled update (D-04, A1)
    - index.md, log.md, and paths under .doughnut-sync are omitted from ordinary create/update rows (D-05)
    - E2E Feature blurb states no sync-metadata mutation; Scenario inventory after dry-run on zip-seeded workspace lists only the note file(s) (invert former baseline-file addition) (D-02, D-07)
    - Rule A later preview Background primes via /export (or successful pull), not a priming dry-run; conflict and .md/Doughnut non-mutation scenarios stay green (D-03, D-07)
    - At least one capability-named E2E create scenario (local-only and/or remote-only) proves create visibility (D-05, D-07)
  </behavior>
  <action>
    Per D-08, land both PUSH-01 gaps plus units and E2E in this single tracer; prefer one implementation commit (code+units+E2E together). RED first on units: replace `seeds the baseline only with…`, `advances the baseline…`, and `keeps the baseline…` with proofs that dry-run does not create or alter `.doughnut-sync`; reseed any directional units that called matching `preview(...)` as priming to use `savePushBaseline` or `writeNotebookExport` (D-03); flip `leaves a note missing from the workspace out of the report` to expect remote-only create; add local-only create, conflict-without-update, and reserved-path-omission cases (D-04, D-05). GREEN in `previewPush.ts` (D-01, D-06 primary surface): delete the `savePushBaseline` / `nextBaseline` call site and unused helper; update the docstring so Doughnut, workspace, and sync metadata are never written (D-02); keep `loadPushBaseline` + existing `classify` for intersecting directional labels; replace intersecting-only `flatMap` with union of exported Markdown paths ∪ workspace Markdown paths; exclude reserved basenames `index.md`/`log.md` and `.doughnut-sync` segments via a local filter (do not edit `previewPullActions.ts` — import-only `classifyCreateOrUpdate` per HYG-02 / D-06); for local-only → create+(push orientation, empty remote); remote-only → create+(pull orientation, empty local); intersecting conflict → `(CONFLICT)` only; intersecting push/pull → keep existing `(push)`/`(pull)` as the update signal (A1 — no mandatory literal `(update)` on intersecting rows). Compose headings in `previewPush` so Story 2 `diffReport` action/status XOR stays untouched (Pitfall 6). Prefer no `diffReport` API change. Then flip E2E in the same change (D-07): invert Feature blurb; rename/replace *The preview's only addition is its own baseline file* to an adds-no-files inventory (mirror `cli_sync_dry_run`); re-prime Rule *A later preview…* Background via the Feature’s existing Export Rule pattern; add capability-named create scenario(s); keep conflict + `.md`/Doughnut untouched proofs. Optionally polish `/push` help text only — do not relax `parsePushArgument`, do not implement mutate push, do not delete `cli_push.feature` (D-06). Tag new E2E scenarios `@wip` only while red; remove before phase close. No new npm packages. After green: post-change-refactor on the uncommitted change, then Jidoka → plan update → prefer one commit → push (local wrap-up).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c bash -c 'cd cli && pnpm exec vitest run tests/previewPush.test.ts' && CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_push_dry_run.feature</automated>
  </verify>
  <acceptance_criteria>
    - Unit: dry-run does not create or alter `.doughnut-sync` (D-02)
    - Unit: directional labels still work when baseline is seeded via savePushBaseline or export — not via prior previewPush (D-03)
    - Unit: local-only and remote-only paths report `(create)`; intersecting keep `(push)`/`(pull)`/`(CONFLICT)`; conflict has no update label (D-04, D-05)
    - Unit: reserved/sync paths omitted from ordinary rows (D-05)
    - E2E: Feature blurb + adds-no-files inventory; export-primed directional Rule; create scenario(s); conflict + non-mutation of `.md`/Doughnut stay green (D-02, D-03, D-07)
    - `git diff --name-only` excludes `cli/src/sync/previewPullActions.ts` and does not delete `cli_push.feature` (HYG-02, D-06)
    - `previewPush.ts` has no `savePushBaseline(` call site (D-02)
    - Capability names only — no phase numbers in new test/scenario titles (D-07)
  </acceptance_criteria>
  <done>PUSH-01 strengthen is unit- and E2E-green: load-only baseline, create/update path-union reporting, Story 6 boundary intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied workspace directory → dry-run preview | Untrusted path argument scoped by existing `parsePushArgument` / `readWorkspace` |
| Dry-run → filesystem | Must only read workspace + export zip + optional baseline JSON; never write `.md`, `.doughnut-sync/`, or Doughnut |
| Sync metadata file → classify | Baseline content influences labels; must not be rewritten by preview |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | previewPush baseline write | high | mitigate | Remove `savePushBaseline` / `nextBaseline` from dry-run path (D-02, task 1) |
| T-12-02 | Tampering | Accidental mutate push | high | mitigate | Keep `parsePushArgument` `--dry-run` mandatory; do not implement applyPush (D-06) |
| T-12-03 | Spoofing | `.doughnut-sync/**` treated as notes | medium | mitigate | Exclude sync-metadata segment and reserved basenames from ordinary create/update rows (D-05) |
| T-12-04 | Information disclosure | Report messages | low | accept | Report only workspace-relative note paths already under the scanned root |
| T-12-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this phase — legitimacy audit N/A for installs |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c bash -c 'cd cli && pnpm exec vitest run tests/previewPush.test.ts'` green
- `CURSOR_DEV=true nix develop -c pnpm cypress run --spec e2e_test/features/cli/cli_push_dry_run.feature` green (assume `pnpm sut` running)
- Diff excludes Terry-authored `previewPullActions.ts` (HYG-02)
- Both TRIAGE Story 5 gaps closed; no leftover `@wip` on new push dry-run scenarios
- Prefer one implementation commit (D-08); post-change-refactor + commit+push per execute-plan / local wrap-up
</verification>

<success_criteria>
PUSH-01 satisfied via strengthen: `/push --dry-run` distinguishes unchanged / local / remote / divergent, reports create and update actions (including non-intersecting paths), keeps conflicts as conflicts, and mutates neither Doughnut, local `.md`, nor sync metadata — proven by units + targeted `cli_push_dry_run.feature`.
</success_criteria>

<output>
Create `.planning/phases/12-resolve-push-dry-run-story-5/12-01-SUMMARY.md` when done
</output>
