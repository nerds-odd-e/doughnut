---
phase: 04-dead-link-findings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java
  - backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java
  - backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java
  - backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
  - backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java
  - backend/src/test/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRuleTest.java
  - backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java
autonomous: true
requirements:
  - DLNK-01
  - DLNK-02
must_haves:
  truths:
    - "HealthRunContext carries the authorized lint caller as viewer for viewer-readable resolve (D-01)"
    - "WikiLinkResolver exposes unresolvedWikiLinkTokens using the same extract, dedupe, and resolveToken path as resolveWikiLinksForCache — not cache-as-truth and not any-target resolve (D-01, D-02, D-04, D-05)"
    - "NoteRepository can load all live notes for a notebook in one query (deletedAt IS NULL, ORDER BY id ASC) (D-03)"
    - "Existing empty_folders and readme_only_folders lint behavior is unchanged after HealthRunContext constructor change"
  artifacts:
    - path: backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java
      provides: "Required User viewer on HealthRunContext (D-01)"
      contains: "viewer"
    - path: backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java
      provides: "unresolvedWikiLinkTokens(Note, User) sharing cache extract/dedupe/resolve loop (D-01, D-02, D-05)"
      contains: "unresolvedWikiLinkTokens"
    - path: backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
      provides: "findLiveNotesByNotebookIdOrderByIdAsc for live source notes only (D-03)"
      contains: "findLiveNotesByNotebookIdOrderByIdAsc"
    - path: backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java
      provides: "lint passes authorizationService.getCurrentUser() into HealthRunContext (D-01, D-11)"
  key_links:
    - from: "NotebookHealthController.lint"
      to: "HealthRunContext(viewer)"
      via: "getCurrentUser after assertAuthorization"
      pattern: "new HealthRunContext"
    - from: "WikiLinkResolver.unresolvedWikiLinkTokens"
      to: "NoteContentMarkdown.wikiLinkInnersInOccurrenceOrder + dedupePreserveOrder + resolveToken"
      via: "invert resolveWikiLinksForCache loop"
      pattern: "unresolvedWikiLinkTokens"
    - from: "findLiveNotesByNotebookIdOrderByIdAsc"
      to: "DeadWikiLinkHealthRule (plan 02)"
      via: "single notebook-scoped live note load"
      pattern: "deletedAt IS NULL"
  prohibitions:
    - statement: "must NOT call resolveAnyTargetWikiLinkToken (or any-target private path) for Health dead-link classification"
      status: resolved
      verification: judgment
    - statement: "must NOT treat note_wiki_title_cache absence as dead; must NOT invent a second wiki parser in health/"
      status: resolved
      verification: judgment
    - statement: "must NOT add DeadWikiLinkHealthRule, OpenAPI regen, frontend, E2E, or rewrite/fix paths in this plan"
      status: resolved
      verification: judgment
---

<objective>
Structure-only prep for Phase 4 Behavior: wire lint `viewer` on `HealthRunContext`, add `WikiLinkResolver.unresolvedWikiLinkTokens` (shared editor extract/dedupe/resolve), and add `NoteRepository.findLiveNotesByNotebookIdOrderByIdAsc` so plan 02 can emit nested `dead_wiki_links` without a second linker (D-01..D-05, D-11).

Purpose: Immediate-next-behavior Structure — no new finding group yet; folder lint stays green.
Output: Viewer-bearing context + call-site updates, unresolved-token helper on `WikiLinkResolver`, live-notes repository query.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dead-link-findings/04-CONTEXT.md
@.planning/phases/04-dead-link-findings/04-RESEARCH.md
@.planning/phases/04-dead-link-findings/04-PATTERNS.md
@.planning/phases/04-dead-link-findings/04-VALIDATION.md
@.cursor/rules/planning.mdc
@.cursor/rules/backend-code.mdc
@.cursor/rules/backend-testing.mdc
@backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java
@backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java
@backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java
@backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
@backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java
@backend/src/test/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRuleTest.java
@backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java
@backend/src/test/java/com/odde/doughnut/services/WikiLinkResolverYamlAndBodyIntegrationTest.java
</context>

## Phase Goal

**As a** notebook owner, **I want to** run Health lint and see dead wiki links from note body and frontmatter nested by note, **so that** I can audit broken links without mutating my notebook.

This plan is Structure only: viewer + resolve helper + live-notes query that plan 02 needs for that Behavior (DLNK-01/02/03 delivered in `04-02`).

## Artifacts this phase produces

| Symbol | Path | Plan |
|--------|------|------|
| `HealthRunContext(viewer)` | `backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java` | 04-01 |
| `unresolvedWikiLinkTokens` | `backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java` | 04-01 |
| `findLiveNotesByNotebookIdOrderByIdAsc` | `backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java` | 04-01 |
| Controller viewer wiring | `backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java` | 04-01 |
| `DeadWikiLinkHealthRule` | `backend/src/main/java/com/odde/doughnut/services/health/DeadWikiLinkHealthRule.java` | 04-02 |
| `DeadWikiLinkHealthRuleTest` | `backend/src/test/java/com/odde/doughnut/services/health/DeadWikiLinkHealthRuleTest.java` | 04-02 |

## Source coverage audit

| SOURCE | ID | Feature/Requirement | Plan | Status |
|--------|-----|---------------------|------|--------|
| GOAL | — | Dead wiki links in body + FM, nested by note, report-only | 04-01+04-02 | COVERED |
| REQ | DLNK-01 | Body dead links via editor resolve | 04-01 (resolve helper), 04-02 (rule) | COVERED |
| REQ | DLNK-02 | Frontmatter dead links same resolve | 04-01 (extract path), 04-02 (rule) | COVERED |
| REQ | DLNK-03 | Report-only / no auto-fix | 04-02 | COVERED |
| RESEARCH | — | HealthRunContext viewer; unresolvedWikiLinkTokens; live-notes query | 04-01 | COVERED |
| RESEARCH | — | DeadWikiLinkHealthRule nested children; backend tests only | 04-02 | COVERED |
| CONTEXT | D-01 | Viewer-readable WikiLinkResolver; no second linker; no cache-as-truth | 04-01+04-02 | COVERED |
| CONTEXT | D-02 | wikiLinkInnersInOccurrenceOrder; one rule id | 04-01+04-02 | COVERED |
| CONTEXT | D-03 | Live source notes only | 04-01+04-02 | COVERED |
| CONTEXT | D-04 | Aliases, qualified, pipe, soft-deleted targets; no any-target | 04-01+04-02 | COVERED |
| CONTEXT | D-05 | Distinct tokens / first-occurrence dedupe | 04-01+04-02 | COVERED |
| CONTEXT | D-06..D-10 | Item fields, nested children shape, always-emit | 04-02 | COVERED |
| CONTEXT | D-11..D-13 | Existing lint endpoint; no OpenAPI unless schema; backend tests | 04-01 (viewer wire), 04-02 | COVERED |

Deferred (not gaps): Health UI (P5), dead-link repair (v2), body-vs-FM message split, resolveAnyTarget nuance.

<tasks>

<task type="auto">
  <name>Task 1: HealthRunContext viewer + call-site updates</name>
  <files>backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java, backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java, backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java, backend/src/test/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRuleTest.java, backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java</files>
  <read_first>
    - .planning/phases/04-dead-link-findings/04-CONTEXT.md (D-01, D-11)
    - .planning/phases/04-dead-link-findings/04-RESEARCH.md (Pattern 2 Viewer on HealthRunContext)
    - .planning/phases/04-dead-link-findings/04-PATTERNS.md (HealthRunContext + call-site table)
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java
    - backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java
    - backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java
    - backend/src/test/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRuleTest.java
    - backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java
  </read_first>
  <action>
    Per D-01, change `HealthRunContext` to a final holder with a required `User viewer` constructor argument and a `viewer()` accessor (imports at top). Do not leave a no-arg constructor that omits viewer.

    Update `NotebookHealthController.lint` per D-11: after `assertAuthorization(notebook)`, pass `new HealthRunContext(authorizationService.getCurrentUser())` into `notebookHealthService.lint`. Keep `@Transactional(readOnly = true)`. Do not add endpoints, request body, or OpenAPI changes.

    Update every existing `new HealthRunContext()` call site: folder-rule tests pass the notebook owner `User`; `HealthRuleRunnerTest` may pass a non-null `new User()` stub (runner does not resolve wiki links). Folder rules continue to ignore `viewer`. Capability naming only — no phase numbers in types.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - `HealthRunContext` requires `User viewer` and exposes `viewer()`
    - Controller lint constructs context with `getCurrentUser()` after write-auth assert
    - EmptyFolderHealthRuleTest, ReadmeOnlyFolderHealthRuleTest, and HealthRuleRunnerTest compile and pass
    - No new HealthRule bean, no OpenAPI regen, no frontend files
  </acceptance_criteria>
  <done>Lint caller is available as HealthRunContext.viewer; existing folder lint tests green (D-01, D-11).</done>
</task>

<task type="auto">
  <name>Task 2: unresolvedWikiLinkTokens + live notes query</name>
  <files>backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java, backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java</files>
  <read_first>
    - .planning/phases/04-dead-link-findings/04-CONTEXT.md (D-01..D-05)
    - .planning/phases/04-dead-link-findings/04-RESEARCH.md (Pattern 1 unresolved tokens, Pattern 3 live source notes)
    - .planning/phases/04-dead-link-findings/04-PATTERNS.md (WikiLinkResolver invert + NoteRepository query)
    - backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java
    - backend/src/main/java/com/odde/doughnut/algorithms/NoteContentMarkdown.java
    - backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
  </read_first>
  <action>
    Per D-01/D-02/D-04/D-05, add public `List&lt;String&gt; unresolvedWikiLinkTokens(Note focusNote, User viewer)` on `WikiLinkResolver` that mirrors `resolveWikiLinksForCache`: same null/blank content guards; extract via `NoteContentMarkdown.wikiLinkInnersInOccurrenceOrder`; iterate `dedupePreserveOrder`; when private `resolveToken(token, viewer, focusNote)` returns null, append the full inner string (including pipe display text) to the dead list; return an immutable copy. Do not reimplement extract, pipe split, or alias/qualified parsing in a Health package helper. Do not use the any-target resolve API for this method. Do not read `note_wiki_title_cache` to classify dead.

    Per D-03, add `NoteRepository.findLiveNotesByNotebookIdOrderByIdAsc(Integer notebookId)` returning `List&lt;Note&gt;` where `n.notebook.id = :notebookId` and `n.deletedAt IS NULL`, ordered by `n.id ASC`. Match existing `@Query` style beside `findLiveNoteFolderIdsByNotebookId`. Do not walk folders with per-folder note loads; do not use root-only notebook scope queries.

    Do not add `DeadWikiLinkHealthRule` in this plan. Optional: if a tiny focused assert helps, extend an existing WikiLinkResolver integration test — otherwise plan 02 rule tests cover semantics. No OpenAPI regen.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - `WikiLinkResolver` defines `unresolvedWikiLinkTokens` sharing extract/dedupe/`resolveToken` with the cache path
    - Method does not call any-target resolve
    - `NoteRepository` exposes `findLiveNotesByNotebookIdOrderByIdAsc` with live-only filter and id order
    - Full backend unit suite green via `CURSOR_DEV=true nix develop -c pnpm backend:test_only`
    - No DeadWikiLinkHealthRule, no packages/generated changes, no frontend
  </acceptance_criteria>
  <done>Resolve helper and live-notes query ready for DeadWikiLinkHealthRule (D-01..D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing lint HTTP → service | Owner write auth already on `NotebookHealthController`; this plan only passes current user as viewer |
| Resolver → DB | Viewer-readable note lookups; tokens come from owned note content, not request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | NotebookHealthController.lint | medium | accept | Existing `assertAuthorization` write-auth unchanged; no new endpoint (D-11); foreign/anon remain rejected by Phase 2 controller tests |
| T-04-02 | Information Disclosure | WikiLinkResolver.unresolvedWikiLinkTokens | high | mitigate | Classify dead only via viewer-readable `resolveToken`; forbid any-target resolve so unreadable targets stay dead for the lint caller (D-04) |
| T-04-03 | Tampering | Lint / resolver helpers | high | mitigate | No rewrite or cache-write from these helpers; controller stays readOnly transactional; plan 02 asserts no mutation |
| T-04-04 | Elevation of Privilege | HealthRunContext.viewer | low | accept | Viewer is session current user after write-auth; folder rules ignore it |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c pnpm backend:test_only` green after context + resolver + repository changes
- Diff has no `DeadWikiLinkHealthRule`, no OpenAPI/TS regen, no Health UI
- `unresolvedWikiLinkTokens` shares the cache extract/dedupe/resolve path (viewer-readable only)
</verification>

<success_criteria>
- HealthRunContext carries required viewer; controller passes getCurrentUser
- unresolvedWikiLinkTokens and findLiveNotesByNotebookIdOrderByIdAsc exist for plan 02
- Existing folder Health rules still green (no observable product behavior change yet)
</success_criteria>

<output>
Create `.planning/phases/04-dead-link-findings/04-01-SUMMARY.md` when done
</output>
