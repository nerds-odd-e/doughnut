---
phase: 04-dead-link-findings
plan: 02
type: tdd
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/src/main/java/com/odde/doughnut/services/health/DeadWikiLinkHealthRule.java
  - backend/src/test/java/com/odde/doughnut/services/health/DeadWikiLinkHealthRuleTest.java
autonomous: true
requirements:
  - DLNK-01
  - DLNK-02
  - DLNK-03
must_haves:
  truths:
    - "NotebookHealthService.lint reports dead wiki links from note body under dead_wiki_links nested by note (DLNK-01, D-01, D-02, D-08, D-09)"
    - "Lint reports dead wiki links from frontmatter/properties with the same extract+resolve path (DLNK-02, D-02)"
    - "Live alias and qualified Notebook:Title links for the viewer are not reported dead; missing and soft-deleted targets are reported dead (D-04)"
    - "Soft-deleted source notes are not audited; distinct unresolved inners are reported once per note in first-occurrence order (D-03, D-05)"
    - "Top group always emits with ruleId dead_wiki_links, title Dead wiki links, severity warning, autoFixable false, top items empty/null, children per note with leaf noteId/wikiLinkToken/label (D-06..D-10)"
    - "Lint remains report-only — no content rewrite, folder mutation, or auto-fix path (DLNK-03, D-07, D-13)"
  artifacts:
    - path: backend/src/main/java/com/odde/doughnut/services/health/DeadWikiLinkHealthRule.java
      provides: "DeadWikiLinkHealthRule Spring HealthRule bean — nested dead_wiki_links findings (D-08..D-11)"
      contains: "DEAD_WIKI_LINKS"
    - path: backend/src/test/java/com/odde/doughnut/services/health/DeadWikiLinkHealthRuleTest.java
      provides: "DLNK-01/02/03 and D-13 coverage via NotebookHealthService.lint"
  key_links:
    - from: "DeadWikiLinkHealthRule.evaluate"
      to: "NoteRepository.findLiveNotesByNotebookIdOrderByIdAsc"
      via: "live source notes only"
      pattern: "findLiveNotesByNotebookIdOrderByIdAsc"
    - from: "DeadWikiLinkHealthRule.evaluate"
      to: "WikiLinkResolver.unresolvedWikiLinkTokens"
      via: "context.viewer()"
      pattern: "unresolvedWikiLinkTokens"
    - from: "DeadWikiLinkHealthRule"
      to: "HealthRuleRunner"
      via: "Spring List HealthRule bean discovery (no runner edits)"
      pattern: "@Service"
    - from: "DeadWikiLinkHealthRuleTest"
      to: "NotebookHealthService.lint"
      via: "stable entry; groups by ruleId"
      pattern: "notebookHealthService.lint"
  prohibitions:
    - statement: "must NOT set autoFixable true on dead_wiki_links top or child groups"
      status: resolved
      verification: judgment
    - statement: "must NOT put leaf dead-link items only on the top group (must use children by note)"
      status: resolved
      verification: judgment
    - statement: "must NOT mutate notebook data, regenerate OpenAPI/TS, add Health UI/E2E, or add rewrite/fix endpoints"
      status: resolved
      verification: judgment
    - statement: "must NOT use any-target resolve or cache absence as dead classification"
      status: resolved
      verification: judgment
---

<objective>
Deliver observable dead-link Health findings (DLNK-01, DLNK-02, DLNK-03, D-06..D-13): Spring `DeadWikiLinkHealthRule` nests unresolved wiki-link tokens under `dead_wiki_links` children by note via existing `POST .../health/lint`, report-only (`autoFixable=false`), proven with backend tests only.

Purpose: Single Phase 4 Behavior slice — full mechanical dead-link audit stop-safe after this plan.
Output: `DeadWikiLinkHealthRule` bean + green `DeadWikiLinkHealthRuleTest` (folder groups still coexist).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dead-link-findings/04-CONTEXT.md
@.planning/phases/04-dead-link-findings/04-RESEARCH.md
@.planning/phases/04-dead-link-findings/04-PATTERNS.md
@.planning/phases/04-dead-link-findings/04-VALIDATION.md
@.planning/phases/04-dead-link-findings/04-01-PLAN.md
@.cursor/rules/planning.mdc
@.cursor/rules/backend-code.mdc
@.cursor/rules/backend-testing.mdc
@backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java
@backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java
@backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java
@backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java
@backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
@backend/src/test/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRuleTest.java
@backend/src/test/java/com/odde/doughnut/services/WikiLinkResolverYamlAndBodyIntegrationTest.java
</context>

## Phase Goal

**As a** notebook owner, **I want to** run Health lint and see dead wiki links from note body and frontmatter nested by note, **so that** I can audit broken links without mutating my notebook.

This plan delivers the full Behavior through `NotebookHealthService.lint` (depends on 04-01 viewer/resolver/live-notes). No new HTTP surface (D-11).

## Artifacts this phase produces

| Symbol | Path |
|--------|------|
| `DeadWikiLinkHealthRule` | `backend/src/main/java/com/odde/doughnut/services/health/DeadWikiLinkHealthRule.java` |
| `DeadWikiLinkHealthRuleTest` | `backend/src/test/java/com/odde/doughnut/services/health/DeadWikiLinkHealthRuleTest.java` |

(Plus 04-01 Structure artifacts already listed in that plan.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DeadWikiLinkHealthRule tests (RED)</name>
  <files>backend/src/test/java/com/odde/doughnut/services/health/DeadWikiLinkHealthRuleTest.java</files>
  <read_first>
    - .planning/phases/04-dead-link-findings/04-CONTEXT.md (D-01..D-13)
    - .planning/phases/04-dead-link-findings/04-RESEARCH.md (Validation Architecture D-13 checklist)
    - .planning/phases/04-dead-link-findings/04-PATTERNS.md (DeadWikiLinkHealthRuleTest scaffold + wiki fixtures)
    - .planning/phases/04-dead-link-findings/04-VALIDATION.md
    - .cursor/rules/backend-testing.mdc
    - backend/src/test/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRuleTest.java
    - backend/src/test/java/com/odde/doughnut/services/WikiLinkResolverYamlAndBodyIntegrationTest.java
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java
  </read_first>
  <behavior>
    - Body dead link: live note content with unresolved [[Missing]] → child for that note; item wikiLinkToken and label equal Missing; noteId set; folderId unset (DLNK-01, D-06, D-07)
    - Frontmatter/property dead link: --- parent: "[[Missing]]" --- body → same token reported (DLNK-02, D-02)
    - Live body link to existing title not reported; live alias after NoteAliasIndexService.refreshForNote not reported; live qualified Other Notebook:Title for owner not reported (D-04)
    - Soft-deleted source note with dead link produces no child for that note (D-03); soft-deleted or missing target reported dead (D-04)
    - Distinct-token dedupe: same unresolved inner twice → one leaf item (D-05)
    - Nested shape: two notes with different dead tokens → two children; top items empty/null; child title = note title; child ruleId dead_wiki_links; child autoFixable false (D-08, D-09)
    - Always emit top dead_wiki_links group with title Dead wiki links, severity warning, autoFixable false, children empty when no dead links (D-10)
    - Coexistence: empty_folders / readme_only_folders / dead_wiki_links all present by ruleId in one lint report
    - No mutation: note content / note count unchanged after lint (DLNK-03, D-13)
    - Prefer @SpringBootTest + @ActiveProfiles(test) + @Transactional + NotebookHealthService.lint(notebook, new HealthRunContext(owner)); look up groups by HealthRuleIds.DEAD_WIKI_LINKS never by index; one behavior per test method
    - Must fail until DeadWikiLinkHealthRule bean exists (RED before Task 2)
  </behavior>
  <action>
    Create capability-named `DeadWikiLinkHealthRuleTest` (no phase numbers) per D-13. Mirror ReadmeOnlyFolderHealthRuleTest scaffold: autowire `NotebookHealthService`, `MakeMe`, and `NoteAliasIndexService` for alias fixtures. Helper runs `notebookHealthService.lint(notebook, new HealthRunContext(owner))` and filters `HealthRuleIds.DEAD_WIKI_LINKS`. Cover all D-13 scenarios listed in behavior (body, FM, live alias/qualified, soft-deleted source/target, dedupe, nested children, always-emit, autoFixable false, coexistence, no mutation). Fixture patterns from WikiLinkResolverYamlAndBodyIntegrationTest (body/FM/alias/qualified/pipe). Use `makeMe.aNote().softDeleted()` for soft-delete. Do not add controller tests, E2E, Health UI, OpenAPI regen, or rewrite/fix code. Optional pipe case: if asserting a dead pipe inner, wikiLinkToken/label must be the full inner string (D-06 discretion default).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - `DeadWikiLinkHealthRuleTest` exists with methods covering DLNK-01/02/03 and D-13 checklist scenarios
    - Backend unit suite fails for the right reason (missing dead_wiki_links group or failing assertions), not env/typo noise
    - No controller, frontend, packages/generated, or Flyway files added
  </acceptance_criteria>
  <done>Failing dead-link finding tests are in place (RED; D-13).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DeadWikiLinkHealthRule (GREEN)</name>
  <files>backend/src/main/java/com/odde/doughnut/services/health/DeadWikiLinkHealthRule.java</files>
  <read_first>
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java
    - backend/src/main/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRule.java
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java
    - backend/src/main/java/com/odde/doughnut/services/WikiLinkResolver.java
    - backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
    - backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingGroup.java
    - backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingItem.java
    - .planning/phases/04-dead-link-findings/04-CONTEXT.md (D-06..D-11)
    - .planning/phases/04-dead-link-findings/04-RESEARCH.md (Pattern 4 nested report shape)
    - .planning/phases/04-dead-link-findings/04-PATTERNS.md (DeadWikiLinkHealthRule compose sketch)
  </read_first>
  <behavior>
    - Same behaviors as Task 1 must pass after implementation
    - Existing EmptyFolderHealthRuleTest and ReadmeOnlyFolderHealthRuleTest remain green
  </behavior>
  <action>
    Add `@Service DeadWikiLinkHealthRule implements HealthRule` (D-11) with constructor DI of `NoteRepository` and `WikiLinkResolver`. Contract: `id()` → `HealthRuleIds.DEAD_WIKI_LINKS` (already reserved), `title()` → `Dead wiki links`, `severity()` → `HealthSeverity.warning`, `autoFixable()` → false (D-07, D-08 — same report-only precedent as readme-only rule).

    In `evaluate(Notebook, HealthRunContext)`: load live notes once via `findLiveNotesByNotebookIdOrderByIdAsc`; for each note call `wikiLinkResolver.unresolvedWikiLinkTokens(note, context.viewer())`; skip notes with empty dead list; for each note with dead tokens build a child `HealthFindingGroup` with same ruleId, title = note title, severity warning, autoFixable false, items = one `HealthFindingItem` per token with `noteId`, `wikiLinkToken` = full inner, `label` = same string, no folderId, no required message (D-06, D-07, D-09). Top group: metadata above, top-level items null or empty, `children` = those child groups (possibly empty list) — always return the top group (D-08, D-10). Do not invent a second linker; do not call any-target resolve; do not query wiki title cache for classification; do not edit HealthRuleRunner, folder rules, DTOs, or OpenAPI (D-11, D-12). Read-only only — no rewrite service calls. Capability naming only (no phase numbers). Discretion: per-note resolver calls are fine for v1; child groups mirror severity/autoFixable explicitly.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - All `DeadWikiLinkHealthRuleTest` methods pass
    - EmptyFolderHealthRuleTest and ReadmeOnlyFolderHealthRuleTest still pass
    - Full backend unit suite green via `CURSOR_DEV=true nix develop -c pnpm backend:test_only`
    - `DeadWikiLinkHealthRule` is a Spring `@Service` implementing `HealthRule` with autoFixable false and nested children shape
    - No new HTTP mappings, no `pnpm generateTypeScript`, no frontend Health UI, no rewrite/fix applicator
    - No any-target resolve usage in the Health rule
  </acceptance_criteria>
  <done>dead_wiki_links reports body+FM dead links nested by note; report-only proven (DLNK-01/02/03 green; D-06..D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing lint HTTP → service | Unchanged write-auth gate; rule consumes HealthRunContext.viewer from controller |
| Rule evaluate → resolver/DB | Read-only live notes + viewer-readable resolve; tokens from owned note content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | NotebookHealthController / lint | medium | accept | No new endpoint (D-11); existing assertAuthorization write-auth; Phase 2 foreign/anon tests remain green |
| T-04-02 | Information Disclosure | DeadWikiLinkHealthRule → WikiLinkResolver | high | mitigate | Only unresolvedWikiLinkTokens / viewer-readable resolve; forbid any-target path so unreadable targets do not look “live” (D-04) |
| T-04-03 | Tampering | DeadWikiLinkHealthRule.evaluate | high | mitigate | autoFixable false on top and children (D-07); no rewrite/delete; Task 1 no-mutation test (DLNK-03, D-13); controller remains readOnly |
| T-04-05 | Tampering | Finding shape / Phase 7 misuse | medium | mitigate | Dead links never marked autoFixable; leaf items carry wikiLinkToken not folderId so empty-folder purge cannot target them by mistake |
| T-04-06 | Elevation of Privilege | Spring HealthRule bean | low | accept | Bean has no auth surface; discovery via existing runner only |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c pnpm backend:test_only` green with body, FM, alias/qualified live, soft-delete, dedupe, nested children, always-emit, autoFixable false, coexistence, and no-mutation cases
- Rule id is `dead_wiki_links`; top and child autoFixable false; findings nested under children by note
- Diff has no OpenAPI/TS regen, no frontend Health UI, no rewrite/fix endpoint
</verification>

<success_criteria>
- DLNK-01: body dead wiki links reported with editor resolve semantics
- DLNK-02: frontmatter/property dead wiki links reported via same extract path
- DLNK-03: report-only (autoFixable false; no mutation)
- Soft-deleted sources excluded; distinct tokens; nested-by-note children always emitted
</success_criteria>

<output>
Create `.planning/phases/04-dead-link-findings/04-02-SUMMARY.md` when done
</output>
