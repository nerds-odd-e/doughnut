---
phase: 03-readme-only-folder-findings
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/odde/doughnut/services/health/FolderSubtreeLiveNotes.java
  - backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java
  - backend/src/main/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRule.java
  - backend/src/test/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRuleTest.java
autonomous: true
requirements:
  - EFOL-03
must_haves:
  truths:
    - "NotebookHealthService.lint reports note-empty folders with non-blank own readmeContent under ruleId readme_only_folders, separate from empty_folders (EFOL-03, D-01, D-03, D-05)"
    - "Fully empty folders (note-empty + blank/null/whitespace own readme) appear only under empty_folders, never under readme_only_folders (D-02, D-03)"
    - "Every matching readme-only folder is its own HealthFindingItem with folderId and label=bare folder name (D-04)"
    - "readme_only_folders group metadata is title Readme-only folders, severity warning, autoFixable false, and the group is always present even when items empty (D-05..D-08)"
    - "Both rules share the same subtree live-notes scan helper so emptiness cannot drift (D-10)"
    - "Lint remains report-only — no folder deletes, dissolve, or notebook mutation (D-09, D-12)"
  artifacts:
    - path: backend/src/main/java/com/odde/doughnut/services/health/FolderSubtreeLiveNotes.java
      provides: "Package-private shared children map, memoized subtreeHasLiveNotes, and isBlankReadme (D-10)"
      contains: "subtreeHasLiveNotes"
    - path: backend/src/main/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRule.java
      provides: "ReadmeOnlyFolderHealthRule HealthRule bean — note-empty + non-blank own readme (D-01..D-09)"
      contains: "README_ONLY_FOLDERS"
    - path: backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java
      provides: "EmptyFolderHealthRule refactored to call shared scan; blank-readme gate and autoFixable true unchanged"
    - path: backend/src/test/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRuleTest.java
      provides: "EFOL-03 coverage: inclusion, mutual exclusion, own-readme, soft-delete, metadata, no mutation (D-12)"
  key_links:
    - from: "ReadmeOnlyFolderHealthRule.evaluate"
      to: "FolderSubtreeLiveNotes.subtreeHasLiveNotes"
      via: "shared recursive emptiness (live notes only)"
      pattern: "subtreeHasLiveNotes"
    - from: "EmptyFolderHealthRule.evaluate"
      to: "FolderSubtreeLiveNotes.subtreeHasLiveNotes"
      via: "same helper — complementary blank-readme gate"
      pattern: "FolderSubtreeLiveNotes"
    - from: "ReadmeOnlyFolderHealthRule"
      to: "HealthRuleRunner"
      via: "Spring List<HealthRule> bean discovery (no runner edits)"
      pattern: "@Service"
    - from: "ReadmeOnlyFolderHealthRuleTest"
      to: "NotebookHealthService.lint"
      via: "stable entry; groups looked up by ruleId"
      pattern: "notebookHealthService.lint"
  prohibitions:
    - statement: "must NOT set autoFixable true on the readme_only_folders group"
      status: resolved
      verification: judgment
    - statement: "must NOT list the same folderId in both empty_folders and readme_only_folders items"
      status: resolved
      verification: judgment
    - statement: "must NOT mutate notebook data from either rule evaluate (no delete, dissolve, or folder writes)"
      status: resolved
      verification: judgment
    - statement: "must NOT add a new HTTP endpoint, regenerate OpenAPI/TS client, Health UI, E2E, dead-link rule, or purge/fix code"
      status: resolved
      verification: judgment
---

<objective>
Deliver `readme_only_folders` findings (EFOL-03, D-01..D-12): authorized lint already exposes `NotebookHealthService.lint`; this plan adds the complementary `ReadmeOnlyFolderHealthRule` bean (note-empty + non-blank own `readmeContent`), extracts shared subtree scan so emptiness cannot drift from `EmptyFolderHealthRule`, and proves mutual exclusion + `autoFixable=false` with backend tests.

Purpose: Single Behavior slice — separate fix-eligibility boundary for note-empty folders that still have a readme, stop-safe after Phase 3.
Output: `FolderSubtreeLiveNotes`, `ReadmeOnlyFolderHealthRule`, refactored `EmptyFolderHealthRule`, green `ReadmeOnlyFolderHealthRuleTest` (and existing `EmptyFolderHealthRuleTest` still green).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-readme-only-folder-findings/03-CONTEXT.md
@.planning/phases/03-readme-only-folder-findings/03-RESEARCH.md
@.planning/phases/03-readme-only-folder-findings/03-PATTERNS.md
@.planning/phases/03-readme-only-folder-findings/03-VALIDATION.md
@.planning/phases/02-empty-folder-findings/02-CONTEXT.md
@.cursor/rules/planning.mdc
@.cursor/rules/backend-code.mdc
@.cursor/rules/backend-testing.mdc
@backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java
@backend/src/main/java/com/odde/doughnut/services/health/HealthRuleRunner.java
@backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java
@backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java
@backend/src/main/java/com/odde/doughnut/services/NotebookHealthService.java
@backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java
</context>

## Phase Goal

**As a** notebook owner, **I want to** run Health lint and see note-empty folders that still have a non-blank readme under their own finding group, **so that** I can distinguish them from fully empty folders and know they are not purge-eligible.

This plan delivers the full EFOL-03 Behavior (rule + shared emptiness helper + proofs) through `NotebookHealthService.lint` — no new HTTP surface (D-09).

## Artifacts this phase produces

| Symbol | Path |
|--------|------|
| `FolderSubtreeLiveNotes` | `backend/src/main/java/com/odde/doughnut/services/health/FolderSubtreeLiveNotes.java` |
| `ReadmeOnlyFolderHealthRule` | `backend/src/main/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRule.java` |
| `EmptyFolderHealthRule` (refactored) | `backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java` |
| `ReadmeOnlyFolderHealthRuleTest` | `backend/src/test/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRuleTest.java` |

Structure (`FolderSubtreeLiveNotes`) exists only to enable this Behavior (D-10) — not prep for Phase 4+.

## Source coverage audit

| SOURCE | ID | Feature/Requirement | Plan | Status |
|--------|-----|---------------------|------|--------|
| GOAL | — | Note-empty + non-blank readme as own finding type | 03-01 | COVERED |
| REQ | EFOL-03 | Separate finding type vs fully empty folders | 03-01 | COVERED |
| RESEARCH | — | Shared scan helper; complementary readme gates | 03-01 | COVERED |
| RESEARCH | — | No OpenAPI regen / no new endpoint | 03-01 | COVERED |
| CONTEXT | D-01 | Note-empty + non-blank own readme → readme_only_folders | 03-01 | COVERED |
| CONTEXT | D-02 | Own-readme only (no ancestor inheritance) | 03-01 | COVERED |
| CONTEXT | D-03 | Mutual exclusion with empty_folders | 03-01 | COVERED |
| CONTEXT | D-04 | Every match: folderId + bare name label | 03-01 | COVERED |
| CONTEXT | D-05 | ruleId readme_only_folders; title Readme-only folders | 03-01 | COVERED |
| CONTEXT | D-06 | severity warning | 03-01 | COVERED |
| CONTEXT | D-07 | autoFixable false | 03-01 | COVERED |
| CONTEXT | D-08 | Always emit group | 03-01 | COVERED |
| CONTEXT | D-09 | Spring HealthRule bean; reuse existing lint | 03-01 | COVERED |
| CONTEXT | D-10 | Shared emptiness helper | 03-01 | COVERED |
| CONTEXT | D-11 | No OpenAPI regen unless schema changes | 03-01 | COVERED |
| CONTEXT | D-12 | Backend tests only (no E2E/UI) | 03-01 | COVERED |

Deferred (not gaps): Health UI (P5), dead links (P4), purge (P7), readme preview in message, ancestor-readme inheritance.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ReadmeOnlyFolderHealthRule tests (RED)</name>
  <files>backend/src/test/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRuleTest.java</files>
  <read_first>
    - .planning/phases/03-readme-only-folder-findings/03-CONTEXT.md (D-01..D-12)
    - .planning/phases/03-readme-only-folder-findings/03-RESEARCH.md (mutual-exclusion fixtures, Validation Architecture)
    - .planning/phases/03-readme-only-folder-findings/03-PATTERNS.md (ReadmeOnlyFolderHealthRuleTest analog)
    - .cursor/rules/backend-testing.mdc
    - backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java
  </read_first>
  <behavior>
    - Nested note-empty folders each with non-blank own readmeContent: both folderIds appear under readme_only_folders with bare name labels (D-01, D-04)
    - Live note in a descendant clears occupancy — neither ancestor nor descendant in readme_only_folders or empty_folders
    - Soft-deleted note does not occupy — note-empty folder with non-blank readme still listed under readme_only_folders (D-01)
    - Mutual exclusion / partition (D-02, D-03): non-blank own readme → only readme_only_folders; null or whitespace-only own readme → only empty_folders; same folderId never in both groups
    - Own-readme only (D-02): parent with non-blank readme and blank-readme child → parent in readme_only_folders, child in empty_folders (not swapped by ancestor inheritance)
    - Always emit readme_only_folders group with ruleId HealthRuleIds.README_ONLY_FOLDERS, title Readme-only folders, severity warning, autoFixable false, items empty list when no matches (D-05..D-08)
    - One lint call can include both empty_folders and readme_only_folders groups (look up by ruleId, never groups.get index)
    - Lint does not change folder count for the notebook (report-only, D-12)
    - Prefer @SpringBootTest + @ActiveProfiles(test) + @Transactional + NotebookHealthService.lint + MakeMe; one behavior per test method
    - Must fail until ReadmeOnlyFolderHealthRule bean exists (RED before Task 2)
  </behavior>
  <action>
    Create capability-named `ReadmeOnlyFolderHealthRuleTest` (no phase numbers) per D-12. Mirror `EmptyFolderHealthRuleTest` scaffold: autowire `NotebookHealthService` and `MakeMe`; helper methods that run `notebookHealthService.lint(notebook, new HealthRunContext())` and filter groups by `HealthRuleIds.README_ONLY_FOLDERS` and `HealthRuleIds.EMPTY_FOLDERS`. Cover inclusion, blank/whitespace exclusion into empty_folders only, live-note exclusion, soft-delete, parent/child own-readme partition, always-emit metadata including autoFixable false, disjoint folderIds across groups, and no mutation (folder count unchanged). Use MakeMe: `aFolder().notebook(...).name(...).readmeContent(...)`, `parentFolder(...)`, `aNote(...).folder(...).softDeleted()`. Do not add controller tests, E2E, Health UI, OpenAPI regen, or purge code. Domain language: readme / readmeContent only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - `ReadmeOnlyFolderHealthRuleTest` exists with methods covering inclusion, mutual exclusion / blank partition, live-note occupancy, soft-delete, own-readme parent/child partition, always-emit metadata with autoFixable false, both groups in one report, and no mutation
    - Backend unit suite fails for the right reason (missing readme_only_folders group or failing assertions), not env/typo noise
    - No controller, frontend, packages/generated, or Flyway files added
  </acceptance_criteria>
  <done>Failing EFOL-03 / D-01..D-08 / D-12 tests are in place (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared scan helper + ReadmeOnlyFolderHealthRule (GREEN)</name>
  <files>backend/src/main/java/com/odde/doughnut/services/health/FolderSubtreeLiveNotes.java, backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java, backend/src/main/java/com/odde/doughnut/services/health/ReadmeOnlyFolderHealthRule.java</files>
  <read_first>
    - backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRuleRunner.java
    - backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingGroup.java
    - backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingItem.java
    - .planning/phases/03-readme-only-folder-findings/03-RESEARCH.md (Pattern 1 complementary gates, shared scan target shape)
    - .planning/phases/03-readme-only-folder-findings/03-PATTERNS.md (FolderSubtreeLiveNotes + ReadmeOnlyFolderHealthRule)
    - .planning/phases/03-readme-only-folder-findings/03-CONTEXT.md (D-01..D-11)
  </read_first>
  <behavior>
    - Same behaviors as Task 1 must pass after implementation
    - Existing EmptyFolderHealthRuleTest remains fully green (EFOL-01 regression after extract)
  </behavior>
  <action>
    Per D-10, extract package-private `final class FolderSubtreeLiveNotes` in `services/health/` (private constructor; static methods only) by moving the existing algorithm out of `EmptyFolderHealthRule` without changing behavior: children-by-parent map, memoized subtreeHasLiveNotes over occupied live folder ids, and isBlankReadme (`null` or `String.isBlank()`). Refactor `EmptyFolderHealthRule.evaluate` to call the shared statics; keep blank-readme gate and autoFixable true; keep load via `FolderRepository.findByNotebookIdOrderByIdAsc` and `NoteRepository.findLiveNoteFolderIdsByNotebookId`.

    Add `@Service ReadmeOnlyFolderHealthRule implements HealthRule` (D-09): `id()` → `HealthRuleIds.README_ONLY_FOLDERS` (already reserved), `title()` → `Readme-only folders`, `severity()` → `HealthSeverity.warning`, `autoFixable()` → false (D-05..D-07 — do not copy true from empty rule). In `evaluate`: same folder + live-occupancy load; use `FolderSubtreeLiveNotes` for tree scan; include folder when subtree has no live notes AND own readmeContent is non-blank (`!isBlankReadme`) (D-01, D-02); one `HealthFindingItem` per match with `folderId` and `label=folder.getName()` (D-04); always return the group (items may be empty) (D-08). Complementary gates enforce mutual exclusion (D-03). Read-only only — no dissolve, delete, or repository writes. Do not edit `HealthRuleRunner`, controller, DTOs, or OpenAPI (D-09, D-11). Capability naming only (no phase numbers). Optional item `message` omitted (discretion).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - All `ReadmeOnlyFolderHealthRuleTest` methods pass
    - All `EmptyFolderHealthRuleTest` methods pass (no EFOL-01 regression)
    - Full backend unit suite green via `CURSOR_DEV=true nix develop -c pnpm backend:test_only`
    - `FolderSubtreeLiveNotes` is package-private and used by both folder health rules for emptiness
    - `ReadmeOnlyFolderHealthRule` is a Spring `@Service` implementing `HealthRule` with autoFixable false and title Readme-only folders
    - No new HTTP mappings, no `pnpm generateTypeScript`, no frontend Health UI, no purge applicator
    - No dissolve/delete/write paths in either rule evaluate
  </acceptance_criteria>
  <done>readme_only_folders rule reports note-empty non-blank-readme folders; mutual exclusion and autoFixable false proven (EFOL-03 green; D-01..D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing lint HTTP → service | Unchanged Phase 2 owner write auth (`assertAuthorization`); this plan does not add routes or weaken the gate |
| Rule evaluate → DB | Read-only folder/note queries; no untrusted user input beyond notebook entity already loaded in-process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | ReadmeOnlyFolderHealthRule.autoFixable | high | mitigate | Group autoFixable must be false (D-07); Task 1 metadata test asserts false so Phase 7 cannot treat readme-only as purge-eligible by metadata alone |
| T-03-02 | Tampering | ReadmeOnlyFolderHealthRule.evaluate / EmptyFolderHealthRule.evaluate | high | mitigate | Report-only evaluate: no folder delete/dissolve/writes; no-mutation test on folder count (D-12, success criterion 3) |
| T-03-03 | Tampering | Mutual exclusion (D-03) | high | mitigate | Complementary blank/non-blank gates on shared scan; tests assert disjoint folderIds and blank→empty_folders only |
| T-03-04 | Information Disclosure | Lint endpoint auth | medium | accept | No new endpoint (D-09); Phase 2 controller write auth unchanged — do not weaken assertAuthorization |
| T-03-05 | Elevation of Privilege | Spring HealthRule bean | low | accept | Bean has no auth surface; discovery via existing runner only |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c pnpm backend:test_only` green with inclusion, mutual exclusion, own-readme partition, soft-delete, metadata autoFixable false, and no-mutation cases
- Rule id is `readme_only_folders`; group autoFixable is false; severity is warning; title is Readme-only folders
- Existing empty_folders behavior still green via `EmptyFolderHealthRuleTest`
- Diff has no controller mapping changes, no packages/generated, no frontend Health UI, no purge endpoint
</verification>

<success_criteria>
- EFOL-03: note-empty folders with non-blank own readmeContent reported under `readme_only_folders`, not lumped into `empty_folders`
- Fully empty folders remain only under `empty_folders`
- Lint remains report-only (no deletes or mutations)
- Shared `FolderSubtreeLiveNotes` used by both rules (D-10)
</success_criteria>

<output>
Create `.planning/phases/03-readme-only-folder-findings/03-01-SUMMARY.md` when done
</output>
