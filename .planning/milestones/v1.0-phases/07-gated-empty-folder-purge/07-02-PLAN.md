---
phase: 07-gated-empty-folder-purge
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/src/components/notebook/NotebookHealthPanel.vue
  - frontend/tests/components/notebook/NotebookHealthPanel.spec.ts
  - e2e_test/features/notebooks/notebook_health.feature
  - e2e_test/start/pageObjects/notebookPage.ts
  - e2e_test/step_definitions/notebook.ts
autonomous: true
requirements:
  - AFIX-02
  - AFIX-03
  - AFIX-04
  - AFIX-05
must_haves:
  truths:
    - "Health action bar shows a single Fix control notebook-health-fix with no per-folder multi-select (AFIX-02, D-01)"
    - "Fix is disabled (visible) unless Remove empty folders is checked AND last report empty_folders items length ≥ 1; enabled otherwise (AFIX-03, D-02)"
    - "Enabled label is Remove N empty folders (N from empty_folders count); fallback Remove empty folders when N is 0 (D-03, UI-SPEC)"
    - "Click Fix with no confirm dialog calls NotebookHealthController.fix with body removeEmptyFolders true only, via apiCallWithLoading (D-04, D-05)"
    - "On fix success, panel automatically re-runs bodyless lint and replaces report so purged empty folders are gone; readme-only may remain (success criterion 5, D-13)"
    - "On fix or re-lint failure, prior report stays visible; no optimistic clear (D-14)"
    - "E2E notebook_health: seed empty + readme-only → check option → Run → Fix → empty folders gone; readme-only remains; no promote shells (D-17)"
  artifacts:
    - path: frontend/src/components/notebook/NotebookHealthPanel.vue
      provides: "Gated Fix + auto re-lint (D-01..D-04, D-13, D-14)"
      contains: "notebook-health-fix"
    - path: frontend/tests/components/notebook/NotebookHealthPanel.spec.ts
      provides: "Enablement + fix then lint order (D-16)"
    - path: e2e_test/features/notebooks/notebook_health.feature
      provides: "Gated purge scenario (D-17)"
    - path: e2e_test/start/pageObjects/notebookPage.ts
      provides: "applyFix / expect helpers for Fix"
  key_links:
    - from: "NotebookHealthPanel Fix click"
      to: "NotebookHealthController.fix"
      via: "apiCallWithLoading body removeEmptyFolders true"
      pattern: "NotebookHealthController.fix"
    - from: "Fix success"
      to: "bodyless runLint"
      via: "replace report after both succeed"
      pattern: "runLint"
    - from: "notebook_health.feature"
      to: "notebookPage.applyFix"
      via: "data-testid notebook-health-fix"
      pattern: "notebook-health-fix"
  prohibitions:
    - statement: "must not use Confirm dialog for purge in v1"
      status: resolved
      verification: judgment
    - statement: "must not send user defaults or folder IDs on the fix request"
      status: resolved
      verification: automated
    - statement: "must not style Fix as primary or error; primary stays on Run lint"
      status: resolved
      verification: judgment
    - statement: "must not hide Fix when disabled"
      status: resolved
      verification: automated
    - statement: "must not run the full E2E suite"
      status: resolved
      verification: judgment
    - statement: "must not encode phase numbers in product or E2E names"
      status: resolved
      verification: judgment
    - statement: "must not optimistic-clear findings before fix and re-lint succeed"
      status: resolved
      verification: automated
---

<objective>
Deliver gated Health Fix UI that calls health/fix then auto re-lints so purged empty folders disappear from findings (AFIX-02, AFIX-03, success criterion 5, D-01..D-04, D-13, D-14, D-16, D-17).

Purpose: Complete Phase 7 Behavior — user-facing gated purge after 07-01 API.
Output: NotebookHealthPanel Fix control + green unit specs + targeted notebook_health E2E green (remove @wip when passing).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gated-empty-folder-purge/07-CONTEXT.md
@.planning/phases/07-gated-empty-folder-purge/07-RESEARCH.md
@.planning/phases/07-gated-empty-folder-purge/07-PATTERNS.md
@.planning/phases/07-gated-empty-folder-purge/07-UI-SPEC.md
@.planning/phases/07-gated-empty-folder-purge/07-VALIDATION.md
@.planning/phases/07-gated-empty-folder-purge/07-01-PLAN.md
@.cursor/rules/planning.mdc
@.cursor/rules/frontend-api.mdc
@.cursor/rules/frontend-testing.mdc
@.cursor/rules/e2e-authoring.mdc
@frontend/src/components/notebook/NotebookHealthPanel.vue
@frontend/tests/components/notebook/NotebookHealthPanel.spec.ts
@e2e_test/features/notebooks/notebook_health.feature
@e2e_test/start/pageObjects/notebookPage.ts
@e2e_test/step_definitions/notebook.ts
@packages/generated/doughnut-backend-api/sdk.gen.ts
</context>

## Phase Goal

**As a** notebook owner, **I want to** optionally bulk-remove fully empty folder trees from Health when I opt in, **so that** empty shells are cleaned up without deleting readme-only folders or dissolving contents into parents.

This plan completes Fix UI + auto re-lint + E2E after 07-01 API.

## Artifacts this plan produces

| Symbol | Path |
|--------|------|
| Health panel Fix | `NotebookHealthPanel.vue` |
| Panel unit tests | `NotebookHealthPanel.spec.ts` |
| Gated purge E2E | `notebook_health.feature` + notebookPage + notebook steps |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gated Fix control and auto re-lint</name>
  <files>frontend/tests/components/notebook/NotebookHealthPanel.spec.ts, frontend/src/components/notebook/NotebookHealthPanel.vue</files>
  <read_first>
    - .planning/phases/07-gated-empty-folder-purge/07-CONTEXT.md (D-01..D-04, D-13, D-14, D-16)
    - .planning/phases/07-gated-empty-folder-purge/07-UI-SPEC.md (Fix enablement, copy, placement, data-testid, classes)
    - .planning/phases/07-gated-empty-folder-purge/07-PATTERNS.md (NotebookHealthPanel Fix patterns)
    - .planning/phases/07-gated-empty-folder-purge/07-RESEARCH.md (Pattern 3 client Fix then re-lint)
    - .cursor/rules/frontend-api.mdc
    - .cursor/rules/frontend-testing.mdc
    - frontend/src/components/notebook/NotebookHealthPanel.vue
    - frontend/tests/components/notebook/NotebookHealthPanel.spec.ts
    - packages/generated/doughnut-backend-api/sdk.gen.ts
  </read_first>
  <behavior>
    - Fix control always rendered with data-testid notebook-health-fix (D-01, D-02 discoverability)
    - Disabled when checkbox unchecked, or report null, or empty_folders item count 0 (D-02, D-16)
    - Enabled when checkbox checked and empty_folders items ≥ 1; label Remove N empty folders (D-02, D-03)
    - Click calls NotebookHealthController.fix once with path notebook and body { removeEmptyFolders: true } only — no folder ids, no user defaults (D-04, D-05, D-16)
    - On fix success, bodyless lint is called and report replaced; on fix error lint not called and prior report kept (D-13, D-14)
    - Replace Phase 5 AFIX-01 assertion that Fix is absent: lint stays path-only / report-only on Run; Fix is separate gated control (AFIX-01 preserved for Run)
    - Classes: daisy-btn daisy-btn-secondary daisy-btn-sm; placement after checkbox before Save as defaults (UI-SPEC)
    - RED failing tests first, then GREEN implementation in this task
  </behavior>
  <action>
    Write failing NotebookHealthPanel.spec.ts cases first (mockSdkService for NotebookHealthController.fix and existing lint). Assert enablement matrix, label with N, fix body shape, fix-then-lint call order, failure leaves prior report. Update the former no-Fix assertion so Run still never mutates while Fix exists as gated control. Prefer data-testid selectors. Capability-named only.

    Then implement in NotebookHealthPanel only per D-01..D-04, D-13, D-14, UI-SPEC: computed emptyFolderCount from ruleId empty_folders items; fixEnabled = checkbox && count > 0; fixLabel Remove N empty folders or fallback; button notebook-health-fix secondary/sm after checkbox before Save; applyFix via apiCallWithLoading → NotebookHealthController.fix({ path, body: { removeEmptyFolders: true } }); on !error await existing runLint(); never confirm dialog; never optimistic-clear report before success. Primary accent stays on Run lint. Do not change backend.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/components/notebook/NotebookHealthPanel.spec.ts</automated>
  </verify>
  <done>Gated Fix + auto re-lint unit tests green (AFIX-02, AFIX-03, D-16, success criterion 5 UI).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gated empty-folder purge E2E</name>
  <files>e2e_test/features/notebooks/notebook_health.feature, e2e_test/start/pageObjects/notebookPage.ts, e2e_test/step_definitions/notebook.ts</files>
  <read_first>
    - .planning/phases/07-gated-empty-folder-purge/07-CONTEXT.md (D-17)
    - .planning/phases/07-gated-empty-folder-purge/07-RESEARCH.md (E2E sketch)
    - .planning/phases/07-gated-empty-folder-purge/07-PATTERNS.md (notebook_health feature, page object)
    - .planning/phases/07-gated-empty-folder-purge/07-UI-SPEC.md (notebook-health-fix)
    - .cursor/rules/e2e-authoring.mdc
    - e2e_test/features/notebooks/notebook_health.feature
    - e2e_test/start/pageObjects/notebookPage.ts
    - e2e_test/step_definitions/notebook.ts
  </read_first>
  <behavior>
    - Scenario: seed fully empty folder(s) + readme-only folder; open Health; check Remove empty folders; Run lint; see empty_folders finding; apply Fix; empty folders gone from findings and notebook structure; readme-only finding still present; no promote-to-parent empty shells (AFIX-04, AFIX-05, D-17, success criterion 5)
    - Tag @wip while red; remove @wip when scenario passes (D-17)
    - Existing Health Run / defaults / AFIX-01 scenarios remain green
    - Capability name notebook_health only; no phase numbers in feature/file names
  </behavior>
  <action>
    Extend notebookPage with applyFix (click notebook-health-fix, wait not loading) and any expect helpers needed for empty-folder absence / readme-only presence. Wire Cucumber steps. Add gated purge scenario to notebook_health.feature tagged @wip until green. Implement until targeted cy:run passes, then remove @wip. Do not run the full E2E suite — only this feature. Assume pnpm sut already running. Do not invent per-folder multi-select or confirm dialogs.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm cy:run --spec e2e_test/features/notebooks/notebook_health.feature</automated>
  </verify>
  <done>Targeted notebook_health E2E proves gated purge + re-lint without @wip (AFIX-02–05, D-17, success criterion 5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → health/fix | Session user Fix; body is opt-in flag only |
| Browser → health/lint | Post-fix refresh must stay bodyless report-only |
| UI enablement → server | Client disable is discoverability only; server gate from 07-01 remains authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | Fix → health/fix | high | mitigate | Call generated fix for current notebook only; backend assertAuthorization from 07-01 remains |
| T-07-02 | Tampering | Bypass UI gate | high | mitigate | Still send removeEmptyFolders true only when enabled; server rejects other bodies (07-01); unit asserts body shape |
| T-07-03 | Tampering | Client folder IDs | high | mitigate | Fix body never includes folder ids; unit asserts body keys (D-08) |
| T-07-07 | Tampering | Optimistic clear / false success | high | mitigate | Replace report only after fix success and re-lint success; failure keeps prior report (D-14) |
| T-07-08 | Spoofing | Defaults hitchhiking on fix | medium | mitigate | Fix body is removeEmptyFolders true only — no healthRemoveEmptyFoldersDefault (D-05) |
| T-07-06 | Information Disclosure | Fix/lint errors | low | accept | Existing apiCallWithLoading toast path; no custom disclosure |
</threat_model>

<verification>
- NotebookHealthPanel.spec.ts green via nix-prefixed frontend:test
- Targeted cy:run --spec notebook_health.feature green without @wip on purge scenario
- Run lint remains bodyless/report-only; Fix is separate gated control
- Diff does not invent multi-select or confirm modal
</verification>

<success_criteria>
- AFIX-02: single bulk Fix on action bar
- AFIX-03: Fix enabled only with checkbox + empty_folders preview
- Success criterion 5: after Fix, auto re-lint no longer shows purged fully empty folders
- D-17: capability-named E2E green; no full suite
</success_criteria>

<output>
Create `.planning/phases/07-gated-empty-folder-purge/07-02-SUMMARY.md` when done
</output>
