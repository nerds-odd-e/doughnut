---
phase: 02-empty-folder-findings
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java
  - backend/src/test/java/com/odde/doughnut/controllers/NotebookHealthControllerTest.java
  - packages/generated/doughnut-backend-api
autonomous: true
requirements:
  - EFOL-01
  - EFOL-02
must_haves:
  truths:
    - "Authorized owner can POST /api/notebooks/{notebook}/health/lint with empty/minimal body and receive NotebookHealthLintReport containing empty_folders group items (EFOL-02, D-08, D-09)"
    - "Empty-folder findings list is returned without any auto-fix or run-option fields required (EFOL-02, D-09)"
    - "Foreign and anonymous callers are rejected when invoking lint (success criterion 4, D-08)"
    - "Lint does not delete folders or otherwise mutate notebook data (success criterion 3)"
    - "TypeScript OpenAPI client is regenerated from the new endpoint (D-11)"
  artifacts:
    - path: backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java
      provides: "Thin package-private POST /{notebook}/health/lint with owner write auth (D-08, D-10)"
      contains: "health/lint"
    - path: backend/src/test/java/com/odde/doughnut/controllers/NotebookHealthControllerTest.java
      provides: "Owner success, foreign/anon rejection, report shape, no-mutate (D-12)"
    - path: packages/generated/doughnut-backend-api
      provides: "Regenerated TS client including health lint operation (D-11)"
  key_links:
    - from: "NotebookHealthController.lint"
      to: "AuthorizationService.assertAuthorization(notebook)"
      via: "owner write gate before service call"
      pattern: "assertAuthorization\\(notebook\\)"
    - from: "NotebookHealthController.lint"
      to: "NotebookHealthService.lint"
      via: "new HealthRunContext() with no fix options"
      pattern: "notebookHealthService\\.lint"
    - from: "POST /api/notebooks/{notebook}/health/lint"
      to: "NotebookHealthLintReport.groups"
      via: "OpenAPI return type"
      pattern: "NotebookHealthLintReport"
  prohibitions:
    - statement: "must NOT mutate notebook data when lint runs (no delete, dissolve, or folder writes on the lint path)"
      status: resolved
      verification: judgment
    - statement: "must NOT include note-empty folders with non-blank own readmeContent under empty_folders items"
      status: resolved
      verification: judgment
    - statement: "must NOT authorize lint with the read-only/bazaar/subscriber authorization entrypoint"
      status: resolved
      verification: judgment
    - statement: "must NOT add Health tab UI, Run button, or frontend Health consumers"
      status: resolved
      verification: judgment
    - statement: "must NOT implement fix/purge endpoint or empty-folder applicator"
      status: resolved
      verification: judgment
---

<objective>
Expose authorized `POST /api/notebooks/{notebook}/health/lint` that returns the empty-folder findings group (EFOL-02, D-08..D-12) without mutating the notebook, and regenerate the TypeScript OpenAPI client (D-11).

Purpose: Complete Phase 2 Behavior — observable authorized lint API for fully empty folders (report-only).
Output: `NotebookHealthController`, green controller tests, regenerated `packages/generated/doughnut-backend-api`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-empty-folder-findings/02-CONTEXT.md
@.planning/phases/02-empty-folder-findings/02-RESEARCH.md
@.planning/phases/02-empty-folder-findings/02-PATTERNS.md
@.planning/phases/02-empty-folder-findings/02-VALIDATION.md
@.planning/phases/02-empty-folder-findings/02-01-PLAN.md
@.cursor/rules/planning.mdc
@.cursor/rules/backend-code.mdc
@.cursor/rules/backend-testing.mdc
@.cursor/skills/generate-api-client/SKILL.md
@backend/src/main/java/com/odde/doughnut/controllers/NotebookBooksController.java
@backend/src/main/java/com/odde/doughnut/services/NotebookHealthService.java
@backend/src/main/java/com/odde/doughnut/services/AuthorizationService.java
@backend/src/test/java/com/odde/doughnut/controllers/NotebookBooksControllerTestBase.java
@backend/src/test/java/com/odde/doughnut/controllers/NotebookBooksAttachControllerTest.java
@backend/src/test/java/com/odde/doughnut/controllers/NotebookCrudControllerTest.java
@backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java
</context>

## Phase Goal

**As a** notebook owner, **I want to** run authorized Health lint and receive fully empty folders under an empty-folder findings group, **so that** I can audit structural decay without mutating my notebook.

This plan delivers the authorized API + OpenAPI client slice (EFOL-02, auth, no-mutate). Plan 01 delivered the rule predicate (EFOL-01).

## Artifacts this phase produces

| Symbol | Path |
|--------|------|
| `findLiveNoteFolderIdsByNotebookId` | `backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java` *(01)* |
| `EmptyFolderHealthRule` | `backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java` *(01)* |
| `EmptyFolderHealthRuleTest` | `backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java` *(01)* |
| `NotebookHealthController` | `backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java` |
| `NotebookHealthControllerTest` | `backend/src/test/java/com/odde/doughnut/controllers/NotebookHealthControllerTest.java` |
| Generated OpenAPI TS client | `packages/generated/doughnut-backend-api/**` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add NotebookHealthController auth and report tests (RED)</name>
  <files>backend/src/test/java/com/odde/doughnut/controllers/NotebookHealthControllerTest.java</files>
  <read_first>
    - .planning/phases/02-empty-folder-findings/02-CONTEXT.md (D-08..D-12)
    - .planning/phases/02-empty-folder-findings/02-RESEARCH.md (controller pattern, auth rejection examples)
    - .planning/phases/02-empty-folder-findings/02-PATTERNS.md (NotebookHealthControllerTest analogs)
    - backend/src/test/java/com/odde/doughnut/controllers/NotebookBooksControllerTestBase.java
    - backend/src/test/java/com/odde/doughnut/controllers/NotebookBooksAttachControllerTest.java
    - backend/src/test/java/com/odde/doughnut/controllers/NotebookCrudControllerTest.java
    - .cursor/rules/backend-testing.mdc
  </read_first>
  <behavior>
    - Owner: create notebook with at least one fully empty folder; currentUser is owner; controller.lint(notebook) returns report with a group ruleId empty_folders and items including that folderId and label (EFOL-01/02, D-08)
    - Report path works with no request body / no fix options (D-09) — method takes only notebook path variable
    - Foreign user: currentUser is another user → UnexpectedNoAccessRightException (D-08)
    - Anonymous: currentUser.setUser(null) → UnexpectedNoAccessRightException (D-08)
    - No-mutate: count folders in notebook before and after lint — count unchanged (success criterion 3)
    - Extend ControllerTestBase (or mirror NotebookBooksControllerTestBase package-private controller autowire pattern)
    - Must fail until NotebookHealthController exists (RED before Task 2)
  </behavior>
  <action>
    Create capability-named `NotebookHealthControllerTest` (no phase numbers) per D-12 MVC/API coverage. Follow `ControllerTestBase` / `NotebookBooksControllerTestBase` patterns: `@Autowired NotebookHealthController`, `currentUser` owner fixtures, `otherUsersNotebook` / foreign user and anonymous rejection via `assertThrows(UnexpectedNoAccessRightException.class, ...)`. Assert report group metadata and items from plan 01 rule. Assert folder count unchanged after lint. Do not add E2E, `@wip`, Health UI, or purge tests. Do not invent lint request DTO or fix-option fields (D-09).
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - `NotebookHealthControllerTest` covers owner success with empty_folders items, foreign rejection, anonymous rejection, and unchanged folder count
    - Backend unit suite fails for the right reason (missing controller) until Task 2
    - No frontend Health files added
  </acceptance_criteria>
  <done>Failing controller tests for authorized report-only lint are in place (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement NotebookHealthController and regenerate OpenAPI client (GREEN)</name>
  <files>backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java, packages/generated/doughnut-backend-api</files>
  <read_first>
    - backend/src/main/java/com/odde/doughnut/controllers/NotebookBooksController.java
    - backend/src/main/java/com/odde/doughnut/services/NotebookHealthService.java
    - backend/src/main/java/com/odde/doughnut/services/AuthorizationService.java
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRunContext.java
    - .planning/phases/02-empty-folder-findings/02-RESEARCH.md (Pattern 3 thin controller)
    - .planning/phases/02-empty-folder-findings/02-PATTERNS.md (NotebookHealthController shell)
    - .cursor/skills/generate-api-client/SKILL.md
  </read_first>
  <behavior>
    - Same behaviors as Task 1 must pass after implementation
  </behavior>
  <action>
    Add thin package-private `NotebookHealthController` colocated under `controllers/` with `@RestController`, `@SessionScope`, `@RequestMapping("/api/notebooks")` matching `NotebookBooksController` cohesion (D-10 — do not extend the ~450-line `NotebookController`). Inject `AuthorizationService` and `NotebookHealthService`. Map `POST /{notebook}/health/lint` returning `NotebookHealthLintReport` (D-08). Use `@Transactional(readOnly = true)` consistent with read sibling methods on `NotebookBooksController`. Path variable: `@PathVariable("notebook") @Schema(type = "integer") Notebook notebook`. Authorize with owner write gate `authorizationService.assertAuthorization(notebook)` only — never the read-only/bazaar/subscriber gate (D-08). Delegate to `notebookHealthService.lint(notebook, new HealthRunContext())` with no request body and no fix/user-default fields (D-09). Capability naming only. After the endpoint compiles and controller tests pass, regenerate the client with `CURSOR_DEV=true nix develop -c pnpm generateTypeScript` per generate-api-client skill (D-11); never hand-edit `packages/generated/doughnut-backend-api`. Do not add Health UI, fix/purge endpoints, or dissolve calls. If generateTypeScript affects frontend usage, run `CURSOR_DEV=true nix develop -c pnpm frontend:test` per the skill; otherwise backend suite green is sufficient for this phase.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - All `NotebookHealthControllerTest` and `EmptyFolderHealthRuleTest` methods pass
    - Full backend unit suite green via `CURSOR_DEV=true nix develop -c pnpm backend:test_only`
    - `NotebookHealthController` maps `POST /{notebook}/health/lint` and calls owner write authorization then `NotebookHealthService.lint`
    - Controller source does not call the read-only authorization entrypoint (grep gate: count of that method name in `NotebookHealthController.java` is 0)
    - `packages/generated/doughnut-backend-api` updated by generateTypeScript (not hand-edited) and includes the health lint operation
    - No Health tab/UI components, no purge/fix endpoint, no dissolve on lint path
  </acceptance_criteria>
  <done>Authorized report-only lint API and regenerated TS client deliver EFOL-02 (D-08..D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST .../health/lint | Untrusted caller; session user may be foreign, anonymous, or owner |
| controller → NotebookHealthService | Authorized owner only may receive folder structure findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | NotebookHealthController.lint | high | mitigate | Call owner write authorization (`assertAuthorization(notebook)`) before lint; reject foreign and anonymous (D-08); controller tests prove both failures |
| T-02-02 | Elevation of Privilege | NotebookHealthController.lint | high | mitigate | Do not use the read-only/bazaar/subscriber authorization entrypoint; write gate only so subscribers/bazaar readers cannot audit (D-08) |
| T-02-03 | Tampering | lint path | high | mitigate | Report-only: empty HealthRunContext, no fix applicator, no dissolve/delete; folder count unchanged assertion in tests (success criterion 3) |
| T-02-04 | Tampering | request body / mass assignment | medium | mitigate | No request DTO and no fix/run-option fields in Phase 2 (D-09) |
| T-02-05 | Information Disclosure | OpenAPI client | low | accept | Generated client documents endpoint for authenticated SPA later; enforcement remains server-side auth |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c pnpm backend:test_only` green (controller + rule tests)
- Owner receives `empty_folders` group/items without fix options; foreign/anon rejected; folder count unchanged
- Generated client present under `packages/generated/doughnut-backend-api` after `pnpm generateTypeScript`
- No Health UI, no purge endpoint
</verification>

<success_criteria>
- EFOL-02: authorized lint lists empty folders under empty-folder findings group without auto-fix selected
- Success criteria 3–4: no mutation; foreign/anon rejected
- D-11: OpenAPI TS client regenerated
- Phase Behavior complete when both 02-01 and 02-02 succeed
</success_criteria>

<output>
Create `.planning/phases/02-empty-folder-findings/02-02-SUMMARY.md` when done
</output>
