---
phase: 02-empty-folder-findings
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
  - backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java
  - backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java
autonomous: true
requirements:
  - EFOL-01
must_haves:
  truths:
    - "Lint evaluation reports folders whose entire subtree has no live notes (recursive; soft-deleted notes do not count as content) under ruleId empty_folders (EFOL-01, D-01, D-03, D-05)"
    - "Every matching fully empty folder is listed as its own HealthFindingItem with folderId and label=folder name (D-04)"
    - "Folders with non-blank own readmeContent are omitted from the empty_folders group items (D-02)"
    - "Empty-folder group metadata is title Empty folders, severity warning, autoFixable true (D-06)"
    - "Rule is a Spring HealthRule bean discovered by HealthRuleRunner with no mutation of notebook data (D-07)"
  artifacts:
    - path: backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
      provides: "findLiveNoteFolderIdsByNotebookId — DISTINCT live note folder_id for notebook (D-03)"
      contains: "findLiveNoteFolderIdsByNotebookId"
    - path: backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java
      provides: "EmptyFolderHealthRule HealthRule bean for fully empty folders (D-01..D-07)"
    - path: backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java
      provides: "Predicate coverage: recursive, soft-delete, blank vs non-blank readme, every folder listed (D-12)"
  key_links:
    - from: "EmptyFolderHealthRule.evaluate"
      to: "FolderRepository.findByNotebookIdOrderByIdAsc"
      via: "one notebook folder load"
      pattern: "findByNotebookIdOrderByIdAsc"
    - from: "EmptyFolderHealthRule.evaluate"
      to: "NoteRepository.findLiveNoteFolderIdsByNotebookId"
      via: "occupied folder ids from live notes only"
      pattern: "findLiveNoteFolderIdsByNotebookId"
    - from: "EmptyFolderHealthRule"
      to: "HealthRuleRunner"
      via: "Spring List<HealthRule> bean discovery"
      pattern: "@Service"
  prohibitions:
    - statement: "must NOT include note-empty folders with non-blank own readmeContent under empty_folders items"
      status: resolved
      verification: judgment
    - statement: "must NOT mutate notebook data from EmptyFolderHealthRule.evaluate (no delete, dissolve, or folder writes)"
      status: resolved
      verification: judgment
    - statement: "must NOT implement readme_only_folders rule, Health UI, or fix/purge in this plan"
      status: resolved
      verification: judgment
---

<objective>
Deliver `EmptyFolderHealthRule` (D-01..D-07) so `NotebookHealthService.lint` / `HealthRuleRunner` reports fully empty folders under `empty_folders` with recursive, soft-delete-aware, blank-readme predicate (EFOL-01).

Purpose: First half of Phase 2 Behavior — injectable rule + proven predicate before the authorized HTTP surface (plan 02).
Output: `NoteRepository.findLiveNoteFolderIdsByNotebookId`, `EmptyFolderHealthRule` bean, green `EmptyFolderHealthRuleTest`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-empty-folder-findings/02-CONTEXT.md
@.planning/phases/02-empty-folder-findings/02-RESEARCH.md
@.planning/phases/02-empty-folder-findings/02-PATTERNS.md
@.planning/phases/02-empty-folder-findings/02-VALIDATION.md
@.planning/phases/01-health-lint-contract/01-CONTEXT.md
@.cursor/rules/planning.mdc
@.cursor/rules/backend-code.mdc
@.cursor/rules/backend-testing.mdc
@backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java
@backend/src/main/java/com/odde/doughnut/services/health/HealthRuleRunner.java
@backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java
@backend/src/main/java/com/odde/doughnut/services/NotebookHealthService.java
@backend/src/main/java/com/odde/doughnut/entities/repositories/FolderRepository.java
@backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
@backend/src/test/java/com/odde/doughnut/services/NoteMotionServiceTest.java
@backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java
</context>

## Phase Goal

**As a** notebook owner, **I want to** run authorized Health lint and receive fully empty folders under an empty-folder findings group, **so that** I can audit structural decay without mutating my notebook.

This plan delivers the rule/predicate slice of that story (EFOL-01). Plan 02 exposes it via authorized `POST .../health/lint` (EFOL-02).

## Artifacts this phase produces

| Symbol | Path |
|--------|------|
| `findLiveNoteFolderIdsByNotebookId` | `backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java` |
| `EmptyFolderHealthRule` | `backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java` |
| `EmptyFolderHealthRuleTest` | `backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java` |
| `NotebookHealthController` | `backend/src/main/java/com/odde/doughnut/controllers/NotebookHealthController.java` *(plan 02)* |
| `NotebookHealthControllerTest` | `backend/src/test/java/com/odde/doughnut/controllers/NotebookHealthControllerTest.java` *(plan 02)* |
| Generated OpenAPI TS client | `packages/generated/doughnut-backend-api/**` *(plan 02)* |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EmptyFolderHealthRule predicate tests (RED)</name>
  <files>backend/src/test/java/com/odde/doughnut/services/health/EmptyFolderHealthRuleTest.java</files>
  <read_first>
    - .planning/phases/02-empty-folder-findings/02-CONTEXT.md (D-01..D-07, D-12)
    - .planning/phases/02-empty-folder-findings/02-RESEARCH.md (Pattern 2 algorithm, fixture patterns, Validation Architecture)
    - .planning/phases/02-empty-folder-findings/02-PATTERNS.md (EmptyFolderHealthRuleTest analogs)
    - .cursor/rules/backend-testing.mdc
    - backend/src/test/java/com/odde/doughnut/services/NoteMotionServiceTest.java
    - backend/src/test/java/com/odde/doughnut/services/health/HealthRuleRunnerTest.java
  </read_first>
  <behavior>
    - Nested fully empty Parent/Child: both folderIds appear as items with labels Parent and Child (D-01, D-04)
    - Live note in a descendant clears occupancy for ancestors — neither parent nor child listed
    - Soft-deleted note in folder does not occupy — folder still listed (D-03)
    - Folder with only non-blank readmeContent is not listed under empty_folders items (D-02); blank/null/whitespace-only readme still lists
    - Group always present with ruleId HealthRuleIds.EMPTY_FOLDERS, title Empty folders, severity warning, autoFixable true even when items empty (D-05, D-06)
    - Prefer @SpringBootTest + @ActiveProfiles(test) + @Transactional + autowired EmptyFolderHealthRule or NotebookHealthService.lint + MakeMe fixtures (one behavior per test method)
    - Must fail until rule bean and repository query exist (RED before Task 2)
  </behavior>
  <action>
    Create capability-named `EmptyFolderHealthRuleTest` (no phase numbers in class name) per D-12 unit/service coverage. Use MakeMe: `aFolder().notebook(...).name(...)`, `parentFolder(...)`, `readmeContent(...)`, `aNote(...).folder(...).softDeleted()`. Exercise evaluate via the Spring bean or `NotebookHealthService.lint(notebook, new HealthRunContext())` so Spring `List&lt;HealthRule&gt;` discovery is implied once the bean exists. Do not add controller tests, E2E, `@wip`, Health UI, or purge/fix code. Do not register a second runner. Domain language: readme / readmeContent only.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - `EmptyFolderHealthRuleTest` exists with separate methods covering recursive listing, live-note occupancy, soft-delete ignored, non-blank readme excluded, and group metadata
    - Backend unit suite fails for the right reason (missing bean/types or failing assertions), not env/typo noise
    - No controller, frontend, or Flyway files added
  </acceptance_criteria>
  <done>Failing predicate tests for EFOL-01 / D-01..D-06 are in place (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement live folder-id query and EmptyFolderHealthRule (GREEN)</name>
  <files>backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java, backend/src/main/java/com/odde/doughnut/services/health/EmptyFolderHealthRule.java</files>
  <read_first>
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRule.java
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRuleIds.java
    - backend/src/main/java/com/odde/doughnut/services/health/HealthRuleRunner.java
    - backend/src/main/java/com/odde/doughnut/entities/repositories/FolderRepository.java
    - backend/src/main/java/com/odde/doughnut/entities/repositories/NoteRepository.java
    - backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingGroup.java
    - backend/src/main/java/com/odde/doughnut/controllers/dto/HealthFindingItem.java
    - .planning/phases/02-empty-folder-findings/02-RESEARCH.md (Pattern 2 O(folders+notes) algorithm)
    - .planning/phases/02-empty-folder-findings/02-PATTERNS.md (EmptyFolderHealthRule compose sketch)
  </read_first>
  <behavior>
    - Same behaviors as Task 1 must pass after implementation
  </behavior>
  <action>
    Add `NoteRepository.findLiveNoteFolderIdsByNotebookId(Integer notebookId)` returning DISTINCT `n.folder.id` for notes in the notebook with `deletedAt IS NULL` and non-null folder (per D-03; match existing soft-delete query style — do not N+1 per folder). Implement `@Service EmptyFolderHealthRule implements HealthRule` (D-07) with `id()` → `HealthRuleIds.EMPTY_FOLDERS`, `title()` → `Empty folders`, `severity()` → `HealthSeverity.warning`, `autoFixable()` → true (D-05, D-06). In `evaluate`: load folders via `FolderRepository.findByNotebookIdOrderByIdAsc`; load occupied ids once; build parent→children map; memoize subtreeHasLiveNotes; report folder when subtree has no live notes AND own readmeContent is blank (`null` or `String.isBlank()`) (D-01, D-02); emit one `HealthFindingItem` per match with `folderId` and `label=folder.getName()` (D-04, no path required); always return a group (items may be empty list, never omit the group). Read-only only — no dissolve, delete, or repository writes. Capability naming only (no phase numbers). Optional: extract a small package-private blank-readme / subtree helper inside the rule class if it stays cohesive and ≤ ~40 lines — do not build purge applicator. Do not add HTTP controller or regenerate TypeScript client in this plan.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm backend:test_only</automated>
  </verify>
  <acceptance_criteria>
    - All `EmptyFolderHealthRuleTest` methods pass
    - Full backend unit suite green via `CURSOR_DEV=true nix develop -c pnpm backend:test_only`
    - `EmptyFolderHealthRule` is a Spring `@Service` implementing `HealthRule`
    - `NoteRepository` exposes `findLiveNoteFolderIdsByNotebookId` with `deletedAt IS NULL`
    - No Health controller route and no `pnpm generateTypeScript` in this plan
    - No dissolve/purge/fix applicator code paths
  </acceptance_criteria>
  <done>Empty-folder rule bean reports fully empty folders correctly (EFOL-01 green; D-01..D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rule evaluate → DB | Read-only folder/note queries; no untrusted user input beyond notebook entity already loaded in-process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | EmptyFolderHealthRule | medium | accept | No HTTP in this plan; plan 02 enforces owner write auth before lint (D-08) |
| T-02-02 | Tampering | EmptyFolderHealthRule.evaluate | high | mitigate | Report-only evaluate: no folder delete/dissolve/writes; tests assert findings only (success criterion 3, D-07) |
| T-02-03 | Elevation of Privilege | rule registration | low | accept | Bean has no auth surface; controller gate is plan 02 |
| T-02-04 | Tampering | predicate scope | medium | mitigate | Exclude non-blank readmeContent from empty_folders so Phase 7 cannot treat readme-bearing folders as purge targets via this group (D-02) |
</threat_model>

<verification>
- `CURSOR_DEV=true nix develop -c pnpm backend:test_only` green with recursive, soft-delete, and readme-exclusion cases
- Rule id is `empty_folders`; group `autoFixable` is true; severity is warning
- Diff has no controller mappings, no frontend Health UI, no purge endpoint
</verification>

<success_criteria>
- EFOL-01 (service-level): fully empty folders reported under `empty_folders` with D-01..D-06 metadata
- Soft-deleted notes ignored; non-blank readme folders omitted from this group
- Rule is Spring-discovered; runner still never mutates
</success_criteria>

<output>
Create `.planning/phases/02-empty-folder-findings/02-01-SUMMARY.md` when done
</output>
