---
phase: 05-health-tab-and-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/commons/WorkspaceReadmeSettingsTabs.vue
  - frontend/src/pages/NotebookPageView.vue
  - frontend/src/components/notebook/NotebookHealthPanel.vue
  - frontend/tests/pages/NotebookPageView.spec.ts
  - frontend/tests/components/notebook/NotebookHealthPanel.spec.ts
  - frontend/tests/pages/FolderPage.healthTab.spec.ts
autonomous: true
requirements:
  - HLTH-01
  - HLTH-02
  - AFIX-01
must_haves:
  truths:
    - "Notebook owner sees Readme | Settings | Health on notebook settings; default tab remains readme (HLTH-01, D-01, D-04)"
    - "Health tab is absent on folder settings (folder-workspace-tab-health does not exist) (D-03)"
    - "Opening Health alone does not call lint or mutate notebook data (HLTH-02, D-04, D-05)"
    - "User can Run lint and move idle → report for the current notebook via bodyless NotebookHealthController.lint and apiCallWithLoading (HLTH-02, D-06, D-07, D-08)"
    - "Action bar shows Run lint and Remove empty folders checkbox; checkbox is UI-only local state; no Fix/Apply control (AFIX-01, D-14, D-15, D-16)"
    - "After successful Run, all report groups render with title and leaf count; flat items and nested child labels show as text only (wire shape; no fabricated findings before Run) (UI empty/populated/partial)"
    - "Run failure keeps idle or last report; errors use apiCallWithLoading toast only (UI error)"
    - "Run in flight uses thin loading bar only — no blockUi, no panel skeleton (UI loading)"
  artifacts:
    - path: frontend/src/components/commons/WorkspaceReadmeSettingsTabs.vue
      provides: "Optional Health tab via includeHealth default false (D-02, D-03)"
      contains: "includeHealth"
    - path: frontend/src/pages/NotebookPageView.vue
      provides: "include-health + explicit three-way panel branch (D-02, D-03)"
      contains: "NotebookHealthPanel"
    - path: frontend/src/components/notebook/NotebookHealthPanel.vue
      provides: "Action bar, idle, Run lint, report groups shell (D-05..D-08, D-14..D-17)"
      contains: "notebook-health-run"
    - path: frontend/tests/components/notebook/NotebookHealthPanel.spec.ts
      provides: "Idle/Run/AFIX-01 unit coverage (D-19)"
    - path: frontend/tests/pages/NotebookPageView.spec.ts
      provides: "Health tab + no settings on Health branch (D-19)"
    - path: frontend/tests/pages/FolderPage.healthTab.spec.ts
      provides: "Folder Health tab absent (D-03, D-19)"
  key_links:
    - from: "NotebookHealthPanel runLint"
      to: "NotebookHealthController.lint"
      via: "apiCallWithLoading path-only options"
      pattern: "NotebookHealthController.lint"
    - from: "NotebookPageView activeTab health"
      to: "NotebookHealthPanel"
      via: "v-else-if activeTab === health"
      pattern: "notebook-workspace-health"
    - from: "WorkspaceReadmeSettingsTabs includeHealth"
      to: "notebook-workspace-tab-health"
      via: "testIdPrefix notebook-workspace"
      pattern: "tab-health"
  prohibitions:
    - statement: "must NOT use bare v-else for Settings after adding Health"
      status: resolved
      verification: judgment
    - statement: "must NOT call lint on tab open, onMounted, or activeTab watch"
      status: resolved
      verification: automated
    - statement: "must NOT send checkbox state in lint body or add Fix/Apply button"
      status: resolved
      verification: automated
    - statement: "must NOT change backend, OpenAPI, or packages/generated"
      status: resolved
      verification: judgment
    - statement: "must NOT add Health on FolderPage or a /health route/dialog"
      status: resolved
      verification: automated
---

<objective>
Deliver the notebook-only Health tab, action bar, and explicit Run lint path (HLTH-01, HLTH-02, AFIX-01, D-01..D-08, D-14..D-19): idle → bodyless lint → report groups visible on-tab, with no mutation and no Fix control.

Purpose: Stop-safe user-facing Run loop; findings expand/collapse polish and E2E land in 05-02.
Output: Tabs prop + NotebookPageView three-way branch + NotebookHealthPanel + green frontend unit specs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-health-tab-and-run/05-CONTEXT.md
@.planning/phases/05-health-tab-and-run/05-RESEARCH.md
@.planning/phases/05-health-tab-and-run/05-PATTERNS.md
@.planning/phases/05-health-tab-and-run/05-UI-SPEC.md
@.cursor/rules/planning.mdc
@.cursor/rules/frontend-api.mdc
@.cursor/rules/frontend-testing.mdc
@frontend/src/components/commons/WorkspaceReadmeSettingsTabs.vue
@frontend/src/pages/NotebookPageView.vue
@frontend/src/pages/FolderPage.vue
@frontend/src/components/notebook/NotebookWorkspaceSettings.vue
@frontend/tests/pages/NotebookPageView.spec.ts
@frontend/tests/pages/folderPageTestSupport.ts
@frontend/tests/helpers/index.ts
@packages/generated/doughnut-backend-api/sdk.gen.ts
</context>

## Phase Goal

**As a** notebook owner, **I want to** open Health on notebook settings, Run lint on demand, and review findings on that tab, **so that** I can audit notebook health without a separate route or mutation on Run.

This plan delivers tab shell + Run + report groups shell (expandable polish in 05-02).

## Artifacts this plan produces

| Symbol | Path |
|--------|------|
| `WorkspaceReadmeSettingsTabs` (extended) | `frontend/src/components/commons/WorkspaceReadmeSettingsTabs.vue` |
| `NotebookHealthPanel` | `frontend/src/components/notebook/NotebookHealthPanel.vue` |
| Page + panel + folder unit tests | `frontend/tests/pages/NotebookPageView.spec.ts`, `NotebookHealthPanel.spec.ts`, `FolderPage.healthTab.spec.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED unit tests for Health tab and Run</name>
  <files>frontend/tests/pages/NotebookPageView.spec.ts, frontend/tests/components/notebook/NotebookHealthPanel.spec.ts, frontend/tests/pages/FolderPage.healthTab.spec.ts</files>
  <read_first>
    - .planning/phases/05-health-tab-and-run/05-CONTEXT.md (D-01..D-08, D-14..D-19)
    - .planning/phases/05-health-tab-and-run/05-RESEARCH.md (Validation Architecture, mock lint fixture)
    - .planning/phases/05-health-tab-and-run/05-PATTERNS.md (NotebookPageView.spec, NotebookHealthPanel.spec, Folder Health-absent)
    - .planning/phases/05-health-tab-and-run/05-UI-SPEC.md (data-testid + copywriting contracts)
    - .cursor/rules/frontend-testing.mdc
    - frontend/tests/pages/NotebookPageView.spec.ts
    - frontend/tests/pages/folderPageTestSupport.ts
    - frontend/tests/helpers/index.ts
  </read_first>
  <behavior>
    - NotebookPageView: notebook-workspace-tab-health present; default paint is readme (health panel and settings absent); clicking Health shows notebook-workspace-health and settings absent (guards v-else trap) (HLTH-01, D-01, D-04)
    - FolderPage: folder-workspace-tab-readme and folder-workspace-tab-settings present; folder-workspace-tab-health does not exist (D-03)
    - NotebookHealthPanel mount: notebook-health-idle and notebook-health-run and notebook-health-remove-empty-folders present; NotebookHealthController.lint not called (D-05, D-08, D-19)
    - Click Run lint: lint called once with path notebook id only and no body; idle replaced by notebook-health-findings; all three groups by ruleId empty_folders, readme_only_folders, dead_wiki_links; flat item label Empty Shell; nested dead-link token label Missing under child title Source (D-06, D-07, D-10 partial, D-18)
    - Checkbox on then Run: still path-only lint call; no Fix or Apply control in DOM (AFIX-01, D-15, D-16)
    - Prefer data-testid selectors; mockSdkService(NotebookHealthController, lint, reportFixture) per RESEARCH; capability-named files only (no phase numbers)
    - Must fail until panel/tabs/page wiring exist (RED before Task 2)
  </behavior>
  <action>
    Extend NotebookPageView.spec.ts for Health tab presence, default readme, Health click → health panel visible and notebook-workspace-settings absent. Add FolderPage.healthTab.spec.ts using folderPageTestSupport mount helpers to assert Health tab absent on folder. Create NotebookHealthPanel.spec.ts covering idle, no lint on mount, Run bodyless path-only call, report groups and labels from RESEARCH mock fixture (including dead_wiki_links children), checkbox UI-only (toggle then Run still path-only), and no Fix/Apply button. Use helper.component patterns from PATTERNS. Do not implement product components in this task.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/pages/NotebookPageView.spec.ts tests/components/notebook/NotebookHealthPanel.spec.ts tests/pages/FolderPage.healthTab.spec.ts</automated>
  </verify>
  <done>Failing Health tab / Run / AFIX-01 unit tests exist and fail for the right reason (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN Health tab shell, panel, and Run</name>
  <files>frontend/src/components/commons/WorkspaceReadmeSettingsTabs.vue, frontend/src/pages/NotebookPageView.vue, frontend/src/components/notebook/NotebookHealthPanel.vue</files>
  <read_first>
    - .planning/phases/05-health-tab-and-run/05-UI-SPEC.md (component inventory, interaction, copywriting, data-testid)
    - .planning/phases/05-health-tab-and-run/05-PATTERNS.md (Patterns 1–3, CheckInput, panel sibling)
    - .planning/phases/05-health-tab-and-run/05-RESEARCH.md (Pattern 1–3, Anti-Patterns)
    - frontend/src/components/commons/WorkspaceReadmeSettingsTabs.vue
    - frontend/src/pages/NotebookPageView.vue
    - frontend/src/components/notebook/NotebookWorkspaceSettings.vue
    - frontend/src/components/form/CheckInput.vue
    - .cursor/rules/frontend-api.mdc
    - packages/generated/doughnut-backend-api/sdk.gen.ts
  </read_first>
  <behavior>
    - Same behaviors as Task 1 must pass after implementation
  </behavior>
  <action>
    Extend WorkspaceReadmeSettingsTabs: tab union includes health; prop includeHealth default false; render Health tab only when includeHealth true with testid `${testIdPrefix}-tab-health` (D-01, D-02, D-03). Leave FolderPage unchanged (D-03).

    NotebookPageView: pass include-health; replace Settings bare v-else with explicit v-else-if settings and v-else-if health mounting NotebookHealthPanel with notebook-id and data-testid notebook-workspace-health (D-02, D-04). Keep default activeTab readme.

    Create capability-named NotebookHealthPanel under frontend/src/components/notebook/ (D-17): section chrome per UI-SPEC (bg-base-100 border border-base-300 rounded-lg p-6). Action bar always visible (flex flex-wrap items-center gap-2): primary daisy-btn daisy-btn-primary daisy-btn-sm labeled Run lint (notebook-health-run); Remove empty folders via CheckInput on local ref(false), wrap or attach notebook-health-remove-empty-folders (D-14, D-15, D-18). Idle block (notebook-health-idle) uses UI-SPEC copy: Run lint to check this notebook for empty folders, readme-only folders, and dead wiki links. Run calls apiCallWithLoading with NotebookHealthController.lint path-only options (notebook id) — bodyless, thin bar, no blockUi (D-05, D-06, D-07). Assign report only when !error (D-08). After success, replace idle with notebook-health-findings listing all groups by ruleId with title and leaf count (items length plus sum of children items); render flat item.label and nested child.title plus child item.label as text only — no v-html, no click-through, no severity badges (D-10, D-12, D-13 partial). Re-Run replaces report. No Fix/Apply control (D-15). Do not call lint on mount or tab selection. No backend/OpenAPI/generated package edits. Findings expand/collapse default-open polish may be minimal here; 05-02 completes daisy-collapse interaction and E2E.
  </action>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c pnpm frontend:test tests/pages/NotebookPageView.spec.ts tests/components/notebook/NotebookHealthPanel.spec.ts tests/pages/FolderPage.healthTab.spec.ts</automated>
  </verify>
  <done>Health tab + Run lint delivers idle→report groups without mutation; unit tests green (HLTH-01, HLTH-02, AFIX-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → existing lint API | User-initiated POST; session cookie; notebook id from trusted page props |
| Health tab visibility | Notebook-only surface; folder must not expose Health |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | NotebookHealthPanel.runLint | high | mitigate | Bodyless path-only lint; checkbox never in request; no Fix/Apply; unit asserts path-only call with checkbox on (AFIX-01, D-07, D-15, D-16) |
| T-05-02 | Elevation of Privilege | WorkspaceReadmeSettingsTabs / FolderPage | high | mitigate | includeHealth default false; FolderPage unchanged; FolderPage.healthTab.spec asserts folder-workspace-tab-health absent (D-03) |
| T-05-03 | Tampering / XSS | Finding labels in panel | medium | mitigate | Render labels as text only (Vue default escaping); forbid v-html on labels (D-12) |
| T-05-04 | Information Disclosure | Lint API | low | accept | Existing backend write-auth on NotebookHealthController; no new endpoint |
| T-05-05 | Denial of Service | Run control | low | accept | Thin-bar loading only; user-initiated; no auto-run loops |
</threat_model>

<verification>
- Targeted frontend specs green via nix-prefixed pnpm frontend:test on the three Health-related unit files
- Diff has no backend, OpenAPI, or packages/generated changes
- Folder remains without Health tab
</verification>

<success_criteria>
- HLTH-01: Health tab on notebook settings only; no /health route or dialog
- HLTH-02: Explicit Run lint; opening Health does not call API or mutate
- AFIX-01: Optional Remove empty folders checkbox; Run never mutates; no Fix button
- Idle → report groups visible with wire-shape labels
</success_criteria>

<output>
Create `.planning/phases/05-health-tab-and-run/05-01-SUMMARY.md` when done
</output>
