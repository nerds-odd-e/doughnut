const colors = function(role) {
  if(role === 'source') return '#ffaaaa'
  if(role === 'target') return '#d4ffaa'
  if(role === 'sourceSecondary') return '#7f0000'
  if(role === 'targetSecondary') return '#3f7f00'
}

export default colors