apis:
  main:
    root: open_api_docs.yaml

extends:
  - recommended

rules:
  operation-operationId-unique: error
  no-duplicated-tag-names: error
  operation-summary: off
  struct: off
  security-defined: off
  operation-4xx-response: off
  no-ambiguous-paths: off
  info-license: off

